# AGENTS.md

## 작업 원칙

- 큰 작업을 완료하면 `AGENTS.md`, `README.md`, `ROADMAP.md`를 현재 구현 상태에 맞게 갱신한다.
- 서로 독립적인 작업은 가능한 범위에서 병렬로 처리한다.
- iOS, macOS, Android 클라이언트 빌드는 동시에 최대 3개까지만 실행한다.
- 기존 사용자 변경사항을 덮어쓰거나 되돌리지 않는다.

## 현재 개발 상태

- P1~P80의 PC·모바일 웹 데모 핵심 구현과 재현·공개 문서화가 완료되어 최대 3개 캐릭터를 각각 Lv.9 초보자 또는 Lv.120 호카게 부스트로 생성·선택하고 Lv.200 성장, 4차 전직, 11개 맵, 던전 원정·인내의 숲·무한 결투장과 자동 회수 펫 두아까지 독립된 로컬 프로필로 플레이할 수 있다.
- 구현 이력은 2026-07-16 P0~P8, 2026-07-17 P9~P24, 2026-07-18 P25~P72의 3일 작업, 2026-07-29 P73~P79 모바일·전송 최적화, 2026-07-30 P80 모바일 생성 입력 보정으로 정리한다. 단계별 당시 수치는 회귀 근거이며 현재 최종 기준은 P80과 `RELEASE_QA.md`의 최신 결과다.
- 전투는 4개 직업 차수, 액티브 11종·패시브 4종, 스프라이트 9종을 사용하는 몬스터 전투 항목 11종과 보스 원거리 패턴을 지원하며 슬롯별 프로필 스키마는 v16이다.
- UI는 반투명·개별 접기 HUD, 소모품을 직접 사용하는 24칸 인벤토리, 스킬, 능력치·표창 장비, 상점, 퀘스트 팝업을 제공한다. 모든 게임 팝업은 현재 게임 영역을 넘지 않는 최대 크기와 자체 스크롤을 공유한다.
- 논리 해상도는 1280×720, 최소 검증 PC 창은 800×600, 모바일 터치 검증 화면은 가로 844×390·세로 390×844이며 Chromium·Firefox·WebKit 자동화와 BGM을 제외한 8MB `dist/` 감사가 릴리스 기준이다.
- P47 관련 3개 독립 캐릭터 슬롯 생성·선택·복구, 기존 1번 프로필 보존과 지원 PC 창 경계 자동화를 추가했다. 273개 단위 테스트, 3엔진 슬롯·뷰포트 E2E 6/6, 타입체크·빌드·7,999,552-byte `dist/` 감사는 통과했다. 전체 E2E의 병행 HUD·밸런스 변경 관련 실패 9건은 `RELEASE_QA.md`에서 추적한다.
- P48 관련 11개 맵의 발판·줄 축소 지형, 실시간 플레이어 마커, NPC 위치 마커와 세 HUD 창 `−/+` 버튼의 제목 영역 수직 중앙 정렬을 추가했다. 273개 단위 테스트, Chromium·Firefox·WebKit HUD E2E 6/6, 타입체크·빌드·7,999,532-byte `dist/` 감사는 통과했다.
- 런타임 BGM은 사용자가 제공한 일반곡·보스곡 MP3 원본 2종을 사용한다. 일반곡은 초기 오디오에 포함해 로그인부터 일반 맵까지 재생 위치를 유지하고, 보스곡은 살아 있는 중간·상위·최종 보스가 있는 맵을 처음 열 때 한 번만 지연 로딩한다. 로딩 중에는 일반곡을 끊지 않고 준비가 끝나면 보스곡으로 전환하며 처치 완료 뒤 일반곡으로 복귀한다. 일반곡 0.90~68.06초·보스곡 0~51.99초 사운드 마커로 앞뒤 무음을 제외해 즉시 시작·무한 반복한다. BGM 파일은 `dist/`에 포함하되 8,000,000-byte 총량 계산에서는 제외한다.
- P49 관련 미니맵 포탈 위치·범례와 포탈마다 생성형 세로 에너지·바닥 이중 광륜·상승 파편 애니메이션을 추가했다. 275개 단위 테스트, Chromium·Firefox·WebKit HUD·포탈 E2E 6/6, 타입체크·빌드·7,999,680-byte `dist/` 감사와 1280×720 인앱 시각 검증은 통과했다.
- P50 관련 커닝시티에 `일용직 개발자 임상진` 전용 NPC와 접근 가능한 채널 홍보 대화창·`구독하기` 새 탭 링크·감사 동작을 추가했다. 20종·320셀 스프라이트 감사, 279개 단위 테스트, Chromium·Firefox·WebKit 개발자 대화·HUD E2E 9/9, 타입체크·빌드·BGM 제외 6,739,763-byte `dist/` 감사와 1280×720 인앱 시각 검증은 통과했다.
- P51 관련 첫 접속의 기본 캐릭터 자동 생성을 제거하고, 빈·손상 저장소에서 캐릭터 생성을 먼저 안내한다. 초보자의 Lv.10 달성·1차 전직 수락·처치·보고를 퀘스트 HUD의 다음 목표로 표시한다. 284개 단위 테스트, Chromium·Firefox·WebKit 첫 생성·퀘스트 E2E 3/3, 타입체크·빌드·BGM 제외 6,740,886-byte `dist/` 감사는 통과했다.
- P52 관련 2·3·4차 전직의 필요 레벨·추천 사냥터·시험 수락·처치·보고를 다음 목표로 연결했다. Lv.100·140·200 경계에서 세라 원정 수락, 이그니카르·루나시온·원펀맨 처치, 보고를 현재 진척에 맞게 표시한다. 296개 단위 테스트, Chromium·Firefox·WebKit 전직·보스 동시 HUD 경계 E2E 3/3, 타입체크·빌드·BGM 제외 6,743,044-byte `dist/` 감사는 통과했다.
- P53 관련 전체 기능 명세, 데이터·아키텍처 단일 기준, 독립 에셋 원칙, 직접 제작 또는 Suno 작곡 BGM 삽입 가이드, 복사 가능한 마스터·P0~P9 단계별 제작 프롬프트와 최종 인수 체크리스트를 `PROMPT.md`에 통합했다.
- P54 관련 외부 레퍼런스 이미지 14종과 문서의 파일 경로를 제거하고, 실제 1280×720 실행 화면 4종을 새로 캡처해 전면 개편한 `README.md`에 추가했다. 공개 `main`은 현재 소스만 담은 단일 루트 커밋으로 유지한다.
- P55 관련 인벤토리 상세 패널에 소모품 `사용하기`를 추가했다. 회복 물약은 HP·MP 최대치의 50%씩 회복하고 완전 회복 상태에서는 소비하지 않으며, 경험의 서는 기존 최대 10레벨 성장 규칙을 재사용한다. 300개 단위 테스트, Chromium·Firefox·WebKit 소모품·뷰포트 E2E 6/6, 타입체크·빌드·BGM 제외 6,744,587-byte `dist/` 감사는 통과했다.
- P56 관련 크리티컬 전용 4번째 데미지 숫자 팔레트와 확대 연출, 오버킬 원피해 표시, 구미호 할퀴기의 기본 투사체 동등 사거리, 호카게 시네마틱 중 투사체 수명 정지, 이동 가능한 레벨업 연출과 초록버섯굴 보조 발판을 추가했다. 306개 단위 테스트, Chromium·Firefox·WebKit 레벨업·크리티컬·호카게 시네마틱 E2E 9/9, 타입체크·빌드·BGM 제외 6,758,643-byte `dist/` 감사는 통과했다.
- P57 관련 구미호 변신 중 매초 최대 MP 1% 소모·자연 MP 회복 정지·고갈 자동 해제, 할퀴기 2배 공격속도와 LUK/DEX·장착 표창 무기 보정, 수평 이동속도 20% 상승을 추가했다. 309개 단위 테스트, Chromium·Firefox·WebKit 구미호 유지비·공격·이동 E2E 3/3, 타입체크·빌드·BGM 제외 6,761,750-byte `dist/` 감사는 통과했다.
- P58 관련 커닝시티의 두아, 50,000 메소 `멍푸치노` 구매·선물·펫 등록, 맵·새로고침을 넘는 추적과 바닥 드롭 자동 회수를 추가했다. 실제 두아 사진의 날렵한 얼굴을 반영한 독자 v2 시트는 참고 원본을 저장하지 않으며, 프로필은 v15로 등록 상태를 슬롯별 저장한다. 21종·336셀 감사, 322개 단위 테스트, Chromium·Firefox·WebKit 펫·뷰포트 E2E 9/9, 타입체크·빌드·BGM 제외 6,845,432-byte `dist/` 감사와 1280×720 실행 화면 검증은 통과했다.
- P59 관련 원펀맨의 넉백·피격 경직 면역, 체력 50% 초과의 보통 펀치 단일 충격파와 50% 이하의 진심 펀치 3방향 충격파를 추가했다. 두 패턴은 자체 제작 투명 SVG를 사용하며 표창 애니메이션과 분리했다. 324개 단위 테스트, Chromium·Firefox·WebKit 전용 충격파 E2E 3/3, 타입체크·빌드·BGM 제외 6,849,221-byte `dist/` 감사는 통과했다.
- P60 관련 두아의 수평 최고속도를 230→360, 점프 속도를 520→700으로 높이고 직상단 플레이어와 진행 방향 장애물을 감지해 제자리·장애물 점프하도록 추적을 보강했다. 150px 상단 발판 동행을 포함한 Chromium·Firefox·WebKit 펫 E2E 9/9, 324개 단위 테스트, 타입체크·빌드·BGM 제외 6,849,525-byte `dist/` 감사는 통과했다.
- P61 관련 무한의 결투장 상층 발판에서 수평으로 날아간 플레이어 투사체가 원펀맨을 지나치던 문제를 수정했다. 원펀맨에게만 실제 물리 바디 위쪽 132px의 전투 피격 영역을 적용하고 다른 몬스터의 피격 판정은 유지했다. 325개 단위 테스트, Chromium·Firefox·WebKit 원펀맨 E2E 3/3, 타입체크·빌드·BGM 제외 6,849,792-byte `dist/` 감사는 통과했다.
- P62 관련 두아 v3 시트의 달리기 4프레임을 대각발 접지·회수·반대 대각발 접지·회수 순서로 교체하고 프레임별 시각 중심과 발 기준점을 다시 맞췄다. sit/idle·fetch·happy 12프레임은 v2와 픽셀 차이 0으로 유지했다. 325개 단위 테스트, Chromium·Firefox·WebKit 펫 E2E 9/9, 타입체크·빌드·BGM 제외 6,990,821-byte `dist/` 감사는 통과했다.
- P63 관련 서적상 레오가 1,000,000 메소 `부활의 부적`을 판매한다. 슬롯마다 최대 1개만 저장하며 사망 시 자동으로 1개를 소비해 같은 맵·위치에서 HP·MP를 완전 회복하고, 다음 사망부터 기존 커닝시티 귀환을 사용한다. 330개 단위 테스트, Chromium·Firefox·WebKit 구매·부활 E2E 6/6, 타입체크·빌드·BGM 제외 6,994,677-byte `dist/` 감사는 통과했다.
- P64 관련 기계·골렘형이 반복되던 일반 사냥 경로를 수정굴 늑대·망각된 기술자 좀비·산호 맹그로브·잿불 광부 좀비·월광 늑대로 교체했다. 독자 생성한 좀비·늑대·고대 나무정령 4×4 시트는 기존 몬스터 ID·레벨·보상과 맵 구성을 보존하면서 각기 다른 속도·크기·바디를 사용하고, 전체 몬스터 11종도 서로 중복되지 않는 이동속도를 가진다. 24종·384셀 감사, 334개 단위 테스트, Chromium·Firefox·WebKit 던전 로드 E2E 3/3, 타입체크·빌드·BGM 제외 7,230,219-byte `dist/` 감사와 1280×720 실행 화면 검증은 통과했다.
- P65 관련 `ESC` 전체 메뉴에서 인벤토리·스탯/장비·스킬·전용 스킬 단축키·환경설정을 연다. `1~0/-` 슬롯은 클릭 선택·드래그·`Alt+←/→`로 자유 배치하고 즉시 저장·입력·HUD에 반영한다. 상단 사운드 UI는 제거하고 음소거·BGM/SFX를 환경설정으로 옮겼으며, 호카게 시네마틱 중 HUD 접기 버튼을 숨긴다. 335개 단위 테스트, Chromium·Firefox·WebKit 메뉴·단축키·시네마틱·뷰포트 E2E 12/12, 타입체크·Terser 빌드·BGM 제외 7,227,793-byte `dist/` 감사와 1280×720 인앱 시각 검증은 통과했다.
- P66 관련 두아가 추적 간격 안에 들어올 때마다 즉시 정지하던 추적 떨림을 제거했다. 플레이어가 움직이는 동안 기본 260·구미호 312 속도를 그대로 이어받아 순항하고, 정지 뒤에는 지면 마찰로 감속한 다음 idle로 전환한다. 336개 단위 테스트, Chromium·Firefox·WebKit 펫 E2E 9/9, 타입체크·Terser 빌드·BGM 제외 7,228,234-byte `dist/` 감사는 통과했다.
- P67 당시 전용 스킬 단축키 화면에 `Shift`, `QWER`, `ASDF`, `ZXCV` 추가 슬롯 13개를 더했다. 숫자/HUD 11칸은 기존 순열을 유지하고 추가 키는 중복·빈 배치를 허용하며, `S`·`Z`는 비어 있을 때 스탯/장비·회수 기본 동작으로 복귀했다. 프로필 v16은 설정을 슬롯별 저장하고 v1~v15의 기존 고정 보조키를 기본 별칭으로 이관했다. 339개 단위 테스트, Chromium·Firefox·WebKit 단축키·뷰포트·HUD E2E 9/9, 타입체크·Terser 빌드·BGM 제외 7,235,797-byte `dist/` 감사와 1280×720 인앱 시각 검증은 통과했다. 현재 `Z` 동작은 P72 기준을 따른다.
- P68 관련 원펀맨의 사망 애니메이션·보상·원정 진척 확정 뒤 18초 픽셀 아트 게임 크레딧을 재생한다. 시스템·던전·몬스터·캐릭터·스킬·FX·테스트는 모두 `임상진 with Codex`, 게임 레퍼런스는 `메이플스토리`로 표시한다. 자동 종료·버튼·`ESC` 뒤 같은 런타임 플레이로 복귀하며 처치 직전 보스 투사체는 제거한다. 342개 단위 테스트, Chromium·Firefox·WebKit 크레딧·플레이 재개 E2E 3/3, 타입체크·Terser 빌드·BGM 제외 7,317,955-byte `dist/` 감사는 통과했다.
- P69 관련 점프·낙하 중 반대 방향 키를 누르면 기본 260·구미호 312의 공중 최고속도로 즉시 반전하고, 방향 키를 놓으면 현재 수평 관성을 유지한다. 기본 공격·스킬의 동작 잠금 중에도 같은 공중 조향을 적용한다. 343개 단위 테스트, Chromium·Firefox·WebKit 공중 반전·공중 공격 E2E 3/3, 타입체크·Terser 빌드·BGM 제외 7,318,341-byte `dist/` 감사와 1280×720 실행 화면 캡처는 통과했다.
- P70 관련 무한의 결투장 2층에서 구미호 변신 `Ctrl` 할퀴기가 원펀맨을 맞히지 못하던 문제를 수정했다. 할퀴기의 전방 사거리 bounds가 원펀맨 실제 바디와 상단 132px 확장 피격 영역을 함께 사용하고, 반대 방향·다른 몬스터의 상단 오판정은 막는다. 344개 단위 테스트, Chromium·Firefox·WebKit 2층 변신 할퀴기 E2E 3/3, 타입체크·Terser 빌드·BGM 제외 7,318,700-byte `dist/` 감사는 통과했다.
- P71 관련 신규 캐릭터의 시작 레벨을 Lv.10에서 Lv.9로 변경해 초록버섯 사냥으로 Lv.10을 달성한 뒤 1차 전직을 시작하도록 했다. 기존 저장 프로필의 명시된 레벨은 유지한다. 344개 단위 테스트, Chromium·Firefox·WebKit 신규 생성·저장·HUD E2E 3/3, 타입체크·Terser 빌드·BGM 제외 7,318,699-byte `dist/` 감사는 통과했다. 기존 필수 루프 E2E의 카메라/HUD 경계 `598 < 596` 실패는 새 성장 구간 전에 재현되어 `RELEASE_QA.md`에서 추적한다.
- P72 관련 `Z`를 스킬 배치로 덮어쓸 수 없는 고정 아이템·메소 회수 키로 변경하고 우측 상단 조작 안내에 `Z 줍기`를 추가했다. 추가 스킬 슬롯은 `Shift/QWER/ASDF/XCV` 12칸이며 기존 v16 저장값의 `Z` 별칭은 로드할 때 제거한다. 344개 단위 테스트, Chromium·Firefox·WebKit 단축키·4개 PC 뷰포트 E2E 6/6, 타입체크·Terser 빌드·BGM 제외 7,318,430-byte `dist/` 감사와 1280×720 실행 화면 검증은 통과했다.
- P73 관련 터치 장치에서 전체 창 가로·세로 레이아웃, 좌하단 이동 조이스틱, 우하단 기본 공격·고정 줍기·직업 차수별 대표 액티브 스킬 최대 3슬롯과 우상단 메뉴를 추가했다. 캐릭터 생성에서는 슬롯별로 Lv.120 호카게·자동 능력치·전체 15개 스킬 Lv.20 부스트를 선택할 수 있다. 350개 단위 테스트, Chromium·Firefox·WebKit 모바일·일반 생성·PC 뷰포트 E2E 9/9, 타입체크·Terser 빌드·BGM 제외 7,329,035-byte `dist/` 감사와 844×390 실행 화면 검증은 통과했다.
- P74 관련 760px 이하 세로 화면에서 게임 Canvas 위아래의 미사용 영역을 모든 DOM 화면·팝업이 `100dvh` 모바일 시트로 사용하도록 확장했다. 로그인·캐릭터 선택/생성·전체 메뉴·환경설정·상점·퀘스트·인벤토리·스탯/장비·스킬·단축키는 수평 넘침 없이 자체 세로 스크롤하고, 제목·닫기·확정 조작을 고정하며 입력 16px·버튼 44px 이상과 safe-area를 보장한다. HUD 접기 버튼은 가운데 16:9 Canvas에 맞추면서 36px로 확대했다. 350개 단위 테스트, Chromium·Firefox·WebKit 세로 전체 화면·모바일 조작·4개 PC 뷰포트 E2E 9/9, 타입체크·Terser 빌드·BGM 제외 7,334,983-byte `dist/` 감사와 390×844 로그인·생성·선택·전체 메뉴·인벤토리·단축키 실행 화면 검증은 통과했다.
- P75 관련 모바일 조이스틱을 좌우 이동 전용으로 바꾸고 우하단 공격 왼쪽에 점프/상황 버튼을 추가했다. 버튼은 NPC 근처에서 대화, 포탈·줄 근처에서 이동 동작으로 전환하며 모바일 스킬 HUD와 단축키 편집은 현재 차수 주 스킬 최대 3개만 표시·정렬한다. 첫 페이지 로딩에는 매너모드 해제 BGM 안내가 3초간 노출되고, 세로 HUD 접기 버튼은 28px로 축소했다. 350개 단위 테스트, Chromium·Firefox·WebKit 모바일·PC 뷰포트 E2E 9/9, 타입체크·Terser 빌드·BGM 제외 7,349,319-byte `dist/` 감사와 390×844 초기 안내·로그인·플레이 HUD 실행 화면 검증은 통과했다.
- P76 관련 커닝시티의 달빛 서고 포탈을 이그니카르 처치 전에는 오브젝트·효과·미니맵에서 함께 숨기고 `upperboss` 단계부터 생성한다. 모바일은 점프를 고정하고 대화·이동을 별도 상황 버튼으로 분리했으며, 조이스틱 아래 입력과 점프를 조합해 단방향 발판을 하강한다. 1차 로그 시험도 그림자 시험장 입구를 사용하고 해당 시험에서는 기존 4개 스폰을 초록버섯으로 치환한다. 351개 단위 테스트, Chromium·Firefox·WebKit 모바일·세로 화면·포탈 해금·1차 시험 E2E 12/12, 타입체크·Terser 빌드·BGM 제외 7,350,872-byte `dist/` 감사와 844×390 실행 화면 검증은 통과했다.
- P77 관련 모바일 우하단 상황 버튼을 `대화/이동 → 줍기 → 점프 → 공격` 순서로 고정하고, 첫 화면 3초 안내에 PC 환경 최적화와 BGM 재생을 위한 매너모드 해제를 두 줄로 함께 표시한다. 351개 단위 테스트, Chromium·Firefox·WebKit 모바일 E2E 6/6, 타입체크·Terser 빌드·BGM 제외 7,351,096-byte `dist/` 감사와 390×844 첫 화면 시각 검증은 통과했다.
- P78 관련 모바일 대표 스킬 HUD와 전용 단축키 편집을 4슬롯으로 확장하고, 호카게 주력기에 미수옥을 추가해 나선환·미수옥·삼인 협공·천뢰옥을 표시한다. 351개 단위 테스트, Chromium·Firefox·WebKit 모바일 E2E 6/6, 타입체크·Terser 빌드·BGM 제외 7,351,560-byte `dist/` 감사는 통과했다.
- P79 관련 핵심 스프라이트 24종과 스킬 아이콘 15종을 원본 PNG에서 픽셀 차이 0인 무손실 WebP로 생성해 런타임 URL만 교체하고, `dist/`의 PNG 포함을 금지했다. 39종은 2,465,785→1,980,136 bytes로 485,649 bytes(19.70%) 감소했다. 보스 BGM은 첫 보스 조우 전 요청하지 않고 조우 시 한 번만 불러오며, 로딩 중 일반곡 유지·중복 요청 병합·실패 재시도·맵 수명 정리를 지원한다. 달빛 마도서고에서 로컬 1번 `루키` 프로필의 실제 사냥 화면을 새 공개 캡처로 사용한다. 359개 단위 테스트, Chromium·Firefox·WebKit WebP·보스 음원 E2E 6/6, 타입체크·Terser 빌드와 BGM 제외 6,865,761-byte `dist/` 감사는 통과했다.
- P80 관련 터치 브라우저에서 빈 캐릭터 슬롯을 누르는 사용자 동작 안에서 실제 닉네임 입력을 먼저 포커스하고 생성 Scene으로 이어받아 iOS Chrome에서도 입력칸과 소프트 키보드가 즉시 준비되도록 했다. 닉네임은 완성형 한글뿐 아니라 호환·결합 자모를 포함한 Unicode Hangul 스크립트 전체를 허용한다. 360개 단위 테스트, Chromium·Firefox·WebKit 모바일 E2E 9/9, 타입체크·Terser 빌드와 BGM 제외 6,866,423-byte `dist/` 감사는 통과했다.
- 단계별 완료 내역은 `ROADMAP.md`, 재현 명세·제작 프롬프트는 `PROMPT.md`, 수동·자동 릴리스 근거는 `RELEASE_QA.md`, 화면 비교 기록은 `VISUAL_QA.md`를 단일 현황 문서로 사용한다.

## 구현 기준

### 플랫폼과 아키텍처

- 결과물은 PC 데스크톱과 모바일·태블릿 웹 브라우저에서 별도 플러그인 없이 실행되어야 한다.
- 터치 장치는 가로·세로 화면에서 게임 영역을 전체 창에 맞추고 화면 조작부가 안전 영역 밖으로 나가지 않아야 한다.
- 760px 이하 세로 화면은 16:9 Canvas 위아래 공간까지 전체 화면과 팝업에 사용한다. 모든 입력·버튼은 가로 경계를 넘지 않고, 내용이 길면 패널 자체가 세로 스크롤되며 제목과 닫기·확정 조작은 계속 접근 가능해야 한다.
- 에셋 감사와 핵심 스프라이트 시트 작업을 기능 구현보다 우선한다.
- 새 기능은 `ASSET_GUIDE.md`에서 필요한 에셋과 규격을 먼저 확정한 뒤 구현한다.
- 스프라이트 프레임 좌표는 `assets/sprites/sprite-manifest.json`을 단일 기준으로 사용한다.
- 런타임 스프라이트는 Vite URL import로 번들링하고 저장소 전체를 public 디렉터리로 노출하지 않는다.
- 화면 배경은 `assets/ui/screens/`에서 불러오고, 입력·버튼·상태 텍스트는 접근 가능한 DOM으로 유지한다.

### 맵과 월드

- 맵 배경은 `assets/maps/screens/`, 결정적 패럴랙스·충돌 정렬 레이어는 `assets/maps/layers/`, imagegen 환경 오브젝트 아틀라스는 `assets/maps/objects/`에서 불러오며, 배경·오브젝트 픽셀을 충돌 판정에 사용하지 않는다.
- 커닝시티와 초록버섯굴의 1920×720 SVG 레이어는 `scripts/generate-map-layers.mjs`로 재생성하고, 그림자 시험장 레이어는 `assets/maps/layers/shadow-trial-*.svg` 규격을 유지한다.
- 11개 맵의 발판·줄·포탈은 각 배경을 참고해 built-in imagegen으로 만든 `assets/maps/objects/map-object-kits-a|b-v1.webp`와 `map-object-kits-c-v2.webp` 프레임을 사용하고, 인내의 숲 장애물은 `patience-hazards-v1.webp`를 사용한다. 런타임에서는 충돌 정렬 전경 SVG를 제외하고 좌표 검증 소스로만 유지한다. 그림자 시험장과 던전 회랑 5종은 지면과 첫 단방향 발판을 잇는 줄 1개를 각각 둔다. 무한의 결투장은 줄 없이 연속 지면·2단 단방향 발판 5개·귀환 포탈·원펀맨 1체만 둔다. 인내의 숲은 1920×1440 월드에 연속 지면·단방향 발판 20개·줄 4개·움직이는 장애물 10개·입구/정상 포탈을 둔다. 사다리 타입과 오브젝트는 사용하지 않는다.
- imagegen 맵 오브젝트 프레임은 아틀라스 셀 전체가 아니라 실제 불투명 연결 영역으로 등록한다. 발판은 최상단 장식 픽셀이 아니라 `src/game/assets/map-assets.ts`의 테마별 `surfaceY` 보행면을 충돌체 윗면에 맞추고, 줄 프레임의 상단은 `top`, 포탈 프레임의 하단과 생성형 세로 에너지·바닥 이중 광륜·상승 파편은 포탈 `y`에 정렬한다. 모든 반복 트윈과 효과 객체는 현재 맵 수명에 묶는다.
- 월드 크기, 배경 레이어 순서·스크롤 비율, 발판, 스폰, 포탈, NPC, 몬스터 스폰·순찰·재생성 설정의 단일 기준은 `src/game/maps/map-definitions.ts`로 유지한다. 커닝시티 `city-library` 포탈의 `requiresDungeonMidBossDefeat` 조건은 원정 단계가 `offer|midboss`일 때 오브젝트·효과·미니맵에서 함께 숨기고 이그니카르 처치로 `upperboss`가 된 뒤 노출한다.
- 줄의 부착·이동·이탈 판정은 `src/game/entities/climb-rules.ts`, 움직이는 장애물의 결정적 왕복 좌표·접촉 판정은 `src/game/maps/map-hazard-rules.ts`, 인내의 숲 정상 최초 보상 1,000,000 메소와 `초대형 고드름` 지급은 `src/game/quests/patience-forest-reward.ts`의 순수 로직을 단일 기준으로 유지한다.

### 게임 로직과 콘텐츠

- 우선순위와 단계별 완료 조건은 `ROADMAP.md`를 기준으로 한다.
- 필수 플레이 루프를 먼저 완성하고 시각적 폴리시는 그 뒤에 진행한다.
- 입력, 전투 수치, 맵·캐릭터·몬스터 데이터는 가능한 한 게임 로직에서 분리한다.
- 로그·어쌔신·허밋·호카게 전직 조건과 해금 스킬, 공격 선딜·타수·MP·스탯 데미지·공격 계열, 드레인 회복·어벤저 대상 수·구미호 변신 배율·할퀴기 2배 공격속도와 무기 보정·미수옥 대상 수·삼인 협공 5타, 그림자 연사 3타·환영 쌍성 2타 2대상·심연 폭우 3타 4대상·천뢰옥 2타 5대상, 예리한 시야 투사체 수명·치명 투척 결정적 판정, 피격 무적 시간·점멸 판정은 `src/game/combat/combat-rules.ts`의 순수 로직을 단일 기준으로 유지한다. 직업 차수 전투 배율은 초보자 1·로그 12·어쌔신 70·허밋 350·호카게 1,400이며 Lv.10~200 레벨 배율을 함께 적용해 Lv.120 허밋도 10만 이상, 호카게 주력기는 수십만 피해를 낸다.
- 럭키세븐·그림자 연사·예리한 시야·드레인·환영 쌍성·치명 투척·어벤저·심연 폭우·그림자 호흡·나선환·구미호 변신·미수옥·삼인 협공·천뢰옥·선인모드의 표시 순서, 전직 차수별 해금 순서를 따르는 액티브 `1~0/-` 기본 배치·정규화·슬롯 교환, `Shift`·`QWER`·`ASDF`·`XCV` 추가 키의 기본 별칭·빈 슬롯·배치·교환과 고정 `Z` 회수, active/passive 구분, 레벨·최대치·직업 잠금·SP 분배·레벨당 효과와 구미호 변신의 1초마다 최대 MP 1% 유지비·고갈 해제는 `src/game/skills/skill-rules.ts`의 순수 로직을 단일 기준으로 유지한다. 모바일 대표 액티브는 로그 럭키세븐·그림자 연사, 어쌔신 드레인·환영 쌍성, 허밋 어벤저·심연 폭우, 호카게 나선환·미수옥·삼인 협공·천뢰옥을 최대 4개 표시하며, 같은 4개 슬롯의 상대 순서를 전용 모바일 단축키 화면에서 편집한다.
- 전직 시험 정의·필수 레벨 10·30·60·120·수락·처치 진척·완료 보고는 `src/game/quests/job-advancement-quests.ts`의 순수 로직을 단일 기준으로 유지한다. 로그·어쌔신·허밋·호카게 시험은 모두 그림자 시험장 입구를 사용하고 각각 초록버섯 5·그림자 파수꾼 6·심연의 골렘 3·심연의 골렘 8체를 요구한다. 로그 시험일 때만 시험장의 기존 4개 몬스터 스폰을 초록버섯으로 치환하고, 처치 진척은 사망 애니메이션과 보상 확정 뒤에만 반영한다.
- `jobQuestTrackerText`는 초보자부터 허밋까지 Lv.10·30·60·120 달성, 레벨 구간별 추천 사냥터, 시험 수락·처치·완료 보고를 퀘스트 HUD의 다음 목표로 계속 표시하고 호카게에서 종료한다.
- 던전 원정 `깨어난 던전 회랑`의 적정 레벨 Lv.100~200·수락·폭열군주 이그니카르→월식현자 루나시온→원펀맨 순차 처치·완료 보고·1회 보상은 `src/game/quests/dungeon-boss-quest.ts`의 순수 로직을 단일 기준으로 유지하고, 보스 처치 진척은 사망 애니메이션과 보상 확정 뒤에만 반영한다.
- 레벨별 보스 다음 목표는 `src/game/quests/boss-progression-guide.ts`가 `DUNGEON_BOSS_QUEST`의 Lv.100·140·200·장소·이름을 재사용해 생성한다. Lv.100 미만에서는 숨기고, 수락·권장 레벨 달성·처치·보고 중 현재 순서를 한 줄로 표시하며 원정 진척을 건너뛰지 않는다.
- 폭열군주 이그니카르의 용광로 파편, 월식현자 루나시온의 월식 탄환, 원펀맨의 보통/진심 펀치 충격파는 `src/game/combat/boss-ranged-attack-rules.ts`의 사거리·수직 허용 범위·선딜·쿨다운·속도·피해·즉사·수명·색상·페이즈·투사체 수·전개각을 단일 기준으로 사용한다. 원펀맨은 체력 50% 초과에서 단일 충격파, 50% 이하에서 24도 범위의 3방향 충격파를 사용한다. 현재 비즉사 보스 원거리 피해는 이그니카르 650·루나시온 1,600이며, 보스 투사체와 선딜 타이머는 명중·만료·맵 정리·씬 종료에서 함께 해제한다.
- 몬스터의 레벨·HP·공격력·방어력·경험치·크기·즉사·보스 등급·드롭·넉백 면역은 `src/game/data/catalog.ts`, 방어 피해 감소와 원펀맨 전용 상층 투사체·구미호 할퀴기 피격 확장 132px 및 전방 근접 bounds는 `src/game/combat/monster-combat-rules.ts`, 공통 순찰·공격·피격·사망·재생성은 `src/game/entities/patrol-monster.ts`에서 관리한다. 일반 사냥터는 초록버섯굴 Lv.10~29의 초록버섯, 수정 개미굴 Lv.30~49의 수정굴 늑대, 시계태엽 탑 Lv.50~69의 망각된 기술자 좀비, 산호 신전 Lv.70~99의 산호 맹그로브, 잿불 광산 Lv.100~139의 잿불 광부 좀비, 달빛 마도서고 Lv.140~199의 월광 늑대, 무한의 결투장 Lv.200의 원펀맨 순으로 배치하고 그림자 시험장은 전직 시험용 원형 몬스터를 유지한다. 원펀맨은 HP 80,000,000·방어력 400을 사용하고 물리 밀림과 비치명 피격 경직·리코일을 무시한다.
- 점프 상승·정점·낙하 애니메이션 선택, 기본 수평 최고속도 260·구미호 변신 최고속도 312, 공중 방향 입력 시 해당 최고속도로 즉시 반전·무입력 시 관성 유지와 수평 이동 속도에 따른 보행 재생 배율·통과 자세 시작 프레임은 `src/game/entities/player-motion.ts`의 순수 규칙을 사용한다. 초보자 `player-v4.png`, 로그·어쌔신·허밋 `player-*-v4.png`, 호카게 `player-hokage-v5.png`는 2행을 왼발 접지·무릎 회수·오른발 접지·반대 무릎 회수의 지면 보행 4단계로 공유하고 물리 발 기준선을 유지한다. 초보자 v4는 낡은 갈색 귀덮개 두건·누더기·붕대·닳은 신발을, 호카게 v5는 금발·이마 보호대·볼 표식·검정/주황 닌자복을 사용하는 독자적인 SD 외형이며 v5는 v4의 인접 셀 오염만 제거한다.
- 드롭 테이블·회수 보상·가까운 드롭 판정은 `src/game/loot/loot-rules.ts`, 착지·소멸 예고 표현 규칙은 `src/game/loot/loot-presentation.ts`, 그림자 호흡·선인모드가 적용된 자연 회복·EXP·Lv.200 성장 상한·레벨업·AP 5·SP 3 지급·LUK 4/DEX 1 자동분배는 `src/game/progression/progression-rules.ts`의 순수 로직을 단일 기준으로 유지한다.
- 두아의 등록 기본값·정규화, 50,000 메소 멍푸치노 선물 조건, 추적 간격·최고속도 360·20 초과 이동속도 순항·점프 속도 700·직상단/장애물 점프·낙오 순간이동, 바닥에 착지한 드롭 중 최근접 목표 선택과 자동 회수 반경은 `src/game/pets/pet-rules.ts`의 순수 로직을 단일 기준으로 유지한다. 추적 간격 안에서는 플레이어의 현재 수평 속도를 이어받고, 플레이어가 멈추면 지면 마찰로 감속한 뒤 idle로 전환한다. 수동 `Z`와 펫 자동 회수는 `play-scene.ts`의 동일 보상 확정 경로를 사용해 중복 지급을 막는다.
- 포탈 전환과 전직·레벨업 연출의 타이밍·색상·입력 잠금·링·카메라 강조·스파크 수치는 `src/game/effects/milestone-effects.ts`를 단일 기준으로 유지하고, 링·배너·트윈·타이머는 현재 맵 수명에 묶는다.
- 호카게 시네마틱의 4종 아틀라스 프레임·나선환/변신/미수옥/삼인 협공/천뢰옥 5개 활성 순간의 길이·카메라 강도와 삼인 협공 5타 역할 타임라인은 `src/game/effects/hokage-cinematic-rules.ts`를 단일 기준으로 유지한다. 천뢰옥은 나선 차크라 프레임을 금빛 짧은 컷인으로 재사용한다. 시네마틱 타이머·트윈·동료 객체·physics pause는 종료·맵 정리·씬 shutdown에서 함께 해제한다.

### 저장, 성장과 경제

- 캐릭터는 최대 3개 슬롯에 저장한다. 1번 슬롯은 기존 `kerning-shadows.local-profile.v1` 키를 그대로 사용하고 2·3번은 `.slot-2`, `.slot-3` 접미 키, 현재 선택은 `.active-slot` 키를 사용한다. 각 슬롯의 프로필 스키마는 v16이며 성장·인벤토리·퀘스트·장비·맵 위치·두아 등록·추가 스킬 키를 서로 독립 저장한다. 신규 프로필은 Lv.9 초보자로 시작하며 2~12자 한글·영문·숫자 닉네임과 각 4~13·합계 25의 주사위 기본 능력치만 생성 경계에서 다시 검증해 저장한다. 기존 v1~v15 단일 프로필은 1번 슬롯에서 닉네임·레벨·직업·스탯·전직·기존 스킬·SP·인벤토리·표창 장비·11개 맵 위치와 v13 액티브 슬롯 순서를 보존하고, v15 이하는 기존 Shift/QWER/XCV 고정키를 추가 키 기본 별칭으로 보충한다. 누락·손상값은 `src/game/profile/local-profile.ts`에서 정규화하고 v10의 `finalboss` 진행은 새 `upperboss` 단계로 옮기며 구미호 변신 토글은 저장하지 않는다.
- 빈·손상 슬롯은 기본 캐릭터로 자동 복구하지 않고, 첫 생성 전에는 프로필·활성 슬롯 키를 쓰지 않는다. 생성 시 부스트를 선택한 슬롯만 Lv.120 호카게, 자동 배분 완료 AP, 전체 15개 스킬 Lv.20과 남은 SP 33으로 시작하며 다른 슬롯은 변경하지 않는다.
- 경험의 서 가격·구매·사용·Lv.200 경계는 `src/game/shop/shop-rules.ts`의 순수 로직을 단일 기준으로 유지하고, 구매와 사용 결과는 기존 성장·프로필 저장 경로로만 반영한다.
- 멍푸치노는 서적상 레오가 정확히 50,000 메소에 판매하는 펫 선물 소모품이며, 보유 중이거나 두아가 이미 등록된 슬롯에서는 중복 구매할 수 없다. 두아에게 줄 때만 1개를 소비하고 등록 상태와 함께 즉시 저장한다.
- 회복 물약의 HP·MP 최대치 50% 회복·상한·완전 회복 미소비·수량 차감은 `src/game/inventory/inventory-item-rules.ts`의 순수 로직을 단일 기준으로 유지한다. 인벤토리에서는 회복 물약과 경험의 서만 사용할 수 있고 재료·표창은 소비하지 않는다.
- 부활의 부적의 아이템 ID·1,000,000 메소 가격·최대 보유 1개·사망 상태 자동 소비·HP/MP 완전 회복은 `src/game/inventory/revival-charm-rules.ts`를 단일 기준으로 유지한다. 직접 사용 버튼은 없으며 부적 없이 사망하면 기존 720ms 뒤 커닝시티 완전 회복 귀환을 사용한다.
- 상점 판매 5등급과 보상 전용 6등급 `초대형 고드름`의 이름·가격·피해 배율·투사체 프레임·크기, 구매·장착·손상 저장값 정규화는 `src/game/equipment/throwing-star-rules.ts`의 순수 로직을 단일 기준으로 유지한다. 초대형 고드름은 상점에서 판매하지 않고 기존 빙결 프레임을 1.75배로 재사용하며 피해 배율은 2.0이다. 표창 피해 배율은 표창 계열 공격과 변신 전용 구미호 할퀴기에 적용한다.

### 오디오

- 기본 맵 BGM은 `src/game/maps/map-definitions.ts`의 `bgm`, 일반/보스 선택은 `src/game/audio/bgm-rules.ts`, 에셋 키·기본 게인·앞뒤 무음 제외 구간은 `src/game/assets/audio-assets.ts`, 같은 키의 재생 위치를 유지하는 중복 없는 무한 반복은 `src/game/audio/game-audio.ts`를 단일 기준으로 유지한다.
- 음향 설정은 캐릭터 프로필과 분리해 `src/game/settings/local-settings.ts`에 저장하고, 상단에 별도 조작을 노출하지 않는다. `ESC` 전체 메뉴의 환경설정에 배치한 접근 가능한 DOM 음소거·BGM/SFX range는 `src/game/ui/audio-settings-controls.ts`에서 관리한다.
- 신규·손상 설정의 기본 볼륨은 BGM 45%·SFX 38%로 효과음을 조금 낮게 두며, 사용자가 이미 저장한 볼륨은 그대로 복구한다.
- 런타임 BGM MP3 2종은 사용자 제공 원본 바이트를 보존하고, SFX PCM 12종은 외부 샘플 없이 `scripts/generate-audio-assets.mjs`로 재생성한다. 출처·권리 확인 상태·배포 조건은 `assets/audio/README.md`와 `ASSET_CREDITS.md`에 기록한다.

### HUD와 DOM UI

- 플레이 HUD의 조작 안내·퀘스트 알림·하단 게이지와 중간/상위/최종보스 HP 표시는 `src/game/ui/play-hud.ts`, 미니맵은 `src/game/scenes/play-scene.ts`, 세 상단 창의 접근 가능한 접기 버튼·이벤트·상태는 `src/game/ui/hud-window-controls.ts`에서 관리한다. 반투명도는 `src/game/ui/ui-theme.ts`의 `HUD_PANEL_ALPHA`를 단일 기준으로 사용하고 캔버스 `aria-label`과 `screen-state.ts` 상태 표식에도 같은 상태를 반영한다.
- 좌상단 미니맵은 현재 맵 이름과 권장 레벨 구간, `map-definitions.ts`의 월드·발판·줄 좌표를 축소 투영한 지형, 실시간 플레이어·NPC·포탈 위치 마커를 표시하고 게임 진입 때 펼침 상태로 복구한다. 축소 투영·클램프·패널 bounds는 `hud-layout-rules.ts`를 단일 기준으로 사용한다. 카메라는 월드 위쪽 y=0을 넘지 않으며 점프 중 상단 경계나 추적 오프셋을 변경하지 않는다.
- gameplay 9-slice 패널의 텍스트 안전 여백·실제 bounds 판정·보스 라벨 최소 간격·만/억/조/경 축약 표시·하단 HUD를 피하는 48px 월드 카메라 상승은 `src/game/ui/hud-layout-rules.ts`, 기본 공격·액티브 스킬의 플레이어 모션 잔상 개수·간격·수명·오프셋·알파·틴트는 `src/game/effects/attack-afterimage-rules.ts`를 단일 기준으로 유지한다. 럭키세븐 평행 쌍투척·그림자 연사 부채꼴·드레인 맥동·환영 쌍성 교차 파동의 스폰·속도·위상·잔상색은 `src/game/effects/skill-projectile-rules.ts`, 전직 차수 1→4에 따라 0→3 시전 링·0→3 타격 링·0→8 방사광과 카메라 강도를 높이는 규칙은 `src/game/effects/skill-spectacle-rules.ts`를 단일 기준으로 사용한다. HUD에는 축약값을 표시하되 캔버스 `aria-label`에는 HP·MP·메소·소지품 합계의 전체 값을 유지한다. 잔상·투사체·타격 연출 객체와 타이머는 현재 맵 수명에 묶는다.
- 하단 HUD는 y=596부터 124px를 사용하고 캐릭터·HP·MP·EXP·재화·성장 정보를 4px 이상 분리된 독립 경계 안에 배치한다. 액티브 11종은 `src/game/ui/play-hud.ts`에서 현재 프로필의 `1~0/-` 순서대로 38px 슬롯·34px 아이콘 한 줄로 표시하며 게이지 행과 겹치지 않는다. 직업 잠금은 흐리게, 활성 구미호 변신은 금색 테두리로 구분한다. `I` 인벤토리는 `src/game/ui/screen-overlay.ts`의 접근 가능한 24칸 DOM 모달로 제공하고, 선택한 소모품의 `사용하기` 버튼과 사용 직후 수량·선택·키보드 포커스를 갱신한다.
- 미니맵·퀘스트 알림·조작 안내는 은회색 프레임과 청회색 제목띠, 반투명 본문을 공유하고 우측 `−/+` DOM 버튼으로 각각 독립적으로 접고 펼친다. 버튼 중심은 펼침·접힘 상태별 제목 영역 중심선에 맞추고, 접힌 상태에서도 제목·접기 버튼을 유지하며 실제 Phaser 텍스트 bounds 안전 여백과 `aria-expanded` 상태를 함께 검증한다.
- 전체 메뉴·환경설정·스킬 단축키·인벤토리·능력치·스킬·상점·퀘스트 DOM 팝업은 제목 바, 숫자 탭 정렬, 본문 줄높이와 한글 단어 경계를 유지한다. `#screen-overlay`의 축소 가능한 Grid 트랙 안에서 게임 영역보다 커지지 않아야 하며, 플레이 중 외곽 여백은 14~28px로 제한한다. 내용이 넘치면 팝업 자체가 세로 스크롤되고 제목·닫기 조작부는 프레임 안에 고정된다. 1100px 이하에서는 능력치·장비를 한 열로, 760px 이하에서는 나머지 복수 열 콘텐츠를 한 열로 전환하며 수평 넘침을 허용하지 않는다. 게임 중 팝업이 열리거나 `data-hokage-cinematic`이 `none`이 아니면 HUD 접기 버튼을 숨긴다.
- 760px 이하 세로 화면의 `#app`과 `#screen-overlay`는 `100vw × 100dvh`와 safe-area를 사용한다. 모든 DOM 화면·팝업은 16:9 Canvas 위아래의 남는 영역을 포함한 전체 높이 모바일 시트가 되고, 입력은 iOS 확대를 막는 16px 이상, 버튼·링크는 44px 이상의 터치 영역을 유지한다. 인벤토리·스킬·스탯·상점·단축키의 다열 구성은 한 열 또는 4열 슬롯으로 재배치하고, 수평 스크롤 없이 패널 세로 스크롤과 고정 제목·닫기·확정 조작을 제공한다.
- `src/game/ui/mobile-game-controls.ts`는 터치 장치의 좌하단 좌우 이동·아래 하강 조합 조이스틱, 우하단 기본 공격·고정 줍기·고정 점프·별도 대화/이동 버튼·대표 스킬 4슬롯과 우상단 메뉴를 접근 가능한 DOM으로 제공한다. 대화는 NPC 근처, 이동은 포탈·줄 근처에서만 표시되며 점프는 항상 유지된다. 유틸리티 조작과 DOM 포커스 순서는 `대화/이동 → 줍기 → 점프 → 공격`이다. 조이스틱 아래 입력을 누른 채 점프하면 현재 단방향 발판을 하강한다. 조이스틱 방향은 `mobile-control-rules.ts`, 가상 입력 합성은 `InputController`를 사용하며 팝업·시네마틱 중에는 컨트롤을 숨기고 보유 입력을 해제한다.
- 원펀맨 처치 크레딧은 `src/game/ending/ending-credits-rules.ts`의 18초 길이·역할·개발자·레퍼런스 문구를 단일 기준으로 사용한다. `assets/ui/screens/ending-credits-v1.webp`는 텍스트 없는 독자 픽셀 배경으로만 번들링하고 실제 크레딧·계속 플레이 버튼은 접근 가능한 DOM으로 렌더링한다. 자동 종료·버튼·`ESC` 모두 입력과 Canvas 포커스를 복구하며 프로필에 일회성 표시 상태를 저장하지 않는다.
- 공통 픽셀 폰트, Phaser UI 깊이, 9-slice 규격과 테마별 에셋 키는 `src/game/ui/ui-theme.ts`를 단일 기준으로 사용한다.
- DOM 패널은 `ui-panel`과 `ui-panel--book|wood|metal` 클래스로 테마를 적용하고, 입력·버튼·대화 텍스트를 래스터 이미지에 포함하지 않는다.
- 능력치 분배와 표창 장비는 추가 스킬이 배치되지 않은 `S` 또는 `ESC` 전체 메뉴로 여는 접근 가능한 단일 DOM 모달에서 좌우로 함께 제공하고, 수동 분배·자동분배 옵션·장착 변경은 즉시 현재 프로필에 저장한다.
- 스킬 분배는 `K` 또는 `ESC` 전체 메뉴로 여는 접근 가능한 DOM 모달에서 4개 차수별 액티브·패시브로 제공한다. `ESC`의 전용 스킬 단축키 화면은 `1~0/-` 숫자/HUD 슬롯과 `Shift`·`QWER`·`ASDF`·`XCV` 추가 키를 함께 제공한다. 선택 배치와 같은 영역의 드래그 앤 드롭·`Alt+←/→` 교환, 추가 키 비우기를 지원하며 빈 `S` 추가 슬롯은 스탯/장비 기본 동작을 사용한다. `Z`는 화면에 배치 슬롯을 제공하지 않는 고정 회수 키다. 직업별 잠금·현재/최대 레벨·남은 SP·효과 설명·분배와 슬롯 변경 결과는 즉시 현재 프로필에 저장하고 입력·HUD에도 반영한다. 패시브는 단축키를 차지하지 않고 습득 즉시 자동 적용한다.
- 표창 장비는 능력치·장비 금속 패널의 장비 구역에서 소유·미소유·장착 중 상태를 제공하며, 서적상 레오의 목재 상점에서 구매한 결과와 장착 변경을 즉시 현재 프로필에 저장한다. 기본 추가 키 `E`는 심연 폭우에 배치된다.
- 커닝시티의 `일용직 개발자 임상진`은 `src/game/data/catalog.ts`의 채널 URL과 `map-definitions.ts`의 위치를 단일 기준으로 사용한다. `↑` 대화의 실제 `<a>` `구독하기`는 새 탭에서 채널을 열고 `noopener noreferrer`를 적용하며, 구독 완료를 프로필에 저장하거나 임의 추정하지 않는다.

### 그래픽, 스프라이트와 에셋

- 액티브·패시브 15종의 128×128 전용 런타임 아이콘은 `assets/ui/skills/`, built-in imagegen 크로마 원본은 `assets/ui/source/skill-*-v1-chroma.png`에 보존하고 `src/game/assets/skill-icon-assets.ts`의 URL을 단일 기준으로 Vite 번들링한다. 액티브 11종은 Phaser HUD용 텍스처로 함께 프리로드하고, `K` 스킬창에서는 CSS 배경이 아닌 접근성 제외된 실제 `<img src>`로 렌더링해 128×128 자연 크기 로드를 E2E로 검증한다. 호카게 4종 배경은 `assets/ui/cinematics/hokage-cinematics-v1.webp`의 2×2 아틀라스로만 번들링한다.
- DOM UI 9-slice 원본은 `assets/ui/panels/`의 96×96 SVG와 16px 고정 슬라이스를 사용한다. 플레이 HUD는 built-in imagegen 원본 `assets/ui/source/hud-metal-panel-v1-source.png`에서 만든 96×96 `hud-metal-panel-v1.webp` 프레임과 `hud-metal-surface-v1.webp` 내부 표면을 사용하며 텍스트·아이콘은 이미지에 포함하지 않는다. 런타임 폰트는 `assets/ui/fonts/`에서 번들링해 외부 네트워크에 의존하지 않는다.
- 런타임 스프라이트 시트 24종은 기준 PNG와 픽셀 동일한 lossless WebP를 사용하고 모든 16개 프레임의 시각 중심을 `assets/sprites/sprite-manifest.json`의 `frameOrigins`로 보정한다. `npm run assets:webp`가 핵심 시트 24종과 스킬 아이콘 15종을 재생성하며 `dist/`에는 WebP만 포함한다. 물리 바디가 있는 플레이어·몬스터·투사체·드롭·펫은 `src/game/entities/sprite-layout.ts`의 공통 offset 계산으로 중심·바닥을 월드 좌표에 고정한다. 인접 셀에서 넘어온 분리 픽셀은 `scripts/clean-player-frame-boundaries.mjs`로 제거만 수행하고, 원본 대비 추가·재색상 없이 지정 픽셀만 사라지는지 검증한다. 24종·384셀의 크기·점유·코너·크로마와 플레이어/지상 엔티티 기준선은 `src/game/assets/sprite-pixel-alignment.test.ts`로 검사하며, 몬스터 9종·144셀은 128px 내부 경계 양쪽에 가시 픽셀이 동시에 연결된 지점도 0건이어야 한다. 좀비·늑대·고대 나무정령은 매니페스트의 `compactMonster` 좌표·속도를 공통 4행 애니메이션으로 확장해 번들 예산을 지킨다. 심연의 골렘 배포본은 `abyss-golem-v3.webp`를 사용한다. `throwingStars` 시트는 상점 5등급에 3·3·3·3·4프레임을 사용하고, 보상 전용 6등급은 3등급 빙결 프레임 `[6,7,8]`을 재사용한다. 호카게의 선인·구미호 오라와 협공 동료 `hokageAllies`는 별도 물리 바디 없이 화면/발 기준선을 따른다. `hokage-allies-v2.webp`의 1·2행은 rival 레퍼런스, 3·4행은 striker 레퍼런스를 SD화하며 기존 5타 역할 타임라인을 유지한다. `duaPet` v3는 sit/idle·접지 run·fetch·happy 4행을 사용하고 run은 대각발 접지와 회수를 교차한다. `emberWarden`·`eclipseArchivist`·`onePunchMan`은 보스 공격 행을, `dungeonScout`는 talk·brief·approve 행을, `gameDeveloper`는 talk·code·thanks 행을 사용한다.

### 입력과 배포 경계

- 브라우저 기본 단축키는 게임 캔버스가 포커스된 동안에만 차단한다.
- 점프 입력은 공격 입력보다 먼저 독립 처리한다. 공중 공격은 현재 수직 속도와 무입력 수평 관성을 보존하되, 동작 잠금 중에도 `←/→` 입력에 따라 즉시 공중 방향을 바꿀 수 있다. `↓+Alt`는 `src/game/entities/platform-drop-rules.ts`의 발 겹침·허용 시간·낙하 속도 규칙으로 현재 단방향 발판 하나만 통과하며 고정 지면에서는 점프하지 않는다. 줄 근처에서는 `↑/↓`로 붙어 오르내리고 `←/→` 또는 `Alt`로 이탈한다.
- 외부 시각 자료가 제공되더라도 복제용 에셋으로 저장하지 않고 레이아웃·색감·동작을 언어화한 독자 제작 규격만 남긴다.
- 시각 회귀는 1280×720 실제 런타임 캡처와 `VISUAL_QA.md` 체크리스트로 기록하며 외부 레퍼런스 이미지 디렉터리는 저장소와 릴리스 `dist/`에 포함하지 않는다.
- P7 자동화·지원 브라우저·장시간 성능 결과와 잔여 릴리스 게이트는 `RELEASE_QA.md`를 단일 기준으로 기록한다.
- `dist/`는 `THIRD_PARTY_NOTICES.txt`, `ASSET_CREDITS.md`, Galmuri OFL, Phaser MIT 고지를 포함하고 `scripts/audit-dist.mjs`의 경계·크기 예산을 통과해야 한다.
- 6개 던전과 인내의 숲 배경을 포함한 `dist/`의 BGM 제외 총량 상한은 8,000,000 bytes이며, 런타임 BGM MP3 2종은 산출물에 포함하되 총량에서 제외하고 JavaScript·gzip 개별 상한은 기존 값을 유지한다.

## 검증 및 커밋

- 중요 단계가 끝날 때마다 `npm run typecheck`, `npm test`, `npm run build`를 실행한다.
- P7 변경은 `npm run release:check`로 단위 테스트, 프로덕션 빌드, Chromium·Firefox·WebKit E2E, `dist/` 감사를 한 번에 검증한다.
- E2E가 플레이 화면에 키보드 입력을 보낼 때는 `data-active-scene="gameplay"`와 캔버스 포커스를 먼저 확인한다.
- 화면 흐름이나 입력이 바뀌면 PC 브라우저에서 캔버스 포커스와 콘솔 오류까지 스모크 검증한다.
- 오디오가 바뀌면 첫 사용자 입력 전후 잠금 해제, 화면·맵별 단일 BGM, 음소거·BGM/SFX 볼륨 저장 복구, 주요 이벤트 SFX 시점을 PC 브라우저에서 확인한다.
- 커밋 전 `git status`와 변경 내용을 확인한다.
- `ROADMAP.md`의 P 단계가 완료될 때마다 의미가 분명한 커밋을 만든다.
- P7 최종 완료 커밋은 `RELEASE_QA.md`의 실제 지원 브라우저 6개 행과 30분 성능 게이트가 모두 Pass인 경우에만 만든다.
