# Release QA

P7부터 P80까지의 자동화 결과와 실제 지원 브라우저 수동 검증 상태를 분리해 기록한다.
단계별 수치는 당시 회귀 근거이며 최종 자동화 기준은 P80이다. 자동화 엔진 통과를
실제 Chrome·Edge·Firefox·Safari의 사용자 입력과 오디오 출력 검증으로 대체하지 않는다.

## 최종 P80 자동화 기준 — 2026-07-30

- 46파일·360개 단위 테스트
- Chromium·Firefox·WebKit 모바일 E2E 9/9
- 타입체크·Terser 프로덕션 빌드·`dist/` 감사 통과
- `dist/` 86파일·9,783,295 bytes, BGM 2,916,872 bytes 제외 적용값 6,866,423 bytes
- JavaScript 1,493,067 bytes, gzip 400,510 bytes
- 빈 슬롯 탭 뒤 닉네임 입력 포커스와 독립·결합 한글 자모 검증을 3엔진 터치 모드에서 확인
- 핵심 스프라이트 24종·스킬 아이콘 15종은 기준 PNG와 픽셀 동일한 lossless WebP이며
  `dist/` PNG 0건
- 로그인·일반 맵의 보스 MP3 요청 0건, 최초 살아 있는 보스 조우 요청 1건

## 초기 P7 기준 환경

- 실행일: 2026-07-18
- 호스트: macOS 26.5.2 (Apple Silicon)
- Node.js: 25.9.0
- npm: 11.12.1
- 기준 산출물: `npm run build`가 만든 상대 경로 기반 `dist/`
- 자동화: Playwright 1.61.1, 프로덕션 preview `http://127.0.0.1:4174/`

## 자동화 결과

| 게이트 | 결과 | 검증 내용 |
| --- | --- | --- |
| `npm run typecheck` | Pass | 앱·Vite·Playwright 설정과 E2E TypeScript 검사 |
| `npm test` | Pass | 3단계 전직·스킬, AP 수동/자동 분배, 전투, 성장, 회복, 드롭, 저장, 입력, 에셋·맵 규칙 |
| Playwright Chromium | Pass | 필수 루프 2회, 스탯 저장, 2·3차 전직, 자동분배 레벨업, 뷰포트 4종 |
| Playwright Firefox | Pass | 필수 루프 2회, 스탯 저장, 2·3차 전직, 자동분배 레벨업, 뷰포트 4종 |
| Playwright WebKit | Pass | 필수 루프 2회, 스탯 저장, 2·3차 전직, 자동분배 레벨업, 뷰포트 4종 |
| Codex 인앱 브라우저 | Pass | 프로덕션 로그인 → 캐릭터 선택 → 커닝시티 → 능력치 모달, 1280×720·800×600 경계, Canvas 포커스, 콘솔 오류 0건 |
| `npm run audit:dist` | Pass | 라이선스·크레딧, 금지 에셋, 상대 URL, 번들 예산 |

P8 최종 `npm run release:check` 결과는 Vitest 18파일·100테스트, Playwright 15테스트
(3엔진×필수 루프·스탯·상위 전직·레벨업·뷰포트 시나리오), `dist/` 40파일·5,635,018
bytes다. JavaScript는 1,290,304 bytes, gzip 합계는 343,750 bytes다.

필수 루프는 매 실행마다 새 프로필로 다음 순서를 수행한다.

`로그인 → 캐릭터 선택 → 포탈 연타 → 다크로드 로그 전직 → 초록버섯굴 → 럭키세븐 전투 →
메소·버섯 갓 회수 → 커닝시티 귀환 → 새로고침 → 직업·EXP·메소·인벤토리 복구`

P8 성장 확장 시나리오는 AP 5의 수동 LUK 분배와 자동분배 전환·저장 복구, Lv.30 로그의
어쌔신 전직·드레인, Lv.60 어쌔신의 허밋 전직·어벤저, EXP 임계값 직전 자동분배
레벨업·강화 연출 완료와 임시 효과 정리를 엔진별로 추가 검증한다.

### P9 전직 시험·그림자 던전 추가 검증

- 최종 `npm run release:check`가 단위 테스트, 타입 검사, 프로덕션 빌드, 3엔진 E2E,
  `dist/` 감사를 한 실행에서 모두 통과했다.
- Vitest는 19파일·108테스트, Playwright는 18개 시나리오가 통과했다. 각 엔진에서 필수 루프
  2회, AP 분배·저장, 2·3차 전직, 그림자 시험장 중대형 몬스터, 레벨업, 뷰포트 4종을 검증했다.
- 첫 재개 실행에서 WebKit 뷰포트 검사가 게임플레이 입력 준비 전에 `S`를 보내 실패했고 단독
  실행에서도 재현됐다. 활성 게임플레이 상태와 캔버스 포커스를 기다리도록 보강한 뒤 WebKit
  단독 검사와 전체 18개 시나리오가 모두 통과했다.
- 최종 감사 산출물은 47파일·6,763,058 bytes다. JavaScript는 1,309,393 bytes,
  gzip 합계는 347,883 bytes로 모든 예산을 지켰다.
- 1280×720 인앱 브라우저에서 로그인·캐릭터 선택·커닝시티를 스모크 검증하고, 결정적
  Chromium 캡처에서 시험장 입구의 로그·파수꾼과 우측의 어쌔신·대형 골렘을 확인했다.
  두 경로 모두 콘솔 오류가 없었다.

P9 필수 흐름은 `시험 수락 → 목표 몬스터 처치 → 저장 진척 → 다크로드 완료 보고 → 외형·표창·
스킬 교체`를 직업별 조건에 맞게 검증한다.

### P10 스킬 메뉴·SP 성장 추가 검증

- `npm run typecheck`, Vitest 20파일·122테스트와 프로덕션 빌드가 통과했다.
- 레벨업 SP 3 지급, `K` 메뉴, 럭키세븐 분배·저장 복구 시나리오가 Chromium·Firefox·
  WebKit에서 통과했다.
- 같은 시나리오에서 자동 슬롯 `1`이 럭키세븐 공격을 발동하며, 입력 단위 테스트에서
  상단 숫자열·숫자패드 `1/2/3`이 세 스킬 순서에 일치하는지 검증한다.
- 1280×720과 800×600 인앱 브라우저에서 차수 탭·표창 아이콘·상세 설명·상단 닫기 배치와
  SP 3→2, 럭키세븐 0→1 갱신을 확인했고 콘솔 warning/error는 0건이었다.
- `dist/` 감사 결과는 47파일·6,775,100 bytes, JavaScript 1,317,840 bytes,
  gzip 350,235 bytes로 모든 예산을 통과했다.

### P11 차수별 패시브·전용 아이콘 추가 검증

- Vitest 20파일·131테스트가 예리한 시야 투사체 수명 `900→1260ms`, 치명 투척의 결정적
  최대 20%·피해 150%, 그림자 호흡 회복 주기 `1600→960ms`, 잠금·SP·정규화·v4→v5
  마이그레이션 경계를 검증하고 통과했다.
- Playwright 18개 시나리오를 엔진별로 분리 재실행해 Chromium 6/6, Firefox 6/6,
  WebKit 6/6이 통과했다. 스킬 성장 시나리오는 럭키세븐·예리한 시야 아이콘 URL,
  액티브·패시브 분배, 미래 차수 잠금, 새로고침 뒤 여섯 레벨과 SP 복구를 포함한다.
- 입력 단위 테스트에서 패시브가 숫자열·숫자패드 `4`를 점유하지 않고 기존 액티브
  `1/2/3`만 유지됨을 확인했다.
- 1280×720 인앱 브라우저에서 3개 차수 탭·6개 전용 아이콘·차수별 2행·상세 효과를 확인했고,
  warning/error는 0건이었다. 800×600 경계는 3엔진 뷰포트 자동화로 통과했다.
- 최종 `npm run release:check` 단일 실행에서 18개 E2E가 4.0분에 통과했고 종료 코드 0을 확인했다.
- `dist/` 감사 결과는 53파일·6,853,224 bytes, JavaScript 1,322,165 bytes,
  gzip 351,618 bytes로 모든 예산과 생성 원본·프롬프트 제외 규칙을 통과했다.

### P12 호카게 4차 전직·테스트 상점 추가 검증

- 최종 `npm run release:check` 단일 실행에서 Vitest 21파일·154테스트와 Playwright 24개
  시나리오가 4.6분에 통과했다. E2E는 3엔진의 필수 루프·기존 성장 회귀에 경험의 서
  구매/사용과 호카게 전직·선인 오라·구미호 토글·할퀴기·미수옥 전환을 추가한다.
- 필수 루프는 첫 초록버섯의 보장 드롭을 12초 소멸 전에 회수하고, 최대 피해 편차에서도
  처치가 확정되도록 공격 재시도와 상태 기반 대기를 보강했다.
- 1280×720 인앱 브라우저에서 서적상 목재 패널, 호카게 idle, 선인·구미호 오라 동시 정렬,
  나선환·미수옥 효과와 HUD 경계를 확인했다.
- `dist/` 감사 결과는 57파일·6,977,207 bytes, JavaScript 1,349,669 bytes,
  gzip 364,075 bytes로 7MB와 JavaScript 예산을 통과했다.

### P13 표창 장비·P14 호카게 시네마틱 추가 검증

- 최종 `npm run release:check` 단일 실행에서 Vitest 23파일·175테스트와 Playwright 30개
  시나리오가 4.9분에 통과했다. 3엔진에서 표창 구매·장착·투사체 저장 복구와
  나선환·구미호·미수옥·삼인 협공 연속 재생을 검증했다.
- 삼인 협공은 `N/7` 입력에서 520/690/860/1030/1200ms의 정확한 5타와 종료 후
  `data-hokage-cinematic=none`, 시네마틱 객체 0개를 요구한다.
- 1280×720 Chromium 실캡처에서 청백 나선환과 청·금·적 협공 궤적, 시온·하나의 양쪽 진입,
  3 HIT 중간 프레임을 확인했다. 표창 상점 6행 스크롤과 740px 장비창도 경계를 통과했다.
- `dist/` 감사 결과는 60파일·6,984,309 bytes, JavaScript 1,365,842 bytes,
  gzip 368,194 bytes로 기존 7MB 예산과 참고·생성 원본 제외 규칙을 통과했다.

### P15 던전 회랑·HUD·인벤토리·공중 공격 추가 검증

- 최종 `npm run release:check` 단일 실행에서 Vitest 23파일·180테스트와 Playwright 33개
  시나리오가 5.0분에 통과했다. Chromium·Firefox·WebKit에서 기존 필수 루프와 성장 회귀,
  5개 던전의 진입·몬스터 구성, 24칸 인벤토리, 공중 공격의 X/Y 궤도 보존을 검증했다.
- 1280×720 인앱 브라우저에서 하단 HUD의 캐릭터·HP·MP·EXP·재화·성장 6개 박스와
  Galmuri11 한글 텍스트, 상단 조작 패널, 인벤토리 24칸·상세 패널의 경계가 겹치지 않음을
  확인했다. 캔버스 포커스 복구와 콘솔 warning/error 0건도 함께 확인했다.
- 신규 배경 원본과 프롬프트는 릴리스에서 제외되고 WebP 5종·충돌 정렬 SVG 5종만 포함된다.
  `dist/` 감사 결과는 70파일·7,724,012 bytes, JavaScript 1,379,469 bytes,
  gzip 371,254 bytes로 8MB와 JavaScript 예산을 통과했다.
- 반복 플레이의 두 번째 실행 전 중복 탐색을 제거해 의도적 페이지 전환이 아직 시작 중인
  에셋 요청을 취소하지 않도록 했으며, 실패한 네트워크 요청 0건 기준을 그대로 유지했다.

### P16 던전 보스·원정 퀘스트 추가 검증

- Vitest 24파일·188테스트에서 보스별 최소 확정 드롭, 퀘스트 순차 진척·중복 무시·1회
  보상, 프로필 v9→v10 이전과 손상 데이터 복구, 시트 18종의 매니페스트 계약을 검증했다.
- Playwright는 커닝시티 세라의 수락, 퀘스트 알림 `aria-label`, 잿불 수호자·월식의 기록자
  보스 등급/HP 상태, 완료 보고 7,500메소·경험의 서 2권과 재접속 저장을 검증한다.
- 1280×720 인앱 브라우저에서 세라 NPC 라벨, 기존 상단 3개 패널, 하단 HUD 경계를
  확인했고 warning/error 0건이었다. Chromium 시나리오에서 인접 치유사가 먼저 선택되던
  결함을 발견해 상호작용 범위 안의 가장 가까운 NPC를 선택하도록 수정했다.
- 최종 `npm run release:check`는 5.5분에 3엔진 Playwright 36개와 73파일·7,965,774-byte
  `dist/` 감사를 통과했다. JavaScript는 1,393,759 bytes·gzip 374,779 bytes이며 생성
  원본과 프롬프트는 배포 번들에서 제외한다.
- 병렬 엔진 부하에서 기존 필수 루프의 재생성 사격 방향과 순간 FPS 샘플이 흔들리던 문제는
  매 처치 전 좌→우 결정적 이동과 제한 시간 내 FPS 50 회복 확인으로 안정화했다.

### P17 퀘스트 적정 레벨·보스 원거리 스킬 추가 검증

- Vitest 25파일·191테스트에서 전직 시험 적정 레벨 10·30·60·120, 던전 원정
  `Lv.80~120`, 보스별 원거리 정의·사거리 판정과 조준 벡터의 일정한 합성 속도를 검증했다.
- Playwright는 전직·원정 대화창의 적정 레벨, 활성 퀘스트 알림, 잿불 수호자의
  `furnaceShard`와 월식의 기록자의 `eclipseBolt` 명중, 실제 플레이어 HP 감소를 검증한다.
- 1280×720 인앱 브라우저에서 Galmuri11 HUD와 하단 독립 경계를 확인했고 warning/error
  0건이었다. 퀘스트 알림 패널 높이를 늘려 적정 레벨이 추가된 두 목표도 시스템 문구와
  겹치지 않게 정리했다.
- 최종 `npm run release:check`는 3엔진 Playwright 36개와 73파일·7,970,769-byte `dist/`
  감사를 통과했다. JavaScript는 1,398,575 bytes·gzip 376,163 bytes이며 기존 전투
  이펙트를 재사용해 생성 원본이나 새 래스터를 추가하지 않았다.

### P18 플레이어 지면 보행 주기 추가 검증

- Vitest 25파일·193테스트에서 수평 속도 `0·130·260·520`에 대한 보행 재생 배율
  `0.78·1.05·1.32·1.32`와 다섯 직업 v2 시트의 walk 프레임 `4·5·6·7` 계약을 검증했다.
- Playwright는 캔버스 포커스 뒤 이동 키를 누른 채 현재 walk 단계 1·2·3·4가 모두 관측되는지
  검사하고, 같은 시나리오에서 점프+공격 뒤 수직·수평 모멘텀이 유지되는 기존 회귀를 반복한다.
- 1280×720 인앱 브라우저에서 새 로그 보행 시트와 기존 HUD 공존을 확인했고 warning/error
  0건이었다. 다섯 시트는 투명 모서리와 16개 셀 점유 검사를 통과했다.
- 최종 `npm run release:check`는 3엔진 Playwright 36개와 73파일·7,687,254-byte `dist/`
  감사를 통과했다. JavaScript는 1,399,124 bytes·gzip 376,357 bytes이며 v1 시트와 생성
  크로마 원본은 번들에서 제외한다.

### P19 플레이어 무릎 회수 자세 추가 검증

- Vitest 25파일·193테스트에서 다섯 직업 v3 경로, walk `4·5·6·7`, animation-local 시작
  frame `1`, 속도 배율과 기존 16프레임 원점 계약을 검증했다.
- v2와 v3의 비대상 14셀을 ImageMagick RGBA AE로 비교해 다섯 시트 모두 `0`을 확인했다.
  런타임 PNG는 512×512 PaletteAlpha 256색이며 생성 크로마 원본은 번들에서 제외한다.
- 1280×720 인앱 브라우저에서 로그인→캐릭터 선택→플레이 진입, 캔버스 포커스, 좌우 입력의
  위치 변화와 warning/error 0건을 확인했다.
- 변경된 보행·점프 공격 시나리오는 Chromium·Firefox·WebKit 3/3으로 통과했다. 전체 실행에서
  간헐 실패한 기존 세라/보스 원거리 공격 시나리오도 세 엔진 분리 재실행에서 3/3 통과했다.
- 첫 `npm run release:check`는 기존 보스 시나리오 1건의 Chromium 간헐 실패로 35/36이었다.
  재실행에서도 Firefox의 같은 시나리오가 간헐 실패해 전체 동시 실행은 잔여 flaky gate로
  유지한다. P19 보행 시나리오의 WebKit 30ms 고정 대기 단언은 제거해 상태 관측을 결정적으로
  유지했다.
- 프로덕션 빌드와 `npm run audit:dist`는 통과했다. `dist/`는 73파일·7,680,373 bytes,
  JavaScript 1,399,153 bytes·gzip 376,372 bytes로 기존 8MB 예산 안이다.

### P20 호카게 레퍼런스 기반 SD 외형 추가 검증

- `player-hokage-v4.png`는 512×512 PaletteAlpha 256색, 투명 모서리, 16/16셀 점유를
  통과했다. 1254×1254 크로마 보존본의 네 모서리는 정확히 `#ff00ff`다.
- Vitest 25파일·193테스트에서 호카게 v4 Vite/매니페스트 경로, 16프레임 원점, 기존 walk
  `4·5·6·7`과 공격 프레임 계약을 검증했다.
- 1280×720 인앱 브라우저에서 v4의 Vite URL import와 Phaser 이미지 요청을 모두 확인했고
  warning/error는 0건이었다.
- 호카게 전직·선인 오라·구미호 전환과 호카게 시네마틱·삼인 협공 집중 E2E는 Chromium·
  Firefox·WebKit에서 6/6 통과했다.
- `npm run build`와 `npm run audit:dist`는 통과했다. `dist/`는 73파일·7,678,297 bytes,
  JavaScript 1,399,176 bytes·gzip 376,380 bytes로 기존 8MB 예산 안이다.

### P21 삼인 협공 동료 레퍼런스 기반 SD 외형 추가 검증

- `hokage-allies-v2.png`는 512×512 PaletteAlpha 256색, 투명 모서리, 16/16셀 점유를
  통과했다. 1254×1254 크로마 보존본의 네 모서리는 정확히 `#ff00ff`다.
- 매니페스트와 Vite URL import는 v2 경로를 사용하고, 기존 rival/striker 역할별 one-shot
  애니메이션 프레임과 screen-space 중심 원점은 유지한다.
- Vitest 25파일·193테스트와 삼인 협공 시네마틱 집중 E2E가 Chromium·Firefox·WebKit에서
  3/3 통과했다. 정확한 5타, 입력/physics 복구, 종료 후 시네마틱 객체 0개를 유지한다.
- `npm run build`와 `npm run audit:dist`는 통과했다. `dist/`는 73파일·7,680,713 bytes,
  JavaScript 1,399,176 bytes·gzip 376,381 bytes로 기존 8MB 예산 안이다.

### P22 거대 최종보스·무한의 결투장 추가 검증

- `one-punch-man-v1.png`는 512×512 PaletteAlpha 256색, 투명 모서리, 16/16셀 점유와
  셀 경계 안쪽 재패킹을 통과했다. 1280×720 캡처에서 망토·강펀치 효과 분리 없이 플레이어보다
  두 배 이상 큰 전신과 100,000 HP 최종보스 HUD가 표시된다.
- Vitest 25파일·195테스트에서 19종 매니페스트, 10개 맵 위치 저장, v10 `finalboss`→v11
  `upperboss` 이전, 3단계 원정, 원펀맨 즉사 피해와 전용 맵 단일 몬스터 계약을 검증했다.
- 원정 집중 E2E는 Chromium·Firefox·WebKit 3/3으로 통과했다. 세 고유 이름과 보스 등급,
  원거리 스킬, 원펀맨 HP 0·커닝시티 부활, 1회 보상을 확인했다.
- 인앱 브라우저 로그인·캐릭터 선택·플레이 스모크에서 warning/error는 0건이었다.
- `npm run build`와 `npm run audit:dist`는 통과했다. `dist/`는 75파일·7,793,912 bytes,
  JavaScript 1,408,186 bytes·gzip 377,991 bytes로 기존 8MB 예산 안이다.

### P23 결투장 수직 회피·발판 하강 추가 검증

- Vitest 26파일·198테스트에서 `ArrowDown` 액션, 발 겹침·단방향 판정, 결투장 5개 회피
  발판과 SVG 충돌 정렬을 검증했다.
- 던전 순회·`↓+Alt` 하강·보스 원정 집중 E2E는 Chromium·Firefox·WebKit 9/9로 통과했다.
  현재 단방향 발판 통과, 아래 지면 착지, 원펀맨 단일 생성·즉사·부활 흐름을 함께 확인했다.
- 1280×720 인앱 브라우저에서 다층 SVG와 HUD `↓+ALT 하강` 문구를 확인했고
  warning/error는 0건이었다.
- `npm run build`와 `npm run audit:dist`는 통과했다. `dist/`는 76파일·7,798,616 bytes,
  JavaScript 1,405,153 bytes·gzip 377,903 bytes로 기존 8MB 예산 안이다.

### P24 HUD 안전 여백·공격 모션 잔상·허름한 초보자 추가 검증

- Vitest 28파일·203테스트에서 19종 매니페스트의 초보자 v4 연결, HUD 안전 영역의 경계 포함·
  침범 판정, 8종 공격별 2~5개 잔상·120~220ms 수명과 방향 오프셋을 검증했다.
- HUD·공중 기본 공격·보스 원정·나선환 집중 시나리오는 Chromium·Firefox·WebKit의 9개
  엔진 조합에서 최종 통과했다. 최초 병렬 실행에서 Firefox 점프 입력과 Chromium 시네마틱
  종료가 부하로 지연된 2건은 각각 단일 엔진·단일 워커 재실행으로 통과했다.
- 1280×720 인앱 브라우저에서 미니맵·조작·하단 HUD 실제 bounds 위반 0건,
  `basicAttack/2` 청백 실루엣 잔상과 warning/error 0건을 확인했다. 퀘스트와 중간·상위·
  최종보스 HUD도 3엔진 E2E에서 `data-hud-padding-violations=0`을 유지했다.
- `player-v4.png`는 512×512 PaletteAlpha 256색·36,898B, 투명 모서리와 16/16셀 점유를
  통과했다. 사용자 참고와 생성 크로마 원본은 `dist/`에서 제외했다.
- `npm run build`와 `npm run audit:dist`는 통과했다. `dist/`는 76파일·7,793,807 bytes,
  JavaScript 1,409,362 bytes·gzip 379,328 bytes로 기존 8MB 예산 안이다.

### P26 스프라이트 셀 경계·HUD 사용성 전수 감사 추가 검증

- `npm run release:check` 한 실행에서 Vitest 30파일·212테스트, 타입체크, 프로덕션 빌드,
  Chromium·Firefox·WebKit E2E 42/42와 `dist/` 감사를 모두 통과했다.
- 스프라이트 픽셀 테스트는 런타임 19시트·304셀의 크기·점유·코너·크로마와 플레이어 5종
  idle/walk 40프레임, 몬스터·보스·NPC 9종 144프레임의 실제 픽셀 기준선을 검증한다.
- 로그·어쌔신·허밋·호카게, 심연의 골렘, 이그니카르, 루나시온 정리본은 이전 버전 대비
  지정된 인접 셀 오염 픽셀만 제거했고 픽셀 추가·재색상은 모두 0건이다.
- 최대 safe integer HP·MP·메소·소지품 5종, 두 퀘스트 동시 표시, 월식현자 루나시온 HUD와
  가장 긴 맵 이름을 3엔진에서 검사해 `data-hud-padding-violations=0`, 위반 항목 빈값을
  유지했다. 1280×720 인앱 브라우저 플레이·인벤토리 스모크도 warning/error 0건이었다.
- 첫 전체 실행은 Chromium 보스 원정 이동 헬퍼가 회피 점프로 단방향 발판에 오른 뒤 몬스터
  넉백을 반복해 41/42로 끝났다. 발판 위 보정에서 기존 `↓+Alt` 하강을 사용하도록 수정하고
  실패 시나리오 단독 통과 후 전체 42개를 재실행해 플래키를 해소했다.
- 최종 `dist/`는 76파일·7,794,679 bytes, JavaScript 1,410,719 bytes·gzip 379,895 bytes로
  전체·JavaScript·gzip 예산을 모두 지켰다.

### P27 몬스터 스프라이트 내부 셀 연결 추가 검증

- 몬스터 6종·96셀의 128px 수직·수평 내부 경계에서 양쪽 가시 알파 픽셀이 동시에 이어지는
  지점을 전수 검사한다. 초록버섯·그림자 파수꾼·이그니카르·루나시온·원펀맨은 최초부터
  0건이었고, 심연의 골렘 v2에서만 수평 1·수직 8곳이 확인됐다.
- 심연의 골렘 v3는 해당 연결 컴포넌트 24픽셀만 추가 제거했다. ImageMagick 전수 비교는
  변경 24픽셀, 알파 추가 0건이며 정리 스크립트 특성상 비투명 픽셀 재색상은 없다.
- Chromium에서 여섯 던전 맵 로드 시나리오 1/1, 일반 몬스터 반복 전투와 세 보스 원정
  시나리오 2/2를 통과했다. 각 시나리오의 콘솔 error·pageerror·실패 네트워크 요청은 0건이다.
- Vitest 30파일·213테스트, 타입체크와 프로덕션 빌드가 통과했다. 최종 `dist/`는 76파일·
  7,794,856 bytes, JavaScript 1,410,719 bytes·gzip 379,899 bytes로 모든 예산을 지켰다.

### P28 차수별 신규 액티브 스킬 추가 검증

- `npm run release:check` 한 실행에서 Vitest 30파일·226테스트, 타입체크, 프로덕션 빌드,
  Chromium·Firefox·WebKit E2E 45/45와 `dist/` 감사를 모두 통과했다.
- 전직별 잠금, MP `16·24·42·70`, 타수 `3·2·3·2`, 최대 관통 `1·2·4·5`, 레벨당 피해
  `+1·+2·+3·+5`, 표창/차크라 계열 경계와 200ms 공격 모션 안의 릴리스 타이밍을 순수
  단위 테스트로 검증했다.
- 3엔진에서 전직 차수별 해금 순서의 `1~0·-` 슬롯, 숫자패드·Q/W/E/R 보조키를 실제
  캔버스 포커스 뒤 발동하고 각 전투 이벤트를 확인했다. 스킬창 15개 아이콘은 128×128
  자연 크기로 로드되며 콘솔 error·pageerror·실패 네트워크 요청은 0건이다.
- v11 프로필의 기존 스킬·SP·직업·장비·퀘스트를 보존하면서 신규 스킬 4종만 0레벨로
  보충하는 v12 마이그레이션을 검증했다. 기존 v1~v10 회귀도 함께 통과했다.
- 최종 `dist/`는 80파일·7,841,386 bytes, JavaScript 1,415,816 bytes·gzip 381,082 bytes로
  전체·JavaScript·gzip 예산을 모두 지켰다. 크로마 생성 원본과 프롬프트는 제외됐다.

### P29 하단 상태창 액티브 아이콘 바 검증

- 액티브 11종의 전용 PNG가 Boot Phaser 텍스처로 모두 프리로드되고, 스킬 DOM 모달과
  하단 HUD가 `skill-icon-assets.ts`의 동일 URL을 사용하는지 단위 테스트로 검증했다.
- Chromium 집중 E2E에서 `1~0/-` 슬롯 순서, 아이콘 11개 상태 표식, 캔버스 접근성 슬롯 설명,
  신규 액티브 `2·4·6·-` 전투 이벤트와 HUD 안전 여백 0건을 확인했다.
- 1280×720 캡처에서 11개 아이콘·숫자 배지가 한 줄로 표시되고 HP·MP·EXP 게이지 및
  재화·성장 셀과 겹치지 않음을 확인했다. 미해금 스킬은 흐리게, 활성 변신은 금색으로 구분한다.
- `npm run release:check`에서 Vitest 30파일·226테스트, Chromium·Firefox·WebKit E2E 45/45,
  타입체크와 프로덕션 빌드를 통과했다. `dist/`는 80파일·7,843,379 bytes,
  JavaScript 1,417,809 bytes·gzip 380,843 bytes로 전체 예산을 지켰다.

### P30 QWER 보조키·능력치/장비 통합 창 검증

- 입력 단위 테스트에서 `Q/W/E/R`이 그림자 연사·환영 쌍성·심연 폭우·천뢰옥에 순서대로
  연결되고, 기존 `T`와 별도 장비 액션이 입력 맵에서 제거됐는지 검증했다.
- Chromium 집중 E2E에서 네 보조키의 전투 이벤트, `S` 통합 창의 능력치·장비 동시 표시,
  5등급 표창 구매·장착·투사체 프레임·새로고침 저장 복구를 확인했다.
- 1280×720 캡처에서 능력치 4종·자동분배와 표창 5등급·장착 버튼·닫기 버튼이 한 화면에
  겹침 없이 표시되며, 뷰포트 E2E에서 4개 지원 PC 창의 패널 경계를 확인했다.

### P31 초반 액티브 고유 궤적·타격 연출 검증

- 순수 규칙 테스트에서 `1=평행 쌍투척`, `2=3갈래 부채꼴`, `3=맥동 흡수`, `4=교차 파동`
  모션 식별자가 모두 다르고, 그림자 연사의 `-95/0/+95` 수직 속도와 환영 쌍성의 반대 위상,
  드레인의 시간별 크기 변화를 검증한다.
- Chromium 집중 E2E에서 숫자키 `1~4`를 순서대로 발동해 런타임 투사체 모션 상태가 각 규칙과
  일치하는지 확인한다. 기존 공격 이벤트와 QWER 발동도 같은 시나리오에서 함께 확인한다.
- 파편·X 타격·흡수 연결선·입자는 `mapObjects`에 등록되고 기존 포탈 귀환 리소스 0건 회귀를
  그대로 적용한다.

### P32 스킬 숫자 슬롯 드래그 앤 드롭 검증

- Vitest에서 기본 전직 차수 순서, `1↔4` 슬롯 교환, 동적 숫자열·숫자패드 입력 매핑,
  중복·누락·알 수 없는 저장값 복구와 v1~v12→v13 마이그레이션을 검증한다.
- Chromium 집중 E2E에서 스킬창 `1` 아이콘을 `4`에 드롭해 `1=환영 쌍성`, `4=럭키세븐`으로
  교환하고 HUD·숫자키 발동·새로고침 복구를 확인한다. QWER는 교환 전후 같은 스킬을 발동한다.
- 지원 뷰포트 E2E는 상단 11칸 편집 행을 포함한 스킬 모달 경계와 콘솔·페이지 오류 0건을
  기존 4개 PC 창에서 계속 검사한다.

### P33 전직 차수별 이펙트 밀도 검증

- Vitest에서 대표 스킬 럭키세븐→드레인→어벤저→천뢰옥의 차수가 `1/2/3/4`, 시전 링이
  `0/1/2/3`, 타격 링이 `0/1/2/3`, 방사광이 `0/0/4/8`로 단조 증가하는지 검증한다.
- 모든 호카게 공격이 4차 밀도를 사용하고 기본 공격은 추가 밀도 0인지 확인한다. 기존 타수·MP·
  피해·관통·회복 단위 테스트를 그대로 통과해 수치 밸런스가 변하지 않았음을 확인한다.
- 3엔진 집중 E2E에서 `R` 천뢰옥 발동 시 4차 밀도 상태와 `thunderOrb` 단축 시네마틱을 확인하고,
  이어서 투사체 0건·스킬창 드래그·새로고침 복구까지 완료한다.
- 최종 `npm run release:check`는 Vitest 32파일·234테스트, Chromium·Firefox·WebKit E2E
  45/45, 타입체크·프로덕션 빌드와 `dist` 감사를 통과했다. 배포본은 80파일·7,853,192 bytes,
  JavaScript 1,425,879 bytes·gzip 383,427 bytes다.

### P34 인내의 숲 검증

- Vitest에서 초록버섯굴↔인내의 숲 입구·정상 포탈의 유효 목적 스폰, 몬스터/NPC 0,
  연속 지면 1개와 단방향 발판 8개 구성을 검증한다.
- 생성 전경의 9개 발판과 2개 포탈 `data-*`가 `map-definitions.ts`의 충돌 좌표와 일치하고,
  프로필 v13이 `patienceForest` 위치를 정확히 저장·복구하는지 검증한다.
- 3엔진 E2E에서 인내의 숲 에셋 로드, 지역 상태, 몬스터 0과 브라우저 오류 0건을 확인했다.

### P35 고난도 인내의 숲·등반·정상 보상 검증

- `npm run release:check` 단일 실행이 종료 코드 0으로 완료됐다.
- Vitest 35파일·244테스트에서 줄 부착/이동/이탈, 장애물 왕복 좌표·접촉, 정상 보상
  1회 지급, 상점 5등급과 보상 전용 6등급 분리를 검증했다.
- Chromium·Firefox·WebKit E2E 48/48에서 입구 줄 등반과 `Alt` 이탈, 인내의 숲 맵 접근,
  초대형 고드름 장착·빙결 프레임·새로고침 저장, 상점 비노출을 확인했다.
- 생성 SVG의 21개 충돌면·4개 등반물·10개 장애물·2개 포탈 좌표가 맵 정의와 일치한다.
- 프로덕션 `dist/` 감사 결과는 82파일·7,961,029 bytes, JavaScript 1,435,344 bytes,
  gzip 386,112 bytes로 전체 8,000,000-byte 예산과 개별 예산을 통과했다.

### P36 기존 던전 줄타기 검증

- `npm run release:check` 단일 실행이 종료 코드 0으로 완료됐다.
- Vitest 35파일·246테스트에서 그림자 시험장과 던전 회랑 6종에 줄이 각각 1개 존재하고,
  7개 줄의 id·kind·x·top·bottom이 전경 SVG 메타데이터와 일치하는지 검증했다.
- Chromium·Firefox·WebKit E2E 51/51에서 수정 개미굴 줄 접근·부착·상승·`Alt` 이탈과 기존
  던전 순회·보행·공중 공격 회귀 없음을 확인했다.
- 프로덕션 `dist/` 감사 결과는 82파일·7,969,682 bytes, JavaScript 1,436,557 bytes,
  gzip 386,363 bytes로 전체 8,000,000-byte 예산과 개별 예산을 통과했다.
- 위 7개 줄은 P36 당시 결과다. 현재 P79에서는 무한의 결투장 줄을 제거해 시험장과 회랑
  5종만 줄을 유지하고 결투장은 원펀맨 전용 발판 맵으로 사용한다.

### P37 전 맵 imagegen 환경 오브젝트·사다리 제거 검증

- `npm run release:check` 단일 실행이 종료 코드 0으로 완료됐다.
- Vitest 35파일·246테스트에서 11개 고유 `objectTheme`, WebP 오브젝트 키트 4개, 런타임
  패럴랙스 SVG 3개, 모든 climbable의 `rope` kind와 중앙 교체 줄을 검증했다.
- Chromium·Firefox·WebKit E2E 51/51에서 생성형 포탈을 통한 필수 루프 2회, 던전 줄 부착·
  상승·이탈, 인내의 숲 포함 전체 맵 순회와 브라우저 오류 0건을 확인했다.
- 1280×720 커닝시티·수정 개미굴·인내의 숲 런타임 캡처에서 투명 가장자리, 발판 기준선,
  배경 팔레트 일치, 사다리 실루엣 0건을 확인했다.
- 프로덕션 `dist/` 감사 결과는 77파일·7,979,947 bytes, JavaScript 1,434,953 bytes,
  gzip 386,202 bytes로 전체 8,000,000-byte 예산과 개별 예산을 통과했다.
- `npm run release:check`는 Vitest 32파일·236테스트, Chromium·Firefox·WebKit E2E 45/45,
  타입체크·프로덕션 빌드와 `dist` 감사를 통과했다. 배포본은 82파일·7,943,174 bytes,
  JavaScript 1,427,427 bytes·gzip 383,747 bytes다.

모든 E2E는 `pageerror`, 콘솔 `error`, 실패한 네트워크 요청을 수집해 0건을 요구한다. 포탈
귀환 뒤에는 투사체·드롭·대기 보상·추적 이펙트·맵 타이머가 0으로 돌아오고, 맵 오브젝트와
콜라이더 수가 시작 시점의 커닝시티 기준선과 같아야 한다. 각 루프 종료 시 관측 FPS는 50
이상을 요구한다. 이 수치는 headless 회귀 경보이며 실제 장치의 체감 성능 인증은 아니다.

### P40 창 크기 대응 팝업·스크롤 검증

- 1280×720·1600×900·1024×768·800×600에서 능력치·인벤토리·스킬 팝업의 사방 경계와
  `overflow-y: auto`를 확인하고, 실제 마우스 휠 입력 뒤 `scrollTop > 0`을 Chromium·Firefox·
  WebKit 3/3에서 검증했다.
- 게임 플레이 중 팝업이 열릴 때 사운드 설정과 HUD 접기 컨트롤이 숨겨지고 로그인·캐릭터
  선택에서는 기존 사운드 설정이 유지된다. 800×500 인앱 브라우저 시각 점검의 콘솔 경고·
  오류는 0건이다.
- Vitest 35파일·247테스트, 타입체크, 프로덕션 빌드와 `dist/` 감사에 통과했다. 산출물은
  77파일·7,989,268 bytes, JavaScript 1,440,766 bytes·gzip 387,973 bytes다.
- 전체 `npm run release:check`는 두 차례 실행했으나 변경 범위와 무관한 기존 `required loop`
  시나리오가 세 엔진에서 포탈 도착·플레이어 위치 타이밍으로 간헐 실패했다. 관련 뷰포트
  시나리오는 분리 재실행에서 3/3 통과했으며, 전체 동시 실행은 잔여 flaky gate로 유지한다.

### P42 신규 캐릭터 생성·닉네임 이름표 검증

- 빈 저장소 로그인에서 신규 생성 Scene, 유효성 오류, 닉네임 확정, 각 4~13·합계 25의
  주사위 능력치, 위로 떠올라 회전하는 주사위 CSS 애니메이션을 검증했다.
- 생성값이 프로필 v14에 저장되고 캐릭터 선택과 플레이 진입 뒤 닉네임·능력치가 복구되며,
  캔버스 접근성 이름과 플레이 상태 표식에도 같은 닉네임이 반영되는지 확인했다.
- 저장소 생성 경계가 잘못된 닉네임·범위·합계 스탯을 거부하고, 주사위 애니메이션 중에는
  재굴림·생성이 잠기며 종료 뒤에만 결과가 확정되는지 확인했다. 플레이 이름표는 물리 앵커가
  아닌 시각적 발 아래 6px을 유지하고 피격 점멸 알파와 분리된다.
- 1280×720·1600×900·1024×768·800×600에서 생성 패널이 게임 영역 안에 유지되고 내부
  스크롤이 가능한지 각 뷰포트마다 빈 프로필로 Chromium·Firefox·WebKit에서 확인했다.
  생성·이름표·직업 외형 관련 E2E는 9/9 통과했다.
- Vitest 37파일·265테스트, 타입체크·프로덕션 빌드와 `dist/` 감사를 통과했다. 산출물은
  77파일·7,975,490 bytes, JavaScript 1,451,481 bytes·gzip 390,827 bytes다.

### P44 팝업 반응형·스크롤 정밀 검증

- 1280×720·1600×900·1024×768·800×600에서 능력치·인벤토리·스킬 팝업의 사방 경계,
  수평 넘침 0, 실제 마우스 휠 스크롤과 스크롤 뒤 닫기 버튼 경계를 세 엔진 3/3에서 확인했다.
- 1024×768에서는 능력치·장비 한 열 전환, 1280×720에서는 확대된 플레이 팝업 가용 높이와
  스킬창 제목·닫기 배치를 인앱 브라우저로 확인했고 콘솔 warning/error는 0건이었다.
- `abyss-golem-v3.png`는 `oxipng -o 4 --strip safe`로 무손실 재압축했다. 19종·304셀 픽셀
  정렬을 포함한 Vitest 37파일·262테스트와 타입체크·프로덕션 빌드를 통과했다.
- 최종 `dist/`는 77파일·7,973,940 bytes, JavaScript 1,450,078 bytes·gzip 390,387 bytes로
  전체 8MB와 JavaScript 개별 상한을 통과했다.

### P46 imagegen HUD 크롬·전체 경계 검증

- built-in imagegen 원본에서 96×96 HUD 외곽 프레임과 내부 표면 WebP를 파생하고, 원본은
  Vite import와 `dist/`에서 제외했다. 런타임 WebP 두 파일은 합계 12,544 bytes다.
- HUD 레이아웃 단위 테스트는 모든 제목·본문 영역의 소유 패널 포함, 하단 셀 간 4px 간격,
  레벨 배지의 캐릭터 셀 포함과 공통 패딩을 검사한다. 전체 Vitest는 37파일·268테스트를 통과했다.
- 최대 저장 수치 HUD bounds와 좌상단 미니맵 카메라 회귀 6건, 1280×720·1600×900·
  1024×768·800×600 게임/오버레이 경계 3건을 Chromium·Firefox·WebKit에서 실행해 9/9 통과했다.
- 타입체크와 프로덕션 빌드를 통과했다. 최종 `dist/`는 79파일·7,992,071 bytes,
  JavaScript 1,454,066 bytes·gzip 391,462 bytes로 전체 8MB와 JavaScript 개별 상한을 통과했다.

### P47 3개 캐릭터 슬롯 검증

- 저장 단위 테스트에서 기존 고정 키 프로필의 1번 슬롯 복구, 1·2·3번 독립 생성·선택·저장,
  잘못된 활성 슬롯 fallback, 점유 슬롯 덮어쓰기 차단과 전체 슬롯 초기화를 검증했다.
- 캐릭터 흐름 E2E는 1→2→3번 캐릭터 생성, 2번 선택 플레이, 새로고침 뒤 선택 복구를
  Chromium·Firefox·WebKit 3/3에서 통과했다. 네 지원 PC 창의 캐릭터 선택 패널 경계·스크롤을
  포함한 뷰포트 E2E도 세 엔진 3/3에서 통과했다.
- 전체 Vitest 37파일·273테스트, 타입체크와 프로덕션 빌드를 통과했다. `dist/`는
  79파일·7,999,552 bytes, JavaScript 1,460,725 bytes·gzip 393,292 bytes로 전체 8MB와
  JavaScript 개별 상한을 통과했다.
- 현재 작업 트리 전체 E2E는 60건 중 51건이 통과했다. 나머지 9건은 세 엔진에서 동일하게
  반복된 기존/병행 변경 범위의 플레이어 화면 y 기대값(596 미만/실제 598), 이그니카르 최대
  HP 기대값(2,000,000/실제 4,000,000), 미니맵 접기 버튼 x 기대값(240 미만/실제 약 295)이며,
  P47 슬롯 생성·선택 시나리오에는 실패가 없다.

### P49 미니맵 포탈·포탈 에너지 애니메이션 검증

- 포탈 표현 순수 규칙과 맵 아틀라스 URL 검증을 포함해 전체 Vitest 38파일·275테스트,
  타입체크와 프로덕션 빌드를 통과했다.
- `hud-boundary-audit.spec.ts`는 커닝시티의 미니맵 포탈 수 3, 포탈 효과 수 3, 신규 생성형
  레이어·상승 파편 27개와 활성 애니메이션 상태를 Chromium·Firefox·WebKit 6/6에서 확인했다.
- 1280×720 인앱 브라우저에서 650ms 간격의 두 화면을 비교해 포탈 내부 에너지 폭·바닥 광륜
  밝기·상승 파편 위치가 바뀌는 것을 확인했다. 미니맵의 청록 포탈 3개와 `포탈` 범례,
  HUD 실제 bounds 위반 0건, 캔버스 포커스 유지와 콘솔 오류 0건도 함께 확인했다.
- 생성 원본은 `source/`에만 두고 런타임 효과는 C v2 오브젝트 아틀라스의 빈 셀에 합쳤다.
  최종 `dist/`는 79파일·7,999,680 bytes, JavaScript 1,463,971 bytes·gzip 394,213 bytes로
  전체 8MB와 JavaScript 개별 상한을 통과했다.

### 일반·보스 BGM 교체 검증

- 사용자 제공 일반곡·보스곡 MP3가 런타임 원본과 SHA-256이 일치하고 각각 69.96초·52.28초,
  48kHz stereo로 프리로드되는 것을 확인했다.
- 일반/보스 선택 순수 규칙과 보스가 있는 잿불 광산·달빛 마도서고·무한의 결투장 판정을
  포함해 전체 Vitest 39파일·277테스트와 타입체크를 통과했다.
- 인앱 Chromium에서 첫 사용자 입력 전 `locked=true`, 입력 뒤 일반곡
  `audio-bgm-game-theme`의 `locked=false`·`playing=true`, 로그인→커닝시티 진입 뒤 같은 키
  유지와 콘솔 경고·오류 0건을 확인했다.
- 프로덕션 빌드는 런타임 BGM MP3 2종만 포함하고 이전 절차적 BGM WAV 3종을 제외했다.
  최종 `dist/`는 78파일·9,599,969 bytes이며, BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,683,097 bytes다. JavaScript 1,463,926 bytes·gzip 394,240 bytes도 개별 상한을 통과했다.
- 일반곡의 0.92초 선행 무음과 1.90초 후행 무음, 보스곡의 0.29초 후행 무음을 감지했다.
  원본 MP3를 변경하지 않고 일반곡 0.90~68.06초·보스곡 0~51.99초 사운드 마커를 반복한다.
- 같은 `GameTheme`을 연속 요청할 때 기존 사운드 객체의 `play`가 한 번만 호출되고,
  `BossTheme`처럼 키가 바뀔 때만 기존 객체를 정리하고 새 곡을 시작하는 단위 테스트를
  포함해 전체 Vitest 40파일·283테스트를 통과했다.
- 인앱 Chromium에서 첫 사용자 입력 직후 일반곡이 `locked=false`·`playing=true`가 되고,
  로그인→캐릭터 선택 화면 전환 뒤에도 같은 `audio-bgm-game-theme`가 재생 중인 상태와 콘솔
  경고·오류 0건을 확인했다.
- 현재 작업 트리의 프로덕션 빌드와 감사는 79파일·9,656,974 bytes, BGM 제외 6,740,102 bytes,
  JavaScript 1,468,600 bytes·gzip 395,545 bytes로 모든 개별 상한을 통과했다.

### P50 개발자 채널 홍보 NPC 검증

- `gameDeveloper` 매니페스트·Vite URL·idle/talk/code/thanks 계약과 커닝시티 배치 간격을
  포함해 전체 Vitest 39파일·279테스트, 타입체크와 프로덕션 빌드를 통과했다.
- 스프라이트 픽셀 테스트는 런타임 20종·320셀의 크기·점유·투명 코너·가시 크로마 0건,
  개발자 NPC 16프레임의 -2~-1px 발 기준선과 행별 시각 중심 편차 상한을 확인했다.
- `game-flow.spec.ts`의 개발자 대화는 Chromium·Firefox·WebKit 3/3에서 개발자 소개,
  “큰 힘이 됩니다” 문구, 정확한 채널 URL, `_blank`, `noopener noreferrer`, 링크 선택 뒤
  감사 상태·전투 이벤트와 닫기 뒤 캔버스 포커스 복구를 확인했다. 외부 이동은 테스트에서
  차단해 네트워크나 실제 구독 상태에 의존하지 않는다.
- `hud-boundary-audit.spec.ts`는 네 번째 커닝시티 NPC 미니맵 마커와 기존 HUD 경계를 세 엔진
  6/6에서 확인했다. 1280×720 인앱 브라우저에서 NPC 외형·긴 이름표 간격·미니맵 마커와
  콘솔 warning/error 0건을 추가 확인했다.
- 최종 `dist/`는 79파일·9,656,635 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,739,763 bytes다. JavaScript 1,468,261 bytes·gzip 395,380 bytes도 개별 상한을 통과했다.

### P52 전직·보스 다음 목표 가이드 검증

- `job-advancement-quests.test.ts`는 Lv.30·60·120 미만 추천 사냥터, 레벨 달성 뒤
  2·3·4차 시험 수락, 처치 진척, 완료 보고와 호카게 이후 안내 종료를 검증했다.
- `boss-progression-guide.test.ts`는 Lv.100 미만 숨김, Lv.100 세라 원정 수락,
  이그니카르·루나시온·원펀맨의 Lv.100·140·200 준비/처치 경계와 완료 보고 순서를 검증했다.
- 전체 Vitest 41파일·296테스트와 타입체크·프로덕션 빌드를 통과했다.
- `hud-boundary-audit.spec.ts`는 Lv.120 허밋의 4차 전직과 Lv.140 보스 준비 목표가
  동시에 표시되는 상태, Lv.140 호카게의 루나시온 처치 목표, HUD 안전 여백 위반 0건을
  Chromium·Firefox·WebKit 3/3에서 확인했다.
- 최종 `dist/`는 79파일·9,659,916 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,743,044 bytes다. JavaScript 1,471,542 bytes·gzip 396,501 bytes도 개별 상한을 통과했다.

### P55 인벤토리 소모품 사용 검증

- `inventory-item-rules.test.ts`는 회복 물약의 HP·MP 최대치 50% 회복, 최대치 상한,
  완전 회복·미보유 미소비와 입력 상태 불변성을 검증했다. 전체 Vitest 42파일·300테스트와
  타입체크·프로덕션 빌드를 통과했다.
- `inventory-item-use.spec.ts`는 Chromium·Firefox·WebKit 3/3에서 재료 사용 버튼 미표시,
  회복 물약의 키보드 사용·HP/MP 증가·수량 감소, 경험의 서의 Lv.10→20·EXP 보존·마지막 슬롯
  제거, 닫기 뒤 캔버스 포커스와 새로고침 복구를 확인했다. 콘솔·페이지·요청 오류는 0건이다.
- `viewport.spec.ts`는 1280×720·1600×900·1024×768·800×600의 인벤토리 포함 팝업 경계를
  Chromium·Firefox·WebKit 3/3에서 다시 확인했다.
- 최종 `dist/`는 79파일·9,661,459 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,744,587 bytes다. JavaScript 1,474,012 bytes·gzip 397,079 bytes도 개별 상한을 통과했다.

### P57 구미호 변신 유지비·전투·이동 강화 검증

- `skill-rules`, `combat-rules`, `player-motion`, `progression-rules` 단위 테스트는 최대 MP 1%의
  1초 유지비, 고갈 해제, 자연 MP 회복 정지, 할퀴기 2배속 타격, 전 등급 장착 표창에서의
  일반공격 대비 우위, 기본 260→변신 312 이동속도를 검증했다. 전체 Vitest 42파일·309테스트와
  타입체크·프로덕션 빌드를 통과했다.
- `game-flow.spec.ts`의 구미호 집중 시나리오는 Chromium·Firefox·WebKit 3/3에서 활성 비용,
  누적 유지비 틱, MP 고갈 자동 해제, 120ms 간격 재공격, 공격속도 2.00 배율과 물리 최고속도
  260→312→260 복구를 확인했다. 콘솔·페이지·요청 오류는 0건이다.
- 기존 호카게 시네마틱 집중 시나리오도 세 엔진 3/3으로 단독 재검증했다. 9개 시나리오를
  한 번에 병렬 실행할 때 기존 천뢰옥 4발 순간 상태를 Chromium·WebKit이 2발로 읽는 간헐 실패가
  있었으며, 단독 3엔진 재실행에서는 통과했다.
- 최종 `dist/`는 79파일·9,678,622 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,761,750 bytes다. JavaScript 1,478,713 bytes·gzip 398,431 bytes로 모든 상한을 통과했다.

### P58 두아 펫 등록·추적·자동 회수 검증

- 펫·프로필·상점·맵·에셋 단위 테스트는 멍푸치노 50,000 메소 가격, 중복 구매 방지, 선물 소비,
  v1~v14→v15 미등록 마이그레이션, 슬롯 독립 등록, 추적·순간이동, 착지 드롭 최근접 선택과
  자동 회수 반경을 검증했다. 전체 Vitest 43파일·322테스트와 타입체크·프로덕션 빌드를 통과했다.
- `pet-dua.spec.ts`는 Chromium·Firefox·WebKit 6/6에서 구매→선물→NPC 제거→펫 생성→추적→
  새로고침 복구와 초록버섯 처치 뒤 `Z` 입력 없는 메소·아이템 자동 회수를 확인했다. 기존 지원
  PC 창 경계도 세 엔진 3/3으로 함께 통과했으며 콘솔·페이지·요청 오류는 0건이다.
- 스프라이트 픽셀 테스트는 두아 v2를 포함한 21종·336셀의 크기·점유·투명 코너·가시 크로마 0건,
  프레임별 중심과 발 기준선을 확인했다. 실제 1280×720 커닝시티 캡처에서는 날렵한 얼굴·이름표·
  발판 접지·플레이어 뒤 추적 간격과 등록 후 미니맵 NPC 5→4 전환을 확인했다.
- 최종 `dist/`는 80파일·9,762,304 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,845,432 bytes다. JavaScript 1,490,493 bytes·gzip 401,504 bytes로 모든 상한을 통과했다.

### P59 원펀맨 넉백 면역·주먹 충격파 2페이즈 검증

- 보스 원거리·카탈로그·에셋 단위 테스트는 원펀맨의 넉백 면역, 체력 50% 정확 경계,
  1페이즈 단일 파동과 2페이즈 24도 3방향 파동, 각 투사체의 동일 속도, 전용 SVG Vite 번들을
  검증했다. 전체 Vitest 43파일·324테스트와 타입체크·프로덕션 빌드를 통과했다.
- `game-flow.spec.ts`의 원펀맨 집중 시나리오는 Chromium·Firefox·WebKit 3/3에서 1페이즈,
  넉백 면역 상태, `normalPunchShockwave` 생성, 보스 투사체 1개 이동, 명중·즉사와 콘솔·페이지·
  요청 오류 0건을 확인했다. 2페이즈 선택과 3방향 전개는 순수 규칙 테스트로 고정했다.
- `one-punch-shockwave-v1.svg`는 외부 이미지와 기존 표창 프레임을 사용하지 않는 192×96
  자체 제작 투명 벡터이며, 런타임에서 진행각으로 회전하고 원펀맨 공격 경로는
  `combatEffects.throwingStar`를 생성하지 않는다.
- 최종 `dist/`는 80파일·9,766,093 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,849,221 bytes다. JavaScript 1,494,035 bytes·gzip 402,697 bytes로 모든 상한을 통과했다.

### P60 두아 상단 발판 추적 강화 검증

- 펫 순수 규칙 테스트는 최고속도 360이 구미호 변신 캐릭터의 312보다 빠르고 점프 속도 700이
  플레이어 660 이상임을 고정했다. 정지 구간의 직상단 목표는 제자리 점프, 좌우 진행 방향이 막힌
  지상 상태는 장애물 점프를 선택하며, 장거리·220px 초과 높이 차는 순간이동 복구를 유지한다.
- `pet-dua.spec.ts`는 Chromium·Firefox·WebKit 9/9에서 구매·등록·재접속, 평지 따라잡기,
  커닝시티 150px 상단 단방향 발판 동행, `Z` 입력 없는 드롭 자동 회수를 확인했다. 콘솔·페이지·
  요청 오류는 0건이며 전체 Vitest 43파일·324테스트와 타입체크·프로덕션 빌드를 통과했다.
- 최종 `dist/`는 80파일·9,766,397 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,849,525 bytes다. JavaScript 1,494,339 bytes·gzip 402,803 bytes로 모든 상한을 통과했다.

### P61 원펀맨 상층 발판 투사체 피격 검증

- 몬스터 전투 순수 규칙 테스트는 결투장 상층 발판 높이의 투사체가 원펀맨의 수평 범위에
  도달하면 명중하고, 다른 몬스터·수평 범위 밖·132px 확장 높이 밖에서는 명중하지 않음을
  검증했다. 전체 Vitest 43파일·325테스트와 타입체크·프로덕션 빌드를 통과했다.
- 기존 `game-flow.spec.ts` 원펀맨 집중 시나리오는 Chromium·Firefox·WebKit 3/3에서 보통 펀치
  충격파 생성·이동·명중·즉사와 콘솔·페이지·요청 오류 0건을 재확인했다. 상층 피격 경계는 실제
  맵 발판·원펀맨 바디 좌표를 사용한 순수 규칙 테스트로 고정했다.
- 최종 `dist/`는 80파일·9,766,664 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,849,792 bytes다. JavaScript 1,494,606 bytes·gzip 402,894 bytes로 모든 상한을 통과했다.

### P62 두아 달리기 발 연결 개선 검증

- 두아 v3 런타임의 2행은 서로 반대인 대각발 접지 두 자세와 그 사이의 회수 두 자세를 사용한다.
  프레임별 불투명 픽셀 중심·발 기준점을 새 `frameOrigins`로 고정했고, 1·3·4행 12프레임은 v2와
  알파 포함 픽셀 차이 0임을 ImageMagick AE 비교로 확인했다.
- 분리 작업 트리에서 전체 Vitest 43파일·325테스트와 타입체크·프로덕션 빌드를 통과했다.
  `pet-dua.spec.ts`는 Chromium·Firefox·WebKit 9/9에서 구매·등록·재접속, 150px 상단 발판 추적과
  `Z` 입력 없는 자동 회수를 재확인했다.
- 최종 `dist/`는 80파일·9,907,693 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,990,821 bytes다. JavaScript 1,494,609 bytes·gzip 402,896 bytes로 모든 상한을 통과했다.

### P63 부활의 부적 구매·1회 즉시 부활 검증

- 부활의 부적 순수 규칙은 1,000,000 메소 가격, 최대 보유 1개, 중복 구매 차단, 저장값 0~1
  정규화, 사망 상태에서만 자동 소비하고 HP·MP를 최대치로 복구하는 불변 처리를 검증했다.
  분리 작업 트리의 전체 Vitest는 44파일·330테스트를 통과했다.
- `game-flow.spec.ts`의 구매·부활 시나리오는 Chromium·Firefox·WebKit 6/6에서 상점 가격·구매
  잠금·인벤토리 자동 사용 설명·새로고침 복구, 원펀맨 즉사 충격파 뒤 같은 맵 완전 회복·수량 0,
  다음 즉사 때 커닝시티 귀환과 콘솔·페이지·요청 오류 0건을 확인했다.
- 최종 `dist/`는 80파일·9,911,549 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  6,994,677 bytes다. JavaScript 1,497,843 bytes·gzip 403,661 bytes로 모든 상한을 통과했다.

### P64 유기 생물형 던전 몬스터 검증

- 매니페스트·픽셀 테스트는 새 좀비·늑대·고대 나무정령을 포함한 24종·384셀의 크기·점유·
  투명 코너·가시 크로마 0건과, 몬스터 9종·144셀의 내부 셀 경계 연결 0건을 확인했다.
  지역 카탈로그 테스트는 Lv.35·50·75·105·150 경계와 종별 시트·속도·바디 차이, 전체 몬스터
  11종 이동속도의 중복 0건을 고정한다.
- 전체 Vitest 44파일·334테스트와 타입체크·프로덕션 빌드를 통과했다. `game-flow.spec.ts`의
  던전 맵 로드 시나리오는 Chromium·Firefox·WebKit 3/3에서 모든 회랑 맵의 몬스터 생성·순찰,
  캔버스 포커스와 콘솔·페이지 오류 0건을 재확인했다.
- 1280×720 Chromium 실행 화면에서 시계태엽 탑의 좀비, 가라앉은 산호 신전의 나무정령,
  달빛 마도서고의 늑대가 지면·단방향 발판에 접지하며 크로마 잔여·셀 오염·HUD 겹침이 없음을
  확인했다.
- 최종 `dist/`는 83파일·10,147,091 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,230,219 bytes다. JavaScript 1,499,865 bytes·gzip 404,412 bytes로 모든 상한을 통과했다.

### P65 ESC 전체 메뉴·스킬 단축키·시네마틱 HUD 검증

- 전체 Vitest 44파일·335테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 입력 테스트는
  `Escape`의 `OpenMenu` 매핑을 고정하고 기존 스킬 슬롯 정규화·교환·프로필 복구 계약도 유지한다.
- `game-flow.spec.ts`와 `viewport.spec.ts`의 관련 시나리오는 Chromium·Firefox·WebKit 12/12에서
  `ESC` 전체 메뉴 진입·하위 화면 이동·재차 `ESC` 닫기와 Canvas 포커스 복구, 환경설정 음소거·
  BGM/SFX 실시간 적용과 새로고침 복구, 전용 단축키 화면의 선택 교환·실제 키 입력·저장 복구,
  시네마틱 중 HUD 접기 버튼 숨김과 종료 뒤 복구를 확인했다.
- 1280×720 인앱 Chromium에서 전체 메뉴, 11칸 단축키 화면과 환경설정의 제목·본문·버튼 경계,
  한글 줄바꿈과 배경 HUD 조작부 비노출을 확인했다. 콘솔 warning/error는 0건이었다.
- 최종 `dist/`는 83파일·10,144,665 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,227,793 bytes다. JavaScript 1,486,401 bytes·gzip 403,853 bytes로 모든 상한을 통과했다.

### P66 두아 추적 순항·정지 떨림 검증

- 펫 순수 규칙 테스트는 추적 간격 경계에서도 플레이어가 260으로 움직이면 같은 속도로 순항하고,
  구미호 312 역방향 이동 중 장애물을 만나면 방향을 유지한 채 점프하며, 속도 20 이하에서만
  idle로 전환함을 고정했다. 전체 Vitest 44파일·336테스트와 타입체크·Terser 빌드를 통과했다.
- `pet-dua.spec.ts`는 Chromium·Firefox·WebKit 9/9에서 구매·등록·재접속, 평지 추적,
  커닝시티 150px 상단 단방향 발판 동행과 `Z` 입력 없는 자동 회수를 재확인했다.
- 최종 `dist/`는 83파일·10,145,106 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,228,234 bytes다. JavaScript 1,486,842 bytes·gzip 403,958 bytes로 모든 상한을 통과했다.

### P67 Shift·문자열 스킬 단축키 검증

- 전체 Vitest 44파일·339테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 순수 규칙은
  추가 키 13종의 유효값 정규화·배치·비우기·교환·복수 키 표시를, 입력 테스트는 좌우 Shift와
  문자키 매핑·S/Z 스킬 우선·빈 슬롯 기본 동작·Canvas 포커스 경계를 고정한다.
- 프로필 테스트는 v15→v16에서 숫자 순서와 기존 Shift/QWER/XCV 별칭을 보존하고, 캐릭터별
  추가 키 저장·명시적 전체 비움·알 수 없는 키/스킬 제거를 확인했다.
- `game-flow.spec.ts`, `viewport.spec.ts`, `hud-boundary-audit.spec.ts` 관련 시나리오는 Chromium·Firefox·WebKit 9/9에서
  13개 추가 키 노출, 숫자 1↔4 교환, A 드레인 배치·실제 시전·새로고침 복구, S 임시 배치·비우기,
  1280×720·1600×900·1024×768·800×600 패널 경계, 추가 키 행 수평 넘침과 새 조작 안내의 HUD
  안전 여백 위반 0건을 확인했다.
- 1280×720 인앱 Chromium 화면에서 숫자/HUD 11칸과 추가 13칸의 아이콘·키 배지·빈 A/D/F,
  S 스탯·Z 회수 기본 동작이 한눈에 구분되고 대화상자가 게임 영역 안에 유지됨을 확인했다.
  콘솔 warning/error는 0건이었다.
- 최종 `dist/`는 83파일·10,152,669 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,235,797 bytes다. JavaScript 1,489,605 bytes·gzip 404,591 bytes로 모든 상한을 통과했다.

### P68 원펀맨 클리어 픽셀 아트 크레딧 검증

- 전체 Vitest 45파일·342테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 순수 규칙은
  원펀맨만 크레딧을 시작하고 7개 개발 역할 모두 `임상진 with Codex`, 게임 레퍼런스는
  `메이플스토리`, 자동 종료 시간은 18초 이하임을 고정한다.
- `game-flow.spec.ts`의 신규 시나리오는 Chromium·Firefox·WebKit 3/3에서 실제 원펀맨 처치,
  사망 애니메이션 뒤 크레딧 표시, WebP 배경, 전체 문구, 원정 `turn-in` 저장, 계속 플레이 버튼,
  보스 투사체 0개, Canvas 포커스와 좌우 이동 재개를 확인했다.
- 크레딧은 프로필에 별도 표시 상태를 저장하지 않으며 버튼·`ESC`·자동 종료가 같은 정리 경로를
  사용한다. 동시 처치로 기존 사망 귀환이 이미 예약된 경우에도 크레딧 종료 뒤 커닝시티에서
  정상 플레이를 계속할 수 있다.
- 최종 `dist/`는 84파일·10,234,827 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,317,955 bytes다. JavaScript 1,492,509 bytes·gzip 405,507 bytes로 모든 상한을 통과했다.

### P69 점프 중 즉시 좌우 방향 전환 검증

- 전체 Vitest 45파일·343테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 순수 규칙은
  무입력 시 현재 공중 관성을 유지하고, 좌우 입력 시 기본 260·구미호 312 최고속도를 즉시
  해당 방향으로 적용함을 고정한다.
- `game-flow.spec.ts`의 신규 시나리오는 Chromium·Firefox·WebKit 3/3에서 우측 이동 중 점프,
  좌측 입력 직후 `260→-260` 반전, 공중 기본 공격 확정 뒤에도 `-260` 속도와 좌측 위치 이동이
  계속되는지 확인했다.
- 지상 가속·감속, 수직 점프 속도와 무입력 공중 관성은 기존 경로를 유지하며 공중에 있는
  점프 시작 프레임·공격 동작 잠금 구간에만 동일한 좌우 조향 규칙을 적용한다.
- 1280×720 인앱 브라우저에서 커닝시티 점프 장면을 직접 확인해 당시 README에 반영했다.
  P79 공개 화면 정리에서는 달빛 마도서고 사냥 캡처로 교체하고 이 파일은 제거했다.
- 최종 `dist/`는 84파일·10,235,213 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,318,341 bytes다. JavaScript 1,492,895 bytes·gzip 405,612 bytes로 모든 상한을 통과했다.

### P70 원펀맨 2층 구미호 할퀴기 피격 검증

- 전체 Vitest 45파일·344테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 순수 규칙은
  할퀴기 전방 bounds가 원펀맨 실제 바디와 상단 132px 확장 영역에 닿는 2층·상층 좌표,
  반대 방향 미명중과 다른 몬스터의 확장 미적용을 고정한다.
- `game-flow.spec.ts`의 신규 시나리오는 Chromium·Firefox·WebKit 3/3에서 구미호 변신,
  결투장 2층 높이 `player-y < 500`, `nineTailsClaw-attack` 확정과 원펀맨 HP 감소를 확인했다.
- 기존 지상 근접 판정과 투사체의 원펀맨 상층 확장 경로는 유지하며, 새 bounds 경로는 구미호
  할퀴기에만 사용한다.
- 최종 `dist/`는 84파일·10,235,572 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,318,700 bytes다. JavaScript 1,493,254 bytes·gzip 405,757 bytes로 모든 상한을 통과했다.

### P71 신규 캐릭터 Lv.9 시작 검증

- 전체 Vitest 45파일·344테스트, 타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- 신규 생성 시나리오는 Chromium·Firefox·WebKit 3/3에서 `character.level=9` 저장,
  캐릭터 선택, 게임 진입과 `초보자 목표 · Lv.10 달성 (9/10)` HUD 안내를 확인했다.
- 기존 필수 루프는 Lv.9에서 초록버섯을 사냥해 Lv.10을 달성한 뒤 시험을 받도록 확장했다.
  다만 새 성장 구간 실행 전에 기존 카메라/HUD 경계 검사가 안정적으로 `598 < 596`에서
  실패하므로 이번 범위에서는 기대값을 완화하지 않고 잔여 회귀로 유지한다.
- 최종 `dist/`는 84파일·10,235,571 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,318,699 bytes다. JavaScript 1,493,253 bytes·gzip 405,757 bytes로 모든 상한을 통과했다.

### P72 Z 고정 줍기 키와 HUD 안내 검증

- 전체 Vitest 45파일·344테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 입력·스킬·프로필
  테스트는 유효한 추가 키가 `Shift/QWER/ASDF/XCV` 12개이고 저장된 `Z` 별칭이 제거되며,
  어떤 별칭 입력에서도 `KeyZ`가 `Loot`로 유지되는 경계를 고정한다.
- `game-flow.spec.ts`와 `viewport.spec.ts` 관련 시나리오는 Chromium·Firefox·WebKit 6/6에서
  `Z` 설정 슬롯 미노출, 나머지 추가 키 배치·저장, 1280×720·1600×900·1024×768·800×600
  팝업 경계와 수평 넘침 없음, 브라우저 오류 0건을 확인했다.
- 1280×720 Chromium 실행 화면에서 우측 상단 `[Z] 줍기`가 공격·포탈·가방 안내와 같은 행에
  선명하게 표시되고 `data-hud-padding-violations=0`임을 확인했다.
- 최종 `dist/`는 84파일·10,235,302 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,318,430 bytes다. JavaScript 1,493,237 bytes·gzip 405,745 bytes로 모든 상한을 통과했다.

### P73 모바일 터치 조작·Lv.120 호카게 부스트 검증

- 전체 Vitest 46파일·350테스트와 타입체크·Terser 프로덕션 빌드를 통과했다. 순수 규칙 테스트는
  조이스틱 dead zone·대각 방향·노브 반경 제한, 직업별 대표 스킬 최대 3개, Lv.9→120 성장과
  자동 AP·전체 15개 스킬 Lv.20·남은 SP 33, 슬롯 독립 저장을 고정한다.
- `mobile.spec.ts`는 Chromium·Firefox·WebKit 3/3에서 터치 입력 감지, 부스트 생성과 저장,
  가로 844×390·세로 390×844 전체 창 경계, 조이스틱 이동, 기본 공격·줍기·호카게 대표 스킬
  3개, 메뉴 중 컨트롤 숨김과 Canvas 포커스 복구를 확인했다.
- 기존 일반 Lv.9 생성과 1280×720·1600×900·1024×768·800×600 PC 뷰포트 회귀는
  Chromium·Firefox·WebKit 6/6에서 통과해 총 관련 E2E는 9/9다.
- 844×390 Chromium 실행 화면에서 생성 부스트 버튼·호카게 미리보기와 플레이 조이스틱·액션
  버튼의 판독성, 팝업 중 컨트롤 비노출과 브라우저 오류 0건을 확인했다.
- 최종 `dist/`는 84파일·10,245,907 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,329,035 bytes다. JavaScript 1,499,247 bytes·gzip 407,553 bytes로 모든 상한을 통과했다.

### P74 세로 모바일 전체 화면·HUD 검증

- 전체 Vitest 46파일·350테스트와 타입체크·Terser 프로덕션 빌드를 통과했다.
- `mobile.spec.ts`는 Chromium·Firefox·WebKit 6/6에서 390×844 로그인·캐릭터 생성·선택·
  전체 메뉴·환경설정·인벤토리·스탯/장비·스킬·단축키가 safe-area 내부 전체 높이를 사용하고
  수평 넘침이 없으며 모든 버튼·링크가 44px 이상, 입력이 16px 이상인지 확인했다.
- 같은 테스트는 세로 조이스틱·공격·줍기·대표 스킬·메뉴와 HUD 접기 버튼이 화면 안에 있고
  HUD 버튼이 36px 이상인지 확인했다. 단축키 숫자 11칸과 추가 키 12칸은 각각 4열로 감싸져
  수평 스크롤이 없다.
- 기존 1280×720·1600×900·1024×768·800×600 PC 뷰포트 회귀는 3엔진 3/3에서 통과해
  총 관련 E2E는 9/9다.
- 390×844 Chromium 실행 화면에서 로그인·캐릭터 생성·선택·전체 메뉴·인벤토리·스킬 단축키를
  직접 확인했고, 패널 전체 높이·고정 조작·가로 넘침·브라우저 오류 모두 이상이 없었다.
- 최종 `dist/`는 84파일·10,251,855 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,334,983 bytes다. JavaScript 1,499,247 bytes·gzip 407,553 bytes로 모든 상한을 통과했다.

### P75 모바일 상황 조작·3슬롯 편집·초기 BGM 안내 검증

- 전체 Vitest 46파일·350테스트와 타입체크·3회 압축 Terser 프로덕션 빌드를 통과했다. 순수
  규칙은 조이스틱 dead zone과 노브 오프셋을 수평축으로 제한하고, 현재 차수 주 스킬을 저장된
  액티브 순서대로 최대 3개 추리는 경계를 고정한다.
- `mobile.spec.ts`는 Chromium·Firefox·WebKit 6/6에서 가로 844×390 좌우 이동, NPC 접근 시
  점프→대화 버튼 전환·대화 실행·NPC 이탈 뒤 점프 복구, 공격·스킬 실행을 확인했다. 같은
  3엔진 세로 시나리오는 모바일 단축키에 호카게 주 스킬 3개만 표시하고 천뢰옥을 1번으로
  바꾼 순서가 HUD에 반영되는지 검증했다.
- 첫 로딩 안내의 정확한 문구·3초 animation·5초 이내 비노출, 세로 HUD 접기 버튼 28×28px,
  전체 팝업 수평 넘침 없음과 safe-area 경계를 자동 확인했다.
- 기존 1280×720·1600×900·1024×768·800×600 PC 뷰포트 회귀는 3엔진 3/3에서 통과해
  총 관련 E2E는 9/9다.
- 390×844 Chrome 실행 화면에서 초기 BGM 안내가 한 줄로 먼저 나타나고, 전체 높이 로그인과
  플레이 HUD 최소화 버튼이 Canvas 위쪽 미사용 영역에 작게 배치되는 것을 직접 확인했다.
- 최종 `dist/`는 84파일·10,266,191 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값은
  7,349,319 bytes다. JavaScript 1,499,027 bytes·gzip 407,446 bytes로 모든 상한을 통과했다.

### P76~P78 원정·모바일 조작·4슬롯 검증 — 2026-07-29

- P76은 351개 단위 테스트와 Chromium·Firefox·WebKit 관련 E2E 12/12에서 이그니카르 처치
  전후 달빛 서고 포탈·미니맵·효과 동시 해금, 고정 점프와 별도 대화/이동, 아래 조이스틱+
  점프 발판 하강, 1차 그림자 시험장 초록버섯 5체를 확인했다.
- P77은 3엔진 모바일 E2E 6/6에서 `대화/이동 → 줍기 → 점프 → 공격` 순서와 PC 최적화·
  매너모드 해제 두 줄 안내의 3초 소멸을 확인했다.
- P78은 351개 단위 테스트와 3엔진 모바일 E2E 6/6에서 호카게
  `나선환 → 미수옥 → 삼인 협공 → 천뢰옥` 4슬롯과 모바일 단축키 순서 저장을 확인했다.
- P78 최종 `dist/`는 84파일·10,268,432 bytes이며 BGM 2,916,872 bytes를 제외한 예산
  적용값 7,351,560 bytes, JavaScript 1,499,605 bytes·gzip 407,538 bytes다.

### P79 무손실 WebP·보스 BGM 지연 로딩 검증 — 2026-07-29

- 핵심 스프라이트 24종과 스킬 아이콘 15종은 기준 PNG와 lossless WebP의 디코딩 픽셀
  차이 0건을 확인했다. 39종은 2,465,785→1,980,136 bytes로 485,649 bytes(19.70%)
  감소했고 배포 감사가 `.png` 산출물을 거부한다.
- 오디오 단위 테스트는 초기 에셋에서 보스곡 제외, 동적 로더의 중복 요청 병합·성공 캐시·
  실패 재시도·늦은 완료 무시를 고정한다.
- `audio-loading.spec.ts`는 Chromium·Firefox·WebKit에서 로그인·일반 맵의 보스 MP3 요청
  0건, 첫 살아 있는 보스 맵의 요청 1건, 로딩 중 일반곡 유지와 준비 뒤 전환을 검증한다.
- `game-flow.spec.ts`의 스킬 이미지 시나리오는 3엔진에서 실제 자연 크기 128×128과 WebP
  `currentSrc`를 확인한다.
- 전체 46파일·359개 단위 테스트, Chromium·Firefox·WebKit 보스 음원·WebP E2E 6/6,
  타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- 최종 `dist/`는 86파일·9,782,633 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  6,865,761 bytes다. JavaScript 1,492,554 bytes·gzip 400,326 bytes로 모든 상한을 통과했다.

### P80 모바일 캐릭터 생성 입력 검증 — 2026-07-30

- 터치 장치의 빈 캐릭터 슬롯 탭 이벤트에서 실제 닉네임 입력을 먼저 포커스하고, Phaser
  Scene 전환 뒤 같은 입력을 생성 폼으로 이어받는 경로를 추가했다.
- `mobile.spec.ts`는 슬롯 1 생성 후 빈 슬롯 2를 탭해 열린 생성 화면에서 닉네임 입력이
  활성 요소인지 확인하고, `ㄱㅏ` 입력의 유효 상태와 생성 버튼 활성화를 검증한다.
- 닉네임 단위 테스트는 호환 자모 `ㄱㅏ`, 결합 자모 `가나`의 NFC 정규화, 이모지 거부를
  고정한다.
- 전체 46파일·360개 단위 테스트와 Chromium·Firefox·WebKit 모바일 E2E 9/9,
  타입체크·Terser 프로덕션 빌드·`dist/` 감사를 통과했다.
- 최종 `dist/`는 86파일·9,783,295 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  6,866,423 bytes다. JavaScript 1,493,067 bytes·gzip 400,510 bytes로 모든 상한을 통과했다.

## 뷰포트 결과

| 뷰포트 | 기대 결과 | 자동화 |
| --- | --- | --- |
| 1280×720 | 16:9 전체 채움 | Pass · 3엔진 |
| 1600×900 | 16:9 전체 채움 | Pass · 3엔진 |
| 1024×768 | 1024×576, 상하 레터박스 | Pass · 3엔진 |
| 800×600 | 800×450, 상하 레터박스·로그인·전체 메뉴·환경설정·스킬 단축키 패널 내부 배치 | Pass · 3엔진 |
| 모바일 가로 844×390 | 전체 창·좌우/아래 조이스틱·별도 점프·대화/이동·공격·줍기·주 스킬 4개·우상단 메뉴 | Pass · 3엔진 |
| 모바일 세로 390×844 | 전체 창·위아래 여백을 쓰는 모든 DOM 화면·4슬롯 편집·28px HUD 접기 | Pass · 3엔진 |

## 릴리스 산출물 기준

- BGM MP3 2종을 제외한 `dist/`: 최대 8,000,000 bytes. 감사 로그에는 BGM을 포함한 실제 총량과 제외 용량을 함께 표시
- JavaScript: 최대 1,500,000 bytes
- JavaScript gzip 합계: 최대 450,000 bytes
- 필수 고지: `THIRD_PARTY_NOTICES.txt`, `ASSET_CREDITS.md`, Galmuri OFL, Phaser MIT
- 금지: 외부 레퍼런스 이미지 디렉터리, 생성 `source/`, `PROMPTS.md`, sourcemap, 미사용 v1 드롭 시트

`scripts/audit-dist.mjs`가 위 조건을 강제한다. Phaser를 포함한 단일 게임 번들이므로 Vite의
기본 500 kB chunk 경고는 남지만, 별도의 실제 크기 예산으로 회귀를 차단한다.

## P7에서 수정한 회귀 위험

- 과대·음수·소수 저장값을 정규화하고 과대 EXP가 무제한 레벨업 반복을 만들지 않도록 제한
- 좌·우 Ctrl/Alt/Shift를 함께 누른 경우 한쪽 키를 떼어도 나머지 물리 키 상태 유지
- 사망 입력 잠금 시작·완료 시 입력 상태를 비워 귀환 직후 포탈이 오작동하지 않도록 처리
- 960px 미만 PC 창에서도 게임과 접근 가능한 로그인 패널이 잘리지 않도록 반응형 조정
- E2E가 플레이어·진행·임시 런타임 객체 상태를 결정적으로 관찰할 수 있는 DOM 상태 표식 추가
- v1 `thief` 저장을 로그·레벨 기반 추천 스탯으로 옮기고 손상된 중첩 스탯·AP·옵션을 정규화
- 능력치 모달 동안 게임·사운드 UI를 함께 비활성화하고 닫은 뒤 캔버스 포커스를 복구
- 강화 레벨업 삼중 링·스파크·배너를 1,650ms 입력 잠금과 맵 객체 수명 안에서 정리

## 실제 브라우저 수동 QA — 릴리스 잔여 게이트

아래 항목은 이 작업 환경에서 실제 키보드 고스팅과 스피커 출력을 판정할 수 없어 아직
`Not run`이다. 따라서 P7 최종 릴리스 체크포인트는 보류한다.

### P38 맵 오브젝트 접지·포탈 광원 검증

- 11개 맵을 각각 저장 위치로 직접 진입해 1280×720 캡처를 만들고 캐릭터 발·생성형 지면
  윗면·입구 포탈 하단의 접지를 전부 확인했다.
- 11개 테마 포탈 모두 맵별 강조색 ADD 외곽광과 흰색 중심광이 바닥에 표시되고, 반복 트윈은
  `mapObjects` 정리 경로에서 제거된다.
- `npm run release:check` 결과 Vitest 35파일·246테스트, Chromium·Firefox·WebKit E2E
  51/51, 타입체크·프로덕션 빌드·배포 감사를 통과했다.
- 최종 `dist/`는 77파일·7,982,128 bytes, JavaScript 1,437,134 bytes·gzip 386,797 bytes로
  8,000,000-byte 총량 상한과 기존 JavaScript 예산을 통과했다.

| OS | 브라우저 | 전체 루프 2회 | 실제 오디오 | 입력 조합 | 상태 |
| --- | --- | --- | --- | --- | --- |
| Windows | Chrome 최신 | — | — | — | Not run |
| Windows | Edge 최신 | — | — | — | Not run |
| Windows | Firefox 최신 | — | — | — | Not run |
| macOS | Chrome 150.0.7871.116 | — | — | — | Not run |
| macOS | Firefox 최신 | — | — | — | Not installed |
| macOS | Safari 26.5.2 | — | — | — | Not run |

각 행에서 실제 키보드로 좌우 이동+점프+공격 동시 입력, 길게 누르기, 포탈 `↑` 연타,
Alt+Tab/Cmd+Tab 뒤 붙잡힌 키 없음, 첫 입력 전후 오디오 잠금 해제, 화면·맵별 BGM 1개,
음소거·볼륨 복구, 콘솔 오류 0건을 확인한다.

## 장시간 성능 수동 QA — 릴리스 잔여 게이트

자동화의 2회 연속 루프와 객체 수 기준선 복귀는 Pass다. 실제 30분 플레이는 아직 `Not run`이며,
다음 기준으로 기록한다.

- 30초 워밍업 뒤 30분 플레이, 1280×720, 브라우저 확대 100%, 하드웨어 가속 활성화
- 포탈 왕복 20회, 투사체 100회 이상, 드롭 생성·회수·소멸 반복
- 중앙 FPS 58 이상, 5초 이상 이어지는 50 FPS 미만 구간 없음
- 같은 idle 상태에서 종료 heap이 기준 대비 `15% 또는 20 MiB` 이상 지속 증가하지 않음
- detached DOM 0, BGM 1개, 임시 sprite·tween·timer·collider 수가 맵 기준선으로 복귀

실제 브라우저 표와 30분 결과가 모두 Pass가 된 뒤 `ROADMAP.md`의 P7을 완료로 바꾸고
`release: complete local game demo` 체크포인트를 만든다.
