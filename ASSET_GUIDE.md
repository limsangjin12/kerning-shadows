# Asset Guide

## 1. 에셋 우선 원칙

게임 기능은 필요한 에셋의 규격과 v1 결과물이 먼저 확정된 뒤 구현한다. 특히 캐릭터·몬스터·NPC의 프레임 수나 셀 크기를 임시 코드에 맞춰 바꾸지 않는다.

우선순위는 다음 순서를 따른다.

1. 플레이를 막는 캐릭터·몬스터·NPC·전투·포탈·드롭 스프라이트
2. 충돌 맵과 함께 사용되는 배경·발판·포탈 위치 에셋
3. 데미지 숫자, HUD, 대화창, 로그인·캐릭터 선택 UI
4. 부가 아이콘, 환경 애니메이션, 사운드, 폴리시 에셋

새 기능을 시작할 때는 이 문서의 에셋 감사표에서 해당 의존성이 `통합 가능`인지 먼저 확인한다.

## 2. 현재 에셋 감사

| 우선순위 | 범주 | 필요한 에셋 | 상태 | 다음 조치 |
| --- | --- | --- | --- | --- |
| A0 | 플레이어 | 초보자·로그·어쌔신·허밋·호카게 idle, walk, jump/fall, hurt, 공격 | 통합 완료 P26 | 5종 4×4 시트·접지 보행·프레임 원점·셀 경계 감사 통과 |
| A0 | 몬스터 | 초록버섯 idle, walk, hurt, defeat | 통합 완료 v2 | `VISUAL_QA.md` P6 기준선 통과 |
| A0 | 전직 NPC | 그림자 길드 멘토 idle, talk, cast, approve | 통합 완료 v3 | 중심·발 기준선과 cast→approve 기준선 통과 |
| A0 | 전투 효과 | 고드름 표창, 스킬 타격, 피격, 전직 링 | 통합 완료 v3 | 선딜·잔상·섬광·마일스톤 기준선 통과 |
| A0 | 최종보스 충격파 | 원펀맨 보통/진심 펀치가 발사하는 방향성 공기 충격파 | 통합 완료 P59 | 자체 제작 투명 SVG 1종을 색·크기·3방향 전개로 재사용하고 표창 프레임과 분리 |
| A0 | 월드/드롭 | 포탈, 메소 주머니, 회복 물약, 버섯 갓 | 통합 완료 v3 | 셀·바닥 정렬과 착지·회수·포탈 기준선 통과 |
| A0 | 전직 외형 | 로그·어쌔신·허밋 장비가 반영된 플레이어 4×4 시트 3종 | 통합 완료 P9 | 직업별 런타임 교체·1280×720 기준선 통과 |
| A0 | 직업 표창 | 기본·럭키세븐·드레인·어벤저 회전/광원 4프레임 | 통합 완료 P9 | 공격별 전용 행과 잔상·명중 팔레트 연결 완료 |
| A0 | 던전 몬스터 | 중형 그림자 파수꾼·대형 심연 골렘 idle/walk/hurt/defeat | 통합 완료 P9 | 종류별 런타임 크기·바디·전투 데이터 분리 완료 |
| A0 | 던전 보스 | 폭열군주 이그니카르·월식현자 루나시온·원펀맨 idle/walk/hurt/attack/defeat | 통합 완료 P22 | 중간·상위·최종 등급, 즉사 공격·전용 맵 통합 완료 |
| A0 | 원정 NPC | 원정대장 세라 idle/talk/brief/approve | 통합 완료 P16 | 수락·진행·보고 상태와 접근 가능한 대화창 통합 완료 |
| A0 | 개발자 NPC | 일용직 개발자 임상진 idle/talk/code/thanks | 통합 완료 P50 | 전용 시트·채널 홍보 대화·안전한 새 탭 링크 통합 완료 |
| A0 | 펫 | 두아 sit/idle, run, fetch, happy | 통합 완료 P62 | 독자 웰시코기 SD 실루엣·접지 달리기 4×4 시트·발 기준선·자동 줍기 동선 검증 |
| A1 | 데미지 숫자 | 숫자 0–9, 일반·강한 공격·플레이어 피격·크리티컬 팔레트 | 통합 완료 P56 | 브라우저 크기·간격·4색 팔레트와 크리티컬 확대 기준선 통과 |
| A1 | 커닝시티 | 도시 외부 배경, 벽돌·배관, 발판, 다크로드 내부 구역 | 통합 완료 v2 | 3단 레이어·충돌 정렬과 1280×720 기준선 통과 |
| A1 | 초록버섯굴 | 동굴 배경, 버섯 발판, 덩굴·발광 장식, 포탈 소켓 | 통합 완료 v2 | 3단 패럴랙스·충돌/포탈 정렬 기준선 통과 |
| A1 | 인내의 숲 | 거목·폭포 원경, 나뭇가지 발판, 줄·장애물, 입구·정상 포탈 | 통합 완료 P37 | 1280×720 원경 + imagegen 오브젝트 + 1920×1440 좌표 검증 SVG 기준선 통과 |
| A1 | 그림자 시험장 | 지하 길드 던전 원경, 룬 기둥·사슬 중경, 충돌 정렬 발판·포탈 | 통합 완료 P9 | 1280×720 원경 + 1920×720 SVG 2단 레이어 기준선 통과 |
| A1 | 플레이 HUD | HP/MP/EXP, 미니맵, 지역명, 조작 힌트, 시스템 메시지 | 통합 완료 P46 | imagegen 건메탈 9-slice·124px 상태바·텍스트 안전 영역 통과 |
| A1 | 회복 NPC | 커닝시티 HP/MP 완전 회복 상호작용 | 통합 완료 v1 | 전용 시트와 회복 SFX 통합 완료 |
| A1 | 대화/전직 UI | NPC 대화창, 선택 버튼, 전직 완료 피드백 | 통합 완료 v2 | 공통 금속 9-slice·전직 연출·SFX 완료 |
| A1 | 스킬 메뉴 | 차수 탭, 15개 전용 아이콘·레벨·SP 분배·상세 설명·단축키 편집 | 통합 완료 P79 | 접근 가능한 DOM 금속 패널·WebP 아이콘·PC 23칸/모바일 4슬롯 편집 적용 |
| A1 | 로그인 UI | 책·숲 배경, 나무·양피지 패널, DOM 입력창과 버튼 | 통합 완료 v2 | 책 9-slice와 번들 픽셀 폰트 적용 완료 |
| A1 | 캐릭터 선택 UI | 하늘·나무 발판 배경, 정보 패널, 선택 버튼, 플레이어 시트 초상 | 통합 완료 v2 | 나무 9-slice와 번들 픽셀 폰트 적용 완료 |
| A2 | 아이콘 | 액티브 11종·패시브 4종 스킬 아이콘, 메소·아이템·포탈·단축키 표현 | 통합 완료 P79 | 기준 PNG 15종과 픽셀 동일한 lossless WebP 런타임·배포 PNG 0건 |
| A2 | 오디오 | 일반·보스 BGM, 점프·공격·피격·드롭·전직 SFX | 통합 완료 P79 | 일반곡 초기 로딩·보스곡 최초 조우 지연 로딩·절차적 PCM 12 SFX 완료; 실제 스피커 수동 QA 잔여 |
| A3 | 폴리시 | 먼지, 빗물, 네온, 동굴 입자, 레벨업, 회복 효과 | 데모 범위 완료 v1 | 포탈·전직·레벨업·회복·드롭 효과 기준선 통과; 환경 입자는 범위 외 선택 개선 |

외부 시각 자료는 저장소에 보관하지 않는다. 필요한 화면 비율, 색감, 동선과 애니메이션 점유율은
이 문서와 제작 프롬프트에 독자 규격으로 언어화하며 권리·배포 경계는 `ASSET_CREDITS.md`를 따른다.

## 3. 핵심 스프라이트 규격

- 편집·픽셀 감사 기준: RGBA PNG
- 런타임 전송 형식: 알파를 보존하는 lossless WebP(VP8L)
- 시트 크기: 512×512
- 그리드: 4열 × 4행
- 프레임 크기: 128×128
- 인덱스: 왼쪽 위부터 행 우선 0–15
- 방향: 원본은 오른쪽을 바라보고, 왼쪽 이동은 런타임 `flipX` 사용
- 보간: nearest-neighbor
- 아틀라스 정책: 프레임 회전·자동 trim 금지, 각 셀 좌표 고정
- 기본 원점
  - 캐릭터·몬스터·NPC·드롭: `(0.5, 1.0)`
  - 투사체·이펙트·포탈: `(0.5, 0.5)`
- 프레임별 실루엣 중심이 다른 경우 매니페스트의 `frameOrigins`로 원점을 보정한다.
  런타임 시트 24종 모두 16개 프레임 원점을
  필수로 정의한다. 캐릭터·몬스터·NPC는 발 또는 바닥, 전투 효과는 광학 중심,
  포탈·드롭은 바닥과 수평 중심을 기준으로 하며 물리 바디 기준은 고정한다.
- 생성 원본: `assets/sprites/source/`
- 투명화·정규화된 기준 PNG와 런타임 lossless WebP: `assets/sprites/core/`
- 애니메이션 좌표와 속도: `assets/sprites/sprite-manifest.json`

## 4. 생성된 시트

아래 표는 현재 매니페스트가 가리키는 런타임 WebP 24종이다. 같은 stem의 PNG는 생성·정렬·
픽셀 감사를 위한 기준 파일로만 보존하며 Vite와 `dist/`에는 포함하지 않는다.

| 파일 | 행 1 | 행 2 | 행 3 | 행 4 |
| --- | --- | --- | --- | --- |
| `player-v4.webp` | idle 4 | grounded walk 4 | jump rise, apex, fall, hurt | basic 2, Lucky Seven 2 |
| `green-mushroom-v1.webp` | idle 4 | walk 4 | hurt 4 | defeat 4 |
| `shadow-mentor-v1.webp` | idle 4 | talk 4 | cast 4 | approve 4 |
| `street-healer-v1.webp` | idle 4 | talk 4 | recovery cast 4 | approve 4 |
| `combat-effects-v1.webp` | throwing star 4 | skill hit 4 | hurt spark 4 | advancement ring 4 |
| `world-effects-loot-v2.webp` | portal 4 | centered meso pouch 4 | centered recovery bottle 4 | centered mushroom cap 4 |
| `player-rogue-v4.webp` | idle 4 | grounded walk 4 | jump rise, apex, fall, hurt | basic 2, Lucky Seven 2 |
| `player-assassin-v4.webp` | idle 4 | grounded walk 4 | jump rise, apex, fall, hurt | basic 2, Lucky Seven 2 |
| `player-hermit-v4.webp` | idle 4 | grounded walk 4 | jump rise, apex, fall, hurt | basic 2, Lucky Seven 2 |
| `player-hokage-v5.webp` | idle 4 | grounded walk 4 | jump rise, apex, fall, hurt | claw 2, Rasengan 2 |
| `equipped-throwing-stars-v1.webp` | tier1 3 + tier2 start | tier2 2 + tier3 2 | tier3 1 + tier4 3 | tier5 4 |
| `hokage-effects-v1.webp` | Rasengan 4 | Nine-Tails aura 4 | Tailed Beast Bomb 4 | Sage aura 4 |
| `hokage-allies-v2.webp` | rival enter 4 | rival throw/exit 4 | striker enter/kick 4 | striker punch/exit 4 |
| `shadow-sentinel-v1.webp` | idle 4 | walk 4 | hurt 4 | defeat 4 |
| `abyss-golem-v3.webp` | idle 4 | walk 4 | hurt 4 | defeat 4 |
| `plague-zombie-v2.webp` | idle 4 | walk 4 | hurt 4 | defeat 4 |
| `moon-wolf-v1.webp` | idle 4 | walk 4 | hurt 4 | defeat 4 |
| `ancient-treant-v1.webp` | idle 4 | walk 4 | hurt 4 | defeat 4 |
| `ember-warden-v2.webp` | idle 4 | walk 4 | hurt 2 + attack 2 | defeat 4 |
| `eclipse-archivist-v2.webp` | idle 4 | walk 4 | hurt 2 + attack 2 | defeat 4 |
| `one-punch-man-v1.webp` | idle 4 | walk 4 | hurt 2 + attack 2 | defeat 4 |
| `dungeon-scout-v1.webp` | idle 4 | talk 4 | brief 4 | approve 4 |
| `game-developer-v1.webp` | idle 4 | talk 4 | code 4 | thanks 4 |
| `dua-pet-v3.webp` | sit/idle 4 | grounded run 4 | fetch 4 | happy 4 |

### P64 좀비·동물·식물형 몬스터 에셋 규격

- `plague-zombie-v2`, `moon-wolf-v1`, `ancient-treant-v1`은 외부 캐릭터를 복제하지 않은 독자
  픽셀 몬스터다. 기존 런타임 시트 3종은 픽셀 밀도·조명·4×4 배치의 스타일 참고에만 사용했다.
- 생성 원본은 `assets/sprites/source/*-v1-chroma.png`, 크로마 제거·최근접 축소·프레임별 4px
  안전 여백·PNG 최적화를 거친 512×512 기준 시트는 `assets/sprites/core/*-v1.png`에 보존한다.
  좀비 기준 시트 `plague-zombie-v2.png`는 v1의 걷기 행 위쪽에 넘어온 분리 픽셀 1,061개만
  `scripts/clean-player-frame-boundaries.mjs`로 제거한다.
- 행은 idle·walk·hurt·defeat 순서다. 늑대는 네발의 낮고 긴 실루엣, 좀비는 비틀린 직립
  실루엣, 나무정령은 뿌리발과 가지팔을 사용하며 로봇 관절·금속 장갑·기어·기계 코어를 금지한다.
- 모든 셀은 배경 알파 0, 바닥과 시각 중심을 보정하는 16개 원점, 인접 셀 연결 0건을 요구한다.
  매니페스트의 `compactMonster`는 `x` 원점 천분율 16개, `y` 원점 픽셀 16개, 행별 `ms`
  4개를 표준 애니메이션과 오른쪽 기본 방향으로 확장한다.

### P59 원펀맨 주먹 충격파 에셋 규격

- `assets/ui/combat/one-punch-shockwave-v1.svg`는 외부 이미지나 기존 표창 시트를 사용하지 않는
  자체 제작 192×96 투명 벡터다. 오른쪽으로 모이는 세 겹의 공기압 호와 속도 파편만 포함한다.
- 런타임은 원펀맨의 전용 `attack` 주먹 타격 프레임에 맞춰 충격파를 손앞에서 생성한다.
  왼쪽 공격은 회전으로 처리하고, 투사체의 진행각과 시각 방향을 항상 일치시킨다.
- 1페이즈는 청백색 단일 충격파, 체력 50% 이하 2페이즈는 금백색 3방향 충격파로 같은 원본을
  재사용한다. 두 패턴 모두 `combatEffects.throwingStar` 애니메이션을 사용하지 않는다.
- 충돌 바디는 투명 여백을 제외한 중심 공기압 영역에 맞추고, 생성·명중·만료·맵 정리 때
  물리 객체와 시각 객체를 같은 수명주기로 제거한다.

### P58·P62 두아 펫 에셋 규격

- `assets/sprites/source/dua-pet-v3-chroma.png`는 달리기 행을 정밀 편집한 생성형 원본,
  `assets/sprites/core/dua-pet-v3.png`는 투명화·정규화한 512×512 기준 시트다. 외부 참조
  이미지는 저장소에 복사하지 않는다. v3 기준 PNG의 1·3·4행은 v2와 픽셀 차이 0으로 보존한다.
- 두아는 연한 황금빛·흰 털, 직립 삼각 귀, 흰 주둥이·목·가슴·배·발, 짧은 다리와 긴 몸통을 가진
  독자적인 SD 웰시코기다. 실제 두아 사진의 특징을 반영해 이마에서 검은 코까지 길고 좁아지는
  쐐기형 얼굴, 덜 부푼 볼털, 바깥쪽이 짙은 아몬드형 눈매, 길쭉한 주둥이와 한쪽 끝이 살짝
  부드럽게 꺾인 귀를 유지한다. 작은 화면에서는
  검은 코와 입, 진한 갈색 눈, 청록색 이름표 목걸이로 플레이어·몬스터와 구분한다.
- 1행은 커닝시티에서 혼자 앉아 있는 자세와 등록 뒤 대기, 2행은 플레이어를 따라가는 오른쪽 방향
  달리기, 3행은 드롭을 향해 달려가 물어 오는 회수 동작, 4행은 멍푸치노를 받은 직후의 기쁨 동작이다.
  2행은 `대각 앞·뒷발 접지 → 다리 회수 → 반대 대각발 접지 → 반대 회수` 순서로, 네 프레임에서
  몸통과 머리 중심을 고정하고 가까운 다리와 어두운 원거리 다리를 분리한다. 네 발이 모두 뜬 자세,
  붙거나 뒤틀린 발, 같은 접지 자세 반복을 허용하지 않는다. 왼쪽 이동은 런타임 `flipX`만 사용한다.
- 모든 프레임은 128×128 고정 셀, `y=116` 공통 발 기준선, 셀 사이 6px 이상 안전 여백을 유지한다.
  털·꼬리·귀 픽셀이 이웃 셀로 넘어가지 않아야 하며 최근접 보간만 사용한다.
- 참조 앱의 캐릭터와 사용자 제공 실제 사진은 체형·얼굴선·색 분할을 언어화하는 용도로만 사용한다.
  참조 사진 원본은 저장소나 배포물에 포함하지 않는다. 런타임 결과물은 게임의 기존
  픽셀 밀도·굵은 외곽선·제한 팔레트에 맞춰 새로 생성하고 원본 프레임을 복사하거나 리샘플링하지 않는다.

### P4 전투 UI 규격

- 데미지 숫자: `assets/ui/combat/damage-numbers-v1.svg`
  - 전체 320×96, 32×32 고정 셀, 10열×3행
  - 1행 일반 몬스터 피해, 2행 강한 공격·럭키세븐, 3행 플레이어 피격
  - 각 행은 숫자 0–9 순서이며 외부 폰트에 의존하지 않는 자체 제작 3×5 픽셀 매트릭스를 사용한다.
- NPC 대화·전직 확인: 접근 가능한 DOM/CSS v1
  - 1280×720 기준 하단 중앙, 폭 `min(860px, 78%)`, 하단 여백 3%
  - 6px 어두운 테두리, 금속성 청회색 패널, 금색 제목과 밝은 본문 팔레트
  - 제목·본문·확인/취소 버튼은 래스터 이미지에 포함하지 않고 `role="dialog"`를 가진 DOM으로 유지한다.
  - P6에서 같은 DOM 구조에 공통 9-slice 디자인 토큰과 전직 완료 연출을 적용했다.

### P5 상태·드롭 UI 규격

- 플레이 HUD는 P46에서 1280×720 기준 y=596부터 하단 124px 안에 캐릭터 정보와
  HP/MP/EXP 게이지, 메소·인벤토리 요약, 액션 바를 분리해 배치한다. 지역명은 화면 왼쪽 위의 작은
  미니맵형 패널로 분리해 실제 플레이 영역을 가리지 않는다.
- 게이지와 상태 텍스트는 Phaser 도형·텍스트로 구성하고 현재 값/최대값을 함께
  표시한다. HP는 붉은색, MP는 푸른색, EXP는 황록색으로 고정한다.
- 메소 주머니·회복 물약·초록버섯의 갓은 셀 혼입 픽셀과 착지 잔상을 제거한
  `world-effects-loot-v2.png`의 `mesoPouch`, `recoveryBottle`, `mushroomCap`
  프레임을 사용한다. 드롭 12개 셀은 수평 중심과 셀 내 `y=116`의 공통 바닥
  기준점을 사용하며, 두 번째 프레임의 위쪽 위치는 의도한 튀어 오름 동작이다.
- 커닝시티 회복 NPC는 전용 `street-healer-v1.png`의 `idle`, `talk`, `cast`,
  `approve` 4행을 사용한다. 기존 `shadow-mentor-v1.png` 청록 틴트 재사용은
  제거했으며, 16개 프레임의 시각 중심과 발 기준선은 매니페스트 원점으로 보정한다.
- 커닝시티 개발자 NPC는 전용 `game-developer-v1.png`의 `idle`, `talk`, `code`,
  `thanks` 4행을 사용한다. 대화·링크 문구는 접근 가능한 DOM으로 분리하고, 16개 프레임의
  시각 중심과 발 기준선은 실제 알파 픽셀을 측정한 매니페스트 원점으로 보정한다.

### P6 공통 픽셀 UI 규격

- 한글·영문·숫자 공통 폰트는 `assets/ui/fonts/`에 번들링한 Galmuri11 v2.40.3을
  사용한다. 일반·Bold WOFF2를 포함하며, Canvas 텍스트가 대체 폰트로 고정되지
  않도록 폰트 로드 완료 후 Phaser를 생성한다.
- Galmuri11 저작권은 Lee Minseo에게 있고 SIL Open Font License 1.1로 배포된다.
  저장소에는 `Galmuri-LICENSE.txt` 전문을 함께 포함한다.
- DOM 책·나무·금속 패널은 `assets/ui/panels/*-panel-v1.svg` 3종을 사용한다.
  원본 크기는 96×96, 고정 모서리는 상하좌우 16px이며 자동 trim과 보간을 하지
  않는다. `scripts/generate-ui-panels.mjs`로 동일한 결과를 재생성할 수 있다.
- DOM은 CSS `border-image`로 SVG 패널을 확장한다. Phaser 플레이 HUD는 P46부터 imagegen
  건메탈 WebP를 `NineSlice`와 내부 표면 이미지로 확장한다.
  DOM 테마는 로그인=책, 캐릭터 선택=나무, 전직 대화=금속이고, Canvas 테마는
  플레이 HUD·미니맵·조작 패널=`hud`로 고정한다.
- 패널 내부의 입력·버튼·제목·상태 텍스트는 접근 가능한 DOM 또는 Phaser Text로
  유지한다. 공통 폰트·깊이·슬라이스·에셋 키는 `src/game/ui/ui-theme.ts`에서 관리한다.
- SVG 중앙 조각은 단색 표면으로 유지한다. DOM은 `border-image`의 `fill`을 쓰지
  않고 테마별 불투명 배경과 고정 3px inset bevel을 사용해 우·하단 렌더링 차이를
  방지하며, 가장자리 장식은 `repeat`로 확장한다.

### P6 커닝시티 환경 레이어 규격

- 기존 1280×720 배경은 화면 고정 원경으로 사용해 1920×720 월드에 비균등 확대하지 않는다.
- `assets/maps/layers/kerning-city-midground-v1.svg`는 1920×720 산업 중경이며
  `scrollFactor 0.55`로 청록·회색 건물, 배관, 케이블 리듬을 보강한다.
- `kerning-city-foreground-v1.svg`는 `scrollFactor 1` 전경이며, 지면 `620`,
  상단 발판 `458`, `508`의 시각 상단을 충돌 발판 데이터와 정확히 일치시킨다.
- 레이어 순서·깊이·스크롤 비율·화면/월드 크기 정책은
  `src/game/maps/map-definitions.ts`를 단일 기준으로 사용한다.
- 두 SVG는 `scripts/generate-map-layers.mjs`로 동일하게 재생성하며 자동 trim,
  보간, 배경 픽셀 기반 충돌을 사용하지 않는다.

### P6 초록버섯굴 환경 레이어 규격

- 기존 1280×720 배경은 화면 고정 원경으로 사용해 1920×720 월드에 가로로
  늘리지 않는다.
- `assets/maps/layers/mushroom-cave-midground-v1.svg`는 1920×720 중경이며
  `scrollFactor 0.38`로 깊은 녹색 수직 암벽·뿌리·발광 버섯 군락을 분리한다.
- `mushroom-cave-foreground-v1.svg`는 `scrollFactor 1` 전경이며, 지면 `610`,
  발판 `453`, `338`, `218`, `168`의 시각 상단과 x 범위·폭을 충돌 발판 데이터에
  일치시킨다. 포탈 소켓은 실제 포탈 좌표 `145,610`과 `1680,180`에 맞춘다.
- 레이어 순서·깊이·스크롤 비율·화면/월드 크기 정책은
  `src/game/maps/map-definitions.ts`를 단일 기준으로 사용하며, SVG의 정렬 메타데이터를
  테스트에서 대조한다.
- 두 SVG는 `scripts/generate-map-layers.mjs`로 결정적으로 재생성하고 배경 픽셀을
  충돌 판정에 사용하지 않는다.

### P6 전투 리듬 규격

- 기본 공격은 2프레임 220ms 중 `110ms`, 럭키세븐은 2프레임 200ms 중
  `55ms`, `160ms`에 표창을 릴리스한다. 선딜과 타격 간격의 단일 기준은
  `src/game/combat/combat-rules.ts`로 유지한다.
- 점프 정점은 수직 속도 `-75..75`, 플레이어 피격 프레임은 `430ms`로 유지한다.
- 초록버섯은 명중 시 `72ms` 흰색 플래시를 표시하고, 4프레임 `520ms` 사망
  애니메이션이 끝난 뒤 EXP와 드롭을 생성한다.
- 표창 잔상은 `45ms` 간격으로 만들고 `105ms` 뒤 제거한다. 잔상과 제거 타이머는
  맵 오브젝트 수명에 묶어 포탈 이동 시 남지 않게 한다.
- 플레이어 피격 무적은 `950ms`, 점멸 간격은 `80ms`이며, 무적 판정과 알파값은
  `src/game/combat/combat-rules.ts`의 순수 로직을 사용한다.

### P6 드롭 착지·획득 연출 규격

- 드롭 본체는 정렬이 끝난 `world-effects-loot-v2.png`의 기존 메소·물약·버섯 갓
  프레임만 사용한다. 착지와 회수 연출 때문에 본체 프레임 원점이나 물리 바디를
  변경하지 않는다.
- 낙하 속도가 충분한 첫 착지와 한 번의 작은 반동 착지에는
  `combat-effects-v1.png`의 `playerHurtSpark`를 드롭 종류별 금색·연두색으로
  틴트해 짧은 바닥 섬광으로 재사용한다. 새 래스터 에셋은 추가하지 않는다.
- 수명이 2.2초 이하로 남은 드롭은 알파 점멸로 소멸을 예고한다. 점멸과 착지 횟수,
  속도 임계값은 `src/game/loot/loot-presentation.ts`의 순수 로직을 기준으로 한다.
- `Z` 회수 시 보상과 저장은 즉시 확정하고, 드롭 이미지는 물리 충돌에서 분리해
  플레이어 상체 방향으로 240ms 이내에 빨려 들어간 뒤 작은 섬광과 함께 제거한다.
  포탈 이동이나 Scene 종료 시 남은 트윈과 이펙트는 맵 오브젝트 수명에 묶어 제거한다.

### P6 포탈·전직·레벨업 연출 규격

- 포탈 전환은 `world-effects-loot-v2.png`의 기존 `portal` 루프와
  `combat-effects-v1.png`의 `jobAdvancementRing`을 청록색으로 재사용한다. 상호작용
  직후 입력을 잠그고 출발 링·카메라 페이드아웃 뒤 목적지 스폰을 배치한 다음,
  페이드인·도착 링까지 끝난 뒤 입력을 복구한다.
- 전직은 저장과 HUD 갱신을 먼저 확정하고 다크로드 `cast`·`approve` 애니메이션,
  플레이어 중심의 보라색/금색 이중 링, 짧은 화면 플래시와 `JOB ADVANCEMENT` 배너를
  순서대로 표시한다. 연출 중에는 이동·공격·포탈 입력을 받지 않는다.
- 레벨업은 EXP·능력치·저장을 먼저 확정하고 플레이어 중심의 금색/연두색 삼중 링,
  12방향 스파크, 화면 플래시·카메라 강조와 `LEVEL UP` 배너를 표시한다. 여러 레벨이
  한 번에 오르면 최종 레벨과 AP 획득량을 배너에 표시한다.
- 세 연출은 새 래스터 에셋을 추가하지 않으며, 타이밍·색상·입력 잠금 길이는
  `src/game/effects/milestone-effects.ts`를 단일 기준으로 사용한다. 생성한 링·배너·트윈과
  타이머는 현재 맵 오브젝트 수명에 묶어 포탈 이동이나 Scene 종료 뒤 남지 않게 한다.

### P8 표창도적 상위 스킬·능력치·강화 레벨업 규격

- 신규 래스터 시트 없이 플레이어 `luckySeven` 투척 모션과 `combat-effects-v1.png`의
  `throwingStar`·`luckySevenHit`을 상위 스킬에 재사용한다. 차별화는 런타임 틴트·크기·관통
  규칙으로 만들며 원본 프레임이나 물리 중심을 변경하지 않는다.
- 럭키세븐은 청백색 2타, 드레인은 연두색 1타와 실제 피해 기반 HP 회복, 어벤저는 보라색
  1타 관통 투사체로 표시한다. 모든 표창 잔상은 기존 맵 수명 정리 규칙을 따른다.
- 능력치 패널은 신규 이미지가 아니라 금속 9-slice와 Galmuri11을 사용하는 DOM 모달로
  구성한다. STR/DEX/INT/LUK 값, 남은 AP, `+` 버튼, 추천 자동분배 설명과 checkbox를
  접근 가능한 텍스트·컨트롤로 유지한다.
- 강화 레벨업은 `jobAdvancementRing`의 금색·연두색 삼중 링과 `playerHurtSpark` 셀을 고정
  이미지로 재사용한 12방향 스파크, 카메라 플래시·짧은 강조, `LV · AP` 배너로 구성한다.
  타이밍·색상·개수는 `src/game/effects/milestone-effects.ts`를 단일 기준으로 사용한다.
- 상위 스킬·능력치·레벨업 강화로 추가되는 외부 에셋은 없으며 기존 자체 제작 에셋과 폰트
  라이선스 범위 안에서 배포한다.

### P9 전직 외형·퀘스트 던전 에셋 규격

- 플레이어 직업 시트 3종은 `player-v1.png`를 포즈·얼굴·셀 배치 편집 기준으로 사용한다.
  로그는 청록 후드·은제 손목 아대, 어쌔신은 적흑 장갑 코트·강화 아대, 허밋은 보라·금색
  망토와 룬 아대를 착용한다. 모든 프레임의 얼굴·체형·발 위치와 4×4 동작 행은 유지한다.
- `throwing-stars-v1.png`는 공격별 한 행을 사용한다. 기본 표창은 은청색, 럭키세븐은
  청록 7광, 드레인은 녹색 흡수 룬, 어벤저는 보라·금색 대형 날개 표창이다. 네 프레임은
  같은 광학 중심에서 90도씩 회전하며 셀 경계를 넘지 않는다.
- 중형 `shadow-sentinel-v1.png`는 가면·사슬 아대를 가진 길드 파수꾼, 대형
  `abyss-golem-v1.png`는 룬 핵과 돌갑옷을 가진 보스로 제작한다. 둘 다 초록버섯과 같은
  idle/walk/hurt/defeat 행을 사용하고, 실제 HP·접촉 피해·크기·바디는 데이터로 분리한다.
- 그림자 시험장 원경은 청흑 지하 석실·룬 문·사슬·보라 광원으로 구성하되 캐릭터가 서는
  하단과 발판 영역은 명암 대비를 낮춘다. 전경 SVG는 지면 `610`, 중단 `440`, 상단 `270`의
  시각 상단을 충돌 데이터와 맞추며 입구 포탈은 `150,610`에 정렬한다.
- 전직 퀘스트는 로그 `초록버섯 5체`, 어쌔신 `그림자 파수꾼 6체`, 허밋
  `심연의 골렘 3체`, 호카게 `심연의 골렘 8체` 처치로 구성한다. 네 시험은 모두 그림자
  시험장 입구를 사용하고 로그 시험 중에는 기존 4개 스폰을 초록버섯으로 치환한다.
  퀘스트명·목표·진척 텍스트는 래스터에 넣지 않고 접근 가능한 DOM 대화와 HUD 상태로 표시한다.
- 생성 원본과 프롬프트는 `assets/sprites/source/`, `assets/maps/source/`, 각 `PROMPTS.md`에
  보존한다. 런타임은 lossless WebP만 번들링하고 기준 PNG와 생성 원본은 `dist/`에서 제외한다.

### P10 스킬 메뉴 UI·아이콘 규격

- 첨부 참고 이미지는 작은 금속 창, 차수 탭, 세로 스킬 목록, 선택 스킬 설명의 정보 위계만
  분석하며 원본 이미지·게임 로고·아이콘·문구를 런타임에 복제하거나 포함하지 않는다.
- 메뉴는 `K`로 여는 접근 가능한 DOM 모달이다. 금속 9-slice와 Galmuri11을 재사용하고,
  직업 차수 탭, 스킬명·현재/최대 레벨, 남은 SP, `+` 버튼, MP·타수·효과 설명을 실제
  텍스트와 버튼으로 유지한다.
- 스킬 목록 순서를 `1→2→…→9→0→-` 11개 액티브 슬롯에 연결하고 각 행·상세 설명에
  숫자키를 텍스트로 표시한다. P28 이후 모든 11개 슬롯에 차수별 액티브가 배정되며 P65 이후
  사용자가 순서를 바꿀 수 있다.
- 럭키세븐·드레인·어벤저 아이콘은 `throwing-stars-v1.png`의 각 직업 행 첫 프레임을 CSS
  배경 위치로 잘라 재사용한다. 셀 원본·매니페스트·투사체 애니메이션은 변경하지 않으며 새
  래스터 에셋을 추가하지 않는다.
- 1280×720에서 메뉴는 HUD를 포함한 게임 화면 안에 들어오고, 800×600 PC 최소 창에서도
  목록과 상세 설명이 잘리지 않도록 내부 스크롤 또는 1열 축소 배치를 사용한다.

### P11 차수별 패시브 스킬·전용 아이콘 규격

- 로그 `예리한 시야`, 어쌔신 `치명 투척`, 허밋 `그림자 호흡`을 차수별 패시브로 추가한다.
  패시브는 숫자 단축키를 차지하지 않고 SP를 분배한 즉시 자동 적용하며, 효과 수치·직업 잠금·
  최대 레벨은 `src/game/skills/skill-rules.ts`를 단일 기준으로 사용한다.
- 액티브·패시브 아이콘 6종은 서로 분리된 정사각형 원본으로 직접 생성한다. 액티브는
  `럭키세븐`의 청록색 숫자 7 형상과 쌍표창, `드레인`의 녹색 흡수 소용돌이와 표창,
  `어벤저`의 보라·금색 날개 표창과 관통 궤적으로 표현한다. 숫자 7 이외의 문자·직업 로고·
  인물 얼굴은 넣지 않는다.
- 패시브는 `예리한 시야`의 청록
  홍채와 멀어지는 은빛 표창, `치명 투척`은 적금색 충격광을 두른 은빛 표창, `그림자 호흡`은
  보라색 후드 실루엣과 청백색 호흡 룬으로 표현한다. 문자·숫자·직업 로고·인물 얼굴은 넣지 않는다.
- 생성 원본은 `assets/ui/source/skill-*-v1-chroma.png`, 정규화 기준은
  `assets/ui/skills/skill-*-v1.png`, 런타임은 같은 stem의 `.webp`에 저장한다. 원본은 균일한
  `#00ff00` 크로마 배경을 사용하고, 배경 제거 뒤 기준 파일을 128×128 RGBA PNG로
  nearest-neighbor 축소한 다음 lossless WebP를 생성한다.
- 아이콘은 16~24색의 단단한 픽셀 클러스터, 중앙의 한 개 실루엣, 가장자리 12px 이상의 투명
  여백을 유지한다. 프레임·배경 장면·문자·워터마크·셀 경계 밖 광원을 금지하고 46×46 CSS 표시에서
  각 패시브의 실루엣과 차수별 청록·적금·보라 팔레트가 구분되어야 한다.
- 여섯 아이콘은 모두 WebP Vite URL import로 번들링하며 `throwing-stars-v1.png`는 기준
  투사체 애니메이션으로만 유지한다. 생성 원본과 기준 PNG는 `dist/`에서 제외한다.

### P12 호카게 4차 외형·차크라 스킬·테스트 상점 에셋 규격 — 구현 완료

- 닌자 실루엣, 청백색 회전 에너지, 방사형 꼬리의 동세와 망토 점유율을 독자 규격으로 정한다.
  기존 작품의 얼굴·문양·의상·동물 캐릭터를 복제하거나 런타임에 포함하지 않는다.
- 배포용 호카게 외형은 `player-hermit-v1.png`의 512×512, 4×4, 128px 셀과 16포즈 계약을
  그대로 따르는 독자적인 여우 그림자 닌자로 제작한다. 남색 닌자 코트, 황동 여우 부적,
  적갈색 허리띠와 비대칭 망토를 핵심 실루엣으로 사용하고 결과물은
  `assets/sprites/core/player-hokage-v1.png`에 둔다.
- `assets/sprites/core/hokage-effects-v1.png`도 같은 4×4 계약을 사용한다. 1행은 손에
  고정되는 청백색 나선환 구체와 명중 파동, 2행은 토글 변신 중 인체를 감싸는 다중 꼬리
  차크라 오라, 3행은 변신 중에만 발사하는 흑자색 미수옥과 폭발, 4행은 금색·청록색
  선인모드 상시 오라로 구성한다. 모든 프레임의 광학 중심은
  `assets/sprites/sprite-manifest.json`의 `frameOrigins`에 기록한다.
- 나선환·구미호 변신·미수옥·선인모드 아이콘은 각각 구체, 꼬리 불꽃, 응축된 흑자색 구체,
  눈과 호흡 룬의 독자적인 실루엣으로 제작한다. P11과 같은 128×128 RGBA PNG, 12px 이상
  투명 여백, 46×46 표시 판독성 규격을 따르며 인물 얼굴·문자·원작 문양은 넣지 않는다.
- 변신 기본공격 할퀴기는 `player-hokage-v1.png`의 기본 공격 포즈와 기존 타격광을 조합하고,
  별도 대형 래스터를 추가하지 않는다. 구미호 오라와 선인모드 오라는 플레이어를 따라가되
  물리 바디·발 기준선을 바꾸지 않으며 두 오라가 동시에 적용될 때도 서로 구분되어야 한다.
- 생성 원본은 `assets/sprites/source/player-hokage-v1-chroma.png`,
  `assets/sprites/source/hokage-effects-v1-chroma.png`,
  `assets/ui/source/skill-*-v1-chroma.png`에 보존하고, 프롬프트와 독자화 판단은 각
  `PROMPTS.md`에 기록한다. 참고 이미지와 생성 원본은 `dist/`에서 제외한다.
- 빠른 테스트용 `서적상인 레오`는 신규 래스터 없이 기존 `streetHealer` 시트의 idle·talk
  동작을 임시 재사용하고 이름표와 상점 역할로 구분한다. 상점은 기존 나무 9-slice를 쓰는
  접근 가능한 DOM 모달로 만들며, `경험의 서`는 실제 텍스트와 CSS 책 표식으로 표현한다.
  전용 상인·아이템 아트는 P12 필수 범위에서 제외하고 기능이 유지될 때 후속 폴리시로 제작한다.
- P12 신규 런타임 이미지 6종은 64/48색 팔레트 최적화로 합계 `101,740B`를 달성해
  110KB 목표와 전체 7MB `dist/` 예산을 지킨다. 새 PCM은 추가하지 않고 기존 자체 제작
  SFX를 재사용한다.

### P13 5등급 표창 장비·상점 에셋 규격 — 구현 완료

- 장비 구역은 `S`로 여는 접근 가능한 능력치·장비 통합 DOM 금속 패널에 둔다. 캐릭터 장착 슬롯, 현재 표창의 등급·
  공격 보너스, 소유 표창 5종과 장착 버튼을 실제 텍스트로 제공하고 래스터에 UI 문구를 넣지 않는다.
- 표창은 1등급 수련생 표창, 2등급 강철 표창, 3등급 청옥 표창, 4등급 홍염 표창,
  5등급 일식 표창으로 구분한다. 같은 실루엣의 단순 틴트가 아니라 날 수·외곽 톱니·중앙 보석·
  잔상 형태가 단계별로 달라야 하며 1280×720 전투 화면에서 회전 중에도 판독되어야 한다.
- `assets/sprites/core/equipped-throwing-stars-v1.png`는 512×512, 4×4, 128px 셀을 사용한다.
  1~4등급은 각 3프레임, 5등급은 4프레임을 사용해 총 16셀을 채우고 프레임별 광학 중심은
  `sprite-manifest.json`에 기록한다. 스킬별 속성색은 런타임 잔상·타격광으로 더하되 장착 표창의
  기본 실루엣과 팔레트는 유지한다.
- built-in imagegen 원본은 1254×1254 균일한 `#ff00ff` 크로마 배경으로 생성해
  `assets/sprites/source/equipped-throwing-stars-v1-chroma.png`에 보존한다. 런타임은 배경 제거,
  nearest-neighbor 512×512 정규화와 4-bit indexed 팔레트 최적화를 거쳐 `20,570B`로 완성해
  22KB 목표를 통과했다.
- 상점은 기존 서적상 레오의 목재 DOM 패널을 확장한다. 표창 상품은 CSS 등급 표식과 런타임
  표창 시트의 정지 프레임을 사용하며 별도 상인·상품 배경 래스터는 추가하지 않는다.

### P14 호카게 시네마틱·삼인 협공 에셋 규격 — 구현 완료

- 나선환, 구미호 변신 활성화, 미수옥, 삼인 협공의 네 액티브는 각각 전용 시네마틱 배경을
  사용한다. `assets/ui/cinematics/hokage-cinematics-v1.webp`는 1280×720 안에 640×360 셀
  4개를 2×2로 배치하고, 청백 소용돌이·적금 꼬리 폭발·흑자색 구체 충돌·청록/진홍 교차 궤적을
  서로 다른 초점과 명암으로 구성한다. 인물 얼굴·문자·원작 문양은 넣지 않는다.
- 시네마틱은 배경 한 장을 복제하지 않고 셀별로 다른 imagegen 장면을 생성한 뒤 WebP로
  최적화한다. 런타임에서는 어두운 비네트, 스캔라인, 짧은 줌·플래시·카메라 진동을 코드로 더하며
  배경 자체에 UI 텍스트를 굽지 않는다.
- 협공 동료는 참고 이미지의 `화면 밖 진입 → 원거리 표창 → 발차기·주먹 → 퇴장` 동작만 분석한다.
  배포용은 원작의 검은 가시머리·분홍머리·이마 보호대·복장·얼굴을 복제하지 않은 독자 캐릭터로,
  은회색 단발과 청록 스카프의 암영 투척가 `시온`, 적갈색 땋은 머리와 비취 권갑의 권격가
  `하나`를 사용한다.
- `assets/sprites/core/hokage-allies-v1.png`는 512×512 4×4 시트다. 1행 시온 진입,
  2행 시온 표창·퇴장, 3행 하나 진입·발차기, 4행 하나 주먹·퇴장으로 구성한다. 원본은
  `assets/sprites/source/hokage-allies-v1-chroma.png`의 균일한 `#ff00ff` 배경으로 보존한다.
- 삼인 협공 아이콘은 128×128 RGBA PNG 한 장으로, 세 방향의 청록·금색·진홍 궤적과 중앙의
  오각 충격광만 표현한다. 문자·인물 얼굴·로고는 금지하고 기존 스킬 아이콘과 같은 46×46 표시
  판독성을 유지한다.
- 런타임은 시네마틱 `105,248B`, 동료 시트 `51,657B`, 협공 아이콘 `5,193B`로 합계
  `162,098B`다. 128색 동료 팔레트와 quality 38 WebP를 직접 비교해 실루엣·광선 판독성을
  유지했고, 전체 `dist/` 상한은 기존 7MB를 유지한다. 참고 이미지·크로마 원본·프롬프트는
  계속 번들에서 제외한다.

### P6 오디오·설정 규격

- BGM 3곡과 SFX 12종은 22,050Hz·16-bit·mono PCM WAV이며
  `scripts/generate-audio-assets.mjs`로 외부 샘플 없이 결정적으로 재생성한다.
- 로그인·캐릭터 선택은 메뉴 BGM, 커닝시티·도적 아지트는 도시 BGM,
  초록버섯굴은 사냥터 BGM을 사용한다. 맵별 BGM의 단일 기준은
  `src/game/maps/map-definitions.ts`의 `bgm` 필드다.
- 효과음은 키 입력이 아니라 점프 성공, 투사체 릴리스·유효 명중, 피해 적용,
  사망 보상·드롭 착지·회수 확정, 포탈·회복·전직·레벨업의 실제 이벤트에 연결한다.
- 전체 음소거와 BGM/SFX 볼륨은 `src/game/settings/local-settings.ts`의 별도 v1 저장소에
  보관한다. 캐릭터 프로필 초기화나 저장과 섞지 않는다.
- 화면별 BGM 중복 재생 방지와 에셋별 기본 게인은 `src/game/audio/game-audio.ts`,
  접근 가능한 음소거 버튼·range UI는 `src/game/ui/audio-settings-controls.ts`가 관리한다.
- 브라우저 자동 재생 정책에 따라 첫 사용자 제스처 전에는 희망 BGM만 기억하고,
  Phaser 오디오 잠금이 해제된 뒤 반복 재생한다.
- 파일별 적용 지점, 생성 방식과 배포 조건은 `assets/audio/README.md`와
  `ASSET_CREDITS.md`에 기록한다.
- 현재 런타임은 P6 절차적 BGM 3종 대신 사용자 제공 48kHz stereo MP3 일반곡·보스곡을
  재인코딩 없이 사용한다. 일반곡은 로그인·캐릭터 화면과 일반 맵에서 같은 키로 연속 반복하고,
  살아 있는 중간·상위·최종 보스가 있으면 보스곡으로 전환한 뒤 처치 완료 시 일반곡으로
  복귀한다. 일반곡 0.90~68.06초·보스곡 0~51.99초 마커로 앞뒤 무음을 제외하고, 같은 키를
  다시 요청하면 사운드 객체와 현재 위치를 유지한다. 두 BGM은 `dist/`에 포함하지만
  8,000,000-byte 총량 계산에서는 제외한다.
- 초기 에셋 목록에는 일반곡과 SFX만 포함한다. 보스 MP3는 최초 보스 조우 시 동적으로
  한 번 요청하고 로딩 중 일반곡을 유지한다. 중복 요청 병합·성공 캐시·실패 재시도와
  맵을 떠난 뒤 끝난 늦은 요청 무시는 `src/game/assets/lazy-audio-loader.ts`가 관리한다.

### P6 시각 회귀 규격

- 기준 화면은 1280×720 CSS px이며 승인된 실제 런타임 캡처와 새 런타임을 나란히 놓고
  구성·색감·정보 위계·화면 점유율을 수동 비교한다.
- 승인 캡처는 픽셀 복제용 원본이 아니라 회귀 확인 기준이며 외부 레퍼런스 파일은 저장하지 않는다.
- 화면·애니메이션·접근성 체크 항목과 P6 기준선 결과는 `VISUAL_QA.md`를 단일 체크리스트로 쓴다.
- 모든 런타임 에셋의 출처·변환·권리 조건과 참고 파일 배포 제외 정책은
  `ASSET_CREDITS.md`를 기준으로 한다.

## 5. 제작 파이프라인

1. 참고 이미지의 역할을 스타일·비율·분위기 참고로 제한한다.
2. 브랜드명, 로고, 기존 캐릭터 복제를 배제한 독자적 디자인을 생성한다.
3. built-in `imagegen`으로 4×4 시트를 단색 크로마 배경에 생성한다.
4. 생성 원본은 덮어쓰지 않고 `source/*-chroma.png`로 보존한다.
5. 원본을 1024×1024로 정규화한다.
6. 설치된 `remove_chroma_key.py`로 배경을 알파로 변환한다.
7. nearest-neighbor로 512×512에 축소하고 메타데이터를 제거한다.
8. 이미지 크기, RGBA 채널, 투명 모서리, 16개 셀의 비어 있지 않음을 검사한다.
9. 런타임 적용 후 프레임 간 실루엣과 발 기준선을 수동으로 정리한다. 드롭 시트는
   `scripts/clean-loot-sprites.mjs`로 연결 성분과 기준선을 재검사한다.

생성에 사용한 프롬프트 세트는 `assets/sprites/PROMPTS.md`에 기록한다.

환경 배경 생성 원본과 런타임 파일, 프롬프트는 `assets/maps/`에 분리해 기록한다. v1 배경은 시각 레이어이며 충돌·스폰·포탈 위치는 `src/game/maps/map-definitions.ts`가 단일 기준이다.

## 6. 품질 게이트

다음 조건을 모두 만족해야 시트를 `통합 가능`으로 표시한다.

- 정확히 512×512 RGBA PNG다.
- 128×128 셀 16개가 모두 비어 있지 않다.
- 네 모서리와 셀 사이 배경이 투명하다.
- 캐릭터의 얼굴, 복장, 팔레트, 크기가 프레임 사이에서 유지된다.
- 셀 경계를 침범하거나 잘린 프레임이 없다.
- 애니메이션의 첫 프레임과 마지막 프레임이 의도한 상태를 표현한다.
- 기존 게임 로고, 텍스트, 워터마크, 직접 복제한 캐릭터가 없다.
- 매니페스트의 프레임 인덱스와 실제 행 구성이 일치한다.

현재 기준 PNG 시트 24종은 크기·알파·셀 점유 검사를 통과했다. 로그·어쌔신·파수꾼·호카게·협공 동료·개발자 NPC 시트는
시각 품질을 유지하는 256색 PNG로 최적화했고, 런타임은 `npm run assets:webp`로 생성한 픽셀
동일 무손실 WebP를 사용한다. 월드/드롭 v2는 이웃 셀 픽셀
혼입 제거와 수평 중심·공통 바닥 기준선 검사를 추가로 통과했다. 최종 아트 확정
전에는 실제 게임 화면에서 속도와 발 기준선을 검수하고 필요한 셀만 수동 보정한다.
P6~P64 통합 결과는 `VISUAL_QA.md`, 최종 번들 에셋 경계와 권리 조건은 `ASSET_CREDITS.md`에 기록했다.

## 7. 에셋 추가 규칙

- 기준 PNG는 `kebab-case-vN.png`, 런타임 파생본은 같은 stem의 `.webp`를 사용한다.
- 기존 파일을 덮어쓰지 않고 `-v2`, `-v3`처럼 버전을 올린다.
- 새 시트를 추가할 때 매니페스트와 이 감사표를 동시에 갱신한다.
- 한 시트에는 같은 크기와 같은 원점 규칙을 공유하는 프레임만 넣는다.
- 배경·UI·스프라이트는 서로 다른 아틀라스로 관리한다.
- 라이선스와 생성·편집 과정을 README 또는 크레딧 문서에 기록한다.
- 핵심 시트·스킬 아이콘의 기준 PNG를 바꾸면 `npm run assets:webp`를 실행하고 디코딩 픽셀
  차이 0, VP8L 헤더, `dist/` PNG 0건을 검증한다.

### P15 던전 회랑·HUD·인벤토리 에셋 규격 — 구현 완료

- 새 던전 5종은 수정 개미굴, 시계태엽 탑, 가라앉은 산호 신전, 잿불 광산, 달빛
  마도서고다. 기존 게임의 지형·간판·캐릭터를 복제하지 않고 초기 2000년대 횡스크롤
  MMORPG의 측면 구도·색 분리·레이어 깊이만 참고한다.
- 원경 원본은 `assets/maps/source/<slug>-v1-source.png`, 런타임은
  `assets/maps/screens/<slug>-v1.webp`에 둔다. 런타임은 1280×720, 텍스트·UI·인물·몬스터·
  포탈을 넣지 않고 하단 22%를 충돌 전경용 저밀도 영역으로 유지한다.
- 충돌 정렬 전경은 `assets/maps/layers/<slug>-foreground-v1.svg`이며 1920×720,
  `shape-rendering="crispEdges"`, `data-platform-*`, `data-portal-*` 속성을 포함한다.
  `scripts/generate-map-layers.mjs`와 `map-definitions.ts`의 좌표가 테스트로 일치해야 한다.
- 수정 개미굴은 호박색 흙·거대 뿌리·청록 수정, 시계태엽 탑은 남청·황동 톱니와 장난감,
  산호 신전은 청록 수중 석조와 분홍 산호, 잿불 광산은 흑철·주황 용암·청색 광석,
  달빛 서고는 남보라 서가·은청 달빛·금색 마력의 팔레트를 사용한다.
- HUD와 인벤토리는 새 래스터를 추가하지 않는다. 기존 금속 9-slice와 로컬
  Galmuri11/Galmuri11 Bold를 재사용하며 텍스트·버튼·아이템 수량은 접근 가능한 Canvas
  텍스트 또는 DOM으로 유지한다. 아이템 아이콘은 CSS 도형과 기존 표창 시트만 사용한다.
- 원본 PNG·프롬프트는 `dist/`에서 제외하고 WebP 5종·SVG 5종만 번들링한다. 5개 WebP
  합계는 약 687KB이며 총 `dist/` 상한은 8,000,000 bytes, JavaScript·gzip 상한은 기존
  1,500,000/450,000 bytes를 유지한다.

### P16 던전 보스·원정 퀘스트 에셋 규격 — 구현 완료

- 중간보스 `폭열군주 이그니카르`는 잿불 광산의 흑철 갑각·용광로 심장·코발트 광맥을 결합한
  독자적인 중장갑 마수이며, 상위보스 `월식현자 루나시온`은 달빛 마도서고의 남보라 석갑·
  금색 봉인문양·은청 책장 파편을 결합한 독자적인 기록 수호자다.
- 퀘스트 NPC `원정대장 세라`는 청록 탐험복·황동 나침반·두루마리를 사용하는 성인
  원정대장으로 제작하며 기존 NPC의 의상·얼굴·소품을 복제하지 않는다.
- 세 시트는 `assets/sprites/source/*-v1-chroma.png`에 생성 원본을 보존하고,
  `assets/sprites/core/*-v1.png`에 512×512 RGBA 기준 시트를 둔다. 같은 stem의 lossless
  WebP를 런타임에 사용한다. 4×4 각 셀은 128×128이며 모든 프레임의 시각 중심과 바닥은
  매니페스트 `frameOrigins`로 보정한다.
- 보스 행은 `idle 4 / walk 4 / hurt 2 + attack 2 / defeat 4`, NPC 행은
  `idle 4 / talk 4 / brief 4 / approve 4` 순서다. 보스는 오른쪽을 향하고 좌측 이동 시
  런타임 flip을 사용하며, NPC는 대화·수락·완료 보고 상태를 서로 다른 행으로 표현한다.
- built-in imagegen의 `stylized-concept` 모드와 균일한 `#ff00ff` 크로마 배경을 사용한다.
  원본에는 격자선·문자·로고·그림자·바닥면을 넣지 않고 각 셀 경계에 충분한 여백을 둔다.
- 퀘스트 알림과 보스 HP 표시는 새 래스터 UI를 만들지 않고 기존 금속 9-slice와 로컬
  Galmuri11을 재사용한다. 보스 전용 재료 아이콘은 접근 가능한 DOM과 CSS 도형으로 만든다.
- 생성 원본·프롬프트·기준 PNG는 `dist/`에서 제외하고 같은 stem의 lossless WebP 3종만
  번들링한다. 아래 크기 수치는 P16 당시 PNG 회귀 근거이며 현재 배포 크기는 P79 감사 결과를 따른다.

### P17 퀘스트 적정 레벨·보스 원거리 스킬 에셋 규격 — 구현 완료

- 전직 시험 4종과 던전 원정 1종의 대화창·HUD 알림에는 로컬 Galmuri11로 `적정 레벨`
  정보를 표시한다. 새 래스터 UI는 추가하지 않고 기존 금속 9-slice와 접근 가능한 DOM/
  Canvas 텍스트를 재사용한다.
- 전직 시험의 적정 레벨은 전직 요구 레벨 10·30·60·120을 단일 기준으로 사용한다.
  던전 원정은 이그니카르·루나시온·원펀맨 난이도를 포괄하는 Lv.80~200으로 표시한다.
- 이그니카르의 `용광로 파편`, 루나시온의 `월식 탄환`, 원펀맨의 `진심 펀치 충격파`는 기존
  `combat-effects-v1.png`의 `throwingStar` 4프레임을 각각 주황·보라색으로 틴트해 사용한다.
  새 스프라이트 시트나 생성 원본은 만들지 않는다.
- 보스 투사체는 물리 바디가 있는 별도 런타임 객체이며 발사 시점의 플레이어 중심을 향한다.
  사거리·수직 허용 범위·선딜·쿨다운·속도·피해·수명·색상은 순수 전투 규칙으로 관리하고,
  명중·만료·맵 전환·씬 종료에서 모두 제거한다.
- 기존 전투 이펙트 재사용 결과 새 래스터 없이 최종 `dist/`는 73파일·7,970,769B,
  JavaScript 1,398,575B·gzip 376,163B로 기존 예산을 통과했다.

### P18 플레이어 보행 주기 에셋 규격 — 구현 완료

- 초보자·로그·어쌔신·허밋·호카게의 걷기 행은 프레임 수를 늘리지 않고 `왼발 접지 →
  통과 → 오른발 접지 → 통과`의 4단계 보행 주기로 다시 만든다. 각 접지 프레임은 앞·뒤
  부츠 실루엣이 작은 게임 화면에서도 구분되어야 하며 공중에 뜬 자세를 사용하지 않는다.
- 기존 4×4·128px 셀·512×512 런타임 계약과 idle/jump/hurt/attack 12개 프레임은 그대로
  유지한다. 걷기 행만 built-in imagegen 결과에서 합성해 전직별 얼굴·장비·팔레트와 기존
  충돌·공격 프레임을 보존한다.
- 생성 원본은 `assets/sprites/source/player*-v2-chroma.png`, 투명 런타임 시트는
  `assets/sprites/core/player*-v2.png`로 버전을 올린다. 런타임 시트는 정확히 512×512 RGBA,
  16개 셀 점유, 투명 모서리, 공통 발 기준선 검사를 통과해야 한다.
- 걷기 애니메이션은 이동 속도에 비례한 재생 속도와 4프레임별 미세한 상하 보폭을 사용하되,
  물리 바디와 월드 발 기준선은 고정한다. 정지·공중·공격·피격 상태에서는 보행 보폭을 즉시
  제거한다.
- 최종 런타임 시트는 초보자 `36,898B`, 로그 `49,500B`, 어쌔신 `55,771B`, 허밋
  `69,642B`, 호카게 `52,964B`다. 다섯 시트 모두 512×512 RGBA와 16개 셀 점유를 유지하며,
  생성 크로마 원본과 사용하지 않는 v1 시트는 `dist/`에서 제외한다.

### P19 플레이어 무릎 회수 자세 에셋 규격 — 구현 완료

- v2의 접지 프레임 `4·6`과 idle·jump·hurt·attack 12개 프레임은 픽셀 단위로 유지하고,
  통과 프레임 `5·7`만 교체한다. 프레임 수는 늘리지 않는다.
- 통과 자세는 한쪽 지지발을 몸통 바로 아래에 두고 반대쪽 무릎을 굽혀 뒤꿈치가 종아리 쪽으로
  들린 삼각형 실루엣이어야 한다. 두 다리가 곧게 벌어진 축소 접지 자세는 허용하지 않는다.
- built-in imagegen 결과는 2행 2·4열만 합성하고 `remove_chroma_key.py`의 despill과
  `edge-contract 1`을 적용한다. 셀 하단의 본체와 분리된 잔여 성분은 제거한다.
- 생성 원본은 `assets/sprites/source/player*-v3-chroma.png`, 런타임은
  `assets/sprites/core/player*-v3.png`로 보존한다. P26 배포 런타임은 v3에서 인접 셀 오염만
  제거한 로그·어쌔신·허밋 `player-*-v4.png`이며 512×512 PaletteAlpha 256색과 원래 팔레트,
  비오염 픽셀을 그대로 보존한다.
- 통과 프레임의 `frameOrigins`는 지지발 기준으로 다시 측정하고, 이동 시작은 animation-local
  frame `1`에서 시작해 첫 접지 자세가 수평으로 미끄러지는 인상을 줄인다.

### P20 호카게 독자 SD 외형 에셋 규격 — 구현 완료

- 4차 직업 호카게는 금발 가시머리, 이마 보호대, 양 볼의 짧은 3줄 표식, 검정·주황 집업
  복장과 오픈토 닌자 부츠를 한 캐릭터로 읽히게 SD화한다.
- 기존 512×512·4×4·128px 셀 계약과 `idle 4 / walk 4 / jump·hurt 4 / attack 4` 순서는
  유지한다. 2행은 접지·무릎 회수·반대 접지·반대 무릎 회수로 구성하고, 4행은 근접 공격
  2프레임과 청백색 나선환 2프레임을 사용한다.
- built-in imagegen에는 텍스트 외형 규격과 기존 `player-hokage-v3.png`를 포즈·배치 기준으로
  입력한다. 최종 크로마 원본은 `assets/sprites/source/player-hokage-v4-chroma.png`,
  투명 변환본은 `assets/sprites/core/player-hokage-v4.png`, P26 경계 정리 배포본은
  `assets/sprites/core/player-hokage-v5.png`로 보존한다.
- 기준 PNG는 512×512 PaletteAlpha 256색, 16셀 점유, 투명 모서리 조건을 만족해야 한다.
  idle·walk·공격 프레임의 `frameOrigins`는 새 얼굴 중심과 실제 발바닥을 다시 측정해 물리
  발 기준선보다 26px 위에 그림의 발이 오도록 맞춘다.
- 외부 레퍼런스 파일은 저장소·Vite import·`dist/`에 두지 않는다. 생성형 런타임 결과의
  공개 배포 전에는 캐릭터 디자인과 생성 서비스 약관에 대한 권리 확인을 선행한다.

### P26 스프라이트 셀 경계 정리 규격 — 구현 완료

- `scripts/clean-player-frame-boundaries.mjs`는 각 128×128 셀에서 주 본체·의도 이펙트와
  분리되고 인접 셀 본체로 이어진 것으로 확정한 컴포넌트만 투명화한다.
- 입력은 로그·어쌔신·허밋 v3, 호카게 v4, 심연의 골렘·이그니카르·루나시온 v1이며,
  출력은 각각 플레이어 v4/v5와 몬스터·보스 v2다. 생성 크로마 원본과 기존 런타임은
  덮어쓰지 않는다.
- 정리 전후 RGBA를 전 픽셀 비교해 추가 0·재색상 0과 시트별 지정 제거량을 강제한다.
  팔레트 PNG와 RGBA PNG의 색 형식, 프레임 원점, 애니메이션 인덱스는 바꾸지 않는다.
- `src/game/assets/sprite-pixel-alignment.test.ts`는 24시트·384셀 점유·투명 코너·크로마와
  플레이어 idle/walk 중심·발 기준선, 지상 엔티티 전 프레임 중심·바닥을 실제 알파 픽셀로
  검증한다.

### P27 몬스터 내부 셀 연결 감사 규격 — 구현 완료

- 몬스터 9종·144셀은 128px 수직·수평 내부 경계를 사이에 두고 양쪽 픽셀이 동시에 가시
  알파를 갖는 연결 지점이 0건이어야 한다. 바닥에 닿는 픽셀과 셀 안의 의도된 파편은 유지한다.
- 심연의 골렘 v2에서 보행 4↔피격 8의 수평 연결 1곳, 피격 10↔11의 수직 연결 8곳을 만든
  분리 컴포넌트 24픽셀만 제거해 `abyss-golem-v3.png`로 배포한다.
- v2 대비 추가·재색상은 0건이며 `frameOrigins`, 애니메이션 인덱스, 512×512 RGBA 형식은
  그대로 유지한다.

### P21 삼인 협공 동료 독자 SD 외형 에셋 규격 — 구현 완료

- 시온 역할의 1·2행은 검푸른 가시머리, 이마 보호대, 남색 상의, 흰 반바지, 붕대와
  오픈토 부츠를 사용하는 독자 SD 외형이다.
- 하나 역할의 3·4행은 분홍 단발, 붉은 민소매 상의와 흰 원형 무늬, 검정 반바지,
  오픈토 부츠를 사용하는 독자 SD 외형이다.
- 기존 512×512·4×4·128px 셀과 `진입 4 → 투척 2 → 퇴장 2 / 진입 2 → 발차기 2 →
  주먹 2 → 퇴장 2` 프레임 순서, screen-space 중심 원점 `0.5, 0.5`를 유지한다.
- built-in imagegen에는 두 동료의 텍스트 외형 규격과 `hokage-allies-v1.png`를 포즈·효과·
  배치 기준으로 입력한다. 크로마 원본은 `source/hokage-allies-v2-chroma.png`, 투명
  런타임은 `core/hokage-allies-v2.png`로 비파괴 버전업한다.
- 기준 PNG는 512×512 PaletteAlpha 256색, 16셀 점유, 투명 모서리, 셀 경계 침범 없음
  조건을 만족해야 한다. 외부 레퍼런스 파일은 저장소·Vite·`dist/`에 두지 않으며 공개·상업
  배포 전 두 캐릭터 디자인과 생성 서비스 약관의 권리를 확인한다.

### P22 거대 최종보스·무한의 결투장 에셋 규격

- 잿불 광산 중간보스의 고유명은 `폭열군주 이그니카르`, 달빛 마도서고 상위보스의 고유명은
  `월식현자 루나시온`으로 바꾼다. 새 최종보스는 대머리·노란 슈트·흰 망토·붉은 장갑과
  부츠를 SD 픽셀 아트로 표현한 `원펀맨` 한 체만 사용한다.
- 최종보스 시트는 `assets/sprites/source/one-punch-man-v1-chroma.png`에 built-in imagegen
  크로마 원본을 보존하고 `assets/sprites/core/one-punch-man-v1.png`에 512×512 PaletteAlpha
  기준 PNG를 둔 뒤 같은 stem의 lossless WebP를 런타임에 사용한다. 프레임 순서는
  `idle 4 / walk 4 / hurt 2 + attack 2 / defeat 4`이며,
  128×128 셀과 프레임별 중심·바닥 원점 계약을 유지한다.
- 런타임에서 원펀맨은 플레이어보다 두 배 이상 크게 표시하고, 물리 바디는 보이는 몸통과
  지면 기준선에 맞춘다. 접촉 공격과 `진심 펀치 충격파`는 피격 시 현재 HP 전부를 잃게 하며,
  투사체 수명과 맵 정리는 기존 보스 원거리 공격 수명주기를 공유한다.
- 전용 맵 `무한의 결투장`은 원경 원본 `assets/maps/source/infinite-duel-ground-v1-source.png`,
  1280×720 런타임 `assets/maps/screens/infinite-duel-ground-v1.webp`, 1920×720 충돌 정렬 전경
  `assets/maps/layers/infinite-duel-ground-foreground-v1.svg`로 구성한다. 연속 지면과 2단
  단방향 발판 5개, 달빛 마도서고 귀환 포탈을 두며 몬스터는 원펀맨 한 체, NPC와 다른
  몬스터는 0체로 고정한다. 발판 높이 차는 기존 660px/s 점프로 층별 이동 가능한 150~170px다.
- 첨부 이미지는 외형·색상 참고로만 사용하고 포스터 배경·문자·구도는 런타임에 포함하지 않는다.
  생성 원본·기준 PNG와 참고 파일은 `dist/`에서 제외하며 외부 공개·상업 배포 전 원 이미지와
  캐릭터의 권리를 확인한다. 현재 배포는 원펀맨 lossless WebP와 결투장 WebP·SVG를 사용한다.

### P24 HUD 안전 여백·공격 잔상·허름한 초보자 에셋 규격 — 구현 완료

- 모든 gameplay 9-slice HUD는 16px 테두리 바깥에 텍스트를 두지 않는다. 패널 내부 텍스트는
  최소 18px 수평·16px 수직 안전 여백을 적용하고, 제목 띠가 있는 패널은 제목 기준선을
  테두리 안쪽 8px 이상에 둔다. 퀘스트·보스·미니맵·조작·하단 상태바의 텍스트 bounds를
  `src/game/ui/hud-layout-rules.ts`의 순수 판정으로 회귀 검증한다.
- 기본 공격과 액티브 공격 스킬은 플레이어 공격 프레임을 복제한 반투명 잔상을 진행 방향
  반대쪽에 2~5개 남기고 120~220ms 안에 감쇠한다. 공격별 개수·간격·오프셋·알파·틴트는
  `src/game/effects/attack-afterimage-rules.ts`를 단일 기준으로 사용하며 맵 정리 때 함께 제거한다.
- 초보자 외형은 사용자 제공 `codex-clipboard-693e895f-176c-4ab9-8a2c-a6e1a378e8bd.png`의
  낡은 갈색 귀덮개 두건, 누더기 민소매 상의, 짧은 바지, 붕대와 닳은 신발 인상만 참고한다.
  얼굴·비율·포즈는 프로젝트의 독자적인 SD 픽셀 캐릭터로 다시 설계한다.
- built-in imagegen에는 사용자 이미지를 외형·재질 참고, 기존 `player-v3.png`를 4×4 포즈·
  배치 참고로 입력한다. 프레임 순서는 `idle 4 / walk 4 / jump rise·apex·fall·hurt /
  basic attack 2 / lucky seven 2`, 각 셀 128×128, 오른쪽 방향을 유지한다.
- 크로마 원본은 `assets/sprites/source/player-v4-chroma.png`, 투명 기준 파일은
  `assets/sprites/core/player-v4.png` 기준 PNG로 비파괴 버전업한다. 기준 파일은 512×512
  PaletteAlpha 256색, 투명 모서리, 16셀 점유, 셀 경계 침범 없음, 실제 발바닥 기준
  `frameOrigins` 재측정을 통과한다. 런타임은 같은 stem의 lossless WebP이며 참고·생성 원본과
  기준 PNG는 Vite와 `dist/`에서 제외한다.

### P28 차수별 신규 액티브 스킬 에셋 규격 — 구현 완료

- 액티브 숫자 슬롯은 전직 차수별 해금 순서대로 `1=럭키세븐`, `2=그림자 연사`,
  `3=드레인`, `4=환영 쌍성`, `5=어벤저`, `6=심연 폭우`, `7=나선환`, `8=구미호 변신`,
  `9=미수옥`, `0=삼인 협공`, `-=천뢰옥`에 자동 배정한다.
- 1차 로그 `그림자 연사`는 청백색 소형 표창 세 개와 짧은 연속 궤적, 2차 어쌔신
  `환영 쌍성`은 적자색·은색 표창 두 개와 서로 어긋난 잔상, 3차 허밋 `심연 폭우`는
  보라색 대형 표창과 아래로 갈라지는 세 갈래 궤적, 4차 호카게 `천뢰옥`은 청록색 구체를
  감싸는 금색 번개 고리로 표현한다. 문자·숫자·직업 로고·인물 얼굴은 넣지 않는다.
- 네 아이콘은 `assets/ui/skills/skill-*-v1.png`의 128×128 RGBA 기준 PNG로 제공하고,
  built-in imagegen 크로마 원본은 `assets/ui/source/skill-*-v1-chroma.png`에 보존한다.
  P11과 같은 단단한 픽셀 클러스터·중앙 단일 실루엣·가장자리 12px 이상 여백을 유지하고
  46×46 표시에서도 각 차수의 타수와 팔레트가 구분되어야 한다.
- 아이콘 원본은 피사체에 쓰이지 않는 균일한 `#00ff00` 크로마 배경으로 생성하고, 설치된
  `remove_chroma_key.py`의 soft matte·despill로 알파를 만든 뒤 nearest-neighbor 128×128로
  축소한다. 원본과 기준 PNG는 `dist/`에서 제외하고 같은 stem의 lossless WebP만 Vite URL
  import로 번들링한다.
- 신규 공격은 기존 장착 표창과 `hokageEffects` 투사체 애니메이션을 재사용한다. 별도 물리
  시트는 추가하지 않고 차수별 크기·속도·잔상·타격광 틴트로 구분하며, 모든 투사체는 기존
  프레임 중심·수명·관통 대상 정리 규칙을 따른다.

### P29 하단 상태창 액티브 아이콘 바 규격 — 구현 완료

- 새 래스터를 만들지 않고 `assets/ui/skills/`의 액티브 11종 128×128 lossless WebP를 재사용한다.
  `src/game/assets/skill-icon-assets.ts`가 스킬창 URL과 Phaser 프리로드 키를 함께 제공한다.
- 하단 상태창은 468×44 action bar 안에 38×38 슬롯 11개를 3px 간격으로 한 줄 배치하고,
  원본 아이콘은 nearest 표시로 34×34에 맞춘다. 슬롯 순서는 `1~0/-` 해금 순서를 따른다.
- 단축키는 각 슬롯 왼쪽 위의 14×13 금속 배지와 10px Galmuri 글자로 표시한다. 미해금
  아이콘 알파는 0.26으로 낮추고 청회색 틴트를 적용하며, 활성 구미호 변신은 금색 테두리와
  배지로 강조한다.
- 아이콘 바는 HP·MP·EXP 게이지 위에 두고 세 게이지 라벨과 값은 아래 행에 정렬한다.
  1280×720에서 하단 9-slice와 각 텍스트 bounds를 벗어나거나 재화·성장 셀과 겹치지 않는다.

### P30 QWER 보조키·능력치/장비 통합 창 규격 — 구현 완료

- 새 래스터는 추가하지 않는다. 기존 금속 9-slice DOM 패널 안에서 능력치 분배를 왼쪽,
  표창 장비 5종을 오른쪽에 배치하고 `S` 한 번으로 함께 연다.
- 통합 패널은 1280×720에서 최대 1040px 너비를 사용하며, 장비 카드는 48px 표창 미리보기와
  등급·피해·소유/장착 버튼을 2열로 표시한다. 작은 PC 창에서는 패널 내부 스크롤을 허용한다.

### P31 초반 액티브 고유 궤적·타격 연출 규격 — 구현 완료

- 새 래스터나 외부 에셋을 추가하지 않고 기존 장착 표창 프레임·전투 링에 Phaser 도형·틴트·
  트윈을 조합한다. 럭키세븐은 높이가 다른 청록 평행 쌍투척, 그림자 연사는 청백 3갈래
  부채꼴과 3개 파편, 드레인은 녹색 맥동과 적→플레이어 흡수 연결선·입자, 환영 쌍성은
  청록/적홍 반대 위상 파동과 X자 타격으로 구분한다.
- 궤적 도형은 기존 투사체 물리 바디와 관통·수명 규칙을 유지하며, 잔상·연결선·파편·X 타격은
  `mapObjects`와 맵 타이머·트윈에 등록해 명중·만료·포탈·씬 종료 뒤 남지 않게 한다.

### P32 스킬 숫자 슬롯 편집 UI 규격 — 구현 완료

- 새 래스터를 만들지 않고 `assets/ui/skills/`의 액티브 11종 128×128 PNG를 재사용한다.
  `K` 금속 패널 상단에 `1~0/-` 11칸을 한 줄로 배치하고 각 42px 아이콘 왼쪽 위에 숫자
  배지를 겹쳐 표시한다.
- 기본 순서는 1차 로그 액티브부터 2·3·4차 순서이며, 아이콘을 다른 칸에 드롭하면 두 슬롯을
  교환한다. 드래그 원본은 반투명, 드롭 대상은 금색 테두리·광택으로 구분하고 키보드 사용자는
  `Alt+←/→`로 같은 교환을 수행한다.
- 작은 지원 PC 창에서는 아이콘을 34px로 줄이고 도움말을 숨겨 패널 내부 너비를 지킨다.
  슬롯 행·차수 탭·스킬 목록은 패널 내부 스크롤 범위에 남기며 실제 텍스트를 래스터에 넣지 않는다.

### P33 전직 차수별 이펙트 밀도 규격 — 구현 완료

- 새 래스터를 추가하지 않는다. 기존 `jobAdvancementRing`과 Phaser 방사형 사각광·카메라 shake를
  조합해 1차는 추가 링 0, 2차는 시전/타격 링 각 1, 3차는 각 2와 4방향 방사광, 4차는 각 3과
  8방향 방사광을 사용한다. 3·4차만 짧은 카메라 타격 진동을 적용한다.
- 4차 천뢰옥의 640ms 단축 컷인은 `hokage-cinematics-v1.webp` 0번 나선 차크라 프레임을
  금빛 wash·제목·프레임으로 재해석해 재사용한다. 2×2 아틀라스와 번들 파일 수는 늘리지 않는다.
- 다중 링·방사광도 맵 오브젝트와 타이머·트윈에 등록하고, 차수 상승에 따른 밀도 외에 기존
  스킬별 고유 궤적·색상·폭발·협공 동료 실루엣을 우선 식별자로 유지한다.

### P34 인내의 숲 환경 에셋 규격 — 구현 완료, P35에서 확장

- 원본 `assets/maps/source/patience-forest-v1-source.png`는 built-in imagegen 생성본을 보존하고,
  런타임 `assets/maps/screens/patience-forest-v1.webp`는 정확히 1280×720으로 정규화한다.
- 원경은 청록 안개·거목·폭포·이끼·호박색 반딧불의 독자적인 픽셀 숲이며 캐릭터·몬스터·UI·
  텍스트·가시적 충돌 발판을 포함하지 않는다.
- P34의 `assets/maps/layers/patience-forest-foreground-v1.svg`는 1920×720이었으며, P35에서
  1920×1440으로 확장한다. crisp edges를 유지하고
  `scripts/generate-map-layers.mjs`로 재생성한다. 나무·이끼 전경의 각 발판과 포탈은
  `data-platform-*`·`data-portal-*`로 `map-definitions.ts` 좌표와 일치해야 한다.
- 런타임은 화면 고정 원경과 월드 정렬 전경 2단만 번들링하고, 원경 픽셀을 충돌 판정에
  사용하지 않는다. WebP와 SVG는 8,000,000-byte 배포 총량 안에 들어야 한다.

### P35 고난도 인내의 숲·줄타기·정상 보상 규격 — 구현 완료

- 인내의 숲은 1920×1440 세로 월드와 같은 크기의 결정적 SVG 전경을 사용한다. 1280×720
  원경은 `scrollFactor 0`으로 화면에 고정하고, 발판·줄·장애물의 실제 판정은
  `map-definitions.ts` 데이터만 사용한다.
- 연속 지면과 단방향 발판 20개 이상, 줄 4개, 움직이는 장애물 8개
  이상을 배치한다. 줄은 `data-climbable-*`, 장애물은 `data-hazard-*`, 발판·포탈은
  기존 `data-platform-*`·`data-portal-*`로 생성 SVG와 월드 좌표를 대조한다.
- 줄은 P37에서 imagegen 이끼 목재 프레임으로 교체하고 SVG에는 좌표 메타데이터만 유지한다.
  장애물은 Phaser 도형과 결정적 왕복 이동 규칙으로 렌더링하며 접촉 시 HP 피해 없이 코스
  시작점으로 돌려보낸다.
- 정상 최초 도달 보상은 1,000,000 메소와 6등급 보상 전용 투척무기 `초대형 고드름`이다.
  기존 빙결 투사체 프레임을 재사용하되 1.75배 크기와 빙청색 강조를 적용하고, 피해 배율은
  기존 5등급 1.5보다 높은 2.0으로 고정한다. 상점 목록에는 노출하지 않는다.

### P36 기존 던전 줄타기 오브젝트 규격 — 구현 완료

- 그림자 시험장과 던전 회랑 5종에 각각 줄 1개를 둔다. 줄은 지면과 첫 단방향 발판을
  연결하고 입구·귀환 포탈, 보스 스폰과 겹치지 않게 배치한다. 무한의 결투장은 줄 없이
  연속 지면·2단 발판 5개·귀환 포탈·원펀맨 1체만 유지한다.
- 실제 부착 범위와 상·하단 좌표는 `map-definitions.ts`의 `climbables`를 단일 기준으로 사용한다.
  그림자 시험장 수작업 SVG와 `generate-map-layers.mjs`가 만드는 회랑 SVG는 각각
  `data-climbable-id|kind|x|top|bottom` 메타데이터로 같은 좌표를 노출한다.
- 줄은 각 던전 팔레트에 맞춘 4px 주선·1px 하이라이트·14px 간격 매듭으로 표현한다.
  새 래스터 파일은 추가하지 않으며 기존 1920×720 전경과 8,000,000-byte 배포 예산을 유지한다.

### P37 전 맵 imagegen 환경 오브젝트·사다리 제거 규격 — 구현 완료

- 11개 맵은 배경 원본을 스타일 참고로 built-in imagegen이 만든 전용 오브젝트 키트를 사용한다.
  `assets/maps/objects/map-object-kits-a|b-v1.webp`와 바닥 포탈 광륜을 합친
  `map-object-kits-c-v2.webp`의 512×512 투명 아틀라스에 맵별 발판·줄·포탈을 2×2 셀로
  담고, `patience-hazards-v1.webp` 384×384에는 통나무·도토리·가시 구체를 담는다.
- 크로마 원본 4개는 `assets/maps/source/*-chroma.png`에 보존한다. flat `#ff00ff` 배경을
  imagegen 스킬의 `remove_chroma_key.py`로 제거하고, 투명 WebP는 512/384px로 축소해 번들링한다.
- 런타임은 SVG 충돌 정렬 전경을 번들링하지 않는다. 기존 SVG와 생성 스크립트는 좌표 메타데이터
  검증 소스로 유지하고 실제 화면은 `map-definitions.ts`의 `objectTheme`·발판·포탈·줄 좌표에
  imagegen 프레임을 배치한다. 배경·중경과 충돌 데이터는 그대로 분리한다.
- 사다리 타입과 사다리 프레임은 전부 제거한다. 기존 인내의 숲 중앙 사다리는 같은 top/bottom을
  가진 `patience-rope-middle` 줄로 바꾸며, 모든 `climbables`의 kind는 `rope`만 허용한다.
- 오브젝트에 텍스트·UI·캐릭터·몬스터·상표·워터마크를 포함하지 않고, `dist/` 총량은 계속
  8,000,000 bytes 이하로 유지한다.

### P38 imagegen 오브젝트 접지·포탈 광원 규격 — 구현 완료

- 256×256 테마 셀을 그대로 자르지 않고 알파 연결 영역으로 발판·줄·포탈의 독립 프레임 경계를
  고정한다. 프레임 사각형에는 다른 오브젝트의 픽셀이 들어가지 않아야 한다.
- 발판은 불투명 프레임 상단을 충돌체 상단에 맞추고, 줄은 불투명 프레임 상단을 등반 `top`에,
  포탈은 불투명 프레임 하단을 포탈 `y`에 맞춘다.
- 포탈 바닥 광원은 신규 래스터를 추가하지 않고 Phaser 타원 2개로 만든다. 외곽은 맵별 강조색과
  ADD 블렌드·860ms 왕복 맥동을 사용하고 중심광은 흰색으로 유지한다.
- 광원 객체와 반복 트윈은 포탈 이미지·라벨과 같은 맵 수명에 묶어 이동·Scene 종료 뒤 남지 않게 한다.

### P46 imagegen 플레이 HUD 패널 규격 — 구현 완료

- built-in imagegen 원본 `assets/ui/source/hud-metal-panel-v1-source.png`는 정면 1:1 건메탈 패널,
  동일한 네 모서리와 균일한 가장자리, 낮은 대비의 중앙 표면만 포함한다. 텍스트·숫자·아이콘·
  로고·캐릭터·외부 그림자·투명 여백을 넣지 않는다.
- 런타임 프레임 `assets/ui/panels/hud-metal-panel-v1.webp`와 내부 표면
  `hud-metal-surface-v1.webp`는 각각 96×96이다. 프레임은 16px 고정 슬라이스로 늘리고,
  내부 표면은 제목띠·본문·정보 셀·게이지 트랙·스킬 슬롯에 색 틴트와 알파만 달리해 재사용한다.
- 하단 HUD는 y=596·높이 124px이며 action bar는 `(281, 612, 468, 44)`, HP/MP/EXP는
  y=662·높이 42px에 배치한다. 캐릭터·재화·성장 셀을 포함한 모든 이웃 영역은 최소 4px를
  두고 분리하며 레벨 배지는 캐릭터 셀 안에 포함한다.
- 미니맵·퀘스트·조작·보스와 플레이 사운드 설정은 같은 생성 프레임/표면을 사용한다. 상단 Canvas
  창은 외곽 패널과 별도 제목/본문 표면을 사용하며 실제 텍스트 bounds는
  `hud-layout-rules.ts`의 `HUD_CONTENT_BOUNDS` 안에 있어야 하고 보스 제목과 HP 게이지는 서로
  다른 행을 사용한다.
- 원본 프롬프트와 정규화 경로는 `assets/ui/PROMPTS.md`, 생성·배포 출처는
  `ASSET_CREDITS.md`에 기록한다. 원본 PNG는 Vite에서 import하지 않고 두 런타임 WebP만
  배포해 8,000,000-byte 총량 제한을 지킨다.

### P49 미니맵 포탈 표식·imagegen 포탈 에너지 규격 — 구현 완료

- 미니맵은 `map-definitions.ts`의 모든 포탈 좌표를 기존 비율 유지 투영으로 표시한다. 포탈은
  플레이어의 노란색·NPC의 초록색과 겹치지 않는 청록색 이중 마커를 사용하고, 제목 영역에
  `포탈` 범례를 추가한다. 접힌 상태에서는 지형·플레이어·NPC·포탈 마커를 함께 숨긴다.
- built-in imagegen 원본 `assets/maps/source/portal-ground-fx-v1-source.png`는 사용자 제공
  포탈 참고 이미지의 유백색·빙청색·연두색 차원 에너지 인상만 참고한 독자적인 수평 마법진이다.
  문틀·문자·상표·워터마크·배경 장면은 넣지 않는다.
- 런타임 효과는 224×112 프레임으로 축소해 기존 512×512 맵 오브젝트 아틀라스의 비어 있는
  셀에 넣는다. 같은 프레임을 문틀 뒤의 세로 에너지·바닥 이중 광륜으로 재사용하고 ADD 블렌드,
  서로 다른 호흡 주기, 작은 각도 흔들림으로 정적 포탈과 분리한다.
- 각 포탈에는 고정 개수의 청백색 에너지 파편을 배치해 아래에서 위로 반복 이동·소멸시킨다.
  효과 이미지·파편·반복 트윈은 전부 `mapObjects`와 현재 맵 수명에 묶어 포탈 이동이나 Scene
  종료 뒤 남지 않게 하며, 생성·제거 중 런타임 객체 수가 흔들리지 않아야 한다.
- 생성 원본은 Vite에서 import하지 않고 갱신된 런타임 아틀라스만 번들링한다. `dist/` 총량은
  8,000,000 bytes 이하를 유지하고, 미니맵 포탈 수·애니메이션 효과 수는 E2E 상태 표식으로
  검증한다.

### P50 개발자 채널 홍보 NPC 규격 — 구현 완료

- 사용자 지정 채널 프로필 이미지는 인물 외형 참고, `street-healer-v1.png`는 4×4 셀 배치와
  픽셀 밀도 참고로만 사용한다. 생성 결과는 갈색 비니·검은 작업복·청록 공구 가방·노트북을
  가진 SD 픽셀 캐릭터이며 실제 로고·상표·문자·UI를 시트에 포함하지 않는다.
- built-in imagegen의 1254×1254 flat `#ff00ff` 생성 원본은
  `assets/sprites/source/game-developer-v1-chroma.png`, 크로마 제거·nearest-neighbor 축소·
  256색 최적화한 512×512 기준 PNG는 `assets/sprites/core/game-developer-v1.png`에 보존한다.
- 행은 `idle·talk·code·thanks` 각 4프레임이며, 16개 셀은 모두 50px 초과 가시 픽셀,
  투명 모서리, 가시 마젠타 0건과 -2~-1px 발 기준선을 만족한다.
- 채널명·설명·`구독하기`는 래스터가 아닌 `role="dialog"` DOM으로 제공한다. 링크는
  `target="_blank" rel="noopener noreferrer"`를 사용하고 실제 구독 여부는 저장하지 않는다.
- 크로마 원본·참고 이미지·기준 PNG는 Vite에서 import하지 않고 같은 stem의 lossless WebP만
  번들링한다. 생성 프롬프트·입력 역할은 `assets/sprites/PROMPTS.md`, 출처·배포 경계는
  `ASSET_CREDITS.md`에 기록한다.

### P55 인벤토리 소모품 사용 UI 규격 — 구현 완료

- 새 래스터 에셋은 추가하지 않는다. 회복 물약과 경험의 서는 기존 인벤토리 CSS 아이콘을
  그대로 사용하고, 재료·표창과 같은 24칸 그리드 안에서 선택한다.
- 선택한 소모품의 `사용하기`는 상세 패널 안의 접근 가능한 DOM 버튼으로 제공한다. 버튼과
  결과 수량은 팝업 자체 스크롤·800×600 최소 PC 창 경계를 침범하지 않아야 한다.
- 회복 물약 성공 효과음은 기존 `AudioAssetKey.Recovery`, 경험의 서 레벨업은 기존 마일스톤
  SFX·링·배너를 재사용한다. 텍스트·버튼 상태·수량을 이미지에 포함하지 않는다.
- 재료와 표창에는 사용 버튼을 표시하지 않으며, 신규 에셋이 없으므로 `dist/`의 BGM 제외
  8,000,000-byte 총량과 기존 개별 상한을 그대로 유지한다.

### P68 원펀맨 클리어 픽셀 아트 크레딧 규격 — 구현 완료

- 생성 배경은 16:9 달빛 결투장 폐허와 금빛 여명, 좌우의 독자 캐릭터 실루엣만 사용하고,
  중앙 48%는 한글 DOM 크레딧을 위한 낮은 디테일·어두운 명도 영역으로 비운다.
- built-in imagegen 1672×941 RGB 원본은 `assets/ui/source/ending-credits-v1-source.png`,
  point 방식 1280×720 정규화·quality 82 WebP는 `assets/ui/screens/ending-credits-v1.webp`에
  보존한다. 런타임 파일은 76,650B이며 Vite URL import와 Boot UI 감사 목록에 포함한다.
- 이미지에는 크레딧 문구·브랜드 로고·워터마크를 넣지 않는다. 시스템·던전 디자인·몬스터
  디자인·캐릭터 디자인·스킬 디자인·FX·테스트, 개발자, 게임 레퍼런스와 계속 플레이 버튼은
  Galmuri11 DOM 텍스트로 렌더링한다.
- 롤은 18초 선형 이동하며 `prefers-reduced-motion`에서는 고정 스크롤 영역으로 바뀐다.
  버튼은 38px 이상 높이와 포커스 테두리를 유지하고 1280×720 게임 경계 안에 둔다.
