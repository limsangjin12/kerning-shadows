# Maple-like Game Demo Roadmap

## 1. 목표

옛 횡스크롤 MMORPG의 감성과 조작감을 참고해, PC·모바일 브라우저에서 바로 실행할 수 있는
로컬 싱글 플레이 게임 데모를 만든다. 입문 플레이 루프는 다음과 같다.

`로그인 → Lv.9 캐릭터 생성 → Lv.10 성장 → 전직 시험 수락 → 그림자 시험장 초록버섯 5체 처치 → 로그 전직 → 드롭 회수 → 커닝시티 귀환`

완료 기준은 화면을 비슷하게 꾸미는 데 그치지 않고, 위 입문 루프부터 Lv.200 성장·4차 전직·
보스 원정·인내의 숲·최종보스까지 PC 키보드와 모바일 터치로 중단 없이 플레이하고 최대
3개 캐릭터 슬롯에서 독립 저장·복구할 수 있는 상태다.

## 2. 범위와 기본 결정

- 플랫폼: PC·모바일 웹 브라우저에서 실행하는 로컬 싱글 플레이 데모
- 실행 환경 제약
  - Windows의 Chrome·Edge·Firefox와 macOS의 Chrome·Firefox·Safari 최신 버전을 지원 대상으로 삼는다.
  - PC는 키보드, 모바일·태블릿은 화면 조이스틱과 터치 액션 버튼을 사용한다. 네이티브 앱은 이번 데모 범위에서 제외한다.
  - PC 브라우저에서 별도 플러그인 설치 없이 URL 접속만으로 실행되어야 한다.
  - 기준 해상도는 1280×720이며, 더 큰 화면에서는 종횡비를 유지하고 작은 PC 창에서는 레터박스로 대응한다.
- 기술 방향: TypeScript, Vite, Phaser 기반의 2D 게임 구조
- 렌더링: 픽셀 아트에 맞춘 정수 배율과 nearest-neighbor 스케일링
- 저장: 서버 없이 `localStorage`에 최대 3개의 독립 캐릭터 슬롯과 현재 선택을 저장
- 초기 캐릭터: 없음. 빈 저장소는 첫 로그인 뒤 1번 슬롯 신규 생성부터 진행하며,
  생성 전에는 프로필과 활성 슬롯 키를 자동으로 쓰지 않음
- 지역 구성: 커닝시티·도적 아지트·초록버섯굴·그림자 시험장·던전 회랑 5종·
  무한의 결투장·인내의 숲을 합친 데이터 기반 11개 맵
  - 커닝시티의 달빛 서고 포탈은 이그니카르 처치 뒤에만 오브젝트·효과·미니맵에 함께 나타난다.
  - 무한의 결투장은 연속 지면·2단 단방향 발판 5개·귀환 포탈·원펀맨 1체만 둔다.
- 전직: 다크로드와 대화해 Lv.10 로그, Lv.30 어쌔신, Lv.60 허밋, Lv.120 호카게 순서로 진행한다.
- 스킬: 기본 공격은 처음부터 사용하고 액티브 11종·패시브 4종을 차수별로 해금한다.
  숫자/HUD 11칸과 `Shift/QWER/ASDF/XCV` 12칸을 편집하고 `Z`는 고정 회수에 사용한다.
- 성장: 레벨당 AP 5를 얻고 STR/DEX/INT/LUK 수동 분배 또는 표창도적 추천 자동분배 사용
- 테스트 지원: 커닝시티 상점과 10레벨 단위 `경험의 서`를 제공해 전직 경계를 빠르게 검증
- 모바일: 좌우·아래 조이스틱, 고정 점프·줍기·공격, 별도 대화/이동, 직업별 대표 스킬
  최대 4슬롯과 `100dvh` 세로 모바일 시트를 제공
- 전송: 핵심 스프라이트 24종·스킬 아이콘 15종은 기준 PNG와 픽셀 동일한 lossless WebP만
  런타임에 사용하고, 보스 BGM은 최초 보스 조우 때 지연 로딩
- 아트 정책: 제공된 이미지는 레이아웃·색감·연출 참고용으로만 사용하고, 배포 가능한 원본 또는 라이선스가 명확한 자체 제작 에셋으로 교체한다.

## 3. 우선순위 원칙

1. 전체 플레이 루프가 끊김 없이 동작하는가
2. 이동·점프·공격의 입력감과 충돌이 안정적인가
3. 전직·전투·드롭·회수·저장이 정확히 이어지는가
4. 참고 이미지의 화면 구성과 픽셀 아트 감성을 잘 전달하는가
5. 부가 연출, 사운드, 편의 기능이 완성도를 높이는가

## 4. 구현 단계

공통 착수 게이트: 각 단계의 기능 구현을 시작하기 전에 `ASSET_GUIDE.md`에서 그 기능에 필요한 에셋과 규격을 확인하고, 최소 v1 에셋을 `통합 가능` 상태로 만든다.

### 작업 이력

| 작업일 | 완료 단계 | 범위 |
| --- | --- | --- |
| 2026-07-16 | P0~P8 | 에셋 계약, 기본 플레이 루프, UI·성장·릴리스 기반 |
| 2026-07-17 | P9~P24 | 전직·스킬·던전·보스·시네마틱·핵심 시각 확장 |
| 2026-07-18 | P25~P72 | 반응형 HUD, 11개 맵, 저장·퀘스트·펫·전투·단축키·크레딧 완성 |
| 2026-07-29 | P73~P79 | 모바일·부스트·포탈/시험 보정·4슬롯·WebP·보스 BGM 최적화 |

단계별 상태의 날짜는 위 작업 이력을 따른다. 중간 단계의 테스트 수치와 번들 크기는 당시 회귀
근거이며, 현재 최종 기준은 P79와 `RELEASE_QA.md`의 최신 결과다.

### P0 — 에셋 감사와 핵심 스프라이트 시트 — 최우선

상태: 1차 완료 — 2026-07-16

목표: 임시 도형이나 불명확한 프레임 규격에 게임 구조가 종속되지 않도록, 필요한 에셋을 먼저 감사하고 핵심 스프라이트 규격을 확정한다.

- `ASSET_GUIDE.md`에 전체 에셋 의존성, 우선순위, 제작 상태 기록
- 플레이어 4×4 시트
  - idle, walk, jump/fall, hurt, 기본 공격, 럭키세븐
- 초록버섯 4×4 시트
  - idle, walk, hurt, defeat
- 전직 NPC 4×4 시트
  - idle, talk, cast, approve
- 전투 효과 4×4 시트
  - 고드름 표창, 스킬 타격, 피격, 전직 링
- 월드·드롭 4×4 시트
  - 포탈, 메소 주머니, 회복 물약, 버섯 갓
- 모든 시트를 512×512 RGBA, 128×128 고정 셀로 정규화
- 프레임 인덱스, 속도, 반복 규칙을 JSON 매니페스트로 고정
- 생성 원본과 투명화된 런타임 에셋을 분리 보존
- 크기·알파·16개 셀 점유 자동 검사와 수동 시각 검수

완료 조건:

- A0 핵심 시트 5종이 `assets/sprites/core/`에 존재한다.
- 모든 시트가 4×4 고정 그리드, RGBA, 투명 모서리 검사를 통과한다.
- 필요한 나머지 맵·UI·데미지 숫자·오디오 에셋이 우선순위별로 감사되어 있다.
- 런타임이 사용할 애니메이션 인덱스와 원점 규칙이 문서·매니페스트에 일치한다.

커밋 체크포인트: `art: add core gameplay sprite sheets`

### P1 — 프로젝트 기반과 플레이 가능한 뼈대

상태: 구현 완료 — 전체 3엔진 최종 재실행은 사용자 요청으로 생략 (2026-07-16)

목표: 이후 기능을 안전하게 추가할 수 있는 최소 게임 구조를 만든다.

- Git 저장소, TypeScript/Vite/Phaser 프로젝트, 개발·빌드·테스트 명령 구성
- 로그인, 캐릭터 선택, 게임 플레이를 독립된 Scene 또는 상태로 분리
- 플레이어, 몬스터, 맵, 포탈, NPC, 아이템 데이터를 코드와 분리
- `assets/sprites/sprite-manifest.json` 로더와 실제 핵심 시트 프리로드 구성
- 입력을 액션 단위로 추상화
  - `Ctrl`: 기본 공격
  - `Alt`: 점프
  - `Shift`: 럭키세븐
  - `1~0`: 스킬 목록 순서 자동 단축키
  - `←/→`: 이동
  - `↑`: 포탈 이용 및 NPC 상호작용
  - `Z`: 아이템 회수
- 브라우저 기본 동작과 충돌하는 키를 게임 포커스 중에만 차단
- 고정 timestep 또는 프레임 독립적인 이동값, 60 FPS 목표 설정
- 실제 플레이어 시트와 최소 충돌 블록아웃으로 Scene 전환·입력 스모크 테스트 작성

완료 조건:

- 개발 서버와 프로덕션 빌드가 오류 없이 실행된다.
- 로그인 → 캐릭터 선택 → 플레이 화면 전환이 자동 테스트 또는 스모크 테스트로 검증된다.
- 입력 재매핑이 게임 로직과 분리되어 있다.

커밋 체크포인트: `chore: bootstrap playable game project`

### P2 — 로그인과 캐릭터 선택 수직 슬라이스

상태: 완료 — 2026-07-16

목표: 게임 진입 전 화면을 참고 이미지의 분위기로 완성한다.

- 로그인 화면
  - 책 페이지와 나무 간판을 연상시키는 프레임
  - 아이디/비밀번호 입력, 로그인 버튼, Enter 제출
  - 값이 비어 있지 않으면 어떤 문자열이든 동일한 로컬 프로필로 접속
  - 비밀번호는 화면에 마스킹하고 저장하지 않음
- 캐릭터 선택 화면
  - 하늘과 나무 발판 배경, 목재 버튼 계열 UI
  - 첫 접속은 빈 슬롯에서 닉네임·주사위 능력치를 정해 Lv.9 초보자를 생성
  - P47에서 최대 3개의 생성·선택 가능한 독립 슬롯으로 확장
  - 캐릭터의 이름, 레벨, 직업, 기본 능력치 요약 표시
- 새 게임은 항상 커닝시티 시작 지점으로 연결
- 로그인 상태와 캐릭터 상태를 로컬 저장소에서 복구

완료 조건:

- 임의의 아이디/비밀번호로 같은 캐릭터에 접속한다.
- 프로필이 없으면 생성 화면, 있으면 캐릭터 선택 화면으로 진입한다.
- 새로고침해도 저장된 직업과 재화가 유지된다.

커밋 체크포인트: `feat: add login and character selection flow`

### P3 — 횡스크롤 이동, 맵, 포탈

상태: 완료 — 2026-07-16

목표: 메이플형 횡스크롤 조작의 핵심 감각을 먼저 확보한다.

- 플레이어 상태 머신
  - `idle`, `walk`, `jump`, `fall`, `attack`, `skill`, `hurt`
  - 좌우 반전, 상태별 프레임 속도, 공격 중 이동 제한 규칙
- 좌우 가속·감속, 점프 상승/낙하, 발판 착지, 월드 경계 처리
- 단방향 발판과 지면 충돌 구현
- 카메라 추적과 맵 경계 클램프
- 포탈 감지 범위에서 `↑` 입력 시 이동
- 커닝시티
  - 도시 외부 구역
  - 커닝시티 내부의 도적 아지트와 다크로드 구역
  - 초록버섯굴로 가는 포탈
- 초록버섯굴
  - 수직으로 이어진 버섯 발판
  - 커닝시티 귀환 포탈
- 포탈 광원·루프 애니메이션과 현재 지역명 표시

완료 조건:

- 두 최상위 지역을 포탈로 왕복할 수 있다.
- 커닝시티 안에서 다크로드까지 이동할 수 있다.
- 발판 가장자리, 점프 착지, 포탈 연속 입력에서 끼임이나 무한 전환이 없다.

커밋 체크포인트: `feat: implement movement maps and portals`

### P4 — 전직과 기본 전투

상태: 완료 — 2026-07-16

목표: 1차 로그 전직과 표창 전투가 하나의 흐름으로 작동하게 한다.

- 다크로드 NPC 대화 UI와 전직 확인 절차
- `Lv. 10 + 초보자` 조건을 만족하면 로그로 전직
- 전직 결과를 즉시 UI와 로컬 저장소에 반영
- 기본 공격
  - `Ctrl` 입력, 고드름 표창 투사체, 공격 모션, 명중 판정
  - 바라보는 방향으로 1회 투척
- 럭키세븐
  - 전직 후 `Shift` 입력으로 사용
  - MP를 소모해 빠르게 2회 타격
  - MP 부족 또는 공격 상태일 때의 실패 피드백
- 초록버섯 몬스터
  - 대기·이동·피격·사망 애니메이션
  - 발판 가장자리 회전, 플레이어 접촉 공격, 일정 시간 후 재생성
- 플레이어 피격, 무적 시간, 넉백, HP 0 처리
- 플레이어·몬스터 머리 위 데미지 숫자
  - 피격 종류별 색상
  - 픽셀 폰트, 위로 떠오른 뒤 사라지는 짧은 애니메이션
- 계산식은 데이터로 분리하고 데모 밸런스에 맞춘 결정적 범위값 사용

완료 조건:

- 전직 전에는 럭키세븐을 사용할 수 없고 안내가 표시된다.
- 전직 후 직업이 로그로 저장되며 럭키세븐이 MP를 정확히 소모한다.
- 기본 공격과 럭키세븐이 각각 올바른 타격 수와 데미지 숫자를 만든다.
- 몬스터 접촉 시 플레이어 HP 감소, 데미지 표시, 무적 시간이 작동한다.

커밋 체크포인트: `feat: add thief advancement and combat`

### P5 — HP/MP, 성장, 드롭과 회수

상태: 완료 — 2026-07-16

목표: 전투 결과가 보상과 캐릭터 상태에 연결되도록 한다.

- 하단 HUD
  - Lv, 직업, 캐릭터명
  - HP/MP/EXP 게이지와 수치
  - 메소, 조작 도움말, 전직 전후 스킬 상태
- 빠른 자연 회복
  - 전투 중에도 확인 가능한 짧은 회복 간격
  - 최대치 초과 방지, 피격 직후 회복 지연 여부를 데이터로 조정
- 커닝시티 회복 NPC
  - `↑` 대화 시 HP/MP를 즉시 최대치까지 회복
  - 이미 최대치인 경우에도 현재 상태를 명확히 안내
- 몬스터 사망 시 메소와 아이템 드롭
- 드롭 튀어 오름, 지면 안착, 자동 소멸 타이머
- 가까운 드롭을 `Z`로 회수하고 획득 피드백 표시
- 간단한 인벤토리 또는 획득 목록과 메소 합산
- EXP 획득과 게이지 표시
  - 데모 범위에서는 레벨업을 지원하되 전직 조건은 Lv. 10 시작값으로 보장
- HP/MP, EXP, 메소, 직업, 인벤토리를 로컬 저장

완료 조건:

- 몬스터 처치마다 설정된 확률에 따라 아이템 또는 메소가 드롭된다.
- 범위 밖 `Z`는 아무것도 줍지 않고, 범위 안 `Z`는 가장 가까운 드롭을 회수한다.
- HP/MP가 설정된 속도로 회복되고 HUD와 저장값이 일치한다.
- 커닝시티 회복 NPC와 대화하면 HP/MP가 최대치가 되고 즉시 저장된다.
- 새로고침 후에도 전직, 메소, 획득 아이템이 유지된다.

커밋 체크포인트: `feat: add stats loot and persistence`

### P6 — UI와 애니메이션 폴리시

상태: 완료 — 2026-07-16

목표: 참고 이미지에서 느껴지는 초기 메이플풍의 시각적 리듬을 일관되게 구현한다.

- 공통 픽셀 폰트, 9-slice 패널, 나무·금속·책 테마 디자인 토큰 구성 — 완료
- 커닝시티의 배관, 벽돌, 간판, 산업적인 청록·회색 팔레트 적용 — 완료
- 초록버섯굴의 깊은 녹색 수직 배경, 발광 버섯 발판, 원경 패럴랙스 적용 — 완료
- 플레이 화면에 미니맵, 지역명, 하단 상태바, 짧은 시스템 메시지 배치 — 완료
- 커닝시티 회복 NPC 전용 4×4 시트와 idle·talk·cast·approve 애니메이션 통합 — 완료
- 캐릭터와 몬스터 애니메이션의 프레임 수·타이밍·실루엣 조정 — 완료
- 메소·회복 물약·버섯 갓의 이웃 셀 픽셀 제거와 수평 중심·공통 바닥 기준선 정렬 — 완료
- 공격 선딜·명중·후딜, 피격 플래시, 표창 궤적, 사망 후 드롭 타이밍 — 완료
- 드롭 착지·아이템 획득 이펙트 폴리시 — 완료
- 포탈, 전직, 레벨업 효과 추가 — 완료
- 음소거 가능한 배경음·효과음과 볼륨 설정 추가 — 완료
- 승인된 실제 런타임 캡처와 나란히 비교하는 `VISUAL_QA.md` 체크리스트 작성 — 완료

완료 조건:

- 모든 화면에서 패널, 폰트, 버튼, 색상 규칙이 일관된다.
- 캐릭터 상태 전환 시 프레임 튐이나 잘못된 방향 표시가 없다.
- 1280×720 기준 주요 HUD가 월드 플레이 영역을 가리지 않는다.
- 에셋 출처와 라이선스가 문서화되어 있다.

커밋 체크포인트: `feat: polish pixel art ui and animations`

### P7 — 통합 QA와 데모 릴리스

상태: 진행 중 — 자동화 게이트 완료, 실제 지원 브라우저·30분 수동 QA 대기 (2026-07-16)

목표: 전체 요구사항을 반복 재현 가능하게 검증하고 로컬 데모를 배포 가능한 상태로 만든다.

- 핵심 로직 단위 테스트 — 자동화 완료
  - 전직 조건, HP/MP 증감·회복, 럭키세븐 MP 소비·2회 타격
  - 드롭 생성·범위 회수·저장 직렬화와 손상 저장값 정규화
- 브라우저 통합 테스트 — Playwright 3엔진 자동화 완료
  - 로그인부터 전직, 전투, 회수, 귀환까지의 필수 루프를 엔진별 2회 실행
  - 새로고침 후 직업·EXP·메소·인벤토리 저장 복구
- 키 고스팅·길게 누르기·창 포커스 상실·포탈 연타 — 순수 입력 테스트와 포탈 연타
  E2E 완료, 실제 키보드 조합 수동 QA 대기
- 1280×720, 1600×900, 1024×768, 800×600 종횡비·레터박스 — 3엔진 자동화 완료
- Windows와 macOS의 실제 지원 브라우저 키보드·전체 플레이 루프 교차 검증 — 대기
- 개발 콘솔·pageerror·네트워크 실패 0건과 맵 전환 뒤 임시 sprite·timer·collider·목록
  기준선 복귀 — 자동화 완료
- 목표 성능: 자동화 루프 종료 FPS 50 이상 완료, 실제 장치 30분·중앙 58 FPS 수동 QA 대기
- README 실행법·조작법·알려진 제한·에셋 크레딧과 `RELEASE_QA.md` — 완료
- 프로덕션 `dist/`의 고지·금지 에셋·상대 URL·크기 예산 감사 — 완료

완료 조건:

- 프로덕션 빌드와 전체 자동 테스트가 통과한다.
- 필수 플레이 루프를 수동 QA 체크리스트로 2회 연속 완료한다.
- 조작 불능, 진행 불능, 저장 손상 등 치명·주요 결함이 없다.

현재 판정:

- 첫 번째 조건은 충족했다.
- 두 번째 조건은 자동화 3엔진에서 충족했으나 실제 지원 브라우저 수동 2회가 남았다.
- 자동화에서 발견된 저장 손상·입력 별칭·사망 입력 누적·작은 창 잘림은 수정했으며,
  실제 브라우저 수동 QA 전까지 최종 결함 판정은 보류한다.
- 상세 환경·결과·잔여 게이트는 `RELEASE_QA.md`를 따른다.

커밋 체크포인트: `release: complete local game demo`

### P8 — 표창도적 전직·스킬·능력치 성장 확장

상태: 완료 — 2026-07-16

목표: 1차 전직 뒤에도 이어지는 표창도적 성장 선택과 레벨업 보상을 플레이 가능한 규칙으로
확장한다.

- 다크로드 전직 대화를 현재 직업 기준의 다음 단계로 일반화
  - Lv.10 로그·럭키세븐
  - Lv.30 어쌔신·드레인
  - Lv.60 허밋·어벤저
- 상위 스킬을 기존 투척 모션과 전투 이펙트의 독자 색상 변형으로 통합
  - 드레인은 실제 적용 피해의 45%만큼 최대 HP 이내 회복
  - 어벤저는 같은 몬스터 중복 없이 최대 4대상 관통
- LUK·DEX가 모든 표창 공격의 결정적 데미지 보너스에 반영
- 레벨업마다 AP 5 지급, `S` DOM 능력치 창에서 4스탯 수동 +1 분배
- 남은 AP 즉시 자동분배와 레벨업 자동분배 옵션을 `LUK 4 : DEX 1`로 제공
- 같은 로컬 저장 키의 스키마를 v2로 올리고 v1 `thief`·고레벨 프로필을 손실 없이 변환
- 레벨업 연출을 삼중 링·12방향 스파크·플래시·카메라 강조·AP 배너로 강화
- Chromium·Firefox·WebKit에서 기존 필수 루프와 신규 스탯·전직·레벨업 시나리오 검증

완료 조건:

- 각 전직은 선행 직업과 레벨 경계에서만 가능하고 새로고침 뒤 유지된다.
- 세 차수의 주력 스킬이 직업·MP 규칙과 고유 효과를 지킨다.
- 수동·자동 분배가 AP를 초과 소비하지 않고 레벨업·저장 복구 뒤 같은 값을 유지한다.
- 강화된 레벨업 효과가 입력 잠금 안에 끝나고 맵 전환 뒤 임시 객체를 남기지 않는다.
- `npm run release:check`가 3엔진에서 통과한다.

커밋 체크포인트: `feat: expand throwing-star progression`

### P9 — 전직 시험·직업 외형·그림자 던전

상태: 완료 — `npm run release:check` 3엔진 통과 (2026-07-17)

목표: 세 전직을 실제 시험과 화려한 시각 변화로 연결하고, 상위 시험을 치를 중대형 몬스터
던전을 추가한다.

- 로그·어쌔신·허밋마다 같은 16포즈 계약을 지키는 전용 장비 시트와 즉시 외형 교체
- 기본·럭키세븐·드레인·어벤저 전용 표창 4행과 공격별 크기·잔상·명중 색상
- 전직 시험 순수 규칙과 저장 스키마 v3
  - 로그: 초록버섯 3체
  - 어쌔신: 그림자 파수꾼 3체
  - 허밋: 심연의 골렘 1체
- 다크로드 DOM 대화의 시험 제안·진행도·완료 보고 상태와 HUD/캔버스 접근성 추적 문구
- 1920×720 그림자 시험장, 왕복 포탈, 3단 배경, 충돌 발판과 시험 활성 포탈 접근 조건
- 중형 파수꾼 3체와 대형 골렘 1체, 종류별 데이터와 공통 순찰 몬스터 엔티티
- Chromium 실제 루프와 1280×720 인앱 브라우저 시각 검증, Firefox·WebKit 회귀

완료 조건:

- 시험을 수락하지 않거나 목표를 채우지 않으면 전직할 수 없다.
- 확정된 몬스터 처치만 저장 진척에 반영되고 새로고침 뒤 이어진다.
- 세 전직마다 장비 외형과 사용 가능한 표창·기술 표현이 명확히 달라진다.
- 시험장에서 중형·대형 몬스터의 크기, 전투 수치, 드롭과 재생성이 데이터대로 작동한다.
- 1280×720에서 퀘스트 HUD·던전·보스와 기존 HUD가 겹치지 않고 콘솔 오류가 없다.
- `npm run release:check`가 단위·타입·빌드, Chromium·Firefox·WebKit 18개 E2E와
  `dist/` 예산을 한 실행에서 통과한다.

커밋 체크포인트: `feat: add advancement trials and shadow dungeon`

### P10 — 스킬 인벤토리·SP 성장

상태: 완료 — 2026-07-17

목표: 참고 이미지의 정보 위계를 독자적인 접근 가능 UI로 재구성하고, 레벨업과 직업 주력
스킬 성장을 영구 저장되는 루프로 연결한다.

- `K`로 여는 금속 9-slice DOM 스킬 메뉴와 1·2·3차 탭, 세로 목록, 선택 스킬 상세 패널
- 기존 직업별 표창 시트 첫 프레임을 재사용한 럭키세븐·드레인·어벤저 아이콘
- 레벨업당 SP 3 지급과 스킬별 최대 20레벨, 직업 차수·잔여 SP·마스터 제한
- 럭키세븐 레벨당 타격 피해 +1, 드레인·어벤저 레벨당 피해 +2
- 로컬 프로필 스키마 v4의 미사용 SP·스킬 레벨 저장과 v1~v3 레벨 기반 SP 마이그레이션
- HUD·캐릭터 선택·캔버스 접근성 상태의 SP·스킬 레벨 표시
- 스킬 표시 순서와 `1→2→…→9→0` 순서를 연결한 상단 숫자열·숫자패드 자동 단축키

완료 조건:

- 레벨을 한 번 올리면 AP와 별개로 SP 3이 정확히 지급되고 여러 레벨 상승도 누적된다.
- 잠긴 차수·SP 0·마스터 레벨에서는 분배되지 않으며 유효 분배만 즉시 저장된다.
- 스킬 레벨 보너스가 기본 공격에는 적용되지 않고 해당 직업 스킬 피해에만 반영된다.
- 현재 스킬은 `1/2/3`으로 발동하고 잠긴 스킬은 기존 직업 제한 안내를 유지하며 `4~0`은 비어 있다.
- 1280×720과 800×600에서 메뉴·상단 닫기·상세 설명이 화면 안에 있고 콘솔 오류가 없다.
- 단위 테스트, 타입 검사, 프로덕션 빌드, 3엔진 스킬 성장 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add skill inventory and SP progression`

### P11 — 차수별 패시브·전용 스킬 아이콘

상태: 완료 — `npm run release:check` 3엔진 통과 (2026-07-17)

목표: 각 도적 전직 차수에 자동 적용 패시브를 하나씩 추가하고, 액티브를 포함한 여섯 스킬을
독자적인 전용 아이콘과 실제 전투·회복 효과로 구분한다.

- 로그 `예리한 시야`: 모든 표창 투사체 수명 레벨당 +2%, `900ms → 최대 1260ms`
- 어쌔신 `치명 투척`: 공격 종류·순서·타수 기반 결정적 치명타 확률 레벨당 +1%p,
  최대 20%·최종 피해 `floor(×1.5)`와 금색 링 피드백
- 허밋 `그림자 호흡`: 자연 HP/MP 회복 주기 레벨당 -2%, `1600ms → 최대 960ms`;
  피격 후 2600ms 회복 지연은 유지
- 표시용 6스킬 순서와 액티브 숫자키 순서를 분리하고 `1/2/3`은 기존 액티브,
  패시브는 단축키 없이 SP 분배 즉시 적용
- 차수 탭 3개와 차수별 액티브·패시브 2행, 현재/다음 효과, 잠금·레벨·progress 접근성 정보
- built-in imagegen으로 직접 생성한 128×128 RGBA 전용 아이콘 6종과 크로마 원본·프롬프트 보존
- 로컬 프로필 스키마 v5에서 여섯 레벨을 저장하고 v4의 SP·액티브 레벨을 보존한 채 패시브 0레벨 보충

완료 조건:

- 차수별 패시브가 잠금·SP·마스터 제한을 따르고 새로고침 뒤 같은 레벨로 복구된다.
- 예리한 시야의 투사체 수명, 치명 투척의 확률·피해, 그림자 호흡의 회복 주기가
  0레벨과 20레벨 경계에서 순수 규칙대로 적용된다.
- 패시브가 입력 액션이나 `4~0` 슬롯을 점유하지 않고 기존 `1/2/3` 액티브가 유지된다.
- 여섯 아이콘은 46×46 표시에서 구분되고 1280×720 스킬 창에서 잘림·콘솔 오류가 없다.
- 단위 테스트, 타입 검사, 프로덕션 빌드, 3엔진 스킬 성장 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add thief passives and skill icons`

### P12 — 호카게 4차 전직·차크라 스킬·빠른 테스트 상점

상태: 완료 — 2026-07-17

목표: 기존 표창도적 성장선 뒤에 4차 호카게를 추가하고, 차크라 근접 공격·토글 변신·
변신 전용 기본 공격과 투사체·상시 회복 패시브를 저장·입력·UI·전투·외형 흐름에 통합한다.

구현 내용:

- 전직은 `Lv.120 허밋 → 호카게`이며, 그림자 시험장에서 심연의 골렘 3체를 순차 처치하고
  다크로드에게 보고한다. 완료 시 나선환·구미호 변신·미수옥·선인모드를 함께 해금한다.
- 스킬 최대 레벨은 모두 20이고 자동 슬롯은 `4=나선환`, `5=구미호 변신`, `6=미수옥`이다.
  보조키는 V/B이며, 변신 중 SHIFT는 럭키세븐 대신 미수옥으로 전환된다.
- 나선환은 MP 30, 72~90 피해, 레벨당 +3의 차크라 단일 대상 근접 공격이다. 표창 전용
  예리한 시야·치명 투척은 차크라 공격에 적용되지 않는다.
- 구미호 변신은 켤 때 MP 60을 소비하고 다시 누르면 무료로 해제되는 런타임 토글이다.
  변신 중 CTRL 기본 공격은 55~70 피해의 `구미호 할퀴기`로 바뀌며, 변신 레벨당 할퀴기와
  미수옥 피해가 2.5% 증가한다. 토글 상태는 저장하지 않는다.
- 미수옥은 변신 중에만 쓸 수 있는 MP 45, 110~140 피해, 레벨당 +4의 차크라 투사체로,
  같은 몬스터를 중복 타격하지 않고 최대 3대상을 관통한다.
- 선인모드는 습득 즉시 청록·금색 차크라 오라를 상시 표시하고 자연 회복 주기를 레벨당 4%
  줄인다. 20레벨은 `1600ms → 320ms`, 그림자 호흡 20레벨과 함께면 `192ms`다.
- 커닝시티 `서적상 레오`의 목재 DOM 상점에서 `경험의 서`를 1메소에 구매·사용한다. 한 권은
  현재 EXP를 보존해 정확히 10레벨 또는 Lv.200 경계까지 올리고, 최고 레벨에서는 소비하지 않는다.
- 경험의 서는 `awardExperience()` 경로를 사용해 AP·SP·HP/MP·자동분배·저장·레벨업 연출을
  사냥과 동일하게 적용한다. 초록버섯 1체의 최소 메소로 Lv.10→120용 11권을 살 수 있다.
- 로컬 프로필은 같은 키의 스키마 v6이며 v1~v5의 직업·스탯·기존 스킬을 보존하고 신규
  4스킬을 0으로 보충한다. 전직 정의는 `unlockedSkills` 배열로 일반화했다.
- 독자적인 여우 그림자 닌자 4×4 시트, 차크라 효과 4×4 시트, 128×128 아이콘 4종을
  built-in imagegen으로 제작했다. 신규 런타임 이미지 합계는 `101,740B`다.
- 기존 로그·어쌔신·허밋과 새 호카게의 프레임별 원점을 다시 감사해 idle 얼굴 중심·발 기준을
  고정하고, 효과 오라의 바닥 기준도 매니페스트 원점으로 플레이어 발에 맞췄다.
- 최종 릴리스 게이트에서 Vitest 21파일·154테스트, Playwright 24개 시나리오와 `dist/` 감사를
  통과했다. 산출물은 57파일·6,977,207 bytes이고 JavaScript는 1,349,669 bytes,
  gzip 합계는 364,075 bytes다.

완료 조건:

- Lv.119 허밋은 전직할 수 없고, Lv.120 허밋은 시험 수락·골렘 3체의 사망 보상 확정·완료
  보고 뒤에만 호카게가 되며 새로고침 후 직업과 외형이 유지된다.
- 기존 v1~v5 프로필은 직업·스탯·기존 스킬을 보존하고 신규 스킬 4종을 0으로 채운다.
- 메소가 부족하면 구매 상태가 바뀌지 않고, 구매 성공 시 메소 1 차감과 경험의 서 1개 추가가
  한 번만 저장된다. 새로고침 뒤 보유 수량이 유지된다.
- 경험의 서 사용은 1개를 소비해 정확히 10레벨 또는 Lv.200 경계까지 올리고 AP·SP·HP·MP·
  자동분배와 EXP 나머지를 순수 성장 규칙대로 반영한다. Lv.200에서는 책을 소비하지 않는다.
- Lv.10 프로필은 초록버섯 1체의 메소로 경험의 서 11권을 구매·사용해 Lv.120 호카게 시험
  경계까지 빠르게 도달할 수 있다.
- 나선환은 `Digit4`·`Numpad4`·V에서 MP·근접 범위·단일 대상·차크라 배율을 지키며 표창
  거리·치명 패시브의 영향을 받지 않는다.
- 구미호 변신은 `Digit5`·`Numpad5`·B에서 MP 60 토글 규칙을 지키고, 켠 상태의 CTRL/SHIFT가
  할퀴기/미수옥으로 전환되며 해제 시 원래 입력으로 복구된다.
- 미수옥은 `Digit6`·`Numpad6` 또는 변신 중 SHIFT에서만 발동하고 SP 피해 보너스와 3대상
  관통 규칙을 지킨다.
- 선인모드는 단축키 없이 SP 분배 즉시 회복 배율과 상시 오라에 적용된다.
- 1280×720과 800×600에서 4차 탭·아이콘·변신 상태·HUD가 잘리지 않고, 효과 스프라이트는
  플레이어 물리 바디와 발 위치를 바꾸지 않는다.
- 신규 런타임 이미지 합계 목표 110KB와 전체 `dist/` 7MB 예산을 지키고 참고 이미지·생성
  원본·프롬프트가 번들에 포함되지 않는다.
- `npm run release:check`가 단위 테스트, 타입 검사, 프로덕션 빌드, Chromium·Firefox·WebKit
  E2E와 `dist/` 감사를 모두 통과한다.

커밋 체크포인트: `feat: add hokage fourth advancement`

### P13 — 5등급 표창 장비·상점

상태: 완료 — 2026-07-17

목표: `S` 능력치·장비 통합 창에서 소유 표창을 장착하고, 서적상 레오의 상점에서 5등급 표창을 메소로
구매하며 장착 등급에 따라 표창 공격의 외형과 피해 보너스가 일관되게 바뀌도록 한다.

구현 내용:

- 카탈로그는 `수련생/강철/청옥/홍염/일식 표창` 5종이며 가격은
  `50/250/1,000/4,000/12,000`메소, 표창 계열 피해 배율은
  `1.00/1.08/1.18/1.32/1.50`이다.
- `throwing-star-rules.ts`에서 구매·장착·피해 배율과 손상 저장값 정규화를 불변 순수 규칙으로
  제공한다. 기본 상태는 1등급 수련생 표창 소유·장착이다.
- 로컬 프로필을 같은 키의 스키마 v7로 올리고 v1~v6의 직업·스탯·스킬·인벤토리를 보존한 채
  기본 표창 장비를 보충한다. 구매와 장착 결과는 즉시 저장하고 새로고침 뒤 복구한다.
- 서적상 레오의 목재 DOM 상점을 5등급 표창 목록으로 확장하고, 메소 부족·이미 소유한 상품은
  disabled 상태와 순수 규칙 양쪽에서 중복 구매를 막는다.
- `S` 금속 DOM 통합 창의 장비 구역은 5종 미리보기·등급·피해 보너스·소유/미소유·장착 중 상태를 접근 가능한
  버튼과 실제 텍스트로 표시한다. 오버레이 종료 뒤 캔버스 포커스를 복구한다.
- 기본·럭키세븐·드레인·어벤저는 장착 표창 등급의 3·3·3·3·4프레임 애니메이션을 사용하고,
  기존 공격별 투사체 크기·잔상·타격 색상은 유지한다. 차크라 계열에는 장비 배율을 적용하지 않는다.
- 독자적인 5등급 표창 원본을 built-in imagegen으로 생성하고 `#ff00ff` 크로마 제거·512×512
  정규화·4-bit 팔레트 최적화를 거쳐 `20,570B` 런타임 PNG로 연결했다.
- 순수 규칙·프로필 마이그레이션·입력·매니페스트 단위 회귀와 Chromium 상점 구매 → `S` 장착 →
  투사체 프레임·저장 복구 E2E를 추가했다.

완료 조건:

- 5등급 가격·피해 배율·구매·장착 규칙과 손상 저장값 정규화를 순수 단위 테스트로 검증한다.
- 프로필 마이그레이션은 기존 인벤토리·스킬을 보존하고 1등급 표창 소유·장착 기본값을 보충한다.
- 상점에서 메소 부족·이미 소유한 표창의 중복 구매를 막고 성공 결과를 즉시 저장한다.
- `S` 능력치·장비 통합 창은 소유/미소유·장착 중 상태와 등급 효과를 접근 가능한 DOM으로 제공한다.
- 기본·럭키세븐·드레인·어벤저의 투사체가 장착 표창 실루엣을 사용하고 스킬 속성 잔상은 유지된다.

커밋 체크포인트: `feat: add throwing star equipment`

### P14 — 호카게 액티브 시네마틱·삼인 협공

상태: 구현 완료 — 2026-07-17

목표: 호카게의 네 액티브에 짧고 화려한 전용 시네마틱을 연결하고, 두 독자 동료 닌자가 화면
밖에서 진입해 표창·발차기·주먹으로 총 5연타 후 퇴장하는 SP 성장형 협공 스킬을 추가한다.

구현 결과:

- 나선환·구미호 활성화·미수옥·삼인 협공은 서로 다른 640×360 imagegen 배경과 코드 기반
  비네트·줌·플래시를 사용하며 입력 잠금·피해 타이밍·정리가 한 수명에 묶인다.
- 삼인 협공은 4차 액티브 슬롯 `7`, 보조키 `N`, 최대 20레벨, MP 80, 정확히 5타 규칙을 지킨다.
- 시온은 화면 한쪽 밖에서 진입해 표창을 던지고, 하나는 반대편에서 진입해 발차기와 주먹을
  수행한 뒤 둘 다 화면 밖으로 퇴장한다. 맵 전환·사망·스킬 종료 뒤 잔여 객체가 없다.
- 스킬 창·HUD·캔버스 접근성 상태에 신규 스킬 레벨·슬롯·시네마틱 진행 상태를 반영한다.
- 시네마틱 동안 이동·충돌을 잠그고 종료·맵 정리에서 physics와 0개 잔여 객체로 복구한다.
  진행 상태와 협공 누적 타수는 DOM dataset으로 노출해 E2E에서 정확히 검증한다.
- 프로필 스키마 v8에서 삼인 협공 레벨을 저장하고 v1~v7 성장·표창 장비를 보존한다.
- 원작 식별 요소를 배제한 동료/배경/아이콘을 built-in imagegen으로 제작했다. 세 런타임 이미지는
  합계 `162,098B`이며 기존 7MB `dist/` 상한을 늘리지 않고 감사를 통과한다.
- 최종 `npm run release:check`에서 Vitest 23파일·175테스트, Playwright 3엔진 30개 시나리오가
  통과했다. 산출물은 60파일·6,984,309 bytes, JavaScript 1,365,842 bytes,
  gzip 368,194 bytes다.

커밋 체크포인트: `feat: add hokage cinematic team assault`

### P15 — 던전 회랑·HUD 재배치·인벤토리·공중 공격

상태: 구현 완료 — 2026-07-17

목표: 독자적인 횡스크롤 던전 5종을 기존 월드에 연결하고, HUD 가독성과 아이템 탐색성을
높이며 점프와 공격 입력을 물리적으로 독립시킨다.

구현 결과:

- 수정 개미굴, 시계태엽 탑, 가라앉은 산호 신전, 잿불 광산, 달빛 마도서고를 각각
  1920×720 월드로 추가했다. 초록버섯굴 상층에서 시작해 커닝시티로 돌아오는 포탈 회랑이다.
- 각 맵은 built-in imagegen으로 만든 1280×720 독자 원경, 결정적으로 재생성하는 1920×720
  충돌 정렬 SVG 전경, 4개 발판, 양방향 포탈, 4체 몬스터를 가진다.
- 하단 HUD를 캐릭터·HP·MP·EXP·재화·성장 6개 경계로 분리하고 상단 조작 안내를 460px로
  넓혔다. Galmuri11 한글 굵은꼴과 11~20px 글자만 사용해 경계 침범을 제거했다.
- `I`는 메소, 소지 아이템, 보유 표창, 장착 상태를 표시하는 접근 가능한 24칸 DOM
  인벤토리를 열며, 닫은 뒤 캔버스 포커스를 복구한다.
- 점프 입력을 공격보다 먼저 처리하고 공중 공격 시작·동작 잠금 동안 수평·수직 속도를
  보존한다. 상승·정점·낙하 궤도는 공격 애니메이션과 독립적이다.
- 프로필 스키마 v9는 기존 v1~v8 성장·장비·인벤토리를 유지하면서 9개 맵의 정확한 위치를
  저장하고 새로고침 뒤 해당 맵의 첫 스폰에서 복구한다.
- 배경 5종 추가로 전체 `dist/` 예산만 8,000,000 bytes로 현실화하고 JavaScript 1,500,000,
  gzip 450,000 bytes 상한과 생성 원본·참고 이미지 제외 규칙은 그대로 유지했다.
- 최종 `npm run release:check`에서 Vitest 180개, 3엔진 Playwright 33개와 프로덕션 빌드,
  70파일·7,724,012-byte `dist/` 감사를 통과했다.

완료 조건:

- 모든 새 던전 포탈은 유효한 상대 맵 스폰으로 연결되고 전경의 발판·포탈 데이터 속성이
  `map-definitions.ts` 좌표와 일치한다.
- `I` 인벤토리는 24칸 안에서 아이템·수량·표창·메소를 표시하고 화면 경계를 넘지 않는다.
- 같은 프레임 또는 공중에서 점프와 공격을 함께 입력해도 수평 이동과 수직 점프가 계속된다.
- 1280×720에서 HUD 텍스트가 박스 경계와 겹치지 않고 브라우저 콘솔 오류가 없다.
- `npm run release:check`가 단위 테스트, 3엔진 E2E, 빌드, 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add dungeon circuit and inventory`

### P16 — 던전 보스·원정 퀘스트

상태: 구현 완료 — 2026-07-17

목표: 던전 회랑에 중간보스와 최종보스를 배치하고, 커닝시티 NPC가 수락·추적·완료
보고를 담당하는 저장 가능한 원정 퀘스트를 제공한다.

구현 결과:

- 잿불 광산에 Lv.85 중간보스 `잿불 수호자`(HP 3,000), 달빛 마도서고에 Lv.120
  최종보스 `월식의 기록자`(HP 6,500)를 추가했다. 두 보스는 전용 공격 애니메이션,
  보스 등급·HP 표시, 경험치·메소·회복 물약·전용 재료 보상과 긴 재생성 시간을 가진다.
- 커닝시티에 `원정대장 세라`를 배치하고 가장 가까운 NPC를 우선 선택하도록 상호작용을
  정리했다. 수락·진행·완료 상태별 talk·brief·approve 동작과 접근 가능한 금속 DOM
  대화창을 사용한다.
- `깨어난 던전 회랑`은 수락 뒤 잿불 수호자, 월식의 기록자 순서로만 진행되며 마지막에
  세라에게 보고하면 7,500메소와 경험의 서 2권을 한 번만 지급한다.
- 플레이 HUD에 퀘스트 알림과 현재 맵의 중간/최종보스 HP 게이지를 추가하고, 같은 상태를
  캔버스 `aria-label`과 결정적 DOM 상태 표식에 반영했다.
- 프로필 스키마 v10은 던전 원정 단계를 저장하고 v1~v9 프로필의 성장·전직·스킬·장비·
  인벤토리·정확한 위치를 보존한다. 손상되거나 없는 원정 값은 `offer`로 복구한다.
- built-in imagegen으로 독자적인 4×4 보스 2종·NPC 1종을 생성해 투명 512×512 PNG로
  정규화하고 모든 프레임 원점을 매니페스트에 기록했다.
- 최종 `npm run release:check`에서 Vitest 24파일·188테스트, 3엔진 Playwright 36개,
  73파일·7,965,774-byte `dist/` 감사를 통과했다.

완료 조건:

- 두 보스가 지정 던전에서 유일하게 생성되고 공격·피격·사망·재생성 상태가 공통 몬스터
  수명과 함께 정리된다.
- 퀘스트는 보스 순서를 건너뛰거나 중복 처치해도 잘못 진행되지 않고 보상도 중복 지급하지 않는다.
- 1280×720에서 퀘스트 알림·보스 HP·기존 상단 패널과 하단 HUD의 경계가 겹치지 않는다.
- `npm run release:check`가 188개 단위 테스트, 3엔진 E2E 36개, 프로덕션 빌드와 8MB
  `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add dungeon bosses and expedition quest`

### P17 — 퀘스트 적정 레벨·보스 원거리 스킬

상태: 구현 완료 — 2026-07-17

목표: 모든 퀘스트에서 도전 시점을 바로 판단할 수 있게 적정 레벨을 표시하고, 두 던전
보스가 접촉 공격에만 의존하지 않도록 서로 다른 조준형 원거리 스킬을 제공한다.

구현 결과:

- 로그·어쌔신·허밋·호카게 전직 시험은 기존 전직 요구 레벨 10·30·60·120을 대화창과
  퀘스트 알림의 적정 레벨로 재사용해 정의 중복을 없앴다.
- `깨어난 던전 회랑`은 대화창과 HUD에 `Lv.80~120`, 경로에는 잿불 수호자 `Lv.80`,
  월식의 기록자 `Lv.110`을 표시한다.
- 잿불 수호자는 느리고 강한 주황색 `용광로 파편`, 월식의 기록자는 더 빠르고 사거리가
  긴 보라색 `월식 탄환`을 선딜 뒤 플레이어 중심으로 발사한다.
- 보스별 사거리·수직 허용 범위·선딜·쿨다운·속도·피해·수명·색상을 순수 전투 규칙으로
  분리하고, 기존 피격 무적과 공격 애니메이션·런타임 상태 표식에 연결했다.
- 보스 투사체는 명중·만료·맵 전환·씬 종료에서 제거되며 플레이어 투사체와 함께 입력 차단
  및 런타임 객체 복귀 조건에 포함된다.
- 기존 전투 이펙트 투척 프레임을 틴트해 재사용하고 새 래스터 에셋 없이 최종
  `npm run release:check`에서 Vitest 25파일·191테스트, 3엔진 Playwright 36개,
  73파일·7,970,769-byte `dist/` 감사를 통과했다.

완료 조건:

- 모든 퀘스트 대화창과 활성 HUD 알림에서 정의와 일치하는 적정 레벨을 확인할 수 있다.
- 두 보스가 지정 거리와 높이 안의 플레이어만 공격하고, 각 투사체가 발사 당시 플레이어
  방향으로 일정한 합성 속도를 가진다.
- 피격 무적이 접촉·원거리 피해에 공통 적용되고 투사체·선딜 타이머가 맵 수명을 넘지 않는다.
- `npm run release:check`가 191개 단위 테스트, 3엔진 E2E 36개, 프로덕션 빌드와 8MB
  `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add quest levels and boss ranged skills`

### P18 — 플레이어 지면 보행 주기

상태: 구현 완료 — 2026-07-17

목표: 이동 중 다리가 움직이지 않는 것처럼 보이던 기존 walk 행을 작은 게임 화면에서도
명확하게 판독되는 지면 보행으로 교체하고, 실제 이동 속도와 애니메이션 박자를 맞춘다.

구현 결과:

- 프레임 수 증가는 보류하고 초보자·로그·어쌔신·허밋·호카게 시트의 2행을 왼발 접지·통과·
  오른발 접지·통과의 명확한 4단계 주기로 다시 만들었다.
- built-in imagegen 결과에서 walk 행만 합성하고 기존 v1의 idle·jump·hurt·attack 12개 프레임,
  4×4 매니페스트 계약, 물리 바디와 발 기준선은 유지했다.
- 수평 속도를 `0.78~1.32` 재생 배율로 변환하는 순수 규칙을 추가하고 walk 상태에만 적용해
  가속·감속과 발놀림의 박자가 함께 변하도록 했다.
- 현재 애니메이션·단계·배율을 접근 가능한 DOM 상태로 노출하고, 키를 누른 동안 1~4단계가
  모두 순환한 뒤 기존 점프 공격의 수평·수직 궤도가 유지되는지 E2E로 검증했다.
- 최종 `npm run release:check`에서 Vitest 25파일·193테스트, 3엔진 Playwright 36개,
  73파일·7,687,254-byte `dist/` 감사를 통과했다.

완료 조건:

- 다섯 직업의 walk 행에서 접지·통과 자세와 앞뒤 다리 실루엣이 작은 화면에서도 구분된다.
- 걷는 동안 네 단계가 모두 순환하고 이동 속도에 따라 재생 속도가 변하되 물리 발 기준선은
  흔들리지 않는다.
- 정지·점프·피격·공격 전환이 기존 애니메이션과 충돌하지 않고 공중 공격 궤도를 바꾸지 않는다.
- `npm run release:check`가 193개 단위 테스트, 3엔진 E2E 36개, 프로덕션 빌드와 8MB
  `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: animate grounded player walk cycles`

### P19 — 플레이어 무릎 회수 자세 보강

상태: 구현 완료 — 2026-07-17 · 전체 E2E 기존 보스 flaky gate 분리 기록

목표: 통과 프레임에서도 두 다리가 곧게 벌어져 수평으로 미끄러지는 것처럼 보이는 문제를
해결하고, 이동 시작부터 접지·회수의 대비가 읽히게 한다.

구현 결과:

- 초보자·로그·어쌔신·허밋·호카게의 walk 2·4번째 셀을 지지발 아래 정렬, 반대 무릎 굽힘,
  뒤꿈치 회수의 명확한 통과 자세로 다시 편집했다.
- v2의 접지 2프레임과 idle·jump·hurt·attack 12프레임은 RGBA 픽셀 단위로 보존하고,
  4×4·16프레임·128px 셀 계약도 유지했다.
- built-in imagegen 결과에 크로마 despill·1px edge contract를 적용하고 본체와 분리된 셀 하단
  잔여 성분을 제거한 뒤 기존 256색 팔레트로 remap했다.
- 로그·어쌔신·허밋 통과 프레임의 원점을 실제 지지발 기준선에 다시 맞추고, 보행 시작 프레임을
  animation-local `1`로 바꿔 첫 접지 자세가 미끄러지는 인상을 줄였다.
- 현재 통과 자세로 시작한 뒤 1~4단계가 모두 순환하고 기존 점프 공격 궤도가 유지되는지
  단위 테스트와 변경 시나리오 3엔진 E2E 3/3으로 검증했다.
- 프로덕션 빌드와 73파일·7,680,373-byte `dist/` 감사는 통과했다. 36개 전체 E2E 동시 실행은
  기존 P17 보스 원거리 공격 시나리오가 간헐 실패했지만, 해당 시나리오만 3엔진 병렬 재실행해
  3/3 통과했으며 잔여 flaky gate는 `RELEASE_QA.md`에 기록했다.

완료 조건:

- 다섯 직업의 두 통과 프레임에서 한쪽 뒤꿈치가 지면을 떠나 종아리 쪽으로 회수된 실루엣이
  1280×720 플레이 화면에서도 판독된다.
- 비대상 14프레임은 v2와 픽셀 단위로 같고, 접지·회수 사이의 지지발 기준선이 흔들리지 않는다.
- 이동 시작 프레임은 회수 자세이며 네 보행 단계, 좌우 반전, 점프·공격 상태 전환이 유지된다.
- 변경 보행 시나리오가 3엔진에서 통과하고, 프로덕션 빌드와 8MB `dist/` 감사를 통과한다.
- 전체 36개 동시 E2E의 기존 보스 flaky gate는 별도 릴리스 안정화 작업에서 제거한다.

커밋 체크포인트: `fix: add bent-knee walk passing poses`

### P20 — 호카게 독자 SD 외형

상태: 구현 완료 — 2026-07-17

목표: 4차 직업 호카게가 금발·이마 보호대·볼 표식·검정/주황 닌자복을 갖춘 일관된
독자 SD 픽셀 캐릭터로 보이게 한다.

구현 결과:

- 금발 가시머리, 이마 보호대, 볼의 3줄 표식, 검정·주황 닌자복과 오픈토 부츠를 16프레임
  전체에 같은 인물로 유지했다.
- 기존 호카게 v3를 포즈·배치 기준으로 사용해 4×4·128px 셀, 오른쪽 원본/왼쪽 반전,
  idle·walk·jump·hurt·근접 공격·나선환 계약을 유지했다.
- 2행은 접지와 무릎 회수 자세가 교차하며, idle·walk·공격의 프레임 원점을 새 얼굴 중심과
  발바닥 기준으로 다시 측정했다.
- 1254×1254 균일 크로마 원본은 `player-hokage-v4-chroma.png`, 512×512 PaletteAlpha
  256색 런타임은 `player-hokage-v4.png`로 비파괴 버전업했다.
- 외부 시각 입력은 런타임·저장소·`dist/`에 보존하지 않고, 공개 배포 전 에셋 권리를
  `ASSET_CREDITS.md`에서 확인하도록 기록했다.
- 25파일·193개 단위 테스트, 호카게 전직·스킬·시네마틱 3엔진 E2E 6/6, 프로덕션 빌드와
  73파일·7,678,297-byte `dist/` 감사를 통과했다.

완료 조건:

- 1280×720 플레이 화면에서 4차 직업의 핵심 외형 요소가 일관된 SD 캐릭터로 판독된다.
- 16개 셀이 모두 점유되고 투명 모서리·발 기준선·좌우 반전·무릎 회수 보행이 유지된다.
- 타입체크, 단위 테스트, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: redesign hokage outfit`

### P21 — 삼인 협공 동료 독자 SD 외형

상태: 구현 완료 — 2026-07-17

목표: 삼인 협공의 두 동료가 검푸른 라이벌 닌자와 분홍 머리 타격 닌자의 서로 다른
독자 SD 픽셀 캐릭터로 보이게 한다.

구현 결과:

- 라이벌 동료 8셀은 검푸른 가시머리, 이마 보호대, 남색 상의, 흰 반바지, 붕대와 오픈토
  부츠를 같은 인물로 유지했다.
- 타격 동료 8셀은 분홍 단발, 이마 보호대, 붉은 민소매 상의와 흰 원형 무늬, 검정 반바지,
  오픈토 부츠를 같은 인물로 유지했다.
- 기존 v1을 포즈·효과·배치 기준으로 사용해 4×4·128px 셀, 진입·투척·발차기·주먹·퇴장과
  정확한 5타 역할 타임라인을 보존했다.
- 1254×1254 균일 크로마 원본은 `hokage-allies-v2-chroma.png`, 512×512 PaletteAlpha
  256색 런타임은 `hokage-allies-v2.png`로 비파괴 버전업했다.
- 외부 시각 입력은 런타임·저장소·`dist/`에 보존하지 않고, 공개 배포 전 에셋 권리를
  확인하도록 기록했다.
- 25파일·193개 단위 테스트, 삼인 협공 시네마틱 3엔진 E2E 3/3, 프로덕션 빌드와
  73파일·7,680,713-byte `dist/` 감사를 통과했다.

완료 조건:

- 16셀에서 두 동료의 외형이 섞이지 않고 각 레퍼런스의 핵심 외형 요소가 SD로 판독된다.
- 기존 프레임 순서, 5타 역할, 시네마틱 입력 잠금·객체 정리와 좌우 진입/교차 퇴장이 유지된다.
- 타입체크, 단위 테스트, 호카게 시네마틱 3엔진 E2E, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: redesign team assault allies from references`

### P22 — 거대 최종보스·무한의 결투장

상태: 구현 완료 — 2026-07-17

목표: 기존 두 던전 보스에 고유 이름과 명확한 서열을 부여하고, 사용자 제공 레퍼런스를
반영한 가장 강한 거대 최종보스를 다른 몬스터가 없는 전용 맵에서 상대하게 한다.

구현 결과:

- 잿불 광산 중간보스는 `폭열군주 이그니카르`, 달빛 마도서고 상위보스는
  `월식현자 루나시온`으로 이름과 HUD 등급을 변경했다.
- `원펀맨`은 Lv.200, HP 100,000, 2.45배 시각 스케일의 최종보스로 추가했다. 접촉 공격과
  `진심 펀치 충격파`는 피격 당시 플레이어 HP 전부를 피해로 확정해 한 번의 명중으로 쓰러뜨린다.
- `무한의 결투장`은 1920×720 연속 지면, 달빛 마도서고 귀환 포탈, 원펀맨 1체만 가지며
  NPC와 다른 몬스터를 두지 않는다. 원경은 1280×720 WebP, 충돌 전경은 결정적 SVG다.
- 원정 경로를 `이그니카르 → 루나시온 → 원펀맨 → 세라 보고`로 확장하고 전체 적정 레벨을
  `Lv.80~200`으로 변경했다. 프로필 스키마 v11은 v10의 `finalboss`를 새 `upperboss`로
  옮기고 무한의 결투장 위치를 저장한다.
- built-in imagegen으로 사용자 제공 이미지를 외형·팔레트 기준으로 사용한 4×4 크로마 시트와
  독자적인 결투장 원경을 만들었다. 런타임 시트는 셀별로 다시 패킹해 망토·펀치 효과가 셀
  경계를 넘지 않으며 생성 원본과 참고 파일은 `dist/`에서 제외한다.
- 25파일·195개 단위 테스트, 보스 원정 Chromium·Firefox·WebKit E2E 3/3, 1280×720 시각
  스모크와 콘솔 오류 0건, 프로덕션 빌드·7,793,912-byte `dist/` 감사를 통과했다.

완료 조건:

- 세 보스의 고유 이름과 중간·상위·최종 등급이 퀘스트, HUD, 접근성 상태에 일치한다.
- 무한의 결투장에는 원펀맨만 1체 생성되고 플레이어보다 두 배 이상 큰 전신 실루엣이 보인다.
- 접촉 또는 진심 펀치 충격파 1회 명중으로 HP가 0이 되고 기존 커닝시티 부활 흐름이 실행된다.
- v10 프로필 진행도와 기존 9개 맵 위치가 보존되며 새 맵 위치는 v11에서 복구된다.
- 타입체크, 단위 테스트, 3엔진 집중 E2E, 프로덕션 빌드와 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add one-punch final boss arena`

### P23 — 결투장 수직 회피·발판 하강

상태: 구현 완료 — 2026-07-17

목표: 원펀맨의 즉사 공격을 좌우 이동뿐 아니라 높이 차로 피할 수 있도록 결투장 동선을
다층화하고, 메이플형 단방향 발판 하강 조작을 추가한다.

구현 결과:

- 무한의 결투장에 지면보다 약 152~162px 높은 하층 3개와 그보다 150~170px 높은 상층
  2개의 단방향 발판을 추가했다. 기존 660 점프 속도로 층별 상승이 가능하다.
- `ArrowDown`을 `MoveDown` 액션으로 분리하고 `↓+Alt`를 누르면 발밑에 가로로 겹치고
  발 높이가 일치하는 현재 단방향 발판만 420ms 동안 통과한다.
- 하강 시작 속도는 180px/s이며 고정 지면에서는 조합 입력을 소비하되 위로 점프하지 않는다.
  맵 전환·정리 시 임시 통과 상태도 함께 해제한다.
- 조작 HUD, README, 결정적 결투장 SVG와 발판 정렬 회귀를 갱신했다.
- 26파일·198개 단위 테스트, 던전 순회·발판 하강·원펀맨 원정 Chromium·Firefox·WebKit
  E2E 9/9, 1280×720 브라우저 스모크·콘솔 오류 0건, 프로덕션 빌드와 7,798,616-byte
  `dist/` 감사를 통과했다.

완료 조건:

- 결투장 하층·상층 발판이 플레이어 점프 높이 안에 있고 원펀맨 외 몬스터는 추가되지 않는다.
- 단방향 발판 위에서 `↓+Alt`를 누르면 아래로 떨어지고, 현재 발판 아래의 다른 발판은
  같은 입력에서 자동으로 통과하지 않는다.
- 일반 `Alt` 점프, 점프 공격, 포탈 `↑`, 고정 지면 충돌은 기존 동작을 유지한다.
- 타입체크, 단위 테스트, 3엔진 집중 E2E, 빌드, `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add vertical dodge platforms`

### P24 — HUD 안전 여백·공격 모션 잔상·허름한 초보자

상태: 구현 완료 — 2026-07-17

목표: 9-slice HUD의 테두리와 텍스트를 명확히 분리하고, 공격 동세에 짧은 실루엣 잔상을
더하며, 최초 초보자가 상위 전직과 대비되는 매우 허름한 외형으로 보이게 한다.

구현 결과:

- 미니맵·조작·퀘스트·보스·하단 HUD의 패널 bounds와 18px 수평·16px 수직 안전 여백을
  `hud-layout-rules.ts`에 정의했다. 실제 Phaser 텍스트 bounds를 매 HUD 갱신 시 검사해
  `data-hud-padding-violations`로 노출하며 1280×720 런타임은 0건이다.
- 기본 공격과 7종 액티브 공격에 현재 공격 프레임을 복제한 2~5개 잔상을 추가했다.
  공격별 간격·수명·오프셋·알파·틴트는 순수 규칙으로 분리하고 맵 전환 때 함께 정리한다.
- 사용자 제공 이미지의 낡은 귀덮개 두건·누더기·붕대·닳은 신발 인상을 참고해 독자적인
  초보자 4×4 시트를 생성했다. 기존 16개 동작 역할과 물리 발 기준선을 유지한다.
- 최종 크로마 원본과 정확한 생성 프롬프트를 보존하고, 런타임에는 512×512 PaletteAlpha
  256색·36,898-byte `player-v4.png`만 번들링한다.
- 28파일·203개 단위 테스트와 HUD·공중 공격·보스 원정·나선환 집중 3엔진 E2E를 통과했다.
  1280×720 인앱 브라우저의 실제 HUD bounds 위반과 warning/error는 모두 0건이며,
  `dist/`는 76파일·7,793,807 bytes로 8MB 상한 안이다.

완료 조건:

- 모든 표시 중인 gameplay HUD 텍스트가 패널 안전 영역 안에 있고 실제 bounds 위반이 0건이다.
- 기본 공격·액티브 스킬 입력에서 2~5개의 잔상이 생성되며 공격별 색과 수명 뒤 모두 정리된다.
- 새 프로필의 초보자는 허름한 v4 외형, 전직한 기존 프로필은 직업별 기존 외형을 사용한다.
- 타입체크, 단위 테스트, 3엔진 E2E, 프로덕션 빌드·감사와 1280×720 콘솔 스모크를 통과한다.

커밋 체크포인트: `feat: polish HUD attacks and beginner sprite`

### P25 — 플레이 영역·스킬 아이콘·고레벨 전투 밸런스

상태: 구현 완료 — 2026-07-18

목표: 하단 HUD가 월드 오브젝트를 가리지 않게 하고, 모든 스킬 아이콘을 안정적으로 표시하며,
전직 차수와 몬스터 티어가 체감될 만큼 전투 수치를 크게 확장한다.

구현 결과:

- 카메라 월드 경계를 48px 확장해 지면 플레이어·NPC·몬스터의 발 기준선이 하단 HUD
  시작선보다 약 40px 위에 유지되도록 했다.
- `K` 스킬창의 11개 런타임 아이콘을 CSS `background-image` 의존에서 실제 `<img src>`로
  바꾸고, 개발·프로덕션에서 128×128 자연 크기 로드와 표시를 검사한다.
- 전직 필수 레벨 10·30·60·120을 수락·완료 보고 양쪽에서 강제하고, 시험 처치량을
  초록버섯 5·그림자 파수꾼 6·심연의 골렘 3·심연의 골렘 8체로 높였다.
- 초보자·로그·어쌔신·허밋·호카게의 직업 전투 배율을 1·12·70·350·1,400으로 두고
  Lv.10~200 레벨 배율을 곱한다. Lv.120 허밋 어벤저는 15만 이상, 호카게 나선환은
  58만 이상 피해를 낸다.
- 몬스터 HP를 초록버섯 180, 파수꾼 10,000, 골렘 120,000, 이그니카르 2,000,000,
  루나시온 12,000,000, 원펀맨 100,000,000으로 재조정하고 접촉·원거리 피해·속도·EXP·
  메소도 티어별로 상향했다. 던전 원정 적정 레벨은 Lv.100~200으로 맞췄다.
- 29파일·207개 단위 테스트, Chromium 스킬·전직·보스·필수 루프 집중 E2E, 1280×720
  아이콘 시각 검증과 브라우저 warning/error 0건을 확인했다. `dist/`는 76파일·
  7,794,275 bytes로 8MB 상한 안이다.

완료 조건:

- 지면의 캐릭터·NPC·몬스터가 하단 HUD 뒤에 숨지 않는다.
- 네 차수 탭의 스킬 아이콘 11개가 모두 실제 이미지로 로드된다.
- 레벨이 부족하면 시험 수락과 완료 보고가 모두 거부되고 강화된 처치 목표만 인정된다.
- Lv.120 허밋이 10만 이상, 호카게가 수십만 피해를 내며 상위 보스 HP도 그 규모에 맞는다.
- 타입체크, 단위 테스트, 프로덕션 빌드·감사와 Chromium 집중 E2E가 통과한다.

커밋 체크포인트: `feat: rebalance progression and skill UI`

### P26 — 스프라이트 셀 경계·HUD 사용성 전수 감사

상태: 구현 완료 — 2026-07-18

목표: 실제 스프라이트 픽셀과 `frameOrigins`의 정렬을 전체 런타임 시트에서 재검증하고,
HUD의 긴 보스 정보와 저장 가능한 최대 숫자도 9-slice 안전 영역 안에서 읽히게 한다.

구현 결과:

- 19개 런타임 시트 304셀의 크기·점유·투명 코너·가시 크로마를 검사하고, 플레이어 5종의
  idle/walk 40프레임과 몬스터·보스·NPC 9종 144프레임의 실제 픽셀 중심·발 기준선을
  매니페스트 원점과 비교하는 단위 테스트를 추가했다.
- 로그·어쌔신·허밋·호카게, 심연의 골렘, 폭열군주 이그니카르, 월식현자 루나시온에서
  현재 셀 본체와 분리되어 인접 셀 본체로 이어진 오염 픽셀만 제거했다. 기존 런타임 파일은
  보존하고 새 버전으로 연결했으며 추가·재색상은 0건이다.
- 보스 HUD는 제목과 HP 숫자를 서로 다른 행으로 분리하고 최소 8px 라벨 간격 충돌을 실제
  Phaser bounds 위반 상태에 포함했다.
- HP·MP·메소·소지품 합계가 safe integer 상한이어도 만/억/조/경 축약값으로 경계 안에
  표시하고, 캔버스 `aria-label`에는 축약하지 않은 전체 값을 유지한다.
- 30파일·212개 단위 테스트와 최대 숫자·두 퀘스트 동시 표시·상위보스·가장 긴 맵 이름을
  포함한 Chromium·Firefox·WebKit E2E 42/42, 타입체크와 프로덕션 빌드를 통과했다.
  `dist/`는 76파일·7,794,679 bytes, JavaScript 1,410,719 bytes·gzip 379,895 bytes로
  8MB 상한 안이다.

완료 조건:

- 런타임 19시트 304셀이 비어 있지 않고 투명 코너·크로마·기준선 검사를 통과한다.
- 정리본은 이전 버전 대비 지정된 인접 셀 오염 픽셀만 제거하며 픽셀 추가·재색상이 없다.
- 표시 중인 모든 HUD 텍스트가 안전 여백 안에 있고 보스 라벨끼리 충돌하지 않는다.
- 저장 가능한 최대 숫자에서도 HUD bounds 위반이 0건이며 정확한 전체 값은 접근성 상태에 남는다.
- 타입체크, 단위 테스트, 3엔진 HUD E2E, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: audit sprite alignment and HUD bounds`

### P27 — 몬스터 스프라이트 내부 셀 연결 감사

상태: 구현 완료 — 2026-07-18

목표: 몬스터 6종의 96개 프레임이 128×128 셀 경계를 넘어 서로 이어지지 않는지 전수
검사하고, 런타임에서 모든 맵·피격·공격·사망 애니메이션이 정상 동작하게 한다.

구현 결과:

- 초록버섯·그림자 파수꾼·심연의 골렘·폭열군주 이그니카르·월식현자 루나시온·원펀맨의
  수직·수평 내부 셀 경계 양쪽에 가시 픽셀이 동시에 놓인 연결 지점을 자동 검사한다.
- 심연의 골렘 v2에서 보행 4↔피격 8 사이 수평 연결 1곳과 피격 10↔11 사이 수직 연결
  8곳을 확인했다. 해당 연결을 만든 분리 컴포넌트 24픽셀만 제거해 v3로 연결했다.
- v2 대비 픽셀 추가·재색상은 0건이며 프레임 원점·애니메이션 순서·크기·RGBA 형식을
  바꾸지 않았다. 몬스터 6종의 최종 셀 간 연결은 0건이다.
- 여섯 던전 맵 로드, 일반 몬스터 피격·사망 반복, 세 보스 공격·피격·사망과 원정 완료를
  포함한 Chromium 집중 E2E 3/3을 통과했다.
- 30파일·213개 단위 테스트, 타입체크와 프로덕션 빌드를 통과했다. `dist/`는 76파일·
  7,794,856 bytes, JavaScript 1,410,719 bytes·gzip 379,899 bytes로 예산 안이다.

완료 조건:

- 몬스터 6종·96셀의 내부 셀 경계 연결 픽셀이 0건이다.
- 심연의 골렘 정리본은 지정 오염만 제거하고 추가·재색상·원점 변경이 없다.
- 단위 테스트, 타입체크, 프로덕션 빌드와 몬스터 집중 E2E를 통과한다.

커밋 체크포인트: `fix: remove monster sprite frame bleed`

### P28 — 차수별 신규 액티브 스킬

상태: 구현 완료 — 2026-07-18

목표: 기존 액티브와 저장 데이터를 유지하면서 로그·어쌔신·허밋·호카게 각 차수에 전투
역할과 시각 언어가 구분되는 액티브 스킬을 하나씩 추가한다.

구현 결과:

- 액티브 자동 슬롯은 전직 차수별 해금 순서대로 `1=럭키세븐`, `2=그림자 연사`,
  `3=드레인`, `4=환영 쌍성`, `5=어벤저`, `6=심연 폭우`, `7=나선환`, `8=구미호 변신`,
  `9=미수옥`, `0=삼인 협공`, `-=천뢰옥`으로 재배치했다. 숫자패드와 `Q·W·E·R`
  보조키도 같은 액션에 연결한다.
- 그림자 연사는 MP 16·단일 대상 3타·레벨당 피해 +1, 환영 쌍성은 MP 24·최대 2대상
  2타·레벨당 +2, 심연 폭우는 MP 42·최대 4대상 3타·레벨당 +3, 천뢰옥은 MP 70·
  최대 5대상 2타·레벨당 +5 규칙을 사용한다.
- 세 표창 스킬은 장착 표창 외형·피해 배율·예리한 시야·치명 투척을 그대로 적용하고,
  천뢰옥은 차크라 계열로 분리해 표창 장비 배율과 치명 투척을 적용하지 않는다.
- 각 공격은 200ms 공격 모션 안에서 모든 투사체를 릴리스하고, 차수별 청백·적자·심연 보라·
  청금 틴트의 투사체 잔상·플레이어 잔상·타격 링을 맵 수명주기에 묶어 정리한다.
- built-in imagegen으로 전용 1254×1254 크로마 원본 4종을 제작하고 배경 제거·point 축소한
  128×128 RGBA 런타임 아이콘을 Vite URL import로 연결했다. `K` 스킬창은 네 차수의
  15개 아이콘을 실제 `<img src>`로 표시한다.
- 프로필을 같은 키의 스키마 v12로 올리고 v1~v11의 직업·기존 스킬·SP·장비·퀘스트·위치를
  보존한 채 신규 액티브 4종을 0레벨로 보충한다.
- 30파일·226개 단위 테스트, Chromium·Firefox·WebKit 신규 단축키·15아이콘·HUD 안전 여백
  포함 E2E 45/45, 타입체크와 프로덕션 빌드를 통과했다. `dist/`는 80파일·7,841,386 bytes,
  JavaScript 1,415,816 bytes·gzip 381,082 bytes로 8MB 상한 안이다.

완료 조건:

- 각 차수의 신규 스킬이 요구 직업·MP·타수·관통 대상·레벨당 피해 규칙을 지킨다.
- 숫자열 `1~0·-`과 숫자패드 슬롯이 액티브 스킬의 전직 차수별 해금 순서를 따르고,
  보조키에서도 신규 스킬이 발동한다.
- 표창 계열과 차크라 계열의 장비 배율·패시브 적용 경계가 유지된다.
- v1~v11 프로필의 기존 성장값이 보존되고 신규 스킬만 안전한 기본값으로 보충된다.
- 네 아이콘이 128×128 자연 크기로 로드되고 HUD·스킬창 bounds와 콘솔 오류가 0건이다.
- 타입체크, 단위 테스트, 3엔진 E2E, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add one active skill per job tier`

### P29 — 하단 상태창 액티브 아이콘 바

상태: 구현 완료 — 2026-07-18

목표: 숫자 슬롯에 배정된 액티브 스킬을 플레이 중 `K` 스킬창을 열지 않아도 하단 상태창에서
아이콘과 단축키로 한눈에 확인할 수 있게 한다.

구현 결과:

- HP·MP·EXP 영역 위쪽에 `1~0/-` 해금 순서의 38px 슬롯 11개와 34px 스킬 아이콘을 한 줄로
  배치하고, 각 슬롯 왼쪽 위에 14×13 단축키 배지를 표시한다.
- 현재 직업에서 잠긴 스킬은 아이콘 알파와 테두리를 낮추고, 활성 구미호 변신 슬롯은 금색
  테두리와 배지로 강조한다. AP·SP와 `K` 안내는 기존 성장 셀에 유지한다.
- HP·MP·EXP 라벨과 게이지를 아이콘 아래 행으로 옮기고 `hud-layout-rules.ts`의 action bar
  bounds 감사에 숫자 배지 텍스트를 포함해 1280×720 하단 HUD 겹침을 막는다.
- `skill-icon-assets.ts`가 15개 아이콘 URL과 액티브 11개 Phaser 텍스처 키를 단일 관리하며,
  스킬 DOM 모달과 하단 Phaser HUD가 같은 번들 에셋을 사용한다.
- 캔버스 접근성 설명과 `data-hud-skill-slots`·`data-hud-skill-icon-count` 상태 표식에 슬롯 순서,
  스킬 이름, 레벨, 잠금 상태를 반영한다.
- 30파일·226개 단위 테스트, Chromium·Firefox·WebKit E2E 45/45, 타입체크와 프로덕션
  빌드를 통과했다. `dist/`는 80파일·7,843,379 bytes, JavaScript 1,417,809 bytes·
  gzip 380,843 bytes로 8MB 상한 안이다.

완료 조건:

- 액티브 11종 아이콘과 `1~0/-` 배지가 해금 순서대로 한 줄에 표시된다.
- 잠긴 스킬과 활성 구미호 변신이 시각적으로 구분되고 HP·MP·EXP와 겹치지 않는다.
- 스킬창과 HUD가 같은 아이콘 URL을 사용하고 모든 액티브 아이콘이 Boot에서 프리로드된다.
- 타입체크, 단위 테스트, 3엔진 E2E, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: show assigned skill icons in hud`

### P30 — QWER 보조키·능력치/장비 통합 창

상태: 구현 완료 — 2026-07-18

목표: 차수별 신규 액티브 4종의 보조키를 연속된 `QWER`로 정리하고, 비워지는 `E` 장비창을
`S` 능력치 창에 통합해 캐릭터 성장과 장비를 한 화면에서 관리한다.

구현 결과:

- 보조키는 `Q=그림자 연사`, `W=환영 쌍성`, `E=심연 폭우`, `R=천뢰옥`을 사용한다.
  기존 심연 폭우 `R`, 천뢰옥 `T`, 별도 장비 `E` 입력은 제거했다.
- `S` 통합 DOM 모달은 왼쪽에서 STR/DEX/INT/LUK·AP·자동분배를, 오른쪽에서 표창 5등급의
  미리보기·피해 배율·소유/장착 상태를 동시에 표시하고 변경 결과를 같은 저장 경로에 반영한다.
- 1280×720에서는 최대 1040px 패널과 2열 장비 카드로 모든 항목·닫기·자동분배 버튼을
  스크롤 없이 표시하며, 작은 PC 뷰포트에서는 패널 내부 스크롤로 경계를 보존한다.
- HUD 조작 안내, 스킬 상세 보조키, 인벤토리 장착 안내, 입력 단위 테스트와 E2E 상태를
  `QWER`·`S 능력/장비` 기준으로 갱신했다.

완료 조건:

- `QWER`가 네 신규 액티브를 차수 순서대로 발동하고 `T`는 더 이상 게임 입력을 점유하지 않는다.
- `S` 한 번으로 능력치와 장비가 함께 표시되며 AP 분배·자동분배·표창 장착·저장이 유지된다.
- 통합 창이 지원 PC 뷰포트 안에 머물고 키보드 포커스·닫기 복구·접근성 텍스트를 유지한다.
- 타입체크, 단위 테스트, 3엔진 E2E, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: unify QWER skills and stats equipment`

### P31 — 초반 액티브 고유 궤적·타격 연출

상태: 구현 완료 — 2026-07-18

목표: 비슷한 직선 표창으로 보이던 `1/3`, `2/4` 스킬을 궤적·실루엣·타격 피드백만으로도
구분하고 각 스킬의 전투 특성을 시각적으로 드러낸다.

구현 결과:

- 럭키세븐은 높이가 다른 청록 평행 쌍투척, 그림자 연사는 상·중·하 3갈래 부채꼴과 청백
  파편 타격으로 분리했다.
- 드레인은 크기가 맥동하는 녹색 표창과 명중 시 적→플레이어 흡수 연결선·입자를 사용하며
  기존 적용 피해 45% HP 회복을 유지한다.
- 환영 쌍성은 두 투사체가 반대 위상의 물결 궤적으로 교차하고, 명중 시 적홍·청록 X자
  타격을 남기며 기존 최대 2대상 관통을 유지한다.
- `skill-projectile-rules.ts`가 스폰 높이·초기 수직 속도·파동 진폭/주기/위상·맥동·잔상색을
  순수 규칙으로 제공하고 단위 테스트에서 네 모션 식별자와 수치 차이를 고정한다.

완료 조건:

- 럭키세븐/드레인과 그림자 연사/환영 쌍성이 각각 서로 다른 궤적과 명중 연출을 사용한다.
- 기존 타수·MP·대상 수·회복·장비/패시브 적용 경계가 변하지 않는다.
- 투사체·잔상·타격 객체가 만료·맵 정리·씬 종료 뒤 모두 정리된다.
- 타입체크, 단위 테스트, 3엔진 E2E와 프로덕션 빌드를 통과한다.

커밋 체크포인트: `feat: differentiate active skill effects`

### P32 — 스킬창 숫자 슬롯 드래그 앤 드롭

상태: 구현 완료 — 2026-07-18

목표: 기본 숫자 슬롯은 1차 전직 액티브부터 습득 순서대로 제공하되, 사용자가 `K` 스킬창에서
원하는 배치로 바꾸고 재접속 뒤에도 그대로 사용할 수 있게 한다.

구현 결과:

- 기본 `1~0/-`는 럭키세븐→그림자 연사→드레인→환영 쌍성→어벤저→심연 폭우→나선환→
  구미호 변신→미수옥→삼인 협공→천뢰옥 순서다.
- 스킬창 상단의 실제 아이콘 11칸은 native drag/drop으로 두 슬롯을 교환하며 `Alt+←/→`
  키보드 대체 조작도 제공한다. 변경 직후 숫자열·숫자패드 입력, 하단 HUD, 상세 숫자키 안내,
  캔버스 접근성 상태가 같은 순서로 갱신된다.
- 프로필 스키마 v13에 `skillHotbar`를 저장한다. v1~v12는 기본 순서를 받고 v13의 중복·누락·
  알 수 없는 값은 유효한 고유 액티브만 보존한 뒤 기본 순서의 빠진 스킬을 채운다.
- QWER와 Shift/X/C/V/B/N 보조키는 사용자 숫자 슬롯과 독립된 고정 액션으로 유지한다.

완료 조건:

- 신규/이전 프로필의 기본 슬롯이 1차 전직 액티브부터 차수·습득 순서를 따른다.
- 드래그 앤 드롭과 키보드 교환이 HUD·숫자열·숫자패드 입력에 즉시 반영된다.
- 변경 순서가 새로고침 뒤 복구되고 손상 저장값은 모든 액티브를 한 번씩 포함하도록 복구된다.
- 지원 PC 뷰포트 경계·포커스 복구·접근성 설명과 3엔진 E2E를 통과한다.

커밋 체크포인트: `feat: customize skill hotbar order`

### P33 — 전직 차수별 이펙트 밀도 상승

상태: 구현 완료 — 2026-07-18

목표: 같은 직업군 안에서도 전직 차수가 높아질수록 시전과 타격의 화면 밀도·카메라 강조·
시네마틱 존재감이 확실히 커지게 한다.

구현 결과:

- 1차는 추가 링 없이 고유 투사체 궤적을 선명하게 유지하고, 2차는 시전/타격 링을 각 1겹,
  3차는 각 2겹과 4방향 방사광·90ms 약한 카메라 진동, 4차는 각 3겹과 8방향 방사광·150ms
  강한 타격 진동을 적용한다.
- `skill-spectacle-rules.ts`가 모든 공격의 전직 차수와 시전 링·타격 링·방사광·카메라 수치를
  순수 규칙으로 제공하며 대표 스킬 1→4차의 밀도가 단조 증가하는지 단위 테스트로 고정한다.
- 기존 나선환·구미호 변신·미수옥·삼인 협공 시네마틱에 천뢰옥 640ms 금빛 단축 컷인을 더했다.
  새 래스터 없이 기존 나선 차크라 프레임을 재사용한다.
- 타수·MP·피해·관통·회복 규칙은 유지해 시각 강화가 전투 밸런스를 바꾸지 않게 했다.
- 전체 결과는 32파일·234개 단위 테스트, Chromium·Firefox·WebKit E2E 45/45, 타입체크·
  프로덕션 빌드를 통과했다. `dist/`는 80파일·7,853,192 bytes, JavaScript 1,425,879 bytes·
  gzip 383,427 bytes로 8MB 예산 안이다.

완료 조건:

- 1→4차 대표 액티브의 시전·타격 레이어와 카메라 강조가 차수에 따라 감소하지 않는다.
- 3차는 다중 링·방사광, 4차는 더 많은 링·방사광과 짧은 컷인으로 하위 차수와 구분된다.
- 다중 효과와 컷인 객체·타이머·physics pause가 맵 정리·씬 종료 뒤 남지 않는다.
- 타입체크, 단위 테스트, 3엔진 E2E, 프로덕션 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: scale skill spectacle by job tier`

### P34 — 인내의 숲 비전투 점프 맵

상태: 구현 완료 — 2026-07-18

목표: 기존 전투·던전 회랑과 다른 템포의 짧은 발판 도전 지역을 초록버섯굴 분기로 추가한다.

구현 결과:

- 초록버섯굴 왼쪽 상단 발판에 인내의 숲 포탈과 복귀 스폰을 추가하고, 인내의 숲 입구와
  정상의 두 포탈이 같은 초록버섯굴 복귀 지점으로 연결되게 했다.
- 1920×720 연속 지면 위에 현재 점프 물리로 도달 가능한 115~130px 고도차와 200~220px
  중심 간격의 단방향 나뭇가지 발판 8개를 지그재그로 배치했다. 몬스터와 NPC는 두지 않는다.
- built-in imagegen으로 만든 독자적인 거목·폭포 픽셀 원경을 1280×720 WebP로 최적화하고,
  `generate-map-layers.mjs`가 발판·포탈 좌표 메타데이터를 가진 1920×720 SVG 전경을 재생성한다.
- `patienceForest` 위치를 프로필 v13 허용 목록에 추가해 새로고침 뒤 정확히 복구한다.
- 최종 결과는 Vitest 32파일·236테스트, Chromium·Firefox·WebKit E2E 45/45, 타입체크·
  프로덕션 빌드를 통과했다. `dist/`는 82파일·7,943,174 bytes, JavaScript 1,427,427 bytes·
  gzip 383,747 bytes로 8MB 예산 안이다.

완료 조건:

- 초록버섯굴↔인내의 숲 포탈의 목적 스폰이 모두 유효하고 입구·정상 귀환이 가능하다.
- 모든 발판과 포탈이 `map-definitions.ts`와 생성 SVG의 `data-*` 좌표 검증을 통과한다.
- PC 키보드 점프 궤적으로 코스를 진행할 수 있고 낙하 시 연속 지면에서 다시 시작할 수 있다.
- 타입체크, 단위 테스트, 프로덕션 빌드와 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add patience forest jump map`

### P35 — 고난도 인내의 숲·줄타기·정상 보상

상태: 구현 완료 — 2026-07-18

목표: 인내의 숲을 여러 종류의 장애물을 통과해 수직으로 오르는 고난도 코스로 확장하고,
줄 등반과 완주 보상을 추가한다.

구현 결과:

- 월드를 1920×1440으로 확장하고 연속 지면 1개와 단방향 나뭇가지 발판 20개를 배치했다.
- 줄 4개에서 `↑/↓`로 이동하고 `←/→` 또는 `Alt`로 이탈하는 등반 상태를
  추가했다. 부착·이동·이탈 계산은 `climb-rules.ts`의 순수 규칙으로 분리했다.
- 통나무·도토리·가시 10개가 결정적으로 왕복하며, 접촉하면 HP 피해 없이 입구로 돌아간다.
  좌표와 접촉 계산은 `map-hazard-rules.ts`에서 관리한다.
- 정상 포탈 최초 이용 시 1,000,000 메소와 보상 전용 6등급 투척무기 `초대형 고드름`을
  지급한다. 이미 소유한 경우 중복 지급하지 않으며 결과를 즉시 프로필에 저장한다.
- 초대형 고드름은 상점 판매 5등급과 분리하고 기존 빙결 프레임을 1.75배로 재사용한다.
  표창 계열 피해 배율은 기존 최고 1.5보다 높은 2.0이다.
- 생성 SVG는 21개 충돌면·4개 등반물·10개 장애물·2개 포탈의 `data-*` 좌표를
  `map-definitions.ts`와 자동 대조한다.
- 최종 결과는 Vitest 35파일·244테스트, Chromium·Firefox·WebKit E2E 48/48, 타입체크·
  프로덕션 빌드를 통과했다. `dist/`는 82파일·7,961,029 bytes, JavaScript 1,435,344 bytes·
  gzip 386,112 bytes로 8MB 예산 안이다.

완료 조건:

- PC 키보드로 줄에 붙어 양방향 이동하고 좌우·점프로 안전하게 이탈할 수 있다.
- 장애물 접촉 시 생명력 감소 없이 입구에서 재시작하며 맵 전환 뒤 임시 객체가 남지 않는다.
- 정상 보상은 정확히 한 번 지급되고 초대형 고드름의 장착·투사체 프레임·새로고침 저장이 유지된다.
- 타입체크, 단위 테스트, Chromium·Firefox·WebKit E2E, 프로덕션 빌드와 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: expand patience forest challenge`

### P36 — 기존 던전 줄타기 오브젝트

상태: 구현 완료 — 2026-07-18

목표: 인내의 숲에서 도입한 등반 조작을 기존 전투 던전의 수직 발판 이동에도 적용한다.

구현 결과:

- 그림자 시험장과 던전 회랑 6종에 지면과 첫 단방향 발판을 잇는 줄 1개씩, 총 7개를
  추가했다. 입구·귀환 포탈과 보스 스폰을 피해 기존 필수 진행 동선을 유지한다.
- `map-definitions.ts`의 `climbables`가 부착 판정 좌표를 관리하고 기존 `climb-rules.ts`의
  `↑/↓` 이동과 `←/→`·`Alt` 이탈 규칙을 모든 던전에서 공유한다.
- 생성 던전 SVG 6종은 팔레트별 주선·하이라이트·매듭을 그리고, 그림자 시험장 수작업 SVG는
  청록 줄과 보라 매듭을 사용한다. 모든 줄은 `data-climbable-*` 좌표 검증을 통과한다.
- 수정 개미굴 E2E에서 지면 접근, 줄 부착, 상승, `Alt` 이탈과 착지를 확인한다.
- 최종 결과는 Vitest 35파일·246테스트, Chromium·Firefox·WebKit E2E 51/51, 타입체크·
  프로덕션 빌드를 통과했다. `dist/`는 82파일·7,969,682 bytes, JavaScript 1,436,557 bytes·
  gzip 386,363 bytes로 8MB 예산 안이다.

완료 조건:

- 7개 던전 정의에 각각 줄 1개가 있고 지면보다 높은 첫 발판을 연결한다.
- 실제 줄 판정과 전경 SVG의 id·kind·x·top·bottom이 일치한다.
- 줄을 사용하지 않는 기존 점프·전투·포탈·보스 원정 동선이 유지된다.
- 타입체크, 단위 테스트, Chromium·Firefox·WebKit E2E, 프로덕션 빌드와 8MB `dist/` 감사를 통과한다.

현재 P79 기준에서는 무한의 결투장 줄을 제거해 그림자 시험장과 회랑 5종만 줄 1개를 유지한다.
결투장은 연속 지면·2단 단방향 발판 5개·귀환 포탈·원펀맨 1체만 사용한다.

커밋 체크포인트: `feat: add ropes to existing dungeons`

### P37 — 전 맵 imagegen 환경 오브젝트·사다리 제거

상태: 구현 완료 — 2026-07-18

목표: 모든 맵의 발판·등반 줄·포탈과 인내의 숲 장애물을 배경별 생성형 이미지로 통일하고
사다리 개념을 제거한다.

구현 결과:

- 11개 배경을 스타일 참고로 built-in imagegen을 4회 사용해 발판·줄·포탈 키트 3장과
  인내의 숲 통나무·도토리·가시 구체 키트 1장을 생성했다.
- flat `#ff00ff` 크로마 원본을 보존하고 공식 크로마 제거 도구로 투명 처리한 뒤 512×512
  오브젝트 WebP 3장과 384×384 장애물 WebP 1장, 합계 141,596 bytes로 최적화했다.
- 각 맵은 고유 `objectTheme`을 가지며 런타임에서 발판·줄·포탈 프레임을 충돌 좌표에 배치한다.
  SVG 전경 10종은 Vite 런타임 번들에서 제외하고 좌표 검증 소스로만 유지한다.
- `ClimbableDefinition.kind`를 `rope`만 허용하도록 좁히고 인내의 숲 중앙 사다리를 동일한
  top/bottom의 `patience-rope-middle`로 교체했다. 데이터·생성 SVG·화면에 사다리가 남지 않는다.
- 1280×720 커닝시티·수정 개미굴·인내의 숲 합성 화면에서 배경 팔레트, 발판 기준선,
  포탈 위치와 HUD 가독성을 확인했다.
- 최종 결과는 Vitest 35파일·246테스트, Chromium·Firefox·WebKit E2E 51/51, 타입체크·
  프로덕션 빌드를 통과했다. `dist/`는 77파일·7,979,947 bytes, JavaScript 1,434,953 bytes·
  gzip 386,202 bytes로 8MB 예산 안이다.

완료 조건:

- 11개 맵이 서로 다른 imagegen 오브젝트 테마를 사용하고 모든 오브젝트 아틀라스가 투명 WebP로 로드된다.
- 발판·포탈·줄·장애물의 시각 객체와 데이터 기반 충돌·상호작용 좌표가 분리된 채 일치한다.
- 저장소 런타임 정의·생성 SVG에 `ladder` kind가 없고 기존 중앙 등반 동선은 줄로 유지된다.
- 타입체크, 단위 테스트, Chromium·Firefox·WebKit E2E, 프로덕션 빌드와 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: replace map objects with imagegen assets`

### P38 — 맵 오브젝트 접지 정렬·포탈 바닥 광원

상태: 구현 완료 — 2026-07-18

목표: 생성형 오브젝트의 투명 여백 때문에 발판·포탈이 판정 좌표에서 떠 보이는 문제를 없애고,
모든 포탈을 바닥 광원으로 즉시 식별할 수 있게 한다.

구현 결과:

- 11개 테마의 발판·줄·포탈 프레임을 실제 불투명 연결 영역 기준으로 등록해 셀 안의 투명 여백과
  인접 오브젝트 픽셀이 표시 프레임에 섞이지 않게 했다.
- 발판은 프레임 상단을 충돌체 상단에, 줄은 프레임 상단을 등반 범위 `top`에, 포탈은 프레임
  하단을 상호작용 좌표 `y`에 정렬한다.
- 포탈 아래에 테마별 색상의 ADD 블렌드 외곽 광원과 흰색 중심광을 배치하고 860ms 맥동 트윈을
  적용했다. 광원과 트윈은 기존 `mapObjects` 수명 주기로 맵 전환 시 제거된다.
- 11개 맵에 각각 직접 진입해 만든 1280×720 캡처에서 플레이어 발·지면 상단·입구 포탈 하단의
  접지와 포탈 광원 가독성을 모두 확인했다.
- 최종 결과는 Vitest 35파일·246테스트, Chromium·Firefox·WebKit E2E 51/51, 타입체크와
  프로덕션 빌드를 통과했다. `dist/`는 77파일·7,982,128 bytes, JavaScript 1,437,134 bytes·
  gzip 386,797 bytes로 8MB 예산 안이다.

완료 조건:

- 모든 테마의 플랫폼·줄·포탈 프레임이 독립적인 불투명 영역만 포함한다.
- 캐릭터 발과 포탈 하단이 실제 지면/발판 윗면에 시각적으로 접지한다.
- 모든 포탈에 맵 팔레트와 어울리는 바닥 광원이 있고 맵 전환 뒤 트윈이 남지 않는다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 브라우저 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: align map objects and add portal glow`

### P39 — 반투명 HUD·접이식 창·팝업 크롬

상태: 구현 완료 — 2026-07-18

목표: 참고 스크린샷의 소형 창 구성과 정보 위계를 현재 게임에 맞게 재해석하고, 플레이 화면을
가리는 상단 HUD는 필요할 때 각각 접을 수 있게 한다.

구현 결과:

- 미니맵·퀘스트 알림·조작 안내에 얇은 은회색 프레임, 청회색 제목띠, 어두운 반투명 본문을
  적용했다. 하단 HUD는 가독성을 위해 높은 불투명도를 유지하면서 이중 상단 레일과 인셋 셀로
  캐릭터·게이지·재화·성장·11개 액션 슬롯의 경계를 강화했다.
- 세 상단 창의 우측에 접근 가능한 `−/+` DOM 버튼을 두고 각각 독립적으로 접기·펼치기 한다.
  접힌 상태에는 제목과 버튼만 남고 Canvas 상태 속성과 `aria-expanded`가 함께 갱신된다.
- 인벤토리·능력치·스킬·퀘스트 DOM 팝업에 공통 금속 창 제목 바, 숫자 고정폭 정렬, 한글 단어
  경계 줄바꿈과 영문 상세 라벨 정렬을 적용했다.
- `HUD_PANEL_ALPHA`가 상단·보스·하단 패널 투명도 계층의 단일 기준이며, 기존 텍스트 안전 여백·
  전체 수치 접근성 설명·보스/퀘스트 동시 표시 계약을 유지한다.
- 사용자 제공 스크린샷은 레이아웃·명암·창 계층 참고에만 사용하고 저장소와 `dist/`에는 포함하지 않는다.
- 최종 결과는 Vitest 35파일·247테스트, Chromium·Firefox·WebKit E2E 51/51, 타입체크·
  프로덕션 빌드를 통과했다. `dist/`는 77파일·7,988,377 bytes, JavaScript 1,440,766 bytes·
  gzip 387,973 bytes로 8MB 예산 안이다.

완료 조건:

- 밝고 어두운 맵에서 세 상단 패널 뒤의 월드가 비치되 제목과 본문이 읽힌다.
- 미니맵·퀘스트·조작 안내가 마우스와 키보드 포커스로 각각 접히며 상태가 접근성 트리에 반영된다.
- 인벤토리·능력치·스킬·퀘스트 팝업의 제목·본문·버튼·숫자 열이 경계 안에서 정렬된다.
- 최대 수치, 두 퀘스트, 보스 HUD와 접힘 상태에서 실제 텍스트 bounds 위반이 0건이다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 브라우저 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: refresh gameplay hud chrome`

### P40 — 창 크기 대응 팝업·내부 스크롤

상태: 구현 완료 — 2026-07-18

목표: PC 창 높이가 작아져도 인벤토리·스킬·능력치·상점·퀘스트 팝업이 게임 영역 아래로
잘리지 않고 모든 내용과 닫기 버튼에 접근할 수 있게 한다.

구현 결과:

- `#screen-overlay`를 축소 가능한 단일 Grid 트랙으로 고정하고 모든 게임 팝업의 최대 폭·높이를
  현재 게임 영역 안으로 제한했다.
- 팝업의 내용이 가용 높이를 넘으면 패널 자체에 세로 스크롤과 오버스크롤 격리를 적용해 하단
  버튼과 상세 정보를 계속 사용할 수 있다.
- 760px 이하에서는 인벤토리·스킬·능력치의 복수 열을 한 열로 바꾸고 스킬 단축 슬롯을 축소해
  가로 넘침도 방지한다.
- 게임 플레이 중 팝업이 열리면 사운드 설정과 HUD 접기 버튼을 숨겨 팝업 제목·닫기 버튼과의
  겹침을 제거했다. 로그인과 캐릭터 선택 화면의 사운드 설정은 그대로 유지한다.
- 뷰포트 E2E가 1280×720·1600×900·1024×768·800×600에서 능력치·인벤토리·스킬 창의
  사방 경계, `overflow-y`, 실제 `scrollTop` 이동과 사운드 바 비표시를 검증한다.
- 관련 Chromium·Firefox·WebKit 뷰포트 E2E 3/3, Vitest 35파일·247테스트, 타입체크·빌드와
  77파일·7,989,268 bytes `dist/` 감사를 통과했다. 전체 동시 E2E의 기존 필수 플레이 루프
  포탈 이동 타이밍 실패는 `RELEASE_QA.md`의 잔여 flaky gate로 분리했다.

완료 조건:

- 지원하는 네 PC 창 크기에서 세 주요 팝업의 상·하·좌·우가 게임 영역을 벗어나지 않는다.
- 내용 높이가 가용 높이보다 큰 경우 마우스 휠·키보드로 팝업 끝까지 스크롤할 수 있다.
- 작은 폭에서 복수 열의 최소 폭 때문에 수평으로 잘리는 요소가 없다.
- 팝업과 사운드 설정·HUD 접기 컨트롤이 겹치지 않는다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 브라우저 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: keep gameplay popups within viewport`

### P41 — 사냥터 몬스터 밸런스·상단 포탈 시야

상태: 구현 완료 — 2026-07-18

목표: 성장 구간과 맞지 않던 몬스터 수치·배치를 사냥터별로 분리하고, 높은 발판의 포탈이
상단 HUD에 가려질 때 점프로 주변을 확인할 수 있게 한다.

구현 결과:

- 초록버섯굴 Lv.10~29, 수정 개미굴 Lv.30~49, 시계태엽 탑 Lv.50~69, 산호 신전
  Lv.70~99, 잿불 광산 Lv.100~139, 달빛 마도서고 Lv.140~199, 무한의 결투장 Lv.200의
  권장 레벨 구간을 맵 정의에 추가했다.
- 기존 몬스터 시트를 공유하는 수정·태엽 파수꾼과 산호·잿불·마도 골렘 지역 변종을 만들고
  각 일반 사냥터에 같은 레벨대 몬스터 4체를 층별로 재배치했다. 그림자 시험장의 전직 대상은
  기존 그림자 파수꾼·심연의 골렘을 유지한다.
- 레벨업 EXP 공식과 직업별 공격 배율을 기준으로 HP·접촉 공격력·EXP를 낮거나 완만한 성장
  곡선으로 다시 맞추고, `raw × 1000 / (1000 + defense)` 방어 피해 감소를 추가했다.
- y=360보다 높은 포탈이 있는 맵에 상단 112px 카메라 경계를 열고, 실제 점프의 최대 650ms
  공중 구간에만 추적 목표를 위로 이동해 포탈 본체와 라벨이 상단 HUD 아래로 내려오게 했다.
  착지하면 즉시 기본 추적으로 복귀한다.
- 좌상단 미니맵은 고정·기본 펼침 상태를 유지하면서 현재 맵 이름 아래에 권장 레벨 구간을
  표시한다. 3엔진 E2E가 실제 점프의 음수 카메라 스크롤과 미니맵 펼침 유지를 검증한다.
- 최종 결과는 Vitest 36파일·252테스트, Chromium·Firefox·WebKit 관련 E2E 6/6,
  타입체크·프로덕션 빌드와 77파일·7,993,373 bytes `dist/` 감사를 통과했다.

완료 조건:

- 일반 사냥터의 권장 레벨과 지역 몬스터 레벨이 순차적으로 증가한다.
- 모든 몬스터 공격에 방어력이 적용되고 실제 적용 피해가 데미지 숫자·드레인 회복에 사용된다.
- 초록버섯굴 등 높은 포탈이 있는 맵에서 점프하면 카메라가 y=0 위를 보여준다.
- 점프 전후 미니맵이 좌상단에 고정되고 펼침 상태와 추천 레벨 표시를 유지한다.
- 타입체크, 단위 테스트, 프로덕션 빌드와 관련 브라우저 E2E를 통과한다.

커밋 체크포인트: `feat: rebalance hunting grounds and reveal upper portals`

### P42 — 신규 캐릭터 생성·닉네임 이름표

상태: 구현 완료 — 2026-07-18

목표: 프로필이 없는 첫 로그인에 캐릭터 생성 단계를 추가하고, 주사위 기본 능력치와 닉네임을
저장해 모든 캐릭터 화면과 실제 플레이에서 식별할 수 있게 한다.

구현 결과:

- 로그인 뒤 저장 프로필 유무를 확인해 신규 생성 또는 기존 캐릭터 선택 Scene으로 분기한다.
- 닉네임은 공백을 정리한 2~12자 한글·영문·숫자만 허용하고 입력 즉시 오류·사용 가능 상태와
  생성 버튼을 함께 갱신한다.
- 주사위는 STR·DEX·INT·LUK를 각각 4~13으로 제한하면서 합계 25를 보장하고 횟수 제한 없이
  다시 굴릴 수 있다. 클릭할 때마다 주사위가 위로 떠올라 회전한 뒤 사라지며, 순수 규칙은
  `src/game/profile/character-creation-rules.ts`에서 관리한다.
- 프로필 v14는 생성 닉네임과 기본 능력치를 저장한다. 기존 v1~v13은 저장된 이름과 성장
  상태를 보존하고 손상된 이름만 기본 이름으로 복구한다.
- 생성 미리보기와 캐릭터 선택, 실제 플레이어 아래에 같은 이름표를 표시하며 플레이 중에는
  이동·점프·줄타기·포탈 이동을 따라가고 피격 점멸에는 영향받지 않는다. 선택 화면은 현재
  직업 외형을 사용하며 HUD와 캔버스 접근성 상태에도 같은 이름을 유지한다.
- 재검토에서 저장 경계의 닉네임·스탯 검증, 애니메이션 종료 후 결과 확정, 굴림 중 중복
  입력 잠금, 시각적 발끝 아래 6px 이름표 위치 규칙·텔레메트리와 각 뷰포트별 빈 프로필 초기화를 보강했다.
- Vitest 37파일·265테스트, Chromium·Firefox·WebKit 신규 생성·4개 PC 창 경계·직업 외형 E2E 9/9,
  타입체크·프로덕션 빌드와 `dist/` 감사를 통과했다.

완료 조건:

- 빈 저장소의 첫 로그인에서는 생성 화면이, 기존 저장소에서는 캐릭터 선택 화면이 열린다.
- 유효한 닉네임과 주사위 결과만 확정할 수 있고 새로고침 뒤 값이 그대로 복구된다.
- 생성·선택·게임 플레이 화면에서 닉네임이 캐릭터 아래에 표시된다.
- 1280×720부터 최소 검증 PC 창 800×600까지 생성 패널이 게임 영역을 벗어나지 않는다.
- 타입체크, 단위 테스트, 프로덕션 빌드와 관련 브라우저 E2E를 통과한다.

커밋 체크포인트: `feat: add character creation and player nameplates`

### P43 — 성장 상한·점프 카메라·최종 보스 회귀 수정

상태: 구현 완료 — 2026-07-18

목표: 사냥터 밸런스와 상단 포탈 시야 개선 뒤 발견된 Lv.200 초과 성장, 착지 후 카메라 잔류,
최종 보스의 과도한 유효 내구도를 바로잡고 실제 화면 노출을 회귀 테스트로 고정한다.

구현 결과:

- 공통 EXP 지급 규칙에 Lv.200 상한을 두어 최대 레벨 보상은 레벨·HP·MP·AP·SP를 더 이상
  증가시키지 않는다. Lv.199에서 큰 보상을 받아도 Lv.200까지만 성장하고 EXP를 안전 범위로
  절삭한다.
- 점프 상단 시야는 점프 입력으로 시작한 최대 650ms 공중 구간에만 활성화한다. 맵 진입 직후
  낙하나 피격 낙하에는 켜지지 않고, 착지하면 즉시 추적 오프셋이 0으로 복구된다.
- 3엔진 E2E는 점프 전 상단 포탈이 퀘스트 HUD에 가려진 상태, 점프 중 포탈 본체가 HUD 아래로
  80px 이상 드러난 상태, 착지 후 플레이어가 하단 HUD 위에 보이는 상태를 직접 검증한다.
- 방어 피해 감소 적용으로 유효 내구도가 약 2억 1,600만까지 늘었던 원펀맨을 HP 80,000,000,
  방어력 400으로 조정해 유효 내구도를 약 1억 1,200만으로 낮췄다.
- 최종 결과는 Vitest 37파일·262테스트, Chromium·Firefox·WebKit HUD 경계 E2E 6/6,
  타입체크·프로덕션 빌드와 77파일·7,999,991 bytes `dist/` 감사를 통과했다.

완료 조건:

- 어떤 EXP 보상도 캐릭터를 Lv.200보다 높게 만들거나 추가 성장 보상을 지급하지 않는다.
- 카메라 상단 시야는 실제 점프 중에만 활성화되고 착지하면 플레이어와 HUD가 함께 보인다.
- 상단 포탈의 실제 노출 높이와 좌상단 미니맵 펼침 상태를 3개 브라우저에서 검증한다.
- 최종 보스의 방어 적용 유효 내구도가 의도한 최종전 범위를 벗어나지 않는다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 관련 브라우저 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: cap progression and restore jump camera`

### P44 — 팝업 반응형·스크롤 정밀 회귀 수정

상태: 구현 완료 — 2026-07-18

목표: P40 팝업 경계 개선을 현재 P43 화면 구성에서 다시 감사하고, 실제 게임 영역의 높이·
수평 넘침·스크롤 뒤 조작 접근성까지 회귀 테스트로 고정한다.

구현 결과:

- 로그인·캐릭터 화면의 넓은 외곽 여백과 플레이 팝업 여백을 분리해 게임 중에는 14~28px만
  사용한다. 1280×720에서 스킬·인벤토리가 불필요하게 작아지지 않고 가용 높이를 활용한다.
- 인벤토리·스킬 헤더, 능력치·상점·퀘스트 제목과 하단 닫기·확정 조작부를 스크롤 프레임에
  고정했다. 팝업별 세로 패딩 변수를 사용해 고정 영역과 금속 프레임 사이로 본문이 비치지 않는다.
- 1024×768 브라우저의 실제 게임 영역이 1024×576인데 기존 높이 미디어 조건이 발동하지 않아
  능력치·장비가 130px가량 수평으로 넘치던 문제를 발견했다. 1100px 이하에서 두 영역을 한 열로
  전환하고 760px 이하 기존 축소 규칙과 결합했다.
- 뷰포트 E2E는 모든 팝업의 `scrollWidth <= clientWidth`, 실제 마우스 휠 스크롤과 스크롤 뒤
  닫기 버튼의 상·하 경계를 1280×720·1600×900·1024×768·800×600에서 검사한다.
- 8MB 상한에 9바이트만 남았던 P43 배포본을 위해 `abyss-golem-v3.png`를 픽셀 변화 없이
  351,160→323,914 bytes로 무손실 재압축했다.
- 최종 결과는 Vitest 37파일·262테스트, Chromium·Firefox·WebKit 뷰포트 E2E 3/3,
  타입체크·프로덕션 빌드와 77파일·7,973,940 bytes `dist` 감사를 통과했다.

완료 조건:

- 네 지원 PC 창에서 주요 팝업이 게임 영역 밖으로 나가거나 수평 스크롤을 만들지 않는다.
- 세로 스크롤 뒤에도 제목과 닫기·확정 조작부가 팝업 프레임 안에 보인다.
- 1024px 이하 능력치·장비가 한 열로 전환되고 모든 장비 행을 세로 스크롤로 확인할 수 있다.
- 스프라이트 픽셀·프레임 경계·기준선 테스트가 무손실 압축 전과 동일하게 통과한다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 관련 3엔진 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: harden responsive popup scrolling`

### P45 — 점프 카메라 롤백·전 맵 몬스터 접지

상태: 구현 완료 — 2026-07-18

목표: 점프 중 월드 위쪽을 추가로 보여주던 상단 포탈 카메라 기능을 사용자 요청에 따라
완전히 되돌리고, 초록버섯굴에서 두드러진 몬스터와 발판 그림의 접지 오차를 전 맵에서 바로잡는다.

구현 결과:

- 점프용 상단 112px 카메라 경계, 320px 추적 오프셋, 650ms 활성 타이머와 런타임 상태 표식을
  제거했다. 카메라는 기존 y=0 월드 상단 경계와 하단 HUD용 48px 월드 여백만 사용한다.
- 좌상단 미니맵은 카메라 롤백과 분리해 기본 펼침 상태, 현재 맵 이름과 권장 레벨 표시를
  그대로 유지한다. 3엔진 E2E는 점프 중 카메라가 음수 y로 열리지 않는 상태도 검증한다.
- 11개 imagegen 오브젝트 테마의 발판 프레임을 감사해 최상단 장식이 아닌 실제 연속 보행면의
  소스 y를 등록했다. `mapPlatformVisualTopY`가 표시 높이에 맞춰 보정해 보행면과 충돌체 윗면을
  일치시킨다.
- 초록버섯굴은 프레임 상단의 작은 장식 버섯 때문에 110px 지면 표시에서 약 24px 아래에 있던
  이끼 표면을 충돌면으로 올렸다. 산호 신전·인내의 숲 등 장식 돌출이 있는 테마와 나머지
  테마에도 같은 공통 규칙을 적용했다.
- 모든 맵의 몬스터 스폰 y와 전체 순찰 범위가 하나의 충돌 발판에 놓이는지 검사해 그림자
  시험장 `shadow-sentinel-3`의 초기 y를 430→428로 맞췄다.
- 몬스터가 있는 8개 맵을 1280×720로 직접 캡처해 초록버섯·파수꾼·골렘·세 보스의 발과
  지면을 비교했다. 최종 결과는 Vitest 37파일·266테스트, Chromium·Firefox·WebKit HUD E2E
  6/6, 타입체크·프로덕션 빌드와 77파일·7,974,761 bytes `dist/` 감사를 통과했다.

완료 조건:

- 점프해도 카메라가 월드 y=0 위로 열리거나 별도 추적 오프셋을 사용하지 않는다.
- 좌상단 미니맵은 게임 진입과 점프 전후에 펼침 상태와 권장 레벨 표시를 유지한다.
- 모든 몬스터의 스폰과 순찰 범위가 실제 충돌 발판 위에 있고 스프라이트 발이 보행면에 닿는다.
- 초록버섯굴을 포함한 몬스터 맵 8종의 1280×720 화면에서 눈에 띄는 부유 간격이 없다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 관련 3엔진 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: ground monsters and roll back jump camera`

### P46 — imagegen HUD 크롬·전체 경계 재정렬

상태: 구현 완료 — 2026-07-18

목표: 플레이 화면의 모든 HUD 테두리와 내부 표면을 하나의 독자적인 생성 에셋 계열로
통일하고, 제목·본문·수치·게이지·스킬 슬롯이 프레임이나 서로와 겹치지 않도록 재배치한다.

구현 결과:

- built-in imagegen으로 정면 대칭형 건메탈 HUD 패널 원본을 생성하고
  `assets/ui/source/hud-metal-panel-v1-source.png`에 보존했다. 이를 96×96 9-slice용
  `hud-metal-panel-v1.webp`와 저대비 내부 표면 `hud-metal-surface-v1.webp`로 정규화했다.
- 미니맵·퀘스트·조작·보스·하단 상태·액션 슬롯·시스템/상호작용 안내·플레이 사운드 설정의 배경을 `hud` 테마와
  생성 표면으로 통일했다. 텍스트·아이콘·게이지 값은 래스터에 포함하지 않고 Phaser Text·
  도형과 캔버스 접근성 상태로 유지한다.
- `HUD_PANEL_BOUNDS`와 신규 `HUD_CONTENT_BOUNDS`가 외곽 패널, 제목띠, 본문, 게이지와 하단 셀의
  단일 좌표 기준이 된다. 하단 HUD는 y=596·높이 124px이며 액션 바와 HP/MP/EXP 게이지 행,
  캐릭터·재화·성장 셀이 최소 4px 간격으로 완전히 분리된다.
- 최대 길이 닉네임은 캐릭터 셀 너비 안에서 16px부터 11px까지만 축소하고, 보스 제목과 HP,
  접기 버튼은 각 제목띠 기준선에 맞췄다. 실제 Phaser bounds 감사는 위반 항목 이름을 먼저
  보고하도록 개선했다.
- 최종 결과는 Vitest 37파일·268테스트, Chromium·Firefox·WebKit HUD 경계·최소 800×600 포함
  뷰포트 E2E 9/9, 타입체크·프로덕션 빌드와 79파일·7,992,071 bytes `dist/` 감사를 통과했다.

완료 조건:

- 펼친 플레이 HUD의 모든 표시 텍스트가 해당 프레임의 안전 영역 안에 있고 실제 bounds 위반이 0건이다.
- 액션 바, HP/MP/EXP, 캐릭터, 재화와 성장 셀이 서로 겹치지 않고 최소 4px 간격을 유지한다.
- 1280×720과 최소 800×600을 포함한 지원 PC 창에서 HUD와 접기 버튼이 게임 영역을 벗어나지 않는다.
- 생성 배경은 텍스트·아이콘·로고 없이 배포되고 원본 프롬프트·파생 경로·출처가 문서화된다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 관련 3엔진 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: unify gameplay HUD with generated chrome`

### P47 — 3개 캐릭터 슬롯 생성·선택

상태: 구현 완료 — 2026-07-18

목표: 기존 캐릭터가 있더라도 2·3번 빈 슬롯에 신규 캐릭터를 만들고, 세 캐릭터의 진행을
서로 덮어쓰지 않은 채 선택해 플레이할 수 있게 한다.

구현 결과:

- 1번 슬롯은 기존 `kerning-shadows.local-profile.v1` 키를 유지하고 2·3번 슬롯은
  `.slot-2`, `.slot-3`, 현재 선택은 `.active-slot` 접미 키로 분리했다. 기존 단일 프로필은
  별도 변환 없이 1번 슬롯에서 그대로 복구된다.
- 캐릭터 선택 화면의 잠긴 슬롯을 접근 가능한 빈 슬롯 생성 버튼으로 바꾸고, 슬롯 번호를
  지정해 생성 Scene으로 이동한다. 생성 취소 시 기존 캐릭터 선택으로 돌아간다.
- 세 슬롯의 닉네임·레벨·직업을 한 화면에 표시하고 선택 카드·세부 능력치·Phaser 미리보기와
  이름표를 함께 갱신한다. 게임 시작과 새로고침은 마지막으로 선택한 슬롯을 사용한다.
- 각 슬롯은 성장·EXP·맵 위치·인벤토리·표창·스킬·전직 및 던전 퀘스트를 독립 저장하고,
  빈 슬롯 자동 탐색·가득 찬 슬롯 거부·점유 슬롯 덮어쓰기 차단·전체 초기화를 단위 테스트한다.
- Chromium·Firefox·WebKit E2E에서 1→2→3번 생성, 2번 선택 플레이와 새로고침 복구를
  검증하고, 네 지원 PC 창에서 캐릭터 선택 패널의 경계·세로 스크롤·수평 넘침을 함께
  검사했다. 관련 E2E 6/6과 37파일·273개 단위 테스트, 타입체크·프로덕션 빌드,
  79파일·7,999,552 bytes `dist/` 감사를 통과했다.

완료 조건:

- 기존 캐릭터를 지우거나 초기화하지 않고 2·3번 슬롯에 신규 캐릭터를 생성할 수 있다.
- 세 슬롯의 진행 데이터와 현재 선택 상태가 서로 독립적으로 저장·복구된다.
- 빈 슬롯은 생성 동작을, 점유 슬롯은 선택 동작을 제공하며 점유 슬롯을 덮어쓰지 않는다.
- 기존 v1~v14 단일 프로필이 1번 슬롯에서 손실 없이 복구된다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 관련 브라우저 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add three character slots`

### P48 — 축소 지형 미니맵과 HUD 창 버튼 정렬

상태: 구현 완료 — 2026-07-18

목표: 좌상단 미니맵에 현재 맵의 실제 이동 지형과 플레이어·NPC 위치를 축소 표시하고,
미니맵·퀘스트·조작 안내의 접기/펼치기 버튼을 제목 영역 중심선에 정확히 정렬한다.

구현 결과:

- `map-definitions.ts`의 월드 크기와 발판·줄 좌표를 `hud-layout-rules.ts`의 비율 유지
  투영 규칙으로 288×116 지도 영역 안에 축소한다. 별도 배경 이미지나 신규 래스터 에셋은
  사용하지 않는다.
- 일반 지면과 단방향 발판은 두께·색을 구분하고 줄은 세로선으로 표시한다. 플레이어는
  흰 테두리의 노란 마커로 이동을 실시간 반영하며 현재 맵 NPC는 초록 마커로 표시한다.
- 미니맵을 제목·지역 정보·축소 지도 영역으로 분리하고 현재 맵 이름, 권장 레벨, 플레이어와
  NPC 범례를 겹침 없이 배치했다. 접을 때 지도·정보·마커는 숨기고 제목 영역만 유지한다.
- 세 HUD 창의 `−/+` DOM 버튼은 `translateY(-50%)`와 상태별 제목 중심 좌표를 사용해
  펼침·접힘 모두 수직 중앙 정렬한다. 접힌 패널의 별도 안전 여백과 실제 텍스트 bounds도
  같은 레이아웃 규칙으로 검사한다.
- 37파일·273개 단위 테스트, Chromium·Firefox·WebKit HUD E2E 6/6, 타입체크·프로덕션
  빌드와 79파일·7,999,532 bytes `dist/` 감사를 통과했다.

완료 조건:

- 모든 맵의 발판·줄이 미니맵 영역 안에 비율을 유지해 표시된다.
- 플레이어 마커가 이동을 따라가고 현재 맵 NPC 수와 위치가 초록 마커로 표시된다.
- 미니맵 접기/펼치기에서 정보·지도·마커 가시성이 함께 전환된다.
- 미니맵·퀘스트·조작 안내 버튼 중심이 펼침·접힘 제목 영역의 수직 중심선과 일치한다.
- 타입체크, 단위 테스트, 프로덕션 빌드, 관련 3엔진 E2E와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add projected minimap markers`

### P49 — 미니맵 포탈 표식과 포탈 에너지 애니메이션

상태: 구현 완료 — 2026-07-18

목표: 모든 포탈 위치를 미니맵에서 즉시 찾을 수 있게 하고, 정적인 포탈 문틀에 참고 이미지의
차원 에너지 인상을 독자적으로 재해석한 반복 애니메이션을 더한다.

구현 결과:

- `map-definitions.ts`의 포탈 좌표를 기존 미니맵 투영 규칙으로 표시하고, 플레이어·NPC와
  구분되는 청록색 이중 마커와 `포탈` 범례를 제목 영역에 추가했다.
- built-in imagegen으로 유백색·빙청색·연두색 수평 마법진 원본을 생성해
  `assets/maps/source/portal-ground-fx-v1-source.png`에 보존했다. 사용자 제공 포탈 캡처는
  에너지 색감·광도 참고로만 사용하고 문틀·문자·장식은 복제하지 않았다.
- 224×112 런타임 프레임을 기존 C 오브젝트 아틀라스의 빈 셀에 합친 v2로 교체했다. 같은
  프레임을 문틀 뒤 세로 에너지·바닥 광륜·테마색 잔광에 재사용해 파일 수와 8MB 예산을 유지했다.
- 포탈마다 생성형 레이어 3개와 고정 상승 파편 6개를 서로 다른 호흡 주기·ADD 블렌드로
  애니메이션하고, 효과 객체와 반복 트윈을 `mapObjects` 정리 경로에 묶었다.
- 38파일·275개 단위 테스트, Chromium·Firefox·WebKit HUD·포탈 E2E 6/6, 타입체크·
  프로덕션 빌드, 79파일·7,999,680 bytes `dist/` 감사를 통과했다. 1280×720 인앱 브라우저에서
  두 시점의 포탈 변화, 미니맵 포탈 3개, HUD bounds 위반 0건과 콘솔 오류 0건을 확인했다.

완료 조건:

- 모든 맵에서 현재 포탈 수와 위치가 미니맵에 표시되고 접힘 상태에서는 지도와 함께 숨겨진다.
- 포탈 내부·바닥·상승 파편이 정적 문틀과 분리된 반복 애니메이션으로 보인다.
- 포탈 이동과 Scene 종료 뒤 이전 효과 객체·트윈이 남지 않고 객체 수가 안정적으로 복구된다.
- 생성 원본은 `dist/`에서 제외되고 타입체크, 단위 테스트, 관련 3엔진 E2E, 빌드와 8MB 감사를 통과한다.

커밋 체크포인트: `feat: animate portal energy and minimap markers`

### 단계 외 후속 변경 — 일반·보스 BGM 교체

상태: 구현 완료 — 2026-07-18

- 사용자 제공 일반곡 MP3를 로그인·캐릭터 화면과 살아 있는 보스가 없는 모든 맵에서 같은
  `GameTheme` 키로 연속 무한 반복한다.
- 중간·상위·최종 보스가 생성되면 `BossTheme`으로 전환하고, 처치 애니메이션·보상 확정 뒤
  일반곡으로 복귀하며 보스가 재생성되면 보스곡을 다시 시작한다.
- 일반곡 0.90~68.06초·보스곡 0~51.99초를 Phaser 사운드 마커로 반복해 MP3 원본을 바꾸지
  않고 앞뒤 무음을 제외한다. 같은 키를 사용하는 화면·일반 맵 전환은 기존 사운드 객체와
  현재 재생 위치를 유지하고 실제 BGM 키가 바뀔 때만 새 곡을 시작한다.
- BGM MP3 2종은 `dist/`에 포함하지만 8,000,000-byte 총량 계산에서는 제외하고,
  JavaScript·gzip 개별 상한은 그대로 유지한다.
- BGM 재생 구간과 같은 키 연속 재생·다른 키 전환을 포함한 40파일·283개 단위 테스트를
  통과했다. 인앱 브라우저에서 첫 입력 직후 일반곡 재생, 로그인→캐릭터 선택 사이 같은 키
  연속 재생과 콘솔 경고·오류 0건을 확인했다.
- 사용자 제공 파일의 원저작자·라이선스는 확인되지 않았으므로 외부 공개·배포 전에 권리자가
  허용한 이용 범위를 확인한다.

### P50 — 개발자 채널 홍보 NPC

상태: 구현 완료 — 2026-07-18

목표: 커닝시티에서 이 게임의 개발자 `일용직 개발자 임상진`을 만나 유튜브 채널 안내를
읽고, 사용자가 직접 `구독하기` 링크를 선택할 수 있게 한다.

구현 결과:

- 커닝시티 초기 동선의 `(650, 620)`에 전용 `gameDeveloper` NPC를 배치했다. 기존 포탈과
  NPC의 105px 상호작용 반경을 침범하지 않고 미니맵의 네 번째 NPC 마커로 표시된다.
- 사용자 지정 채널 프로필을 인물 참고로, 기존 `streetHealer` 시트를 픽셀 밀도·셀 배치
  참고로 사용해 갈색 비니·검은 작업복·노트북 차림의 독자적인 4×4 SD 픽셀 시트를 built-in
  imagegen으로 생성했다. `idle·talk·code·thanks` 네 행과 16개 발/중심 원점을 등록했다.
- 금속 DOM 대화창에 게임 개발자 소개와 “`구독하기`를 눌러주시면 이 게임을 계속 만드는 데
  큰 힘이 됩니다” 문구를 제공한다. 실제 `<a>` 링크는 지정 채널을 `_blank`와
  `noopener noreferrer`로 열며, 클릭 뒤 감사 상태와 `thanks` 동작을 재생한다.
- 링크 클릭만 기록하고 실제 구독 완료는 추정하거나 프로필에 저장하지 않는다. 닫은 뒤 NPC는
  idle로, 게임 입력은 캔버스 포커스로 복구된다.
- 전체 20종·320셀 스프라이트 감사와 39파일·279개 단위 테스트, Chromium·Firefox·WebKit
  개발자 대화 3/3 및 HUD 6/6, 타입체크·프로덕션 빌드를 통과했다. `dist/`는 79파일·
  9,656,635 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값 6,739,763 bytes,
  JavaScript 1,468,261 bytes·gzip 395,380 bytes로 모든 상한을 통과했다.

완료 조건:

- 커닝시티에서 고유 외형과 이름표를 가진 개발자 NPC를 발견하고 `↑`로 대화할 수 있다.
- 대화는 이 게임의 개발자임을 밝히고 구독이 제작에 힘이 된다는 내용을 정확히 전달한다.
- `구독하기`는 접근 가능한 실제 링크로 지정 채널을 안전한 새 탭에서 열며 구독 여부를
  임의로 판정하지 않는다.
- 스프라이트 원본·프롬프트·참고 출처가 문서화되고 생성 원본은 `dist/`에서 제외된다.
- 타입체크, 단위 테스트, 관련 3엔진 E2E, 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add game developer promo npc`

### P51 — 첫 캐릭터 생성 강제·초보자 다음 목표 안내

상태: 구현 완료 — 2026-07-18

목표: 새 계정에 자동 생성된 기본 캐릭터를 두지 않고, 사용자가 첫 캐릭터를
명시적으로 생성한 뒤 퀘스트 HUD에서 초보자의 다음 성장·전직 목표를 계속 확인하게 한다.

구현 결과:

- `LocalProfileStore.loadOrCreate` 자동 생성 경로를 제거했다. 빈·손상 저장소는 프로필과
  활성 슬롯 키를 쓰지 않으며, 생성 폼의 닉네임·주사위 능력치 검증을 통과해야만
  슬롯을 저장한다.
- 첫 로그인은 `저장된 캐릭터가 없습니다.` 안내와 함께 1번 슬롯 생성 화면으로
  진입한다. 프로필 없이 플레이 신으로 진입해도 생성 화면으로 복구해 우회 진입을 막았다.
- `jobQuestTrackerText` 가 초보자의 `Lv.10 달성 → 다크로드 시험 수락 → 초록버섯 5체 처치
→ 완료 보고`를 현재 레벨과 퀘스트 진척에 맞게 표시한다. 로그 이후에는 초보자 안내를 제거한다.
- 40파일·284개 단위 테스트, Chromium·Firefox·WebKit 첫 생성·퀘스트 안내 E2E 3/3,
  타입체크·프로덕션 빌드를 통과했다. `dist/`는 79파일·9,657,758 bytes이며 BGM
  2,916,872 bytes를 제외한 예산 적용값 6,740,886 bytes로 감사를 통과했다.

완료 조건:

- 첫 캐릭터를 생성하기 전에는 세 프로필 키와 활성 슬롯 키가 모두 비어 있다.
- 생성을 완료하기 전에는 플레이에 진입할 수 없고, 정상 생성 뒤에만 캐릭터 선택과 게임 시작이 가능하다.
- 초보자는 퀘스트 알림에서 Lv.10 달성 여부와 1차 전직의 수락·처치·보고 다음 행동을 확인한다.
- 단위 테스트, 관련 3엔진 E2E, 타입체크, 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: guide first character creation and beginner goals`

### P52 — 전 차수 전직·레벨별 보스 다음 목표 안내

상태: 구현 완료 — 2026-07-18

목표: 초보자 다음에도 목표가 끊기지 않도록 2·3·4차 전직을 연결하고,
현재 레벨과 원정 진척에 맞는 다음 보스를 퀘스트 HUD에 함께 표시한다.

구현 결과:

- `jobQuestTrackerText`를 로그·어쏌신·허밋으로 확장했다. 각 직업은 Lv.30·60·120 미만에서
  현재 레벨·목표 레벨과 초록버섯굴·수정 개미굴·시계태엽 탑·가라앉은 산호 신전·잿불
  광산 중 레벨 구간에 맞는 추천 사냥터를 표시한다.
- 필요 레벨을 달성하면 다크로드 시험 수락, 그림자 파수꾼·심연의 골렘 처치 진척,
  완료 보고로 문구를 자동 전환하고 호카게 달성 뒤에는 전직 목표를 종료한다.
- `boss-progression-guide.ts`를 추가해 `DUNGEON_BOSS_QUEST`의 기존 레벨·장소·보스 정의를
  재사용한다. Lv.100 미만에서는 숨기고, Lv.100 세라 원정 수락과 이그니카르,
  Lv.140 루나시온, Lv.200 원펀맨, 세라 완료 보고를 원정 진척 순서대로 표시한다.
- 보스 안내는 한 줄로 축약해 전직 안내 2줄과 동시에 표시되어도 68px 퀘스트 본문
  경계를 유지한다. 접힘 상태와 캔버스 `aria-label`에도 같은 목표를 반영한다.
- 41파일·296개 단위 테스트, Chromium·Firefox·WebKit 전직·보스 동시 HUD 경계 E2E 3/3,
  타입체크·프로덕션 빌드를 통과했다. `dist/`는 79파일·9,659,916 bytes이며 BGM
  2,916,872 bytes를 제외한 예산 적용값 6,743,044 bytes로 감사를 통과했다.

완료 조건:

- Lv.30·60·120 미만은 다음 전직 레벨과 추천 사냥터, 도달 후는 시험 수락·처치·보고를 표시한다.
- 보스 안내는 Lv.100부터만 나타나며, 현재 원정 단계의 권장 레벨 달성 또는 처치·보고를 안내한다.
- 전직과 보스 목표가 함께 표시되어도 HUD 텍스트 안전 여백 위반이 0건이다.
- 단위 테스트, 관련 3엔진 E2E, 타입체크, 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: guide later advancements and level bosses`

### P53 — 전체 기능 명세·재현용 제작 프롬프트

상태: 구현 완료 — 2026-07-18

목표: 현재 게임을 처음 접하는 사람도 구현 순서와 최종 규칙을 잃지 않고 새 저장소에서 따라 만들 수
있도록 전체 제품 명세, 에셋·BGM 제작법, 검증 기준과 실행 가능한 AI 코딩 프롬프트를 한 문서로 제공한다.

구현 결과:

- `PROMPT.md`에 PC 지원 범위, Scene 흐름, 캐릭터 생성·3슬롯 v14 저장, 11개 맵·이동·충돌,
  전투·성장·전직·15개 스킬, 몬스터·보스·퀘스트·경제, HUD·DOM 접근성과 에셋·릴리스 경계를 통합했다.
- 과거 단계 기록과 충돌하기 쉬운 최종 규칙인 점프 전용 카메라 제거, 사다리 미사용, Lv.200·최종보스
  HP 80,000,000/방어력 400, 원정 Lv.100~200, 첫 캐릭터 자동 생성 금지와 프로필 v14를 명시했다.
- 직접 작곡과 Suno Custom Instrumental 두 BGM 제작 경로, 독자적인 일반/보스곡 예시 프롬프트,
  생성 당시 권리·플랜·약관 기록, 48kHz stereo 출력, 루프 구간 측정, 일반↔보스 전환과 오디오 QA를 정리했다.
- 문서 전체와 함께 쓰는 마스터 프롬프트, P0~P9 단계별 복사 프롬프트, 16단계 최종 인수 시나리오,
  자동·수동 릴리스 체크리스트와 흔한 실패 방지법을 제공했다.

완료 조건:

- 한 문서만으로 지원/제외 범위, 핵심 플레이 루프, 데이터 수치, 단일 기준 모듈과 완료 기준을 찾을 수 있다.
- BGM을 직접 제작하거나 Suno로 생성한 뒤 권리 확인, 원본 보존, 루프 편집, 런타임 삽입과 검증까지 수행할 수 있다.
- 마스터 또는 단계별 프롬프트를 복사해 에셋 계약→순수 규칙→런타임→검증→문서·커밋 순서로 구현할 수 있다.
- `AGENTS.md`, `README.md`, `ROADMAP.md`가 `PROMPT.md`를 공식 재현 문서로 연결한다.

커밋 체크포인트: `docs: add complete game recreation prompt`

### P54 — 공개 문서·스크린샷·저장소 이력 정리

상태: 구현 완료 — 2026-07-18

목표: 외부 레퍼런스 파일과 과거 공개 이력을 제거하고, 누구나 현재 상태와 재현 문서를 바로
이해할 수 있는 공개 저장소를 만든다.

구현 결과:

- 외부 레퍼런스 이미지 14종과 문서의 삭제된 경로·파일명 언급을 제거했다.
- `README.md`의 기존 내용을 모두 걷어내고 프로젝트 소개, 실행·조작·플레이 흐름,
  `PROMPT.md` 기반 재현 순서, 검증·에셋 정책으로 새로 구성했다.
- 캐릭터 선택, 커닝시티, 초록버섯굴, 무한의 결투장 원펀맨의 실제 1280×720 실행 화면을
  새로 캡처해 `docs/screenshots/`와 README에 추가했다.
- 공개 `main`을 현재 정리된 트리 하나만 가리키는 단일 루트 커밋으로 교체했다.

완료 조건:

- Git 트리와 문서에서 삭제된 레퍼런스 이미지 경로가 발견되지 않는다.
- README의 네 스크린샷이 실제 게임 실행 화면이며 모두 1280×720이다.
- `main`의 도달 가능한 커밋 수가 1이고 원격 `main`이 같은 루트 커밋을 가리킨다.
- 사용자 작업 중이던 비문서 코드 변경은 새 공개 스냅샷에 포함되지 않고 로컬에 보존된다.

커밋 체크포인트: `chore: publish sanitized source snapshot`

### P55 — 인벤토리 소모품 사용

상태: 구현 완료 — 2026-07-18

목표: 획득한 소모품을 상점으로 돌아가지 않고 `I` 인벤토리에서 직접 사용하고,
효과·남은 수량·성장 상태를 현재 캐릭터 슬롯에 즉시 반영한다.

구현 결과:

- 인벤토리 상세 패널에 회복 물약과 경험의 서 전용 `사용하기` DOM 버튼을 추가했다.
  슬롯을 마우스나 키보드로 선택하면 사용 버튼으로 포커스를 옮기고, 사용 뒤 마지막 수량이
  사라져도 남은 선택 항목과 포커스를 복구한다.
- `inventory-item-rules.ts`는 회복 물약 1개를 소비해 HP·MP를 각각 최대치의 50% 회복하고,
  최대치를 넘기지 않으며 둘 다 가득 찬 상태와 미보유 상태에서는 소비하지 않는다.
- 경험의 서는 기존 `shop-rules.ts`의 불변 성장 규칙을 재사용해 현재 EXP 보존, 최대 10레벨,
  Lv.200 미소비, AP·SP·HP/MP·자동분배와 레벨업 연출을 동일하게 적용한다.
- 재료·보스 재료와 표창에는 사용 버튼을 표시하지 않는다. 성공 결과는 프로필 저장, HUD,
  인벤토리 수량, 회복 SFX·시스템 메시지와 결정적 상태 표식에 즉시 반영한다.
- 42파일·300개 단위 테스트, Chromium·Firefox·WebKit 소모품 사용 E2E 3/3과 4개 지원
  PC 창 인벤토리 경계 E2E 3/3, 타입체크·프로덕션 빌드를 통과했다. `dist/`는 79파일·
  9,661,459 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값 6,744,587 bytes,
  JavaScript 1,474,012 bytes·gzip 397,079 bytes로 감사를 통과했다.

완료 조건:

- 회복 물약은 HP·MP가 부족할 때만 1개를 소비하고 각각 최대치의 50%를 상한 안에서 회복한다.
- 경험의 서는 인벤토리에서도 기존 성장 경계와 레벨업 연출을 그대로 사용한다.
- 사용 직후 아이템 수량·슬롯 수·HP/MP·EXP·레벨이 갱신되고 새로고침 뒤 복구된다.
- 재료와 표창은 소비할 수 없고, 키보드 사용·닫기 뒤 캔버스 포커스와 800×600 경계를 유지한다.
- 타입체크, 단위 테스트, 관련 3엔진 E2E, 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: use consumables from inventory`

### P56 — 전투 피드백·시네마틱 투사체 안정화

상태: 구현 완료 — 2026-07-18

목표: 크리티컬과 오버킬 피해를 더 명확하게 전달하고, 호카게 시네마틱과 레벨업 연출이
투사체 수명·조작 흐름을 끊지 않도록 전투 피드백과 이동 경계를 정리한다.

구현 결과:

- 데미지 숫자 아틀라스를 10열×4행으로 확장해 밝은 아이보리 숫자와 분홍 그림자의 크리티컬
  전용 팔레트를 추가하고, 크리티컬 숫자는 일반 피해보다 크게 시작해 더 높고 오래 떠오르게 했다.
- 몬스터 방어 적용 피해와 실제 HP 차감량을 분리해 마지막 타격에서도 남은 HP로 잘린 값이 아닌
  원래 계산된 오버킬 피해를 표시한다. 결정적 E2E 상태에는 숫자 값과 팔레트를 함께 노출한다.
- 구미호 변신 할퀴기는 예리한 시야가 적용된 기본 표창의 수평 도달 거리와 같은 탐색 범위를 사용한다.
- 플레이어·보스 투사체의 수명을 절대 시각 대신 활성 시뮬레이션 누적 시간으로 관리해 호카게
  시네마틱 동안 정지하고, 종료 뒤 남은 수명과 다중 투사체를 그대로 이어 간다.
- 전직 연출은 기존 1.4초 입력 잠금을 유지하되 레벨업 연출은 이동·전투를 막지 않도록 효과 지속
  시간과 입력 잠금 시간을 분리했다.
- 초록버섯굴의 수정 개미굴 방향 상단 포탈 아래에 두 번의 쉬운 점프로 오를 수 있는 보조 발판을
  추가하고 충돌 데이터와 생성 SVG 전경을 같은 좌표로 갱신했다.
- 42파일·306개 단위 테스트, Chromium·Firefox·WebKit 레벨업·크리티컬·호카게 시네마틱
  E2E 9/9, 타입체크·프로덕션 빌드를 통과했다. `dist/`는 79파일·9,675,515 bytes이며 BGM
  2,916,872 bytes를 제외한 예산 적용값 6,758,643 bytes, JavaScript 1,475,606 bytes·gzip
  397,594 bytes로 감사를 통과했다.

완료 조건:

- 모든 크리티컬 공격은 전용 팔레트와 강조된 크기·이동·지속 시간으로 표시된다.
- 마지막 타격의 숫자는 몬스터 잔여 HP가 아니라 방어 적용 뒤 계산된 전체 피해량을 표시한다.
- 호카게 시네마틱 중 투사체 활성 수명이 증가하지 않고 반복 시전된 다중 투사체가 정상 복귀한다.
- 레벨업 연출 중 플레이어 입력은 유지되며 전직 연출의 입력 잠금은 기존 경계를 지킨다.
- 초록버섯굴 보조 발판이 지면과 포탈 발판 사이의 점프 높이·수평 겹침 조건을 충족한다.
- 타입체크, 단위 테스트, 관련 3엔진 E2E, 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: refine combat feedback and timing`

### P57 — 구미호 변신 유지비·전투·이동 강화

상태: 구현 완료 — 2026-07-18

목표: 구미호 변신을 지속 자원을 소비하는 강화 상태로 만들고, 변신 전용 기본 공격이 현재
장착 무기와 성장 스탯을 반영해 일반공격보다 강하면서 공격·이동 체감도 분명히 빨라지게 한다.

구현 결과:

- 변신 활성화 1초 뒤부터 매초 최대 MP의 1%를 정수 올림해 소모한다. 변신 중 자연 MP 회복은
  정지하고 MP가 0이 되면 오라·입력 전환·이동 보너스를 함께 자동 해제한다.
- 구미호 할퀴기의 공격 애니메이션 잠금을 200ms에서 100ms로, 타격 릴리스 지연을 90ms에서
  45ms로 줄여 공격속도를 정확히 2배로 높였다.
- 할퀴기는 차크라 계열을 유지해 치명 투척을 받지 않지만 기본 표창과 같은 LUK/DEX 무기 보너스와
  장착 표창 피해 배율을 적용한다. 1~6등급 모두 같은 캐릭터의 비치명 일반공격보다 강하다.
- 변신 중 수평 물리 최고속도와 줄 좌우 이탈 속도를 260에서 312로 20% 높이고, 보행 모션도
  같은 비율로 빨라진다. 수동 해제와 MP 고갈은 같은 setter로 260을 즉시 복구한다.
- 스킬 상세에는 초당 유지비·2배 공격속도·이동속도 +20%·장착 표창 보정을 표시한다. 신규
  그래픽·오디오 에셋과 프로필 스키마 변경은 없다.
- 42파일·309개 단위 테스트, Chromium·Firefox·WebKit 구미호 집중 E2E 3/3, 타입체크와
  프로덕션 빌드를 통과했다. `dist/`는 79파일·9,678,622 bytes이며 BGM 2,916,872 bytes를
  제외한 예산 적용값 6,761,750 bytes, JavaScript 1,478,713 bytes·gzip 398,431 bytes로
  감사를 통과했다.

완료 조건:

- 변신 활성 중 경과한 매 1초마다 최대 MP 1%가 빠지고 자연 MP 회복은 적용되지 않으며,
  MP 0에서 변신과 모든 속도·오라 보너스가 해제된다.
- 할퀴기는 100ms 주기·45ms 타격 지연을 사용하고 120ms 뒤 두 번째 입력을 받을 수 있다.
- 할퀴기는 LUK/DEX와 현재 장착 표창 배율을 반영하면서 다른 차크라 스킬에는 표창 보정을
  누출하지 않고, 모든 표창 등급에서 일반 기본공격보다 강하다.
- 변신 중 수평 최고속도는 312, 해제 뒤에는 260이며 줄 이탈과 보행 애니메이션도 같은 상태를 따른다.
- 타입체크, 309개 단위 테스트, 관련 3엔진 E2E, 빌드와 `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: empower nine tails transformation`

### P58 — 두아 펫 등록·추적·자동 회수

상태: 구현 완료 — 2026-07-18

목표: 커닝시티에 혼자 앉아 있는 웰시코기 두아에게 상점의 50,000 메소 멍푸치노를 주면
캐릭터별 펫으로 등록되고, 플레이어를 따라다니며 착지한 드롭을 대신 회수하게 한다.

구현 결과:

- 서적상 레오의 상점에 정확히 50,000 메소인 `멍푸치노`를 추가했다. 보유 중이거나 이미 등록한
  슬롯에서는 중복 구매를 막고, 커닝시티 두아 대화에서 명시적으로 선물할 때만 1개를 소비한다.
- 등록 뒤 NPC 두아를 제거하고 물리 발판과 충돌하는 `DuaPet`을 생성한다. 펫은 플레이어 뒤의
  추적 지점, 정지 구간, 달리기 속도, 낙오 순간이동을 순수 규칙으로 계산하고 맵 전환 뒤 재생성된다.
- 바닥에 착지한 드롭 가운데 탐지 반경 안의 최근접 대상을 선택해 fetch 동작으로 접근한다.
  수동 `Z`와 같은 보상 확정 함수를 사용하고 드롭을 먼저 분리해 메소·아이템 중복 지급을 막았다.
- 프로필 스키마를 v15로 올려 `pets.dua.registered`를 슬롯별 저장한다. v1~v14는 기존 모든 값을
  보존하면서 두아 미등록 기본값을 받으며, 손상된 펫 상태도 안전하게 정규화한다.
- `apps/utilities/token-dog`의 독자 웰시코기 실루엣과 사용자 제공 실제 사진 2장의 특징을 언어화해
  두아 v2 4×4 시트를 built-in imagegen으로 제작했다. 참고사진은 저장소·`dist/`에 포함하지 않고,
  날렵한 쐐기형 얼굴·긴 흰 주둥이·아몬드형 눈매·연한 황금빛 털을 런타임 픽셀 아트로 반영했다.
- 43파일·322개 단위 테스트, Chromium·Firefox·WebKit 펫 E2E 6/6과 지원 PC 뷰포트 3/3,
  타입체크·프로덕션 빌드를 통과했다. `dist/`는 80파일·9,762,304 bytes이며 BGM 2,916,872
  bytes를 제외한 예산 적용값 6,845,432 bytes, JavaScript 1,490,493 bytes·gzip 401,504
  bytes로 감사를 통과했다.

완료 조건:

- 멍푸치노는 정확히 50,000 메소이며 구매·보유·소비·등록 상태가 현재 캐릭터 슬롯에 저장된다.
- 등록 전 두아는 커닝시티 NPC로 앉아 있고, 등록 뒤 NPC가 사라진 자리에 펫이 생성되어 맵 이동과
  새로고침 뒤에도 플레이어를 따라온다.
- 두아는 착지한 가까운 메소·아이템을 자동 회수하며 공중 드롭과 탐지 반경 밖 드롭은 건드리지 않고,
  수동 회수와 경합해도 보상을 한 번만 지급한다.
- 21종·336셀 스프라이트 감사, 322개 단위 테스트, 관련 3엔진 E2E, 타입체크·빌드·`dist/` 감사를
  통과하고 1280×720 실행 화면에서 얼굴·접지·이름표·추적 간격을 확인한다.

커밋 체크포인트: `feat: add Dua pet and automatic loot`

### P59 — 원펀맨 넉백 면역·주먹 충격파 2페이즈

상태: 구현 완료 — 2026-07-18

목표: 원펀맨이 플레이어 공격에 밀리거나 피격 경직으로 패턴이 끊기지 않게 하고, 표창처럼
보이던 기존 투사체를 주먹 동작과 연결된 전용 충격파로 교체한다. 체력 50%를 경계로 서로 다른
두 공격 패턴을 사용한다.

구현 결과:

- `MONSTER_CATALOG.onePunchMan.knockbackImmune`을 단일 기준으로 추가했다. `PatrolMonster`는
  해당 보스의 Arcade body를 밀 수 없게 설정하고, 비치명 피해를 받아도 `hurt/recoil` 애니메이션,
  이동 정지와 원거리 공격 잠금을 적용하지 않는다. 피격 섬광과 피해·사망 처리는 유지한다.
- 1페이즈 `보통 펀치 충격파`는 체력 50% 초과에서 청백색 단일 파동을 발사한다. 2페이즈
  `진심 펀치 삼중 충격파`는 체력 50% 이하에서 즉시 선택되며, 조준 방향을 중심으로 총 24도에
  걸친 금백색 3방향 파동을 더 빠른 주기로 발사한다. 두 패턴의 기존 즉사 판정은 유지한다.
- `one-punch-shockwave-v1.svg`를 프로젝트 자체 192×96 투명 벡터로 제작해 손앞 생성 위치,
  진행 각도, 142×50 충돌 영역을 연결했다. 원펀맨 경로에서는 `combatEffects.throwingStar`를
  더 이상 재생하지 않는다.
- HUD 보스 제목과 접근성 테스트 상태에 현재 1/2페이즈를 노출한다. 최초 2페이즈 진입 때 금빛
  링·카메라 진동·시스템 메시지를 한 번 표시하고, 공격 ID로 보통/진심 충격파를 구분한다.
- 324개 단위 테스트에서 50% 정확 경계, 3방향 속도·각도, 전용 SVG 번들, 넉백 면역 카탈로그를
  검증했다. Chromium·Firefox·WebKit 전용 E2E 3/3에서 전용 충격파 생성·이동·명중·즉사와
  콘솔 오류 부재를 확인했고 타입체크·프로덕션 빌드도 통과했다. 최종 `dist/`는 80파일·
  9,766,093 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값 6,849,221 bytes,
  JavaScript 1,494,035 bytes·gzip 402,697 bytes로 감사를 통과했다.

완료 조건:

- 원펀맨은 플레이어 공격에 밀리지 않고 살아 있는 동안 피격 리코일로 이동·주먹 공격이 끊기지 않는다.
- 체력이 최대치의 50%를 초과하면 단일 보통 펀치 충격파, 정확히 50% 이하이면 3방향 진심 펀치
  충격파를 사용하며 HUD가 같은 페이즈를 표시한다.
- 원펀맨의 두 원거리 패턴은 전용 충격파 SVG만 사용하고 표창 프레임을 생성하지 않는다.
- 단위 테스트, 3엔진 전용 E2E, 타입체크·빌드와 BGM 제외 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add One Punch Man phase attacks`

### P60 — 두아 상단 발판 추적 강화

상태: 구현 완료 — 2026-07-18

목표: 두아의 낮은 점프력과 캐릭터보다 느린 이동속도 때문에 높은 발판에서 뒤처지는 문제를
해결하고, 일반 이동·변신 이동·직상단 점프·장애물 상황에서 안정적으로 플레이어를 따라가게 한다.

구현 결과:

- 월드 중력 1,250에서 두아 점프 속도 520은 이론상 약 108px만 오를 수 있어 커닝시티와
  초록버섯굴의 145~150px 첫 발판에 닿지 못했다. 점프 속도를 700으로 높여 약 196px 상승 여유를
  확보하고, 플레이어보다 32px 이상 낮으면 일찍 점프하도록 기준을 48→32px로 조정했다.
- 수평 최고속도를 230에서 360으로 높여 기본 캐릭터 260과 구미호 변신 312보다 빠르게 따라잡게
  했다. 가속도도 1,650→2,100으로 올려 방향 전환과 낙오 복구가 지연되지 않게 했다.
- 수평 정지 구간 안에서도 플레이어가 바로 위에 있으면 제자리 점프하고, 진행 방향의 Arcade body가
  벽에 막힌 상태면 장애물 점프를 선택한다. 직접 오를 수 없는 높이는 수직 차 220px에서 기존
  순간이동 복구 경로를 사용한다.
- 펫 E2E에 커닝시티 지면에서 150px 상단 발판으로 캐릭터와 두아가 함께 올라가고 다시 같은 높이에
  정착하는 시나리오를 추가했다. 자동 줍기 검증은 빠른 회수 뒤 몬스터 재생성 이벤트가 마지막
  이벤트를 덮지 않도록 회수 발생 시점에 바로 판정한다.
- 전체 43파일·324개 단위 테스트, Chromium·Firefox·WebKit 펫 E2E 9/9, 타입체크와 프로덕션
  빌드를 통과했다. `dist/`는 80파일·9,766,397 bytes이며 BGM 2,916,872 bytes를 제외한 예산
  적용값 6,849,525 bytes, JavaScript 1,494,339 bytes·gzip 402,803 bytes로 감사를 통과했다.

완료 조건:

- 두아는 캐릭터의 기본·변신 최고속도보다 빠르게 따라잡고 평지에서 추적 간격으로 복귀한다.
- 두아는 145~150px 상단 단방향 발판에 직접 점프해 올라가며, 플레이어가 바로 위에 있거나 진행
  방향이 막힌 경우에도 점프 판정을 놓치지 않는다.
- 등록·맵 전환·자동 회수 동작에 회귀가 없고 3엔진 펫 E2E, 단위 테스트, 타입체크·빌드와 BGM 제외
  8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: improve Dua pet platform following`

### P61 — 원펀맨 상층 발판 투사체 피격

상태: 구현 완료 — 2026-07-18

목표: 무한의 결투장 상층 발판에서 수평으로 공격할 때 플레이어 투사체가 지상의 원펀맨 물리
바디 위를 지나가며 피해를 주지 못하는 문제를 해결한다.

구현 결과:

- 기존 Arcade 물리 바디 직접 겹침을 우선 적용하고, 직접 겹치지 않은 투사체에는 원펀맨 전용으로
  실제 바디 위쪽 132px까지 전투 피격 영역을 확장했다. 상층 발판의 투사체가 원펀맨의 수평 범위에
  도달하면 기존 피해·방어·페이즈 처리 경로를 그대로 사용한다.
- 확장 판정은 `monster-combat-rules.ts`의 순수 함수로 분리했다. 원펀맨 이외의 보스와 일반
  몬스터, 수평으로 빗나간 투사체, 확장 높이보다 위를 지나는 투사체에는 적용하지 않는다.
- 실제 결투장 상층 투사체와 원펀맨 바디 좌표를 반영한 단위 테스트를 추가했다. 전체 43파일·
  325개 단위 테스트, Chromium·Firefox·WebKit 기존 원펀맨 집중 E2E 3/3, 타입체크와 프로덕션
  빌드를 통과했다. `dist/`는 80파일·9,766,664 bytes이며 BGM 2,916,872 bytes를 제외한 예산
  적용값 6,849,792 bytes, JavaScript 1,494,606 bytes·gzip 402,894 bytes로 감사를 통과했다.

완료 조건:

- 무한의 결투장 상층 발판에서 원펀맨 방향으로 날린 투사체가 수평 범위에 닿으면 피해를 준다.
- 다른 몬스터의 피격 영역과 원펀맨의 접촉 판정은 넓어지지 않는다.
- 단위 테스트, 원펀맨 3엔진 E2E, 타입체크·빌드와 BGM 제외 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: allow upper-floor attacks on One Punch Man`

### P62 — 두아 달리기 발 연결 개선

상태: 구현 완료 — 2026-07-18

목표: 두아가 달릴 때 비슷한 공중 자세가 반복되어 발이 튀거나 뒤틀려 보이던 문제를 해결하고,
짧은 코기 다리가 지면을 딛고 자연스럽게 다음 자세로 이어지는 4프레임 주기를 만든다.

구현 결과:

- built-in imagegen 정밀 편집으로 v2 시트의 2행만 `대각 앞·뒷발 접지 → 다리 회수 → 반대
대각발 접지 → 반대 회수` 순서로 다시 제작했다. 가까운 다리와 어두운 원거리 다리를 분리하고,
  머리·몸통 중심의 상하 흔들림은 최소화했다.
- 생성 원본은 `dua-pet-v3-chroma.png`, 투명화·최근접 축소 뒤 v2 런타임의 2행에만 합성한 결과는
  `dua-pet-v3.png`로 등록했다. 1행 sit/idle, 3행 fetch, 4행 happy의 12프레임은 v2 대비 실제
  알파 포함 픽셀 차이 0이다.
- 달리기 네 프레임의 불투명 픽셀 중심과 공통 발 기준선을 다시 측정해 매니페스트 `frameOrigins`를
  각각 `(0.52262025, 0.84375)`, `(0.49876917, 0.8515625)`,
  `(0.47797329, 0.8359375)`, `(0.46251711, 0.859375)`로 보정했다.
- 분리 작업 트리에서 전체 43파일·325개 단위 테스트, 타입체크와 프로덕션 빌드를 통과했다.
  Chromium·Firefox·WebKit 펫 E2E는 구매·등록·재접속, 상단 발판 추적과 자동 회수 9/9를
  통과했다. `dist/`는 80파일·9,907,693 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  6,990,821 bytes, JavaScript 1,494,609 bytes·gzip 402,896 bytes로 감사를 통과했다.

완료 조건:

- 달리기 4프레임이 서로 반대인 두 접지 자세와 각 회수 자세를 번갈아 보여 발이 자연스럽게
  연결되고, 모든 발이 동시에 공중에 뜨거나 같은 접지 자세가 반복되지 않는다.
- 두아의 얼굴·털색·체형과 run 이외 12프레임은 바뀌지 않으며 프레임별 시각 중심과 발 기준선이
  월드 좌표에서 안정적으로 유지된다.
- 단위 테스트, 3엔진 펫 E2E, 타입체크·빌드와 BGM 제외 8MB `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: smooth Dua pet run animation`

### P63 — 부활의 부적 1회 즉시 부활

상태: 구현 완료 — 2026-07-18

목표: 고레벨 보스 전투에서 한 번의 사망을 막을 수 있는 고가 소모품을 추가하되, 반복 구매로
부활을 쌓을 수 없도록 캐릭터 슬롯별 최대 보유 수량을 1개로 제한한다.

구현 결과:

- 서적상 레오의 상점에 `부활의 부적`을 정확히 1,000,000 메소로 추가했다. 보유 수량이 1개이면
  구매 버튼을 `보유 중`으로 잠그고, 순수 구매 규칙도 `maximum-owned`로 중복 구매를 거부한다.
- 저장소 정규화는 부활의 부적 수량을 0~1로 제한한다. 인벤토리에서는 CSS 기반 부적 아이콘,
  자동 사용 설명과 보유 수량을 표시하되 직접 사용하는 버튼은 제공하지 않는다.
- 플레이어 HP가 0이 되는 피해를 받은 직후 부적을 자동 소비하고 같은 맵·위치에서 HP와 MP를
  최대치로 회복한다. 기존 피격 연출·무적 시간은 유지하며, 부적이 없거나 이미 소비된 다음
  사망은 기존 720ms 뒤 커닝시티 귀환 경로를 그대로 사용한다.
- 별도 래스터 에셋을 추가하지 않고 상점·인벤토리 부적 표식은 CSS로 구성했다. 분리 작업 트리에서
  전체 44파일·330개 단위 테스트, Chromium·Firefox·WebKit 구매·부활 E2E 6/6, 타입체크와
  프로덕션 빌드를 통과했다. `dist/`는 80파일·9,911,549 bytes이며 BGM 2,916,872 bytes를 제외한
  예산 적용값 6,994,677 bytes, JavaScript 1,497,843 bytes·gzip 403,661 bytes로 감사를 통과했다.

완료 조건:

- 부활의 부적은 정확히 1,000,000 메소이며 캐릭터 슬롯마다 최대 1개만 구매·저장된다.
- 사망하면 부적 1개를 자동 소비해 맵과 위치를 유지하고 HP·MP를 완전 회복한다.
- 소비 뒤 다음 사망은 기존 커닝시티 귀환을 사용하며, 부적은 수동으로 소비할 수 없다.
- 단위 테스트, Chromium·Firefox·WebKit 구매·부활 E2E, 타입체크·빌드와 BGM 제외 8MB
  `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: add single-use Revival Charm`

### P64 — 유기 생물형 던전 몬스터 다양화

상태: 구현 완료 — 2026-07-18

목표: 일반 사냥터에서 반복되던 기계·골렘 실루엣을 좀비·동물·나무/식물 계열로 교체해
던전별 인상을 분명히 하되, 기존 레벨 진행·드롭·퀘스트·저장 호환성은 그대로 유지한다.

구현 결과:

- built-in imagegen으로 `plagueZombie`, `moonWolf`, `ancientTreant` 4×4 스프라이트 시트를
  독자 제작했다. 각 시트는 idle·walk·hurt·defeat 4행, 투명 128×128 고정 셀과 개별
  `frameOrigins`를 사용하며 셀 연결·크로마 잔여를 전수 검사한다.
- 수정 개미굴은 `수정굴 늑대`, 시계태엽 탑은 `망각된 기술자 좀비`, 산호 신전은 `산호
맹그로브`, 잿불 광산은 `잿불 광부 좀비`, 달빛 마도서고는 `월광 늑대`를 사용한다. 늑대는
  빠르고 낮은 바디, 나무정령은 가장 느리고 넓은 바디, 두 좀비도 서로 다른 속도의 직립 바디로
  실루엣과 순찰감이 다르다. 전체 몬스터 11종의 이동속도는 모두 고유한 값으로 고정했다.
- 기존 다섯 지역 몬스터 ID와 레벨·HP·경험치·드롭 테이블, 맵별 스폰 수와 위치를 보존해
  프로필·퀘스트·E2E 상태 계약을 깨지 않았다. 그림자 시험 몬스터와 세 보스도 변경하지 않았다.
- 새 매니페스트는 세 시트의 원점 좌표·행별 속도만 담는 `compactMonster` 형식을 런타임 검증
  단계에서 표준 애니메이션으로 확장한다. 최종 번들은 JavaScript 1,499,865 bytes와 BGM 제외
  7,230,219 bytes로 기존 릴리스 상한을 통과했다.
- 전체 44파일·334개 단위 테스트, Chromium·Firefox·WebKit 던전 맵 로드 E2E 3/3,
  타입체크·프로덕션 빌드·`dist/` 감사와 1280×720 좀비·늑대·나무정령 실행 화면을 확인했다.

완료 조건:

- 초록버섯 이후의 일반 사냥 경로가 동물·언데드·식물 계열을 포함하고 기계형 시트를 사용하지 않는다.
- 새 3개 시트의 48프레임이 투명 셀·공통 발 기준선·애니메이션 행 규격을 지킨다.
- 기존 맵·퀘스트·보상 호환성과 3엔진 던전 진입이 유지된다.
- 전체 몬스터 종류가 서로 중복되지 않는 이동속도를 사용한다.
- 단위 테스트, 타입체크·빌드와 BGM 제외 8MB·JavaScript 1.5MB `dist/` 감사를 통과한다.

커밋 체크포인트: `feat: diversify dungeon monsters`

### P65 — ESC 전체 메뉴·스킬 단축키·시네마틱 HUD 정리

상태: 구현 완료 — 2026-07-18

목표: 플레이 중 필요한 화면을 `ESC` 전체 메뉴에 모으고, 액티브 스킬 단축키를 별도 화면에서
자유롭게 배치하며, 사운드 설정과 시네마틱 HUD 조작부의 노출 위치를 정리한다.

구현 결과:

- `ESC`를 누르면 인벤토리·스탯/장비·스킬·스킬 단축키·환경설정을 여는 접근 가능한 전체 메뉴가
  표시된다. 메뉴나 하위 팝업에서 `ESC`를 다시 누르면 닫히고 게임 Canvas 포커스가 복구된다.
- 전용 스킬 단축키 화면은 고정된 `1~0/-` 11개 슬롯과 액티브 스킬 목록을 함께 보여 준다.
  슬롯과 스킬을 차례로 선택하는 배치, 슬롯 드래그 앤 드롭, `Alt+←/→` 교환을 지원하며 결과를
  기존 v15 `skillHotbar`에 즉시 저장해 입력 매핑과 HUD에 반영한다. `K` 스킬 창은 SP 분배에
  집중하고 중복 단축키 편집 UI는 제거했다.
- 상단의 상시 사운드 조작부를 제거하고 전체 음소거·BGM·SFX 조절을 `ESC` 환경설정으로 옮겼다.
  오디오 설정은 기존 캐릭터와 분리된 로컬 저장 및 실시간 적용 계약을 그대로 사용한다.
- 호카게 시네마틱의 `data-hokage-cinematic` 상태가 활성화된 동안 미니맵·퀘스트·조작 안내의
  HUD 접기 버튼을 숨기고, 시네마틱 종료 뒤 다시 표시한다.
- 새 DOM 구조는 정적 HTML 템플릿으로 제공하고 프로덕션 JavaScript는 Terser로 최소화했다.
  전체 44파일·335개 단위 테스트, Chromium·Firefox·WebKit 메뉴·단축키·시네마틱·뷰포트 관련
  E2E 12/12, 타입체크·빌드·`dist/` 감사를 통과했다. 1280×720 인앱 브라우저에서 전체 메뉴,
  단축키와 환경설정의 경계·가독성·콘솔 오류 0건도 확인했다.

완료 조건:

- `ESC` 전체 메뉴에서 주요 플레이 화면과 환경설정에 접근하고 다시 `ESC`로 안전하게 닫는다.
- `1~0/-` 액티브 단축키를 전용 화면에서 자유롭게 교환하고 저장·입력·HUD에 즉시 반영한다.
- 사운드 조작은 게임 상단에 남지 않고 환경설정에서 기존 저장값과 실시간 출력을 제어한다.
- 호카게 시네마틱 중 HUD 접기 버튼이 보이지 않으며 종료 뒤 원래 상태로 복구된다.
- 단위 테스트, 3엔진 관련 E2E, 타입체크·빌드와 BGM 제외 8MB·JavaScript 1.5MB `dist/`
  감사를 통과한다.

커밋 체크포인트: `feat: add ESC game menu and skill shortcuts`

### P66 — 두아 추적 순항·정지 떨림 제거

상태: 구현 완료 — 2026-07-18

목표: 두아가 플레이어 뒤의 추적 간격 20px 안에 들어올 때마다 속도를 즉시 0으로 만들고 대기했다가
다시 출발해, 계속 달리는 플레이어 뒤에서 주기적으로 멈춰 보이던 문제를 해결한다.

구현 결과:

- `duaMovementDecision`에 추적 대상의 수평 속도를 전달한다. 목표 간격 안이어도 플레이어 속도의
  절댓값이 20을 초과하면 정지 판정 대신 같은 방향·속도의 접근 판정을 반환한다.
- 평상시 캐릭터 260과 구미호 변신 312를 그대로 순항 속도로 사용하고, 멀어진 경우에만 기존
  최고속도 360으로 따라잡는다. 방향 앞이 막히면 순항 중에도 기존 장애물 점프를 유지한다.
- 플레이어가 멈추면 두아의 속도를 한 프레임에 0으로 만들지 않고 기존 지면 마찰 1,900으로
  감속한다. 절댓값이 20 이하가 된 뒤에만 속도를 0으로 고정하고 idle 애니메이션으로 전환한다.
- 새 순수 규칙 테스트는 추적 간격 경계의 260 순항, 구미호 312 역방향 순항·장애물 점프,
  임계 속도 20의 idle 전환을 고정한다. 전체 44파일·336개 단위 테스트와 타입체크·Terser
  프로덕션 빌드를 통과했다. Chromium·Firefox·WebKit 펫 E2E는 구매·등록·재접속, 평지 추적,
  150px 상단 발판과 자동 회수 9/9를 통과했다.
- `dist/`는 83파일·10,145,106 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,228,234 bytes, JavaScript 1,486,842 bytes·gzip 403,958 bytes로 감사를 통과했다.

완료 조건:

- 플레이어가 계속 이동하면 두아가 추적 간격 안에서 기본·변신 이동속도를 맞춰 run을 유지한다.
- 플레이어가 멈추면 두아가 지면 마찰로 감속한 뒤 idle로 전환하며 주기적인 정지·재출발이 없다.
- 상단 발판 점프·낙오 순간이동·자동 회수에 회귀가 없고 단위 테스트, 3엔진 펫 E2E,
  타입체크·빌드와 BGM 제외 8MB·JavaScript 1.5MB `dist/` 감사를 통과한다.

커밋 체크포인트: `fix: smooth Dua follow movement`

### P67 — Shift·문자열 스킬 단축키 자유 배치

상태: 구현 완료 — 2026-07-18

목표: 숫자/HUD 슬롯뿐 아니라 왼손으로 접근하기 쉬운 `Shift`, `QWER`, `ASDF`, `ZXCV`에도
액티브 스킬을 자유롭게 배치하고 캐릭터별로 저장한다.

구현 결과:

- 기존 `1~0/-` 11칸은 하단 HUD와 연결된 중복 없는 순열로 유지하고, 별도의 추가 키 13칸은
  같은 스킬의 중복 배치와 빈 슬롯을 허용한다. 기본 별칭은 기존 조작을 보존하는 Shift=럭키세븐,
  Q/W/E/R=그림자 연사/환영 쌍성/심연 폭우/천뢰옥, X/C/V=드레인/어벤저/나선환이며 A/D/F는
  비어 있다.
- `S`와 `Z`의 추가 키가 비어 있으면 각각 기존 스탯/장비와 수동 회수 동작을 사용한다. 스킬을
  배치하면 스킬 입력이 우선하고 `선택한 추가 키 비우기`로 언제든 기본 동작을 복구한다. 요청
  범위 밖의 B/N 구미호 변신·삼인 협공 고정 보조키는 호환성을 위해 유지했다.
- 단축키 화면은 숫자/HUD와 추가 키를 두 영역으로 나누고 클릭 선택·스킬 지정, 같은 영역의
  드래그 교환과 `Alt+←/→`, 추가 키 비우기를 지원한다. 모든 스킬 행에는 숫자와 추가 키의 현재
  배치를 함께 표시하며 13개 추가 키는 800px 폭에서도 수평 스크롤 없이 한 줄에 유지된다.
- 프로필을 v16으로 올리고 `skillHotkeyAliases`를 슬롯별 저장한다. v1~v15는 기존 숫자 순서를
  보존하고 과거 고정 Shift/QWER/XCV를 기본 별칭으로 이관하며, v16의 명시적인 빈 맵과 유효한
  중복 배치를 그대로 복구하고 알 수 없는 키·스킬은 제거한다.
- 전체 44파일·339개 단위 테스트, Chromium·Firefox·WebKit 단축키 입력·저장·4개 PC 뷰포트·
  HUD 경계 E2E 9/9, 타입체크·Terser 빌드·`dist/` 감사를 통과했다. 1280×720 인앱 브라우저에서 숫자
  11칸과 추가 13칸, 빈 S/Z 기본 동작, 제목·닫기 고정과 콘솔 warning/error 0건을 확인했다.

완료 조건:

- `Shift`, `QWER`, `ASDF`, `ZXCV` 13개가 전용 화면에 표시되고 원하는 액티브를 배치·비운다.
- 숫자/HUD 순서와 추가 키 별칭이 서로 독립 저장되며 입력 변경이 즉시 적용된다.
- 빈 S/Z는 기존 스탯/장비·회수를 유지하고 스킬 배치 시에만 해당 스킬을 우선한다.
- v1~v15 마이그레이션, 손상값 정규화, 800×600 경계, 단위·3엔진 관련 E2E와 배포 예산을
  통과한다.

커밋 체크포인트: `feat: expand configurable skill hotkeys`

### P68 — 원펀맨 클리어 픽셀 아트 게임 크레딧

상태: 구현 완료 — 2026-07-18

목표: 원펀맨 처치가 확정되면 독자 픽셀 아트 엔딩 화면과 정확한 제작 크레딧을 보여 주고,
연출 종료 뒤 현재 게임을 계속 플레이할 수 있게 한다.

구현 결과:

- 원펀맨 사망 애니메이션이 끝나 보상·EXP·원정 `turn-in` 진척을 먼저 저장한 뒤 크레딧을 연다.
  다른 몬스터 처치에는 표시하지 않으며 표시 여부를 프로필에 저장하지 않아 원펀맨이 다시
  생성되어 재처치되면 다시 볼 수 있다.
- 18초 롤에는 시스템·던전 디자인·몬스터 디자인·캐릭터 디자인·스킬 디자인·FX·테스트를
  모두 `임상진 with Codex`로, 게임 레퍼런스를 `메이플스토리`로 표시한다. 문구와 길이는
  `src/game/ending/ending-credits-rules.ts`를 단일 기준으로 사용한다.
- built-in imagegen으로 달빛 결투장·여명·독자 닌자와 반려견 실루엣을 사용한 텍스트 없는
  1280×720 픽셀 배경을 만들었다. 생성 원본은 `assets/ui/source/ending-credits-v1-source.png`,
  76,650-byte 런타임 WebP는 `assets/ui/screens/ending-credits-v1.webp`에 보존한다.
- 크레딧은 접근 가능한 DOM `role="dialog"`로 제공하고 우하단 `크레딧 닫고 계속 플레이`
  버튼, `ESC`, 18초 자동 종료를 지원한다. 종료 시 입력 상태와 Canvas 포커스를 복구하며,
  처치 직전 살아 있던 보스 투사체는 크레딧 시작 때 제거해 복귀 직후 피격을 막는다.
- 전체 45파일·342개 단위 테스트, Chromium·Firefox·WebKit 실제 원펀맨 처치·크레딧 문구·
  원정 진척·닫기 후 이동 재개 E2E 3/3, 타입체크·Terser 프로덕션 빌드와 `dist/` 감사를
  통과했다. `dist/`는 84파일·10,234,827 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,317,955 bytes, JavaScript 1,492,509 bytes·gzip 405,507 bytes다.

완료 조건:

- 원펀맨 보상 확정 뒤 요청한 7개 개발 분야와 게임 레퍼런스가 정확한 문구로 표시된다.
- 크레딧 자동 종료·버튼·`ESC` 뒤 게임 입력과 포커스가 복구되어 플레이를 계속할 수 있다.
- 생성 원본은 배포물에서 제외하고 런타임 WebP만 Vite URL import로 번들링한다.
- 단위 테스트, 3엔진 크레딧 E2E, 타입체크·빌드와 BGM 제외 8MB·JavaScript 1.5MB 감사를
  통과한다.

커밋 체크포인트: `feat: add final boss pixel credits`

### P69 — 점프 중 즉시 좌우 방향 전환

상태: 구현 완료 — 2026-07-18

목표: 점프·낙하·공중 공격 중에도 좌우 입력을 즉시 반영해 반대 방향으로 자유롭게 전환한다.

구현 결과:

- `playerAirborneHorizontalVelocity` 순수 규칙은 공중에서 방향 입력이 있으면 기본 260,
  구미호 변신 312의 최고속도를 즉시 해당 부호로 적용하고, 입력이 없으면 현재 수평 관성을
  유지한다.
- 지상 가속·감속 규칙은 유지하고 공중에서만 가속도와 drag를 제거해 방향 전환 지연을 없앴다.
  점프 시작 직후와 기본 공격·스킬 동작 잠금 중에도 같은 공중 조향 경로를 사용한다.
- 단위 테스트는 무입력 관성, 기본 좌→우·우→좌 반전과 변신 최고속도를 고정한다. 실제 E2E는
  지상 우측 이동→점프→좌측 즉시 반전→공중 기본 공격 뒤에도 `-260` 속도와 좌측 이동이
  유지되는지 Chromium·Firefox·WebKit 3/3에서 확인했다.
- 1280×720 실제 커닝시티 점프 화면을 촬영해 당시 README에 반영했다. P79 공개 화면
  정리에서는 달빛 마도서고 사냥 캡처로 교체하고 이 파일은 제거했다.
- 전체 45파일·343개 단위 테스트, 관련 3엔진 E2E 3/3, 타입체크·Terser 프로덕션 빌드와
  `dist/` 감사를 통과했다. `dist/`는 84파일·10,235,213 bytes이며 BGM 2,916,872 bytes를
  제외한 예산 적용값 7,318,341 bytes, JavaScript 1,492,895 bytes·gzip 405,612 bytes다.

완료 조건:

- 공중에서 반대 방향 키를 누르면 감속 구간 없이 즉시 해당 방향 최고속도로 전환된다.
- 방향 키를 놓으면 기존 공중 관성을 유지하고, 공격·스킬 중에도 좌우 입력을 사용할 수 있다.
- 지상 가속·감속, 점프 높이·수직 속도, 구미호 이동속도 배율은 기존 규칙을 유지한다.
- 단위 테스트, Chromium·Firefox·WebKit E2E, 타입체크·빌드·배포 감사와 새 README 캡처를
  통과한다.

커밋 체크포인트: `feat: allow instant airborne direction changes`

### P70 — 원펀맨 2층 구미호 할퀴기 피격

상태: 구현 완료 — 2026-07-18

목표: 무한의 결투장 2층에서 구미호 변신 `Ctrl` 할퀴기의 긴 전방 사거리가 지상의 원펀맨에게
닿아도 높이 차 검사에 막혀 피해를 주지 못하던 회귀를 수정한다.

구현 결과:

- 기존 할퀴기 근접 경로는 캐릭터와 몬스터 중심의 수직 차이가 105px 이내일 때만 명중했다.
  표창과 같은 수평 사거리를 사용하면서도 원펀맨 전용 상단 132px 확장 피격 영역은 검사하지
  않아 2층 높이에서 대상 후보가 제거되는 것이 원인이었다.
- `forwardMeleeCombatBounds`가 캐릭터 전방 18px 후방 여유부터 할퀴기 전체 사거리까지,
  캐릭터 중심 위 50px의 36px 높이 공격 영역을 생성한다. 구미호 할퀴기는 이 bounds가
  원펀맨 실제 바디 또는 상단 132px 확장 영역과 겹칠 때 명중한다.
- 반대 방향을 향한 공격과 상단 확장을 사용하지 않는 다른 몬스터는 명중하지 않으며, 기존
  지상 근접 공격·투사체 피격 경로는 유지한다.
- 순수 규칙 테스트는 결투장 2층·상층, 반대 방향과 다른 몬스터 경계를 고정했다. 실제 E2E는
  구미호 변신 뒤 `player-y < 500` 높이에서 `Ctrl` 할퀴기를 발동해 원펀맨 HP가 감소하는지
  Chromium·Firefox·WebKit 3/3에서 확인했다.
- 전체 45파일·344개 단위 테스트, 타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
  `dist/`는 84파일·10,235,572 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,318,700 bytes, JavaScript 1,493,254 bytes·gzip 405,757 bytes다.

완료 조건:

- 구미호 할퀴기는 결투장 2층·상층에서 전방 사거리가 원펀맨과 겹치면 피해를 준다.
- 반대 방향 공격과 다른 몬스터에는 원펀맨 전용 상단 확장을 적용하지 않는다.
- 단위 테스트, Chromium·Firefox·WebKit 실제 입력 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `fix: let nine-tails claw hit one punch man upstairs`

### P71 — 신규 캐릭터 Lv.9 시작

상태: 구현 완료 — 2026-07-18

목표: 새 캐릭터가 1차 전직 조건을 이미 달성한 Lv.10이 아니라 Lv.9 초보자로 시작해,
직접 사냥으로 첫 레벨업을 경험한 뒤 전직 시험으로 이어지게 한다.

구현 결과:

- `DEFAULT_PLAYER_PROFILE.level`을 9로 변경해 모든 신규 슬롯이 Lv.9 초보자로 생성된다.
  `local-profile.ts`의 기존 저장값 파싱은 명시된 정상 레벨을 그대로 복구하므로 이미 생성한
  캐릭터의 레벨은 바뀌지 않는다.
- 신규 생성 E2E는 저장된 `character.level=9`와 퀘스트 HUD의
  `초보자 목표 · Lv.10 달성 (9/10)`을 고정한다. 필수 루프는 초록버섯 사냥으로 Lv.10을
  달성한 뒤 다크로드 시험을 받는 순서로 확장했다.
- 전체 45파일·344개 단위 테스트, Chromium·Firefox·WebKit 신규 생성·저장·HUD E2E 3/3,
  타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,235,571 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,318,699 bytes, JavaScript 1,493,253 bytes·gzip 405,757 bytes다.
- 기존 필수 루프 E2E는 새 Lv.9 성장 로직 실행 전에 카메라/HUD 경계에서 플레이어 화면 좌표
  598이 기존 `<596` 기대를 넘는 실패가 재현됐다. 요청 범위 밖 기준 완화는 하지 않고
  `RELEASE_QA.md`에 잔여 회귀로 기록한다.

완료 조건:

- 신규 슬롯은 Lv.9 초보자로 생성되고 저장·새로고침 뒤에도 같은 레벨을 유지한다.
- 퀘스트 HUD는 Lv.10 달성 전 사냥 목표를 표시하고, Lv.10부터 기존 1차 전직 흐름을 유지한다.
- 기존 저장 캐릭터의 레벨과 다른 슬롯의 독립 상태를 변경하지 않는다.
- 단위 테스트, 3엔진 신규 생성 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `feat: start new characters at level 9`

### P72 — Z 고정 줍기 키와 HUD 안내

상태: 구현 완료 — 2026-07-18

목표: `Z`를 사용자 스킬 배치와 무관하게 항상 아이템·메소 회수에 사용하는 고정 키로 만들고,
우측 상단 조작 안내에서 즉시 확인할 수 있게 한다.

구현 결과:

- `EXTRA_SKILL_HOTKEYS`에서 `Z`를 제거해 추가 키를 `Shift/QWER/ASDF/XCV` 12칸으로 정리했다.
  고정 입력의 `KeyZ → Loot`는 스킬 별칭으로 덮어쓸 수 없으며, 기존 v16 프로필에 저장된 `Z`
  별칭도 정규화 과정에서 제거된다.
- 스킬 단축키 화면과 전체 메뉴 설명에서 `Z` 배치 슬롯을 제거하고, 빈 `S`의 스탯/장비 기본
  동작은 유지했다. 우측 상단 `GUIDE · 조작 안내`에는 `[Z] 줍기`를 추가했다.
- 전체 45파일·344개 단위 테스트, Chromium·Firefox·WebKit 단축키·4개 PC 뷰포트 E2E 6/6,
  타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다. 1280×720 실행 화면에서 가이드
  텍스트와 HUD 안전 여백 위반 0건을 확인했다.
- `dist/`는 84파일·10,235,302 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,318,430 bytes, JavaScript 1,493,237 bytes·gzip 405,745 bytes다.

완료 조건:

- `Z`는 신규·기존 프로필의 스킬 키 설정으로 덮어쓸 수 없고 항상 최근접 드롭을 회수한다.
- 스킬 단축키 화면에는 `Z` 슬롯이 없으며 나머지 12개 추가 키의 배치·비우기·저장은 유지된다.
- 우측 상단 조작 안내에 `Z 줍기`가 표시되고 1280×720 HUD와 지원 PC 창 경계를 넘지 않는다.
- 단위 테스트, 3엔진 단축키·뷰포트 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `fix: keep Z as the item pickup key`

### P73 — 모바일 터치 조작·Lv.120 호카게 부스트

상태: 구현 완료 — 2026-07-29

목표: PC 입력과 저장 호환성을 유지하면서 모바일 가로·세로 화면을 지원하고, 터치 플레이에
필요한 이동·공격·회수·대표 스킬 조작과 Lv.120 호카게 부스트 생성 선택지를 제공한다.

구현 결과:

- 터치 가능 장치에서는 게임 컨테이너를 `100vw × 100dvh`로 맞추고 safe-area를 고려한 좌하단
  이동 조이스틱, 우하단 기본 공격·고정 줍기·대표 액티브 3슬롯과 우상단 `MENU`를 표시한다.
  가로 844×390과 세로 390×844에서 모든 조작부가 화면 안에 유지된다.
- 조이스틱은 좌우 이동, 위쪽 점프·상호작용·줄 오르기, 아래쪽 줄 내려가기·단방향 발판 하강을
  기존 `InputController`의 가상 입력으로 합성한다. 팝업과 호카게 시네마틱 중에는 컨트롤을
  숨기고 입력을 해제한다.
- 모바일 스킬은 현재 직업 차수의 주 스킬만 표시한다. 로그는 럭키세븐·그림자 연사, 어쌔신은
  드레인·환영 쌍성, 허밋은 어벤저·심연 폭우, 호카게는 나선환·삼인 협공·천뢰옥이다.
- 캐릭터 생성의 선택 버튼으로 일반 Lv.9 초보자와 Lv.120 호카게 부스트를 전환한다. 부스트는
  레벨업 규칙을 재사용해 자동 능력치 배분, HP/MP 완전 회복, 전체 15개 스킬 Lv.20과 남은
  SP 33을 해당 빈 슬롯에만 저장한다.
- 전체 46파일·350개 단위 테스트, Chromium·Firefox·WebKit 모바일·일반 생성·PC 뷰포트 E2E
  9/9, 타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,245,907 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,329,035 bytes, JavaScript 1,499,247 bytes·gzip 407,553 bytes다.

완료 조건:

- 터치 장치에서 가로·세로 전체 창 레이아웃과 좌우 이동·점프·상호작용·발판 하강이 동작한다.
- 우하단에 기본 공격, 고정 줍기와 현재 직업의 대표 액티브 스킬 최대 3개만 표시·실행된다.
- 일반 생성은 기존 Lv.9를 유지하고 부스트 생성은 선택한 슬롯만 정확한 Lv.120 호카게로 만든다.
- 단위 테스트, 3엔진 모바일·PC 회귀 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `feat: 모바일 조작과 호카게 부스트 지원`

### P74 — 세로 모바일 전체 화면·HUD 최적화

상태: 구현 완료 — 2026-07-29

목표: 세로 모바일에서 16:9 게임 Canvas 위아래의 미사용 영역을 UI 공간으로 활용하고, 로그인부터
게임 내 모든 HUD·DOM 팝업의 입력과 버튼을 가로 넘침 없이 끝까지 조작할 수 있게 한다.

구현 결과:

- 760px 이하 세로 화면에서 `#app`을 `100vw × 100dvh`로 확장하고 화면 오버레이가 safe-area
  안의 전체 높이를 사용하도록 했다. Canvas는 기존 1280×720 논리 화면과 중앙 배치를 유지한다.
- 로그인·캐릭터 선택/생성·전체 메뉴·환경설정·직업/원정/NPC 대화·상점·인벤토리·스탯/장비·
  스킬·스킬 단축키를 공통 전체 높이 모바일 시트로 통일했다. 패널은 수평 넘침을 막고 자체 세로
  스크롤하며 제목과 닫기·취소·확정 영역을 고정한다.
- 모바일 입력은 최소 16px로 iOS 자동 확대를 막고 버튼·링크는 최소 44px 터치 영역을 사용한다.
  상점 행과 다열 상세 화면은 한 열, 인벤토리는 4열, 숫자·추가 스킬 단축키는 각각 4열로 감싸
  좁은 세로 화면에서도 가로 스크롤이 생기지 않는다.
- 세로 터치 조이스틱은 128px, 전투 슬롯은 60px, 공격·줍기 버튼은 72px로 확대했다. 미니맵·
  퀘스트·조작 안내 접기 버튼은 36px 터치 영역으로 키우고 중앙 16:9 Canvas의 실제 HUD 제목
  위치에 맞췄다.
- 전체 46파일·350개 단위 테스트, Chromium·Firefox·WebKit 세로 전체 화면·모바일 조작 E2E
  6/6과 4개 PC 뷰포트 회귀 E2E 3/3, 타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,251,855 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,334,983 bytes, JavaScript 1,499,247 bytes·gzip 407,553 bytes다.
- 390×844 Chromium 실행 화면에서 로그인·캐릭터 생성·선택·전체 메뉴·인벤토리·스킬 단축키의
  전체 높이 사용, 가로 넘침 0건과 끝단 버튼 접근성을 직접 확인했다.

완료 조건:

- 세로 화면의 모든 UI가 Canvas 위아래 공간을 포함한 safe-area 내부 전체 높이를 사용한다.
- 모든 입력·버튼·링크가 가로 경계를 넘지 않고, 긴 내용은 닫기·확정 조작을 잃지 않은 채 세로
  스크롤할 수 있다.
- 플레이 HUD 접기 버튼과 터치 컨트롤이 세로 화면 경계와 Canvas HUD 위치를 함께 지킨다.
- 단위 테스트, 3엔진 세로 모바일·PC 뷰포트 회귀 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `fix: 세로 모바일 전체 화면 UI 최적화`

### P75 — 모바일 좌우 조이스틱·상황 버튼·3슬롯 편집

상태: 구현 완료 — 2026-07-29

목표: 모바일 이동 방향을 좌우로 단순화하고 점프·NPC 대화·맵 이동을 별도 상황 버튼에 모으며,
현재 차수 주 스킬 3슬롯만 보고 직접 순서를 지정할 수 있게 한다.

구현 결과:

- 좌하단 조이스틱은 수평 오프셋과 좌우 이동만 합성한다. 우하단 공격 왼쪽에 점프 버튼을
  추가하고 NPC 근처에서는 대화, 포탈·줄 근처에서는 이동 조작으로 자동 전환한다.
- 모바일 HUD와 `MENU`의 스킬 단축키 화면은 현재 차수의 주 스킬만 최대 3개 노출한다. 모바일
  편집 화면에서 세 슬롯의 상대 순서를 바꾸면 기존 v16 액티브 순서에 저장되어 HUD와 입력에
  즉시 반영되고, 데스크톱의 11개 숫자·12개 추가 키 편집은 유지된다.
- 최초 페이지 로딩 시 `이 게임은 PC 환경에 최적화되어 있습니다.`와
  `BGM과 함께 플레이하려면 매너모드를 해제해주세요` 안내를 두 줄로 먼저 표시하고 3초 뒤
  자동 fade-out한다.
- 세로 터치 화면의 미니맵·퀘스트·조작 안내 접기 버튼을 28px로 축소하고, 3개 유틸리티 버튼과
  수평 조이스틱이 하단 safe-area와 16:9 Canvas 경계를 넘지 않게 재배치했다.
- 전체 46파일·350개 단위 테스트, Chromium·Firefox·WebKit 모바일 E2E 6/6과 PC 뷰포트
  회귀 3/3, 타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,266,191 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,349,319 bytes, JavaScript 1,499,027 bytes·gzip 407,446 bytes다.
- 390×844 Chromium 실행 화면에서 첫 안내의 한 줄 표시, 전체 높이 로그인과 플레이 HUD의
  상하 미사용 영역·최소화 버튼 배치를 직접 확인했다.

완료 조건:

- 모바일 조이스틱이 상하 입력 없이 좌우만 이동하고 상황 버튼으로 점프·NPC 대화·포탈·줄
  이동을 중단 없이 수행한다.
- 모바일 HUD와 단축키 편집에는 현재 차수 주 스킬 최대 3개만 표시되고 세 슬롯 순서가 저장된다.
- 첫 로딩 안내가 즉시 나타나 3초 뒤 사라지며 세로 HUD 최소화 버튼이 화면을 과도하게 가리지
  않는다.
- 단위 테스트, 3엔진 모바일·PC 회귀 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `feat: 모바일 상황 조작과 3슬롯 편집 지원`

### P76 — 원정 포탈 해금·모바일 조작 분리·1차 시험장

상태: 구현 완료 — 2026-07-29

목표: 원정 진행과 커닝시티 포탈 노출을 일치시키고, 모바일 점프·대화·이동을 독립 조작으로
분리하며 1차 전직부터 전용 그림자 시험장을 사용한다.

구현 결과:

- 커닝시티 `city-library` 포탈에 중간보스 처치 요구 조건을 추가했다. 원정 `offer|midboss`
  단계에서는 포탈 오브젝트·9개 애니메이션 객체·미니맵 마커를 모두 만들지 않고, 폭열군주
  이그니카르의 사망·보상 확정으로 `upperboss`가 된 뒤 세 요소를 함께 생성한다.
- 모바일 우하단 점프 버튼을 항상 유지하고 NPC 대화, 포탈·줄 이동을 각각 별도 버튼으로
  분리했다. 한 상황에서는 대화 또는 이동 중 필요한 버튼만 노출해 390px 세로 화면에서도
  줍기·상황·점프·공격 4개 터치 영역이 조이스틱과 겹치지 않는다.
- 좌하단 조이스틱은 좌우 이동을 유지하면서 아래 방향을 발판 하강 보조 입력으로 제공한다.
  아래로 누른 채 고정 점프를 터치하면 기존 `MoveDown + Jump` 단방향 발판 통과 규칙을
  재사용하며 위 방향 이동은 합성하지 않는다.
- 도적 아지트의 그림자 시험장 입구 허용 목록에 `rogueTrial`을 추가하고 1차 시험 목적지를
  그림자 시험장으로 변경했다. 로그 시험 중에는 기존 시험장 4개 몬스터 스폰만 초록버섯으로
  치환해 초보자가 기존 초록버섯 5마리 조건을 전용 시험장에서 진행하며, 다른 차수 구성은
  유지한다.
- 전체 46파일·351개 단위 테스트, Chromium·Firefox·WebKit 모바일 조작·세로 전체 화면·
  달빛 서고 포탈 해금·1차 그림자 시험장 E2E 12/12, 타입체크·Terser 프로덕션 빌드와
  `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,267,744 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,350,872 bytes, JavaScript 1,499,602 bytes·gzip 407,537 bytes다.

완료 조건:

- 커닝시티 달빛 서고 포탈과 미니맵·애니메이션은 이그니카르 처치 전에는 없고 처치 직후부터
  함께 나타난다.
- 모바일 점프는 NPC·포탈·줄 근처에서도 유지되고, 대화와 이동은 별도 버튼으로 동작한다.
- 모바일 조이스틱 아래 입력과 점프를 동시에 누르면 2층 이상 단방향 발판을 통과한다.
- 1차 로그 시험을 수락한 초보자가 그림자 시험장에 입장해 초록버섯 5마리 조건을 진행한다.
- 단위 테스트, 3엔진 모바일·포탈·전직 회귀 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `feat: 원정 포탈과 모바일·1차 시험 흐름 개선`

### P77 — 모바일 상황 버튼 순서·첫 환경 안내

상태: 구현 완료 — 2026-07-29

목표: 모바일의 상황 조작을 줍기보다 먼저 찾을 수 있게 정렬하고, 첫 화면에서 PC 최적화와
BGM 재생 조건을 함께 안내한다.

구현 결과:

- 모바일 우하단 유틸리티 버튼의 DOM과 화면 순서를 `대화/이동 → 줍기 → 점프 → 공격`으로
  변경했다. NPC 근처에서는 대화, 포탈·줄 근처에서는 이동 중 하나만 표시되며 두 경우 모두
  줍기 왼쪽을 유지한다.
- 첫 3초 안내를 PC 환경 최적화 문구와 BGM 재생을 위한 매너모드 해제 문구의 두 줄로 구성해
  390px 세로 화면에서도 가로 넘침 없이 표시한다.
- 전체 46파일·351개 단위 테스트, Chromium·Firefox·WebKit 모바일 E2E 6/6, 타입체크·Terser
  프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,267,968 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,351,096 bytes, JavaScript 1,499,602 bytes·gzip 407,537 bytes다.
- 390×844 Chromium 실행 화면에서 두 줄 안내의 가로 넘침 없는 표시와 3초 자동 소멸을
  직접 확인했다.

완료 조건:

- 가로·세로 모바일에서 대화와 이동이 각각 표시될 때 모두 줍기 버튼 왼쪽에 배치된다.
- 첫 화면에서 PC 최적화와 BGM 안내가 즉시 함께 보이고 3초 뒤 사라진다.
- 단위 테스트, 3엔진 모바일 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `fix: 모바일 상황 버튼과 첫 안내 정리`

### P78 — 모바일 대표 스킬 4슬롯

상태: 구현 완료 — 2026-07-29

목표: 모바일 HUD와 전용 단축키 편집에 현재 차수의 대표 액티브 스킬을 최대 4개 표시한다.

구현 결과:

- 모바일 스킬 버튼을 4개로 확장하고 호카게 대표 스킬에 미수옥을 추가했다. 호카게는
  `나선환 → 미수옥 → 삼인 협공 → 천뢰옥`을 표시하며 구미호 변신은 토글형이라 제외한다.
- `MENU`의 모바일 스킬 단축키 화면도 4열·4슬롯으로 확장하고 네 스킬의 상대 순서를 바꾸면
  HUD와 입력에 즉시 반영되도록 기존 v16 액티브 순서 저장을 재사용한다.
- 로그·어쌔신·허밋은 각 차수의 직접 공격기 2개만 유지하며 비어 있는 모바일 버튼은 잠금
  상태로 표시한다.
- 전체 46파일·351개 단위 테스트, Chromium·Firefox·WebKit 모바일 E2E 6/6, 타입체크·Terser
  프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 84파일·10,268,432 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  7,351,560 bytes, JavaScript 1,499,605 bytes·gzip 407,538 bytes다.

완료 조건:

- 가로·세로 모바일에서 스킬 버튼 4개가 모두 화면 안에 표시된다.
- 호카게 HUD와 모바일 단축키 편집에서 나선환·미수옥·삼인 협공·천뢰옥 4개를 실행·정렬한다.
- 단위 테스트, 3엔진 모바일 E2E, 타입체크·빌드·배포 감사를 통과한다.

커밋 체크포인트: `feat: 모바일 대표 스킬 4슬롯 확장`

### P79 — 무손실 WebP·보스 BGM 지연 로딩·공개 화면 정리

상태: 구현 완료 — 2026-07-29

목표: 초기 전송량과 일반 플레이의 불필요한 오디오 요청을 줄이고, 공개 화면과 문서를 현재
런타임 상태에 맞춘다.

구현 결과:

- 매니페스트 핵심 스프라이트 24종과 스킬 아이콘 15종의 기준 PNG는 제작·픽셀 감사용으로
  보존하고, `npm run assets:webp`로 같은 stem의 lossless VP8L WebP를 결정적으로 생성한다.
  Vite URL·DOM 스킬 이미지·매니페스트는 WebP만 참조하고 `dist/`의 PNG 포함은 감사에서
  금지한다.
- 39개 입력 2,465,785 bytes를 디코딩 픽셀 차이 0인 1,980,136-byte WebP로 전환해
  485,649 bytes(19.70%)를 줄였다.
- 초기 오디오 에셋에서 보스 MP3를 제외하고, 살아 있는 보스가 있는 맵을 처음 열 때만 동적
  로더가 한 번 요청한다. 로딩 중에는 일반곡을 유지하고 중복 요청 병합, 실패 재시도, 맵·보스
  변경 뒤 늦은 완료 무시와 Scene 종료 정리를 지원한다.
- README의 이전 5·6번 화면을 제거하고 Chrome의 `http://127.0.0.1:4173/`에 남아 있던 로컬
  1번 `루키` 프로필로 달빛 마도서고에서 월광 늑대를 사냥하는 1280×720 화면을 새 5번 공개
  캡처로 추가했다.
- 번들에서 2KB 초과 이미지의 인라인을 제한해 Phaser와 소형 SVG의 기존 이점을 유지하면서
  JavaScript 상한을 지킨다.
- 전체 46파일·359개 단위 테스트, Chromium·Firefox·WebKit 보스 음원·WebP E2E 6/6,
  타입체크·Terser 프로덕션 빌드와 `dist/` 감사를 통과했다.
- `dist/`는 86파일·9,782,633 bytes이며 BGM 2,916,872 bytes를 제외한 예산 적용값
  6,865,761 bytes, JavaScript 1,492,554 bytes·gzip 400,326 bytes다.

완료 조건:

- 기준 PNG와 런타임 WebP 39종의 디코딩 픽셀이 같고 배포물에 PNG가 없다.
- 로그인·일반 맵에서는 보스 MP3를 요청하지 않으며 첫 보스 조우에서 한 번만 로드한 뒤 전환한다.
- 단위 테스트, Chromium·Firefox·WebKit WebP·오디오 E2E, 타입체크·빌드·배포 감사를 통과한다.
- README와 에셋·오디오·QA 문서가 현재 전달 형식과 공개 화면을 가리킨다.

커밋 체크포인트: `성능: 런타임 에셋 전송량 최적화`

## 5. 시각 설계 원칙

- 로그인은 책·목재 패널과 밝은 숲, 캐릭터 선택은 하늘·목재 발판과 명확한 정보 위계를 사용한다.
- 커닝시티는 청록·회색 산업 팔레트, 벽돌·배관·간판과 붉은 포인트 조명을 사용한다.
- 초록버섯굴은 깊은 녹색 수직 공간, 발광 발판, 포탈 소켓과 전투 동선의 대비를 유지한다.
- 닌자 캐릭터와 전투 효과는 작은 화면에서도 읽히는 독자 실루엣, 강한 윤곽과 제한된 팔레트를 사용한다.
- 시각 회귀 기준은 `docs/screenshots/`의 승인된 실제 실행 화면과 `VISUAL_QA.md`이며,
  외부 레퍼런스 이미지 원본은 저장소나 배포물에 보관하지 않는다.

## 6. 최종 인수 시나리오

1. 임의의 아이디와 비밀번호를 입력해 로그인한다.
2. 첫 접속이면 세 슬롯과 활성 슬롯 저장 키가 모두 비어 있고, 닉네임과 주사위 능력치를 정해야만 첫 캐릭터가 생성되는지 확인한다.
3. 캐릭터 선택의 빈 2·3번 슬롯에서 캐릭터를 추가 생성하고 세 슬롯이 함께 보이는지 확인한다.
4. 원하는 슬롯을 선택해 닉네임·직업 미리보기가 바뀌고 새로고침 뒤 선택이 유지되는지 확인한다.
5. 커닝시티에 진입해 퀘스트 알림의 Lv.10 달성·1차 전직 다음 목표와, 좌우 이동·점프 중 이름표 추적을 확인한다.
6. 포탈을 이용해 다크로드에게 로그 시험을 받고 도적 아지트의 그림자 시험장으로 들어간다.
7. 그림자 시험장에서 `Ctrl`로 초록버섯 5체를 처치한다.
8. 다크로드에게 완료 보고해 로그 전용 장비로 바뀌고 `Shift` 럭키세븐이 해금되는지 확인한다.
9. 피격·공격 데미지 숫자, HP/MP 감소와 빠른 회복을 확인한다.
10. 드롭된 아이템과 메소를 `Z`로 회수한다.
11. 귀환 포탈로 커닝시티에 돌아온다.
12. `S`에서 AP를 분배하고 자동분배 옵션을 설정한다.
13. 레벨업으로 SP 3을 얻고 `K`에서 럭키세븐·예리한 시야를 올린 뒤 `ESC`의 스킬 단축키 화면에서 `1↔4`를 드래그 교환하고 A에 드레인을 배치한다. 숫자키 `1`과 A가 각 설정 스킬을 발동하고 새로고침 뒤 스킬 레벨·숫자 순서·추가 키가 함께 유지되는지 확인한다.
14. Lv.30/60/120 프로필에서 2·3·4차 전직의 레벨·수락·처치·보고 안내와 어쌔신·허밋·호카게 외형을 확인한다.
15. Lv.100/140/200 프로필에서 세라 원정 수락, 이그니카르·루나시온·원펀맨 처치, 완료 보고가 원정 진척과 레벨에 맞게 표시되는지 확인한다.
16. 원펀맨 처치 뒤 픽셀 아트 크레딧의 7개 개발 분야 `임상진 with Codex`와 게임 레퍼런스 `메이플스토리`를 확인하고, 자동 종료·버튼·`ESC` 뒤 이동과 전투를 계속한다.
17. 다른 캐릭터 슬롯과 왕복한 뒤 닉네임·기본 능력치·직업, 전직 시험 진행도, 능력치·AP 옵션, 15개 스킬·SP·액티브 슬롯, HP/MP, 메소, 아이템 상태가 각 슬롯에서 복구되는지 확인한다.

## 7. 주요 위험과 대응

- 브라우저 단축키 충돌: 게임 캔버스가 포커스된 동안에만 `Ctrl`, `Alt`, `Shift`, 방향키의 기본 동작을 막는다.
- 조작감 지연: 아트 폴리시 전에 임시 도형으로 가속도, 점프 높이, 공격 타이밍을 먼저 확정한다.
- 애니메이션 제작량 증가: 상태별 최소 프레임 세트를 정의하고, 플레이 루프에 필요한 상태부터 완성한다.
- 이미지 저작권: 참고 이미지 자체를 제품 에셋으로 복제하지 않고 자체 제작·허용된 에셋과 독자 명칭을 사용한다.
- 저장 데이터 변경: 저장 스키마에 버전을 두고 기본값 병합과 초기화 경로를 제공한다.
- 범위 팽창: 멀티플레이, 계정 서버, 표창 외 일반 장비 상점·교체 UI, 호카게 이후 직업과 독립 월드 지역은 이번 데모에서 제외한다.

## 8. 커밋 및 문서 규칙

- 각 P 단계의 완료 조건을 만족한 뒤 기능 단위 커밋을 만든다.
- 큰 작업을 완료할 때 `ROADMAP.md`, `README.md`, `AGENTS.md`의 현재 상태를 함께 점검한다.
- 커밋 전 최소한 빌드, 관련 테스트, 변경 파일 목록을 확인한다.
- 사용자 변경사항이 있는 경우 덮어쓰거나 되돌리지 않고 작업 범위를 분리한다.
